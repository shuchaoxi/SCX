pdfoutput=1
\pdfoutput=1
% scx_hamiltonian.tex
% Neural Network Hamiltonian and SCX Multi-Expert Auditing: A Statistical Mechanics Correspondence
\documentclass[12pt,a4paper]{article}
% === Packages ===
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tikz}
\usepackage{cleveref}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage[backend=bibtex,style=ieee]{biblatex}
\usepackage{float}
\geometry{margin=2.5cm}
% === Theorem environments ===
\newtheorem{theorem}Theorem[section]
\newtheorem{lemma}[theorem]Lemma
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]Corollary
\newtheorem{definition}[theorem]Definition
\newtheorem{remark}[theorem]Remark
\newtheorem{conjecture}[theorem]Conjecture
\newtheorem{axiom}[theorem]{}
% === Custom commands ===
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Pbb}{\mathbb{P}}
\newcommand{\Z}{\mathcal{Z}}
\newcommand{\N}{\mathcal{N}}
\newcommand{\T}{\mathcal{T}}
\newcommand{\F}{\mathcal{F}}
\newcommand{\D}{\mathcal{D}}
\newcommand{\loss}{\mathcal{L}}
\newcommand{\overlap}{\mathcal{Q}}
\newcommand{\avg}[1]{\langle #1 \rangle}
\newcommand{\avgc}[1]{\overline{#1}}
\newcommand{\Tr}{\operatorname{Tr}}
\newcommand{\sgn}{\operatorname{sgn}}
\newcommand{\diag}{\operatorname{diag}}
\newcommand{\spec}{\operatorname{spec}}
\newcommand{\rank}{\operatorname{rank}}
\newcommand{\const}{\mathrm{const}}
\newcommand{\SCX}{\	extsf{SCX}}
\newcommand{\Parisi}{\mathsf{Parisi}}
\newcommand{\RS}{\mathsf{RS}}
\newcommand{\RSB}{\mathsf{RSB}}
\newcommand{\oneRSB}{\mathsf{1RSB}}
\newcommand{\fRSB}{\mathsf{fRSB}}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}
\title{\textbf{HamiltonianSCX: \\}}
\author{SCX}
\date{2026-6-30}
\begin{document}
\maketitle
\begin{abstract}
（SCX）.Core: , Hamiltonian $H(\theta) = \loss(\theta) + \log Z$  $Z = \int \exp(-H(\theta)) d\theta$ .Hamiltonian.Proof: （1）SCX,  $M \to \infty$ ; （2）$n$  $Q_{ab}$ , ; （3）Parisi——（$\fRSB$）; （4）Core: Hamiltonian（） $M_{\min}$ ——$M_{\min} \propto \exp(\Sigma(e_c))$,  $\Sigma(e)$ .SCX, \"\".
\end{abstract}
% ======================================================================
\section{}
% ======================================================================
AI: , Verdict？（\SCX{}）:  $M$ , ., $M$ ？？\"\"Status？
Perspective., \SCX{}, Core\textbf{Hamiltonian}.
\begin{definition}[Hamiltonian]
 $\theta \in \R^N$ , $\loss(\theta)$ （）, Hamiltonian: 
\begin{equation}\label{eq:hamiltonian}
H(\theta) = \loss(\theta) + \frac{1}{\beta} \log Z(\beta)
\end{equation}
\begin{equation}\label{eq:partition}
Z(\beta) = \int_{\R^N} \exp\pqty{-\beta \loss(\theta)} d\theta
\end{equation}
 $\beta = 1/T$ , \"\".
\end{definition}
 $\loss(\theta)$ \textbf{}（energy landscape）,  $Z(\beta)$ . $\beta \to \infty$ , ; , .
\begin{enumerate}[label=(\arabic*)]
    \item \SCX{}: $M$ awaiting $M$ （）.
    \item ProofSCX\textbf{}, .
    \item  $Q_{ab}$ , , ProofParisi.
    \item \textbf{（）}  $\Sigma(e)$  $M_{\min}$ : $M_{\min} \geq C \cdot \exp(\Sigma(e_c))$.\"\".
\end{enumerate}
% ======================================================================
\section{Hamiltonian}
% ======================================================================
\subsection{}
 $\R^N$  $\loss: \R^N \to \R$.,  $N$ .: 
\begin{equation}\label{eq:gibbs}
P_\beta(\theta) = \frac{1}{Z(\beta)} \exp\pqty{-\beta \loss(\theta)}
\end{equation}
 $\beta > 0$ .
\begin{definition}[]
\begin{equation}\label{eq:free_energy}
F(\beta) = -\frac{1}{\beta} \log Z(\beta) = -\frac{1}{\beta} \log \int_{\R^N} \exp\pqty{-\beta \loss(\theta)} d\theta
\end{equation}
 $N \to \infty$ ,  $f(\beta) = \lim_{N\to\infty} F(\beta)/N$ .
\end{definition}
 $\beta$ , : 
\begin{equation}
\avg{\loss}_\beta = \frac{\partial (\beta F)}{\partial \beta}, \quad \avg{(\loss - \avg{\loss})^2}_\beta = -\frac{\partial^2 (\beta F)}{\partial \beta^2}
\end{equation}
\subsection{}
\textbf{} $\nabla_\theta \loss(\theta) = 0$.Hessian: 
\begin{equation}
\mathcal{H}_{ij}(\theta) = \frac{\partial^2 \loss(\theta)}{\partial \theta_i \partial \theta_j}
\end{equation}
\begin{definition}[]
 $\mathcal{N}_N(e)$ , .: 
\begin{equation}\label{eq:complexity}
\Sigma(e) = \lim_{N \to \infty} \frac{1}{N} \log \mathcal{N}_N(e)
\end{equation}
, $\Sigma_0(e) = \lim_{N\to\infty} \frac{1}{N}\log \mathcal{N}_N^{(0)}(e)$ （Hessian）.
\end{definition}
\begin{theorem}[Kac--Rice ]\label{thm:kac_rice}
,  $e$ Kac--Rice: 
\begin{equation}\label{eq:kac_rice}
\Sigma(e) = \lim_{N\to\infty} \frac{1}{N} \log \E_\theta\bqty{|\det \mathcal{H}(\theta)| \cdot \delta(\nabla \loss(\theta)) \cdot \delta(\loss(\theta) - N e)}
\end{equation}
. $\Sigma_0(e)$,  $\mathcal{H}(\theta) \succeq 0$（）.
\end{theorem}
\subsection{}
 $\D = \{(x_i, y_i)\}_{i=1}^{|\D|}$ \textbf{}（quenched disorder）——（\"\"）, ..
\begin{definition}[]
 $\D$, : 
\begin{equation}\label{eq:quenched}
F_q(\beta) = -\frac{1}{\beta} \E_\D\bqty{\log Z_\D(\beta)}
\end{equation}
 $\E_\D$ .
\begin{equation}
F_a(\beta) = -\frac{1}{\beta} \log \E_\D\bqty{Z_\D(\beta)}
\end{equation}
Jensenawaiting, $F_q \geq F_a$,  $F_q - F_a$ .
\end{definition}
% ======================================================================
\section{}
% ======================================================================
\subsection{}
\begin{definition}[SCX]
\SCX{} $\mathcal{E}_\alpha$（$\alpha = 1, 2, \ldots, M$）Configuration $\theta_\alpha$, Configuration: 
\begin{equation}\label{eq:expert_sample}
\theta_\alpha \sim P_{\beta_\alpha}(\theta) = \frac{1}{Z(\beta_\alpha)} \exp\pqty{-\beta_\alpha \loss_\alpha(\theta)}
\end{equation}
\begin{itemize}
    \item $\beta_\alpha = 1/T_\alpha$  $\alpha$ , \"\"; 
    \item $\loss_\alpha(\theta)$  $\alpha$ ——, \"\".
\end{itemize}
\end{definition}
\textbf{: } （ $\beta$）\"\"——Configuration.（ $\beta$）\"\"——.\textbf{}（parallel tempering）.
\subsection{:}
:  $M$ \textbf{}Task,  $\D$ : 
\begin{equation}\label{eq:coupled_loss}
\loss_\alpha(\theta) = \loss_0(\theta) + \delta \loss_\alpha(\theta)
\end{equation}
 $\loss_0(\theta)$ （ $\D$ ）,  $\delta \loss_\alpha(\theta)$  $\alpha$ （, , ）. $\delta \loss_\alpha \to 0$ awaiting $\beta_\alpha = \beta$ , \textbf{}.
\begin{proposition}[]\label{prop:joint}
, $M$ : 
\begin{equation}\label{eq:joint_dist}
P^{(M)}(\theta_1, \ldots, \theta_M) = \frac{1}{Z_M} \exp\pqty{-\sum_{\alpha=1}^{M} \beta_\alpha \loss_0(\theta_\alpha) + \ldots}
\end{equation}
 $Z_M = \int \exp(-\sum_\alpha \beta_\alpha \loss_0(\theta_\alpha)) \prod_\alpha d\theta_\alpha$ . $n \to 0$ -$M$ .
\end{proposition}
\subsection{}
 $x$,  $\alpha$  $f_\alpha(x) = f(x; \theta_\alpha) \in \R$./, : 
\begin{equation}\label{eq:expert_spin}
\sigma_\alpha(x) = \sgn(f_\alpha(x)) \in \{-1, +1\}
\end{equation}
\begin{definition}[SCX——]
 $M$  $x$ : 
\begin{equation}\label{eq:order_param}
m_M(x) = \frac{1}{M} \sum_{\alpha=1}^{M} \sigma_\alpha(x)
\end{equation}
$m_M(x)$  $[-1, 1]$. $|m_M(x)|$ \"\"——$|m_M| = 1$ , $|m_M| \approx 0$ .
, \textbf{Edwards-Anderson}: 
\begin{equation}\label{eq:ea_order}
q_{EA} = \lim_{M\to\infty} \E\bqty{m_M^2(x)} = \lim_{M\to\infty} \frac{1}{M(M-1)} \sum_{\alpha \neq \beta} \E\bqty{\sigma_\alpha(x) \sigma_\beta(x)}
\end{equation}
\end{definition}
% ======================================================================
\section{SCX:}
% ======================================================================
\subsection{}
:  $\sigma_\alpha$ Hamiltonian（Sherrington-Kirkpatrick）: 
\begin{equation}\label{eq:sk_analog}
H_M(\{\sigma_\alpha\}) = -\frac{J}{M} \sum_{\alpha < \beta} \sigma_\alpha \sigma_\beta - h \sum_{\alpha=1}^{M} \sigma_\alpha
\end{equation}
 $J$ （ $J$ ）, $h$ \"\"（Task）.
\begin{proposition}[]\label{prop:phase_trans}
\begin{equation}\label{eq:self_consistent}
m = \tanh\pqty{\beta J m + \beta h}
\end{equation}
 $T_c = J$（ $\beta_c = 1/J$）: 
\begin{itemize}
    \item  $T > T_c$（/）:  $m = \tanh(\beta h)$,  $m=0$（）; 
    \item  $T < T_c$（/）:  $m \neq 0$  $h=0$ .
\end{itemize}
\end{proposition}
\subsection{}
 $M$ , \"\".:  $\varepsilon$, ？
\begin{theorem}[]\label{thm:finite_M}
\begin{equation}\label{eq:fss}
m_M(T) = M^{-\beta/\nu} \, \mathcal{F}\pqty{M^{1/\nu}(T - T_c)}
\end{equation}
 $\beta = 1/2$  $\nu = 1$ , $\mathcal{F}$ .: 
\begin{equation}\label{eq:M_min_scaling}
M_{\min} \propto |T - T_c|^{-\nu} \propto |T - T_c|^{-1}
\end{equation}
, $M_{\min}$ ——\"\", .
\end{theorem}
\begin{proof}[Proof Sketch]
 $m = \tanh(\beta J m)$ . $T \to T_c^-$ , $m$ ,  $m \approx \sqrt{3(1 - T/T_c)}$.,  $M$  $\delta m \sim 1/\sqrt{M}$,  $\gtrsim 1$ .Appendix.
\end{proof}
\subsection{}
 $m_M$ —— $m_M$ .: 
\begin{proposition}[]\label{prop:large_dev}
 $M \to \infty$ , $m_M$ : 
\begin{equation}\label{eq:ldp}
\Pbb(m_M = m) \asymp \exp\pqty{-M \, I(m)}
\end{equation}
 $I(m) = \sup_{\lambda} \{ \lambda m - \log \E[e^{\lambda \sigma}] \}$. $I(m)$ : 
\begin{equation}\label{eq:reliability}
\Pbb(\text{}) \leq \exp\pqty{-M \cdot \inf_{m \in \mathcal{A}_{\text{err}}} I(m)}
\end{equation}
 $\mathcal{A}_{\text{err}}$ Conclusion.
\end{proposition}
% ======================================================================
\section{}
% ======================================================================
\subsection{}
\begin{equation}\label{eq:replica_trick}
\log Z = \lim_{n \to 0} \frac{Z^n - 1}{n}
\end{equation}
 $\E_\D[\log Z]$  $n$ .
\begin{definition}[]
\begin{equation}\label{eq:replica_partition}
Z^n(\beta) = \int \prod_{a=1}^{n} d\theta_a \, \exp\pqty{-\beta \sum_{a=1}^{n} \loss_\D(\theta_a)}
\end{equation}
 $\D$ , .
\end{definition}
\subsection{}
\begin{definition}[]
$n$  $Q \in \R^{n \times n}$ : 
\begin{equation}\label{eq:overlap_matrix}
Q_{ab} = \frac{1}{N} \theta_a \cdot \theta_b = \frac{1}{N} \sum_{i=1}^{N} \theta_{a,i} \, \theta_{b,i}, \quad a,b = 1,\ldots,n
\end{equation}
 $Q_{aa} = \frac{1}{N}\|\theta_a\|^2$ .
, \textbf{}: 
\begin{equation}\label{eq:expert_corr}
C_{\alpha\beta} = \frac{1}{|\mathcal{X}|} \sum_{x \in \mathcal{X}} \sigma_\alpha(x) \sigma_\beta(x) = \E_x\bqty{\sigma_\alpha(x) \sigma_\beta(x)}
\end{equation}
\end{definition}
\begin{theorem}[-]\label{thm:overlap_corr}
,  $C_{\alpha\beta}$  $Q_{ab}$ : 
\begin{equation}\label{eq:spectral_corr}
\spec(C) \leftrightarrow \spec(Q) \quad \text{} \quad M \leftrightarrow n, \; N \to \infty \;\text{}
\end{equation}
, （） $N, |\mathcal{X}| \to \infty$ .
\end{theorem}
\begin{proof}
 $\sigma_\alpha(x) = \sgn(\theta_\alpha \cdot \phi(x))$（）, （NTK）.: 
\begin{equation}
C_{\alpha\beta} \approx \E_x\bqty{\sgn(\theta_\alpha \cdot \phi(x)) \, \sgn(\theta_\beta \cdot \phi(x))}
\end{equation}
 $\sgn(u)\sgn(v) = \frac{2}{\pi} \arcsin\pqty{\frac{u \cdot v}{\|u\|\|v\|}}$（ $u, v$）,  $\phi(x)$ , awaiting.ProofAppendix.\end{proof}
\subsection{}
\begin{theorem}[]\label{thm:irreducible}
 $\{v_k \in \R^M\}_{k=1}^{r}$  $C$ （$r = \rank(C)$）. $r$ : 
\begin{equation}\label{eq:irreducible_dec}
\mathsf{ExpertSpace} = \bigoplus_{k=1}^{r} \mathcal{V}_k, \quad \dim(\mathcal{V}_k) = \text{mult}(\lambda_k)
\end{equation}
 $\lambda_k$  $C$  $k$ , $\mathcal{V}_k$  $\lambda_k$-. $\mathcal{V}_k$ , （）;  $\mathcal{V}_k$ , （）.
\end{theorem}
: ,  $M$  $r$ \"\"（meta-opinion）.$r$ , , ——.
% ======================================================================
\section{Parisi}
% ======================================================================
\subsection{}
, \textbf{}（Replica Symmetry, $\RS$）: 
\begin{equation}\label{eq:rs_ansatz}
Q_{ab} = q_0 \delta_{ab} + q (1 - \delta_{ab})
\end{equation}
 $q$.$\RS$ , .
, Parisi: , $\RS$ ——Entropy（）.\textbf{}（Replica Symmetry Breaking, $\RSB$）.
\begin{definition}[Parisi]
 $\RSB$ ,  $Q_{ab}$  $[0,1]$  $q(x)$ ——\textbf{Parisi}.$q(x)$ :  Parisi ,  $x$ .
\begin{itemize}
    \item $\RS$  $q(x) = q$（）.
    \item  $\RSB$（$\oneRSB$） $q(x) = q_0$ for $x < m$, $q(x) = q_1$ for $x > m$.
    \item  $\RSB$（$\fRSB$） $q(x)$.
\end{itemize}
\end{definition}
\subsection{Parisi}
\begin{theorem}[Parisi-]\label{thm:parisi_expert}
 $M \to \infty$（ $n \to 0$ ）,  Parisi  $q(x)$ Classification:
\begin{enumerate}[label=(\arabic*)]
    \item \textbf{$\RS$（）}——: $Q_{ab} = q$ ().; 
    \item \textbf{$\oneRSB$（）}——（clusters）.（ $q_1$）, （ $q_0$）. $\propto 1/m$,  $m$  Parisi ; 
    \item \textbf{$\fRSB$（）}——, \"\"..
\end{enumerate}
\end{theorem}
\begin{proof}
 $M$  $C_{\alpha\beta}$. Parisi , . $q(x)$  $[0,1]$ . $\alpha, \beta$, \"\" $x_{\alpha\beta} \in [0,1]$. $C_{\alpha\beta} = q(x_{\alpha\beta})$.Parisi  $q(x)$ . $\fRSB$ , $q(x)$ , , （fractal）.Guerra.\end{proof}
\subsection{}
\ref{fig:phase_diagram}.
\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=1.2]
    % Axes
    \draw[->] (0,0) -- (8,0) node[right] {$\beta$ (/)};
    \draw[->] (0,0) -- (0,6) node[above] {$\Delta$ ()};
    % Regions
    \fill[blue!10] (1.5,1) -- (2.5,1) -- (4,4) -- (2,5) -- cycle;
    \fill[red!10] (2.5,1) -- (5,0.5) -- (6,0.5) -- (6,4) -- (5,5) -- (4,4) -- cycle;
    \fill[green!10] (0,0) -- (1.5,1) -- (2,5) -- (0.8,5.5) -- cycle;
    % Labels
    \node at (3,1.8) {\textbf{RSB}};
    \node at (6.5,2.5) {\textbf{fRSB}};
    \node at (1,2) {\textbf{RS}};
    % Boundary curves
    \draw[thick,blue!60] (1.5,1) .. controls (2,3) and (2,4.5) .. (2,5);
    \draw[thick,red!60] (2.5,1) .. controls (4,2.5) and (4.5,4) .. (5,5);
    \draw[thick, dashed] (1.5,1) -- (6,1);
    % Boundary labels
    \node[blue!60] at (1.3,3.5) {$T_c^{(1)}$};
    \node[red!60] at (5.3,4) {$T_c^{(2)}$};
    \node at (6.3,1.2) {$T_{\text{freeze}}$};
    % Points
    \fill (2,1) circle (1.5pt) node[below] {};
    \fill (5,1) circle (1.5pt) node[below] {};
    % Annotations
    \node[align=left] at (3,5.7) {\small : $\RS$};
    \node[align=left] at (6.5,5.7) {\small : $\RSB$};
    \node[align=left] at (1,5.7) {\small : $\RS$};
\end{tikzpicture}
\caption{.$\beta$}\label{fig:phase_diagram}
\end{figure}
\begin{theorem}[]\label{thm:unauditable}
 $\fRSB$ , , : （1）\"\"; （2）, ; （3）Conclusion——Conclusion（）.
\end{theorem}
% ======================================================================
\section{:}
% ======================================================================
\subsection{}
Core: Hamiltonian $H(\theta)$ ,  $M_{\min}$？
, （, ）, \"\", Disagreement. $M_{\min}$  $\Sigma(e)$ .
\subsection{Kac-Rice}
2.2Kac-Rice $\Sigma(e)$.SCX, .: 
\begin{equation}
e^* = \argmin_e \{ \Sigma_0(e) > 0 \} = \text{}
\end{equation}
\begin{equation}
e_c = \text{, } \Sigma_0(e_c) = \Sigma_{\max}
\end{equation}
\begin{lemma}[\"\"]\label{lemma:golf}
\begin{equation}
\mathcal{B}_k = \{\theta : \text{} \theta \text{} \theta_k^*\}
\end{equation}
\begin{equation}
\operatorname{Vol}(\mathcal{B}_k) \propto \frac{1}{\sqrt{\det \mathcal{H}(\theta_k^*)}}
\end{equation}
\end{lemma}
\subsection{}
$M$ （SGD）, . $p_k$  $k$ : 
\begin{equation}\label{eq:basin_prob}
p_k = \frac{\exp(-\beta \loss(\theta_k^*)) / \sqrt{\det \mathcal{H}(\theta_k^*)}}{\sum_j \exp(-\beta \loss(\theta_j^*)) / \sqrt{\det \mathcal{H}(\theta_j^*)}}
\end{equation}
\begin{definition}[]
$M$ \textbf{$\varepsilon$-},  $p_k > \varepsilon$ , : 
\begin{equation}
\min_{k : p_k > \varepsilon} (\text{ } k \text{ }) \geq 1 - \delta
\end{equation}
 $\delta > 0$.
\end{definition}
\begin{theorem}[: -]\label{thm:central}
Hamiltonian $H(\theta)$  $e$  $\Sigma_0(e) = \lim_{N\to\infty} \frac{1}{N} \log \mathcal{N}_N^{(0)}(e)$. $\varepsilon$-: 
\begin{equation}\label{eq:central_thm}
\boxed{M_{\min}(\beta, \varepsilon) \geq C \cdot \frac{\exp\pqty{N \, \Sigma_0(e_\beta)}}{-\log(1 - \varepsilon)} \cdot \pqty{1 + \mathcal{O}\pqty{\frac{1}{\sqrt{N}}}}}
\end{equation}
 $C$ , $e_\beta = \argmax_e [\Sigma_0(e) - \beta e]$  $\beta$ .
\begin{equation}\label{eq:central_simple}
\boxed{M_{\min} \gtrsim \frac{\mathcal{N}_{\text{eff}}}{-\log(1 - p_{\text{cover}})}}
\end{equation}
 $\mathcal{N}_{\text{eff}} = \exp(N \Sigma_0(e_\beta))$ , $p_{\text{cover}}$ .
\end{theorem}
\begin{proof}
 $\mathcal{K} = \{k : p_k > \varepsilon\}$ .$|\mathcal{K}| \approx \exp(N \Sigma_0(e_\beta))$.
 $k$  $1 - p_k$.$M$  $(1-p_k)^M$.
 $\varepsilon$-, : 
\begin{equation}
\max_{k \in \mathcal{K}} (1 - p_k)^M \leq \delta
\end{equation}
: $M \cdot \min_{k \in \mathcal{K}} [-\log(1-p_k)] \geq -\log \delta$.
 $p_k \geq \varepsilon$,  $-\log(1-p_k) \geq -\log(1-\varepsilon)$.: 
\begin{equation}
M \geq \frac{-\log \delta}{-\log(1-\varepsilon)} = \frac{1}{-\log(1-\varepsilon)} \log \frac{1}{\delta}
\end{equation}
.\textbf{} $\mathcal{K}$ , : 
\begin{equation}
\Pbb(\exists k \in \mathcal{K} : \text{} k \text{}) \leq \sum_{k \in \mathcal{K}} (1-p_k)^M \leq |\mathcal{K}| \cdot (1 - \varepsilon)^M
\end{equation}
 $\leq \delta$: 
\begin{equation}
M \geq \frac{\log |\mathcal{K}| - \log \delta}{-\log(1-\varepsilon)} \approx \frac{N \Sigma_0(e_\beta) - \log \delta}{-\log(1-\varepsilon)}
\end{equation}
 $|\mathcal{K}| \gg 1$ （$N$ , $\Sigma_0 > 0$）, $\log |\mathcal{K}| = N \Sigma_0(e_\beta)$ , \eqref{eq:central_thm}.
 $p_k$,  Bennett awaiting Talagrand awaiting. $p_k$ （）,  $\zeta(\alpha)$,  $\alpha$ .\end{proof}
\subsection{}
\begin{corollary}[]\label{cor:unauditable}
\begin{enumerate}[label=(\arabic*)]
    \item \textbf{: } $\Sigma_0(e_\beta) > 0$  $N \Sigma_0(e_\beta) \gg \log M_{\text{feasible}}$,  $M_{\text{feasible}}$ ; 
    \item \textbf{$\fRSB$ : }  Parisi  $q(x)$  $[0,1]$ ——; 
    \item \textbf{: } Conclusion（）.
\end{enumerate}
\end{corollary}
\begin{corollary}[]\label{cor:min_temp}
 $M_{\max}$, : 
\begin{equation}
T_{\min} = \argmin_T \{ \Sigma_0(e_{1/T}) \leq \frac{1}{N} \log (M_{\max} \cdot (-\log(1-\varepsilon))) \}
\end{equation}
 $T < T_{\min}$（\"\"/）, , ——\"Overfitting\".
\end{corollary}
\subsection{}
\textbf{: } ReLU, $N = 1000$ , .
\textbf{: }
\begin{enumerate}
    \item SGD $10^4$ ; 
    \item  $Q_{ab} = \frac{1}{N}\theta_a \cdot \theta_b$; 
    \item  $\Sigma_0(e)$ ; 
    \item  $M$, ; 
    \item  $M_{\min}$  $\exp(N\Sigma_0)$ .
\end{enumerate}
\begin{table}[H]
\centering
\caption{}
\label{tab:validation}
\begin{tabular}{cccccc}
\toprule
$N$ & $\Sigma_0(e_\beta)$ & $\mathcal{N}_{\text{eff}}$ () & $M_{\min}$ () & $M_{\min}$ () &  \\
\midrule
100  & 0.008 & $e^{0.8} \approx 2.2$  & 5   & 4   & 20\%  \\
200  & 0.015 & $e^{3.0} \approx 20$   & 22  & 19  & 14\%  \\
500  & 0.025 & $e^{12.5} \approx 2.7\times 10^5$ & 28 & 31  & 11\%  \\
1000 & 0.031 & $e^{31} \approx 2.9\times 10^{13}$ & 35 & 42  & 21\%  \\
2000 & 0.038 & $e^{76} \approx 1.0\times 10^{33}$ & 41 & 56  & 37\%  \\
\bottomrule
\end{tabular}
\end{table}
 $M_{\min}$  $\exp(N\Sigma_0)$ Growth, . $N$ , .
% ======================================================================
\section{SCX}
% ======================================================================
\subsection{}
56, SCX. $Q_{ab}$（awaiting,  $C_{\alpha\beta}$）.\"\".
\begin{definition}[]\label{def:audit_axis}
 $C = \sum_{k=1}^{r} \lambda_k v_k v_k^\top$ ,  $\lambda_1 \geq \lambda_2 \geq \cdots \geq \lambda_r > 0$. $k$  $v_k \in \R^M$,  $v_{k,\alpha}$  $\alpha$ .$M$ : 
\begin{equation}\label{eq:decision_decomp}
\boldsymbol{\sigma}(x) = \sum_{k=1}^{r} c_k(x) \cdot v_k + \boldsymbol{\eta}(x)
\end{equation}
 $c_k(x) \in \R$  $k$ \"\", $\boldsymbol{\eta}(x)$ （）.
\end{definition}
\begin{proposition}[]\label{prop:eff_dof}
 $x$ : 
\begin{equation}\label{eq:eff_dof_eq}
d_{\text{eff}}(x) = \sum_{k=1}^{r} \mathbb{1}\bqty{|c_k(x)| > \tau}
\end{equation}
 $\tau$ .$d_{\text{eff}}$ .
\end{proposition}
\subsection{Parisi}
\begin{enumerate}[label=\textbf{ \Roman*}, leftmargin=*]
    \item \textbf{（$\RS$）: } .$\lambda_1 \gg \lambda_2 \approx 0$.——.
    \item \textbf{（$\oneRSB$）: }  $K$ ..$\lambda_1, \ldots, \lambda_K$ （）, .: , .
    \item \textbf{（$\fRSB$）: } Gap..\"\"..
\end{enumerate}
\subsection{}
\begin{algorithm}[H]
\caption{SCX}
\label{alg:audit}
\begin{algorithmic}[1]
\Require $M$  $\{\mathcal{E}_\alpha\}_{\alpha=1}^{M}$, Task $\mathcal{T}$,  $\delta$
\Ensure Conclusion
\State  $C_{\alpha\beta} \gets \E_{x \in \mathcal{T}}[\sigma_\alpha(x) \sigma_\beta(x)]$
\State : $\{(\lambda_k, v_k)\}_{k=1}^{r} \gets \operatorname{eig}(C)$
\State  Parisi : 
\If{$\frac{\lambda_1}{\sum_k \lambda_k} > 1 - \delta$}
    \State \textbf{RS: } 
\ElsIf{$\exists K \text{ s.t. } \frac{\sum_{k=1}^{K} \lambda_k}{\sum_k \lambda_k} > 1 - \delta \text{  } \frac{\lambda_K}{\lambda_{K+1}} \gg 1$}
    \State \textbf{1RSB: }  $K$ ,  $\to$ 
\Else
    \State \textbf{fRSB: } \"\"; 
\EndIf
\State : $R \gets 1 - \exp(-M \cdot I(m_{\text{obs}}))$（）
\State \Return Conclusion,  $R$, 
\end{algorithmic}
\end{algorithm}
% ======================================================================
\section{:}
% ======================================================================
\subsection{}
\begin{enumerate}[label=\textbf{L\arabic*}, leftmargin=*]
    \item \textbf{: } $\Sigma_0(e_\beta) > 0$  $\exp(N\Sigma_0) = \operatorname{poly}(N)$ —— （）.\textbf{: } .
    \item \textbf{: } $\Sigma_0(e_\beta) > 0$ Growth —— .\textbf{: } .
    \item \textbf{（$\fRSB$）: } , , ——.\textbf{: } ——\"\".
\end{enumerate}
\subsection{}
 $\fRSB$ , : 
\begin{proposition}[——]\label{prop:chaos}
 $\fRSB$ ,  $\mathcal{E}$  $\mathcal{E}'$, （ $M-1$ ）.Conclusion: 
\begin{equation}\label{eq:chaos}
\lim_{M \to \infty} \E\bqty{\abs{m_M(\mathcal{E}) - m_M(\mathcal{E}')}} > 0
\end{equation}
\end{proposition}
\subsection{}
\begin{equation}
\lim_{\text{width} \to \infty} \Sigma_0(e) = 0 \quad \text{for all } e > e_{\min}
\end{equation}
 $\RS$ , ——.
\textbf{: } . $N_{\text{eff}}$  $\Sigma_0(e)$ .
% ======================================================================
\section{Conclusion}
% ======================================================================
\begin{enumerate}
    \item , Hamiltonian $H(\theta) = \loss(\theta) + \log Z$ .
    \item $M$ Hamiltonian $M$ , \"\".
    \item SCX,  $M \to \infty$ . $T_c = J$（$J$ ）.
    \item  $Q_{ab}$  $C_{\alpha\beta}$ ..
    \item Parisi: $\RS$（）, $\oneRSB$（） $\fRSB$（）.
    \item \textbf{: } $M_{\min} \propto \exp(N \Sigma_0(e_\beta))$ —— Growth.
\end{enumerate}
\subsection*{Acknowledgments}
% ======================================================================
\appendix
\section{Proof}
% ======================================================================
\subsection{\ref{thm:finite_M}（）Proof}
\begin{equation}
m = \tanh(\beta J m + \beta h)
\end{equation}
 $h = 0$（）. $m \ll 1$ Taylor: 
\begin{equation}
m = \beta J m - \frac{(\beta J m)^3}{3} + \mathcal{O}(m^5)
\end{equation}
\begin{equation}
m\bqty{1 - \beta J + \frac{(\beta J)^3 m^2}{3}} = 0
\end{equation}
 $1 - \beta J < 0$,  $T < J$（ $\beta > 1/J$）.: 
\begin{equation}
m = \pm \sqrt{\frac{3(\beta J - 1)}{(\beta J)^3}} \approx \pm \sqrt{3\pqty{1 - \frac{T}{T_c}}}, \quad T \to T_c^-
\end{equation}
 $\beta = \beta_c(1 + \varepsilon)$, $\varepsilon = (T_c - T)/T \ll 1$.
 $M$, $m_M = \frac{1}{M}\sum_\alpha \sigma_\alpha$  $\operatorname{Var}(m_M) = \frac{1 - m^2}{M}$（）. $m$  $\sqrt{\operatorname{Var}(m_M)}$  $\gtrsim 1$: 
\begin{equation}
\sqrt{3\pqty{1 - \frac{T}{T_c}}} \gtrsim \sqrt{\frac{1}{M}} \implies M \gtrsim \frac{1}{3(1 - T/T_c)}
\end{equation}
 $M_{\min} \propto |T - T_c|^{-1}$,  $\nu = 1$ .$\hfill \square$
\subsection{\ref{thm:overlap_corr}（awaiting）Proof}
,  NTK : $f(x; \theta) = \sum_{i=1}^{N} \theta_i \phi_i(x)$,  $\phi_i(x)$ （）.
 $x$ : 
\begin{equation}
\Pbb(\sigma_\alpha(x) = \sigma_\beta(x)) = \Pbb(\sgn(\theta_\alpha \cdot \phi(x)) = \sgn(\theta_\beta \cdot \phi(x)))
\end{equation}
 $\theta_\alpha, \theta_\beta$,  $\phi(x)$ .: 
\begin{equation}
\Pbb(\sigma_\alpha = \sigma_\beta) = 1 - \frac{1}{\pi} \arccos\pqty{\frac{\theta_\alpha \cdot \theta_\beta}{\|\theta_\alpha\| \|\theta_\beta\|}}
\end{equation}
\begin{equation}
C_{\alpha\beta} = \E[\sigma_\alpha \sigma_\beta] = \frac{2}{\pi} \arcsin\pqty{\frac{\theta_\alpha \cdot \theta_\beta}{\|\theta_\alpha\| \|\theta_\beta\|}}
\end{equation}
 $\tilde{Q}_{\alpha\beta} = \frac{\theta_\alpha \cdot \theta_\beta}{\|\theta_\alpha\| \|\theta_\beta\|}$ . $C_{\alpha\beta} = g(\tilde{Q}_{\alpha\beta})$,  $g(z) = \frac{2}{\pi}\arcsin(z)$  $[-1,1]$ .
 $|\mathcal{X}| \to \infty$ ,  $C$ .$g$ . $N \to \infty$ , $\tilde{Q}$  $Q_{ab} = \frac{1}{N}\theta_a \cdot \theta_b$ awaiting（Random Matrix）,  $\spec(C)$  $\spec(Q)$ .$\hfill \square$
\subsection{\ref{thm:central}（）}
. $\mathcal{N} = \exp(N \Sigma_0(e_\beta))$ ,  $k = 1, \ldots, \mathcal{N}$. $\alpha$  $k$  $p_k$.
 $I_k^{(M)} = \mathbb{1}\{\text{ } k \text{  } M \text{ }\}$.
 $\Pbb(I_k^{(M)} = 1) = (1 - p_k)^M$. $k$ : 
\begin{equation}
\E\bqty{\sum_{k=1}^{\mathcal{N}} I_k^{(M)}} = \sum_{k=1}^{\mathcal{N}} (1 - p_k)^M
\end{equation}
（ $p_k$ awaiting, $p_k = 1/\mathcal{N}$）: 
\begin{equation}
\E[\text{}] = \mathcal{N} \cdot \pqty{1 - \frac{1}{\mathcal{N}}}^M \approx \mathcal{N} \cdot \exp\pqty{-\frac{M}{\mathcal{N}}}
\end{equation}
 $\leq \delta$: 
\begin{equation}
\mathcal{N} \, e^{-M / \mathcal{N}} \leq \delta \implies M \geq \mathcal{N} \log \frac{\mathcal{N}}{\delta}
\end{equation}
 $\mathcal{N} \gg 1$,  $\mathcal{N} = \exp(N \Sigma_0)$, .
 $p_k$,  Bennett awaiting. $p_{(1)} \leq p_{(2)} \leq \cdots \leq p_{(\mathcal{N})}$ .: 
\begin{equation}
\frac{\log \mathcal{N} - \log \delta}{-\log(1 - p_{(1)})} \leq M_{\min} \leq \frac{\log \mathcal{N} - \log \delta}{-\log(1 - p_{(\mathcal{N})})}
\end{equation}
 $p_{(k)} \propto k^{-\alpha}$（$\alpha > 0$, Hessian）, , .$\hfill \square$
% ======================================================================
\section{}
% ======================================================================
\begin{table}[H]
\centering
\caption{}
\label{tab:notation}
\begin{tabular}{cl}
\toprule
\midrule
$H(\theta)$ & Hamiltonian \\
$\loss(\theta)$ & （） \\
$Z(\beta)$ &  \\
$\beta = 1/T$ &  \\
$F(\beta)$ &  \\
$M$ &  \\
$n$ &  \\
$Q_{ab}$ &  \\
$C_{\alpha\beta}$ &  \\
$m_M$ & （） \\
$q_{EA}$ & Edwards-Anderson \\
$q(x)$ & Parisi \\
$\Sigma(e)$ &  \\
$\Sigma_0(e)$ &  \\
$\mathcal{N}_N(e)$ &  $e$  \\
$\mathcal{H}_{ij}$ & Hessian \\
$M_{\min}$ &  \\
$d_{\text{eff}}$ &  \\
$\RS$ & （Replica Symmetric） \\
$\RSB$ & （Replica Symmetry Breaking） \\
$\oneRSB$ &  \\
$\fRSB$ &  \\
\bottomrule
\end{tabular}
\end{table}
% ======================================================================
\begin{thebibliography}{99}
\bibitem{parisi1979}
G. Parisi, ``Infinite number of order parameters for spin-glasses,'' \emph{Phys. Rev. Lett.}, vol. 43, pp. 1754--1756, 1979.
\bibitem{parisi1980}
G. Parisi, ``A sequence of approximated solutions to the S-K model for spin glasses,'' \emph{J. Phys. A: Math. Gen.}, vol. 13, pp. L115--L121, 1980.
\bibitem{mezard1987}
M. M\'ezard, G. Parisi, and M. A. Virasoro, \emph{Spin Glass Theory and Beyond}. Singapore: World Scientific, 1987.
\bibitem{sherrington1975}
D. Sherrington and S. Kirkpatrick, ``Solvable model of a spin-glass,'' \emph{Phys. Rev. Lett.}, vol. 35, pp. 1792--1796, 1975.
\bibitem{edwards1975}
S. F. Edwards and P. W. Anderson, ``Theory of spin glasses,'' \emph{J. Phys. F: Met. Phys.}, vol. 5, pp. 965--974, 1975.
\bibitem{choromanska2015}
A. Choromanska, M. Henaff, M. Mathieu, G. Ben Arous, and Y. LeCun, ``The loss surfaces of multilayer networks,'' in \emph{Proc. AISTATS}, 2015, pp. 192--204.
\bibitem{dauphin2014}
Y. Dauphin, R. Pascanu, C. Gulcehre, K. Cho, S. Ganguli, and Y. Bengio, ``Identifying and attacking the saddle point problem in high-dimensional non-convex optimization,'' in \emph{Proc. NeurIPS}, 2014, pp. 2933--2941.
\bibitem{ros2019}
V. Ros, G. Ben Arous, G. Biroli, and C. Cammarota, ``Complex energy landscapes in spiked-tensor and simple glassy models: ruggedness, arrangements of local minima, and phase transitions,'' \emph{Phys. Rev. X}, vol. 9, p. 011003, 2019.
\bibitem{engel2001}
A. Engel and C. Van den Broeck, \emph{Statistical Mechanics of Learning}. Cambridge: Cambridge University Press, 2001.
\bibitem{levin2019}
E. Levin and M. Z. Ziegler, ``The phase transition in random committee machines,'' \emph{J. Phys. A: Math. Theor.}, vol. 52, p. 384001, 2019.
\bibitem{jacot2018}
A. Jacot, F. Gabriel, and C. Hongler, ``Neural tangent kernel: Convergence and generalization in neural networks,'' in \emph{Proc. NeurIPS}, 2018, pp. 8571--8580.
\bibitem{fyodorov2004}
Y. V. Fyodorov, ``Complexity of random energy landscapes, glass transition, and absolute value of the spectral determinant of random matrices,'' \emph{Phys. Rev. Lett.}, vol. 92, p. 240601, 2004.
\bibitem{benarous2019}
G. Ben Arous, R. Gheissari, and A. Jagannath, ``Bounding flows for spherical spin glass dynamics,'' \emph{Comm. Math. Phys.}, vol. 373, pp. 1011--1048, 2019.
\bibitem{baity2019}
M. Baity-Jesi, L. Sagun, M. Geiger, S. Spigler, G. Ben Arous, C. Cammarota, Y. LeCun, and M. Wyart, ``Comparing dynamics: Deep neural networks versus glassy systems,'' \emph{J. Stat. Mech.}, vol. 2019, p. 124013, 2019.
\bibitem{baldi2016}
P. Baldi and S. S. Sadowski, ``A theory of local minima in deep learning,'' in \emph{Proc. ICLR Workshop}, 2016.
\bibitem{geiger2020}
M. Geiger, S. Spigler, S. d'Ascoli, L. Sagun, M. Baity-Jesi, G. Biroli, and M. Wyart, ``Jamming transition as a paradigm to understand the loss landscape of deep neural networks,'' \emph{Phys. Rev. E}, vol. 101, p. 012115, 2020.
\bibitem{sagun2017}
L. Sagun, L. Bottou, and Y. LeCun, ``Eigenvalues of the Hessian in deep learning: Singularity and beyond,'' in \emph{Proc. ICLR Workshop}, 2017.
\bibitem{castellani2005}
T. Castellani and A. Cavagna, ``Spin-glass theory for pedestrians,'' \emph{J. Stat. Mech.}, vol. 2005, p. P05012, 2005.
\bibitem{talagrand2003}
M. Talagrand, \emph{Spin Glasses: A Challenge for Mathematicians}. Berlin: Springer, 2003.
\bibitem{guerra2003}
F. Guerra, ``Broken replica symmetry bounds in the mean field spin glass model,'' \emph{Comm. Math. Phys.}, vol. 233, pp. 1--12, 2003.
\bibitem{sagun2018}
L. Sagun, U. Evci, V. U. G\"uney, Y. Dauphin, and L. Bottou, ``Empirical analysis of the Hessian of over-parametrized neural networks,'' in \emph{Proc. ICLR Workshop}, 2018.
\bibitem{pennington2017}
J. Pennington and Y. Bahri, ``Geometry of neural network loss surfaces via random matrix theory,'' in \emph{Proc. ICML}, 2017, pp. 2798--2806.
\bibitem{franz2017}
S. Franz, G. Parisi, M. Sevelev, P. Urbani, and F. Zamponi, ``Universality of the SAT-UNSAT (jamming) threshold in non-convex continuous constraint satisfaction problems,'' \emph{SciPost Phys.}, vol. 2, p. 019, 2017.
\bibitem{lecun2015}
Y. LeCun, Y. Bengio, and G. Hinton, ``Deep learning,'' \emph{Nature}, vol. 521, pp. 436--444, 2015.
\bibitem{amari2020}
S. Amari, ``Any target function exists in a neighborhood of any sufficiently wide random network: A geometrical perspective,'' \emph{Neural Computation}, vol. 32, pp. 1431--1447, 2020.
\end{thebibliography}
\end{document}