% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Theorem 1: Multi-Expert Consistency Guarantees for Label Noise
Detection}\label{theorem-1-multi-expert-consistency-guarantees-for-label-noise-detection}

\begin{quote}
\textbf{Core Claim}: 当 \(M\)
个在不相交Data子集上独立Training的专家对某个Sample的Consistency Score \(C(x)\)
超过阈值 \(\theta\) 时，该SampleYes标签Noise的置信度以指数RateConvergence到 1。

\textbf{Revision Record}: 2026-06-27 --- Lemma 3 Proof重构（新增 A6
Balanced Error DistributionAssumptions）；Chernoff 附录 KL 方向修正；主Theorem StatementUpdate为
(A1)-(A6)。两个 bug 均来自验证报告。
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1 Theorem Statement}\label{ux5b9aux7406ux9648ux8ff0}

\subsubsection{1.1 Symbol与设定
(Setup)}\label{ux7b26ux53f7ux4e0eux8bbeux5b9a-setup}

Define the following objects:

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Symbol
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Meaning
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\(\mathcal{X}\) & Input space, measurable \\
\(\mathcal{Y}\) & Label space,\(|\mathcal{Y}| = K\)（classification) or
\(\mathcal{Y} \subseteq \mathbb{R}^d\)（regression) \\
\(\mathcal{D}\) & Data distribution,\((X, Y) \sim \mathcal{D}\) \\
\(f^* : \mathcal{X} \to \mathcal{Y}\) & True labeling function (unknown Oracle) \\
\(\{f_m\}_{m=1}^M\) & \(M\)
expert models,\(f_m : \mathcal{X} \to \mathcal{Y}\) \\
\(\Pi = \{s_1, \dots, s_K\}\) & Status划分，\(\mathcal{X}\) measurable partition of \\
\(\ell : \mathcal{Y} \times \mathcal{Y} \to [0, B]\) & Bounded loss function \\
\(\tau > 0\) & Expert error threshold \\
\(\theta \in (0, 1)\) & Noise detection threshold \\
\end{longtable}

\textbf{Label Noise Model}: 对于每个Sample \((x, y^*)\)（where \(y^* = f^*(x)\)
is the true label), the observed label \(y\) is generated as follows:

\[y = \begin{cases}
y^* & \text{with probability } 1 - \eta \\
\text{Uniform}(\mathcal{Y} \setminus \{y^*\}) & \text{with probability } \eta
\end{cases}\]

where \(\eta \in (0, 1/2)\) is the global noise rate, and noise events are independent of \(x\)
and all training data.

\textbf{Consistency Score}: 对Sample
\((x, y)\)，定义专家Error指示变量和Consistency Score：

\[e_m(x, y) = \mathbf{1}\{\ell(f_m(x), y) > \tau\}, \quad
C(x) = \frac{1}{M} \sum_{m=1}^M e_m(x, y)\]

\textbf{Detection Rule}: Sample \((x, y)\) is flagged as noise if and only if
\(C(x) > \theta\)。

\subsubsection{1.2 Assumptions (Assumptions)}\label{ux5047ux8bbe-assumptions}

\textbf{(A1) Disjoint Training Sets}: \(M\) 个专家在 \(M\)
个不相交的独立同Distribution子集上Training：

\[D_m \sim \mathcal{D}^{n_m}, \quad D_m \cap D_{m'} = \varnothing, \quad D_m \perp D_{m'} \text{ for } m \neq m'\]

\textbf{(A2) CleanData上的Conditions独立}: 对任意CleanSample
\((x, y)\)（\(y = y^*\)），给定 \(x\) 的Conditions下，Error指示变量
\(\{e_m(x, y)\}_{m=1}^M\) YesConditions独立的。

\emph{JustificationExplanation与局限}: A2
Yes一种\textbf{Structure性Assumptions}，由专家Experiment设计（Disjoint Training Sets
A1、独立初始化）所论证，但\textbf{A1 并不蕴含
A2}。即使Training集完全不相交，在相似DataDistribution上Training的专家可能在Distribution外Sample上产生\textbf{相关Error}（源于共享归纳偏置，非共享TrainingData）。因此
A2 在 SCX 流水线的Data中\textbf{不可经验检验}------对连续输入空间
\(\mathcal{X}\)，每个 \(x\) 在有限Data集中最多出现一次，联合Distribution
\(P(e_1, \dots, e_M \mid x)\) 不可观测。 \textgreater{} \textgreater{}
\textbf{When A2 被违反时}（实际中常见):用
\(M_{\text{eff}} = M/(1 + (M-1)\bar{\rho})\) 替换所有Concentration Inequalities中的
\(M\)，where \(\bar{\rho}\)
为专家Error指标的平均成对相关性。对典型深度集成，\(\bar{\rho} \approx 0.1\)--\(0.3\)。建议在留置验证集上估计
\(\bar{\rho}\)，并用 \(M_{\text{eff}}\) 得到保守Guarantee。详细Analysis参见
\texttt{01\_symbol\_system.md} §12.5。

\textbf{(A3) Bounded Loss}:
\(\ell(a, b) \in [0, B], \; \forall a, b \in \mathcal{Y}\)，where
\(B < \infty\)。

\textbf{(A4) Uniform Independent Noise}: 标签翻转事件与 \(x\) 和所有 \(D_m\)
独立。Noise标签在 \(\mathcal{Y} \setminus \{y^*\}\) 上均匀Distribution。

\textbf{(A5) StatusHomogeneous性 (Status划分充分性)}: Status划分 \(\Pi\)
使得在每个Status \(s\)
内，专家的CleanDataError率近似均匀。形式化地，存在Status级常数
\(\{\mu_s\}_{s \in \mathcal{S}}\) 使得：

\[\sup_{x \in s} \, \mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s, \quad \forall s \in \mathcal{S}\]

即Status \(s\) 内CleanSample的平均专家Error率被 \(\mu_s\) 一致Upper Bound控制。

\textbf{(A6) Balanced Error Distribution}:
专家的Error概率在所有ErrorCategory上不能过度集中。存在常数
\(C_{\text{bal}} \geq 1\) 使得对任意Status \(s\) 和 \(x \in s\)：

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\(C_{\text{bal}} = 1\)
对应完全均匀的ErrorDistribution（每个ErrorCategory等概率）；\(C_{\text{bal}} = 2\)
Yes典型的保守Selection。此Assumptions可检验且在实践中几乎自动满足------在不相交CleanData上Training的专家没有理由将Error集中到特定Category。

\subsubsection{1.3 Key Lemma
(Mean Separation)}\label{ux5173ux952eux5f15ux7406-ux5747ux503cux5206ux79bb}

\textbf{Lemma 1 (Mean Separation).} 在Assumptions (A1)-(A5) 下（A6
不影响期望），对任意 \(x \in s\)：

\textbf{(CleanSample)}
\[\mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s\]

\textbf{(NoiseSample)}
\[\mathbb{E}[C(X) \mid \text{noise}, X = x] = 1 - \frac{1}{K-1} \cdot \mathbb{E}[C(X) \mid \text{clean}, X = x] \geq 1 - \frac{\mu_s}{K-1}\]

因此，当 \(\mu_s < \frac{K-1}{K}\) 时，存在 \(\theta\) 使得：

\[\mathbb{E}[C \mid \text{clean}] < \theta < \mathbb{E}[C \mid \text{noise}]\]

且分离间隙为：

\[\Delta_s(\theta) = \min\left(\theta - \mu_s, \; 1 - \frac{\mu_s}{K-1} - \theta\right)\]

当SelectionOptimal阈值
\(\theta_s^* = \frac{1}{2}\left(1 + \mu_s \cdot \frac{K-2}{K-1}\right)\)
时，分离间隙最大化（在 \(C_{\text{bal}}=1\) 的理想情况下):

\[\Delta_s^* = \frac{1}{2}\left(1 - \mu_s \cdot \frac{K}{K-1}\right)\]

当 \(C_{\text{bal}} > 1\) 时，Optimal阈值向 \(\mu_s\)
方向微调以补偿非均匀误差Distribution。\(\theta_s^*\) 的精确表达式需求解
\(\theta - \mu_s = 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta\)。

\textbf{Proof.} CleanSample的界由 (A5) 直接给出。对NoiseSample：

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, X = x] 
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(\ell(f_m(x), y) > \tau \;\big|\; \text{noise}, x\bigr) \\
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) \neq y \;\big|\; \text{noise}, x\bigr) \quad (\text{对 } 0\text{-}1 \text{ 损失}) \\
&= 1 - \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) = y \;\big|\; \text{noise}, x\bigr)
\end{aligned}\]

由 (A4)，Noise标签 \(y\) 在 \(\mathcal{Y} \setminus \{y^*\}\)
上均匀Distribution，且独立于所有专家：

\[\begin{aligned}
\mathbb{P}(f_m(x) = y \mid \text{noise}, x) 
&= \sum_{c \neq y^*} \mathbb{P}(y = c \mid \text{noise}) \cdot \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{P}(f_m(x) \neq y^* \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{E}[e_m \mid \text{clean}, x]
\end{aligned}\]

因此：

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, x] 
&= 1 - \frac{1}{K-1} \cdot \frac{1}{M} \sum_{m=1}^M \mathbb{E}[e_m \mid \text{clean}, x] \\
&= 1 - \frac{1}{K-1} \cdot \mathbb{E}[C \mid \text{clean}, x] \\
&\geq 1 - \frac{\mu_s}{K-1} \quad \text{由 (A5)}
\end{aligned}\]

分离间隙的存在性由 \(\mu_s < \frac{K-1}{K}\) Guarantee，此时
\(1 - \frac{\mu_s}{K-1} > \mu_s\)。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsubsection{1.4 Main Result (Main
Result)}\label{ux4e3bux5b9aux7406-main-result}

\textbf{Theorem 1 (SCX NoiseDetectionGuarantee).} 设Assumptions (A1)-(A6) 成立，且
\(M \geq 1\)（至少一个专家），\(K \geq 2\)（至少两个标签Category，以Guarantee
\(K-1 > 0\)）。令 \(\rho_s = \mathbb{P}(X \in s)\)
为Status概率。对任意阈值 \(\theta\) 满足
\(\mu_s < \theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) 的Status
\(s\)，定义Status级分离间隙：

\[\Delta_s = \min\left(\theta - \mu_s,\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right) > 0\]

当 \(C_{\text{bal}} = 1\)（最佳情形）时，\(\Delta_s\) 恢复原始定义；当
\(C_{\text{bal}} > 1\)
时，Noise侧的间隙收窄（因为Error可能不Yes完全均匀Distribution的）。

则 SCX NoiseDetection器的 F1 得分满足以下Lower Bound：

\[\text{F1} \;\geq\; 1 - \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-2M\Delta_s^2\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-2M\Delta_s^2\bigr) \Bigr]\]

或等价地：

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\!\bigl(-2M\Delta_s^2\bigr)\]

where \(\eta\) 为全局Noise率。

\textbf{更紧的 Chernoff 形式}: 对相同设定，使用 Chernoff 界代替
Hoeffding 可得更紧的指数Rate：

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\bigr) \Bigr]\]

where
\(\text{KL}(p \,\|\, q) = p \log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\)。Chernoff
界在 \(\mu_s\) 接近 \(\theta\) 或
\(1 - C_{\text{bal}} \cdot \mu_s/(K-1)\) 接近 \(\theta\) 时显著优于
Hoeffding（可紧 2-5 倍）。

\begin{quote}
\textbf{2026-06-27 修正}：原版 Chernoff 使用了
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\)，KL 方向有误，且未纳入
\(C_{\text{bal}}\)。现已修正为
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\)。
\end{quote}

\textbf{Key Corollary}: 当 \(M \to \infty\) 时，对所有满足
\(\mu_s < \frac{K-1}{K}\) 的Status，F1 以指数RateConvergence到 1：

\[\text{F1} \;=\; 1 - \mathcal{O}_P\!\left(\frac{1}{\eta} \cdot e^{-2M\Delta_{\min}^2}\right)\]

where \(\Delta_{\min} = \min_{s \in \mathcal{S}} \Delta_s\)
为最差Status分离间隙。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2 Proof}\label{ux8bc1ux660e}

\subsubsection{2.1 False Positive Rate on Clean Data (False Positive
Rate)}\label{ux6e05ux6d01ux6570ux636eux7684ux5047ux9633ux6027ux7387-false-positive-rate}

\textbf{Lemma 2 (FPR Upper Bound).} 对任意Status \(s \in \mathcal{S}\) 满足
\(\mu_s < \theta\)，CleanSample被误标为Noise的概率满足：

\[\mathbb{P}(C > \theta \mid \text{clean}, X \in s) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

\textbf{Proof.} 固定 \(x \in s\)，由 (A5) 知
\(\mathbb{E}[C \mid \text{clean}, X = x] \leq \mu_s\)。由
(A1)-(A2)，给定 \(x\) 时 \(\{e_m\}\) Conditions独立。由 (A3) 知
\(e_m \in [0, 1]\)。对 \(C = \frac{1}{M}\sum_m e_m\) 应用 Hoeffding
不等式（Pollard, 1990):

\[\mathbb{P}(C - \mathbb{E}[C] > \theta - \mu_s \mid \text{clean}, x) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

由于 \(\theta > \mu_s\)，\((\theta - \mu_s) > 0\)。该界对任意
\(x \in s\) 成立，因此：

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, X \in s) 
&= \mathbb{E}_{X \mid s}\bigl[ \mathbb{P}(C > \theta \mid \text{clean}, X) \bigr] \\
&\leq \sup_{x \in s} \mathbb{P}(C > \theta \mid \text{clean}, x) \\
&\leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)
\end{aligned}\]

此即所需。\(\square\)

\subsubsection{2.2 True Positive Rate on Noisy Data (True Positive
Rate)}\label{ux566aux58f0ux6570ux636eux7684ux771fux9633ux6027ux7387-true-positive-rate}

\textbf{新增Assumptions (A6) --- Balanced Error Distribution}: 对任意Status \(s\) 和Sample
\(x \in s\)，专家的Error概率在所有ErrorCategory上不能过度集中。形式化地，存在常数
\(C_{\text{bal}} \geq 1\) 使得：

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \text{where } \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\emph{Justification}：专家在不相交的CleanData子集上Training，没有理由将Error集中到特定的ErrorCategory。均匀NoiseModel下，每个ErrorCategory的概率天然约为
\(\mu_s/(K-1)\)。\(C_{\text{bal}} = 2\)
已Yes保守Selection。此Assumptions可检验：在实际Data上估计
\(\max_c \mu_c(x) / (\mu_s/(K-1))\)。

\textbf{Lemma 3 (TPR Lower Bound).} 在Assumptions (A1)-(A6) 下，对任意Status
\(s \in \mathcal{S}\) 满足
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\)，NoiseSample被CorrectDetection的概率满足：

\[\mathbb{P}(C > \theta \mid \text{noise}, X \in s) \geq 1 - \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)\]

\textbf{Proof}：固定 \(x \in s\)。给定Noise标签
\(y = c\)（\(c \neq y^*\)），对于 0-1 损失，专家 \(m\) 的Error指示为
\(e_m = \mathbf{1}\{f_m(x) \neq c\}\)。由于 \(f_m\) 仅Dependencies
\(D_m\)（A1），给定 \(x\) 时 \(\{f_m(x)\}\) Conditions独立（A2），因此
\(\{e_m\}\) 在给定 \((x, c)\) 下Conditions独立。

每个 \(e_m\) 的期望为
\(\mathbb{E}[e_m \mid x, c] = 1 - \mathbb{P}(f_m(x) = c \mid x) = 1 - \mu_{c,m}(x)\)。因此：

\[\mathbb{E}[C \mid x, c] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} \quad \text{（由 A6）}\]

由于 \(e_m \in [0, 1]\) Conditions独立，应用 Hoeffding 不等式：

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid x, c)
&= \mathbb{P}\!\left(\frac{1}{M} \sum_{m=1}^M e_m \leq \theta \;\Big|\; x, c\right) \\
&\leq \exp\!\left(-2M\left(1 - \mu_c(x) - \theta\right)^2\right) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

where Hoeffding 的 gap Conditions \(1 - \mu_c(x) > \theta\) 由定理Assumptions
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) 和 A6 Guarantee：
\(1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} > \theta\)。

对Noise标签 \(c\) 取平均（均匀Distribution over \(K-1\) 类):

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid \text{noise}, x)
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(C \leq \theta \mid x, c) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

因此
\(\mathbb{P}(C > \theta \mid \text{noise}, x) \geq 1 - \exp(-2M(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta)^2)\)。对
\(x \in s\) 取期望即得。\(\square\)

\textbf{Remark}：当 \(C_{\text{bal}} = 1\)（完全均匀的ErrorDistribution）时，Lemma
3 恢复原始命题中的Optimal bound。\(C_{\text{bal}}\)
可在实际Data上估计，并用于校准Detection阈值 \(\theta\)。

\subsubsection{2.3 F1 Lower Bound}\label{f1-ux4e0bux754c}

现在组合 Lemma 2 和 Lemma 3 推导 F1 Lower Bound。

令Detection Rule为 \(R(x) = \mathbf{1}\{C(x) > \theta\}\)。定义：

\[\begin{aligned}
\text{TPR}_s &= \mathbb{P}(R = 1 \mid \text{noise}, X \in s) \geq 1 - \exp(-2M(1 - \mu_s/(K-1) - \theta)^2) \\
\text{FPR}_s &= \mathbb{P}(R = 1 \mid \text{clean}, X \in s) \leq \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

由 (A4)，Noise事件与 \(X\) 独立，因此
\(\mathbb{P}(\text{noise} \mid X \in s) = \eta\) 对所有Status成立。整体
TPR 和 FPR 为：

\[\begin{aligned}
\text{TPR} &= \sum_s \rho_s \cdot \text{TPR}_s, \quad
\text{FPR} = \sum_s \rho_s \cdot \text{FPR}_s
\end{aligned}\]

F1 得分为：

\[\begin{aligned}
\text{F1} &= \frac{2 \cdot \text{TP} }{2 \cdot \text{TP} + \text{FP} + \text{FN}} \\
&= \frac{2\eta \cdot \text{TPR}}{2\eta \cdot \text{TPR} + (1-\eta) \cdot \text{FPR} + \eta \cdot (1 - \text{TPR})} \\
&= \frac{2\eta \cdot \text{TPR}}{\eta(1 + \text{TPR}) + (1-\eta) \cdot \text{FPR}}
\end{aligned}\]

代入 TPR \(\geq 1 - \delta_1\) 和 FPR \(\leq \delta_2\)，where：

\[\begin{aligned}
\delta_1 &= \sum_s \rho_s \cdot \exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \\
\delta_2 &= \sum_s \rho_s \cdot \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

得到：

\[\begin{aligned}
\text{F1} &\geq \frac{2\eta(1 - \delta_1)}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&= 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&\geq 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta} \quad (\text{因分母 } \geq \eta) \\
&= 1 - \delta_1 - \frac{1-\eta}{\eta}\delta_2
\end{aligned}\]

代入 \(\delta_1, \delta_2\) 的表达式，并注意当
\(\Delta_s = \min(\theta - \mu_s, \; 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)\)
时有 \(\exp(-2M(\theta - \mu_s)^2) \leq \exp(-2M\Delta_s^2)\) 和
\(\exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \leq \exp(-2M\Delta_s^2)\)，因此：

\[ \text{F1} \geq 1 - \sum_s \rho_s \left[\exp(-2M\Delta_s^2) + \frac{1-\eta}{\eta}\exp(-2M\Delta_s^2)\right] = 1 - \frac{1}{\eta}\sum_s \rho_s \exp(-2M\Delta_s^2) \]

证毕。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3 Corollaries (Corollaries)}\label{ux63a8ux8bba-corollaries}

\subsubsection{3.1 Corollaries 1：Symmetric Experts (Symmetric
Experts)}\label{ux63a8ux8bba-1ux5bf9ux79f0ux4e13ux5bb6-symmetric-experts}

当所有专家具有相同的CleanDataError率
\(\varepsilon\)（称为''Symmetric Experts''）时，\(\mu_s = \varepsilon\)
对所有Status一致。此时：

\[\Delta = \frac{1}{2}\left(1 - \varepsilon \cdot \frac{K}{K-1}\right)\]

且 F1 Lower Bound简化为：

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M}{2}\left(1 - \frac{\varepsilon K}{K-1}\right)^2\right)\]

当 \(K = 2\)（二分类）时，进一步简化为：

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-2M\left(\frac{1}{2} - \varepsilon\right)^2\right)\]

\textbf{意义}: 在二分类Symmetric Experts场景下，Detection质量完全由专家Error率
\(\varepsilon\) 与 \(1/2\) 的差距决定。\(\varepsilon\) 每远离 \(1/2\)
一个标准差（\(\approx 1/\sqrt{2M}\)），F1 Lower Bound提升一个指数数量级。

\subsubsection{3.2 Corollaries 2：Optimal Threshold Selection (Optimal
Threshold)}\label{ux63a8ux8bba-2ux6700ux4f18ux9608ux503cux9009ux62e9-optimal-threshold}

对给定的Noise率 \(\eta\)、Category数 \(K\)、专家数 \(M\) 和最大CleanError率
\(\mu_{\max} = \max_s \mu_s\)，OptimalDetection阈值 \(\theta^*\) 应满足：

\[\theta^* = \arg\max_\theta \min_s \Delta_s(\theta) = \frac{1}{2}\left(1 + \mu_{\max} \cdot \frac{K-2}{K-1}\right)\]

此时最小分离间隙为：

\[\Delta_{\min}^* = \frac{1}{2}\left(1 - \mu_{\max} \cdot \frac{K}{K-1}\right)\]

所需的最少专家数（达到 F1 \(\geq 1 - \varepsilon_0\)）为：

\[M \geq \frac{1}{2\Delta_{\min}^{*2}} \log\left(\frac{1}{\eta \varepsilon_0}\right)\]

\textbf{Proof}: 直接解
\(1 - \frac{1}{\eta}e^{-2M\Delta^2} \geq 1 - \varepsilon_0\) 得
\(e^{-2M\Delta^2} \leq \eta\varepsilon_0\)，取对数即得。若需simultaneously控制多个Status的误差（联合界），分子中的
\(1\) 替换为Status数 \(|\mathcal{S}|\)。\(\square\)

\subsubsection{3.3 Corollaries 3：一致可Detection的充分Conditions (Uniform
Detectability)}\label{ux63a8ux8bba-3ux4e00ux81f4ux53efux68c0ux6d4bux7684ux5145ux5206ux6761ux4ef6-uniform-detectability}

如果存在 \(\delta > 0\) 使得对所有Status \(s\) 有
\(\mu_s \leq \frac{K-1}{K} - \delta\)，则令
\(\theta = \frac{1}{2}\)（固定阈值），对所有Status有
\(\Delta_s \geq \frac{\delta}{2}\)，且：

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M\delta^2}{2}\right)\]

\textbf{意义}: 只要每个Status的Clean平均专家Error率一致低于 \((K-1)/K\)（当
\(K=2\) 时为 \(1/2\)），使用固定阈值 \(\theta = 1/2\) 即可获得指数级别的
F1 Guarantee。

\subsubsection{3.4 Corollaries 4：有限Sample校正 (Finite-Sample
Correction)}\label{ux63a8ux8bba-4ux6709ux9650ux6837ux672cux6821ux6b63-finite-sample-correction}

在实践中，\(\mu_s\) 必须From有限验证Data估计。设每个Status \(s\) 有 \(n_s\)
个Clean验证Sample。用
\(\hat{\mu}_s = \frac{1}{M}\sum_m \hat{\varepsilon}_m(s)\) 估计
\(\mu_s\)，where \(\hat{\varepsilon}_m(s)\) is the probability that experts \(m\) 在Status \(s\)
上的经验Error率。

由 Hoeffding 不等式和联合界：对任意 \(\delta_0 \in (0, 1)\)，with probability
\(\geq 1 - \delta_0\)：

\[|\hat{\mu}_s - \mu_s| \leq B \sqrt{\frac{\log(2M/\delta_0)}{2n_s}}, \quad \forall m, s\]

设校正后的阈值为
\(\tilde{\mu}_s = \hat{\mu}_s + B\sqrt{\frac{\log(2M/\delta_0)}{2n_s}}\)（保守Upper Bound）。则
Theorem 1 的Conclusion对校正后的 \(\tilde{\mu}_s\) with probability
\(\geq 1 - \delta_0\) 成立，只需在 \(\Delta_s\) 中用 \(\tilde{\mu}_s\)
替代 \(\mu_s\)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4 紧致性讨论
(Tightness)}\label{ux7d27ux81f4ux6027ux8ba8ux8bba-tightness}

\subsubsection{4.1 何时紧致}\label{ux4f55ux65f6ux7d27ux81f4}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{大 M 极限}: 当
  \(M \to \infty\)，\(C \xrightarrow{\text{a.s.}} \mathbb{E}[C]\)（由大数定律），Detection变成确定性的：\(C > \theta\)
  当且仅当 \(\mathbb{E}[C] > \theta\)。此时 F1 由 \(\eta\) 和 \(\mu_s\)
  完全决定，定理Lower Bound紧致。
\item
  \textbf{StatusHomogeneous性}: 当 (A5)
  严格成立（Status内CleanError率完全均匀）且专家对称，Hoeffding 界的常数因子
  \(2\) YesOptimal的（由 Hoeffding Lower BoundGuarantee）。
\item
  \textbf{\(K \to \infty\)}: 分离间隙 \(\Delta_s \to \frac{1}{2}\)（固定
  \(\mu_s\)），Detection渐近完美。
\end{enumerate}

\subsubsection{4.2 何时松弛}\label{ux4f55ux65f6ux677eux5f1b}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{小 M}: 当 \(M \leq 3\) 时，Hoeffding
  界较松弛。此时应用精确二项式Distribution（而非
  Hoeffding）可得更紧的界。经验规则：\(M \geq 10\) 时 Hoeffding
  界合理紧致；\(M \leq 5\) 时使用精确二项式累积Distribution（Corollary 4.1）。
\item
  \textbf{\(\mu_s\) 接近 \(\frac{K-1}{K}\)}: 当
  \(\mu_s \uparrow (K-1)/K\) 时
  \(\Delta_s \to 0\)，指数衰减因子消失。此时所需专家数
  \(M \propto 1/\Delta_s^2\)
  发散。这Yes本质性的：当专家在新CleanData上接近随机猜测时，NoiseDetection必然不可靠。
\item
  \textbf{弱Characteristics退化}:
  当Status划分None法分离Noise主导区域和Clean主导区域时，(A5) 不成立。此时
  Theorem 1 不适用，需借助交叉验证或外部DistributionDetection（详见 Prop 6
  二层Status发现）。这Yes SCX
  框架的已知边界（参见\emph{弱Characteristics失效}概念文档）。
\item
  \textbf{Noise非均匀}: 当 \(\eta\) Dependencies \(x\)
  时（\(P(\text{noise} \mid x)\) 非常数），F1 的表达式需用
  \(\eta_s = \mathbb{P}(\text{noise} \mid X \in s)\) 替代全局 \(\eta\)：
\end{enumerate}

\[\text{F1} \geq 1 - \frac{1}{\min_s \eta_s} \sum_s \rho_s \exp(-2M\Delta_s^2)\]

在 \(\eta_s \to 0\) 的极端Status，DetectionNone意义------该Status本身没有Noise。

\subsubsection{4.3
紧致性对比Experiment}\label{ux7d27ux81f4ux6027ux5bf9ux6bd4ux5b9eux9a8c}

对典型值 \(K=10\)、\(\eta=0.1\)、\(M=20\)、\(\mu_s=0.2\)，Theorem 1
给出：

\[\Delta_s = \frac{1}{2}(1 - 0.2 \cdot 10/9) \approx 0.389, \quad \text{F1} \geq 1 - 10 \cdot e^{-2\cdot 20 \cdot 0.389^2} \approx 1 - 10 \cdot e^{-6.05} > 0.976\]

\textbf{注意}：上述ComputationAssumptions
\(\mu_s = 0.2\)，即专家在CleanData上的Error率为 20\%。在实际 CIFAR-10
Experiment中（\(M=20\)，\(\eta=0.1\)），SCX-Noise 的 F1 实测为
\(0.617\)。这个值远低于 \(0.976\) 的Lower Bound，Explanation CIFAR-10
Experiment中\textbf{Assumptions不满足}------最可能的ReasonYes：(i) 专家仅在 3 epoch 的
CPU Training后使用，实际 \(\mu_s \approx 0.45\)（而非假定的 \(0.2\)），此时
\(\Delta_s \approx 0.25\)，Lower Bound退化为 \(\text{F1} \geq 0.18\)（与实测
\(0.617 > 0.18\) 一致）；(ii) (A5)
StatusHomogeneous性在低质Characteristics下不严格成立。\textbf{教训}：定理Lower Bound对 \(\mu_s\)
高度敏感，实践中必须在验证集上准确估计 \(\mu_s\) 后再应用Formula。在 AlN v3
全 GPU Training场景中，\(\mu_s\) 可低至
\(0.03\text{--}0.18\)，Lower Bound得以紧致（见正文 Table 2）。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5 与 Dawid-Skene
的关系}\label{ux4e0e-dawid-skene-ux7684ux5173ux7cfb}

\subsubsection{5.1 Dawid-Skene
Model回顾}\label{dawid-skene-ux6a21ux578bux56deux987e}

Dawid 和 Skene (1979) 提出用 EM
AlgorithmFrom多标注者（专家）的独立标注中估计真实标签和标注者可靠性。核心AssumptionsYes每个标注者
\(m\) 有一个\textbf{全局混淆矩阵}：

\[\pi_m(c' \mid c) = \mathbb{P}(f_m(x) = c' \mid y^* = c), \quad \forall x \in \mathcal{X}\]

即标注者的ErrorMode不Dependencies具体Sample \(x\)------这is a全局常数。

最终的标签估计通过加权投票完成：

\[\hat{y}_{\text{DS}} = \arg\max_c \sum_{m=1}^M w_m \cdot \mathbf{1}\{f_m(x) = c\}\]

where权重 \(w_m\) 由 \(\pi_m\)
的对角元素（Accuracy）决定，或更一般地，由混淆矩阵的逆决定。

\subsubsection{5.2 SCX
的改进：StatusConditions可靠性}\label{scx-ux7684ux6539ux8fdbux72b6ux6001ux6761ux4ef6ux53efux9760ux6027}

SCX 将混淆矩阵推广为\textbf{StatusConditions}形式：

\[\pi_m(c' \mid c, s) = \mathbb{P}(f_m(x) = c' \mid y^* = c, x \in s)\]

SCX 权重因此Dependencies于Status：

\[w_m(x) = \frac{\exp(-\alpha \hat{R}_m(s(x)))}{\sum_{m'} \exp(-\alpha \hat{R}_{m'}(s(x)))}\]

where \(\hat{R}_m(s) = \sum_{c} \pi_m(c \mid c, s)\) 为StatusConditions风险估计。

\subsubsection{5.3 SCX 严格优于 Dawid-Skene
的Conditions}\label{scx-ux4e25ux683cux4f18ux4e8e-dawid-skene-ux7684ux6761ux4ef6}

\textbf{Proposition 5.1 (SCX-DS 比较).} 令 \(R_{\text{DS}}\) 为
Dawid-Skene 加权的期望 0-1 损失，\(R_{\text{SCX}}\) 为 SCX
StatusConditions加权的期望损失。则：

\[R_{\text{SCX}} \leq R_{\text{DS}}\]

等号成立当且仅当对每个标注者 \(m\) 和所有Status
\(s, s' \in \mathcal{S}\)，混淆矩阵在Status间不变：

\[\pi_m(\cdot \mid \cdot, s) = \pi_m(\cdot \mid \cdot, s'), \quad \forall s, s' \in \mathcal{S}\]

即标注者可靠性不DependenciesSampleStatus。

\textbf{Proof.} 对任意
\(x \in s\)，加权投票的贝叶斯Optimal权重YesStatusConditions权重
\(w_m^*(s)\)，它最小化点态期望损失：

\[w^*(s) = \arg\min_{w \in \Delta^{M-1}} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\]

Dawid-Skene 使用的全局权重 \(\bar{w}\) Yes常数：

\[\bar{w} = \arg\min_{w} \mathbb{E}_{X, Y^*}[\mathbb{E}[\ell(\text{vote}_w(X), Y^*) \mid X]]\]

由PointwiseOptimal性，对每个 \(x \in s\)：

\[\mathbb{E}[\ell(\text{vote}_{w^*(s)}(x), y^*) \mid x \in s] \leq \mathbb{E}[\ell(\text{vote}_{\bar{w}}(x), y^*) \mid x \in s]\]

在StatusDistribution上取期望即得 \(R_{\text{SCX}} \leq R_{\text{DS}}\)。

等号Conditions由Pointwise不等式的严格性导出：只有当全局权重也Yes每个Status的Optimal权重时等号成立，即
\(\bar{w} \in \arg\min_{w} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\)
对所有 \(s\)
成立。这要求各Status的Optimal权重相同，等价于混淆矩阵不Dependencies于Status。\(\square\)

\subsubsection{5.4
在NoiseDetection语境下的具体Difference}\label{ux5728ux566aux58f0ux68c0ux6d4bux8bedux5883ux4e0bux7684ux5177ux4f53ux5deeux5f02}

Theorem 1
的Detection Rule建立在''一致性''概念上------NoiseSample对所有专家都呈现高Error。这Yes
SCX 框架的特有优势，与 Dawid-Skene 的全局可靠性估计有本质Difference：

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3611}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4722}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Dimension
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Dawid-Skene
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
SCX (Theorem 1)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
可靠性 & 全局常数 \(\pi_m\) & StatusConditions \(\pi_m(s)\) \\
NoiseDetection & Dependencies标注者一致性\emph{后验} & Dependencies多专家Error\emph{一致性} \\
所需标注 & 需要多个(通常 \(\geq 3\))标注者 & 需要多个专家Model \\
抗Category不平衡 & 弱（全局混淆矩阵受主导Category支配） &
强（Status级估计不受影响） \\
区分 Noise vs Hard & 不能（仅给出后验概率） & 能（Consistency Score \(C\)
天然分离两者） \\
TheoryGuarantee & 渐近一致性（\(M \to \infty\)） & 指数Convergence（\(M\) 和 \(K\)
都加速） \\
\end{longtable}

\subsubsection{5.5 Dawid-Skene 作为 SCX
的特例}\label{dawid-skene-ux4f5cux4e3a-scx-ux7684ux7279ux4f8b}

当Status划分退化到平凡划分
\(\mathcal{S} = \{\mathcal{X}\}\)（即所有Sample在一个Status中）时：

\begin{itemize}
\tightlist
\item
  SCX 权重退化为
  \(\bar{w}_m \propto \exp(-\alpha \hat{R}_m)\)，即全局常数权重
\item
  Consistency Score \(C(x)\) 退化为全局平均Error率
\item
  Theorem 1 的DetectionGuarantee退化为Dependencies
  \(\mu = \frac{1}{M}\sum_m \varepsilon_m\) 的全局Guarantee
\end{itemize}

此时 SCX 的NoiseDetection等价于 Dawid-Skene 的全局可靠性加权Method。这Proof了 SCX
框架的严格推广性。

\subsubsection{5.6
改进幅度的量化}\label{ux6539ux8fdbux5e45ux5ea6ux7684ux91cfux5316}

SCX 相对于 Dawid-Skene 的改进幅度与Status间的可靠性变异直接相关：

\[\Delta R = R_{\text{DS}} - R_{\text{SCX}} = \sum_{s \in \mathcal{S}} \rho_s \cdot \mathcal{D}_s\]

where
\(\mathcal{D}_s = \sum_{m=1}^M (\bar{w}_m - w_m^*(s)) \cdot R_m(s)\)
YesStatus \(s\) 上使用全局权重而非StatusConditions权重的额外风险。

由 Proposition 3（StatusConditions权重命题）的定理 3.1：

\[\Delta R \geq \frac{1}{\alpha} \cdot I(s; w)\]

where \(I(s; w) = \mathbb{E}_s[\text{KL}(w(s) \| \bar{w})]\)
YesStatus与权重之间have mutual information。

\textbf{Conclusion}: SCX 严格优于 Dawid-Skene 当且仅当专家可靠性的Status变异
\(I(s; w) > 0\)------这在实践中几乎总Yes成立。Theorem 1 的DetectionGuarantee只Yes
SCX 整体框架优势在NoiseDetection任务上的集中体现。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{References}\label{ux53c2ux8003ux6587ux732e}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  Dawid, A. P., \& Skene, A. M. (1979). Maximum likelihood estimation of
  observer error-rates using the EM algorithm. \emph{Journal of the
  Royal Statistical Society: Series C (Applied Statistics)}, 28(1),
  20-28.
\item
  Hoeffding, W. (1963). Probability inequalities for sums of bounded
  random variables. \emph{Journal of the American Statistical
  Association}, 58(301), 13-30.
\item
  Pollard, D. (1990). Empirical Processes: Theory and Applications.
  \emph{NSF-CBMS Regional Conference Series in Probability and
  Statistics}, 2, i-86.
\item
  Bartlett, P. L., \& Mendelson, S. (2002). Rademacher and Gaussian
  complexities: Risk bounds and structural results. \emph{Journal of
  Machine Learning Research}, 3(Nov), 463-482.
\item
  SCX Framework Definitions.
  \texttt{../definitions/01\_state\_conditioned\_risk.md}.
\item
  Proposition 3: State-Conditioned Expert Weighting.
  \texttt{../propositions/03\_state\_conditioned\_weighting.md}.
\item
  Proposition 4: Compression Fidelity.
  \texttt{../propositions/04\_compression\_fidelity.md}.
\item
  Data防中毒概念. \texttt{../../knowledge/02\_概念/Data防中毒.md}.
\item
  Data四分类概念. \texttt{../../knowledge/02\_概念/Data四分类.md}.
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{附录 A：Chernoff
界的推导}\label{ux9644ux5f55-achernoff-ux754cux7684ux63a8ux5bfc}

Lemma 2 和 Lemma 3 可以使用 Chernoff 界（而非
Hoeffding）获得更紧的指数Rate。

对CleanData，令 \(\{e_m\}\) 为独立的 Bernoulli 变量，均值为
\(\mu = \mathbb{E}[C \mid \text{clean}, x] \leq \mu_s\)。对
\(\theta > \mu\)：

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, x) 
&= \mathbb{P}\!\left(\frac{1}{M}\sum_m e_m > \theta\right) \\
&\leq \inf_{\lambda > 0} e^{-\lambda M\theta} \mathbb{E}\!\left[e^{\lambda\sum_m e_m}\right] \\
&= \inf_{\lambda > 0} e^{-\lambda M\theta} \prod_{m=1}^M (1 - \mu_m + \mu_m e^\lambda) \\
&\leq \exp\!\left(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\right)
\end{aligned}\]

where
\(\text{KL}(p \,\|\, q) = p\log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\)，且最后一步使用了联合界和
\(\mu_m \leq \mu_s\)。

对NoiseData，由 Lemma 3 的修正Proof（给定 \(c\) Conditions下
Hoeffding），Chernoff 形式为：

\[\mathbb{P}(C \leq \theta \mid \text{noise}, x, c) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - \mu_c(x)\right)\right) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\right)\]

注意 KL 的方向：\(\text{KL}(\theta \| q)\) 对应 Chernoff Upper Bound
\(\mathbb{P}(\hat{p} \leq \theta) \leq \exp(-n \cdot \text{KL}(\theta \| p))\)，where
\(\hat{p}\) Yes \(p\) 的Sample均值。这里
\(p = \mathbb{E}[C \mid \text{noise}] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\)，我们
bound \(\mathbb{P}(C \leq \theta)\)（下尾），所以Part二个ParameterYes真实期望
\(p \approx 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\)。

\begin{quote}
\textbf{2026-06-27 修正}：原版 Chernoff 附录使用了
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\)，KL 方向有误。Correct形式为
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\)------下尾
bound 中Part一个ParameterYes阈值 \(\theta\)，Part二个ParameterYes真实期望。
\end{quote}

Chernoff 界优于 Hoeffding 界的程度由以下不等式刻画（Hoeffding Yes
Chernoff 的一阶近似):

\[\exp(-2M\Delta^2) \geq \exp(-M \cdot \text{KL}(\mu + \Delta \,\|\, \mu))\]

等号仅当 \(\Delta \to 0\)
时渐近成立。在实际Parameter范围（\(\Delta \approx 0.1\text{-}0.4\)）内，Chernoff
界可紧 \(2\text{-}5\) 倍。

\begin{quote}
\textbf{DEFECT-06 同步 (2026-06-28) --- Bahadur-Rao 格点修正}:
当专家Error为 Bernoulli Distribution（格点跨度 \(h=1\)）时，Correct的 Bahadur-Rao
展开在 Chernoff 界中引入因子 \((1-e^{-\lambda^*})^{-1}\) 替代
\(1/\lambda^*\)。修正后的极小极大常数 \(C_{\min}^{\text{(corr)}}\)
与原始 \(C_{\min}\) 相差约
\(5\)--\(12\%\)，极小极大Optimal性Conclusion不变（可达界与Lower Bound接受相同的格点修正因子）。详细推导见
\texttt{06\_fixed\_point\_convergence.md} §12（Remark on Bahadur-Rao
Lattice Correction）。
\end{quote}

\subsection{附录 B：非 0-1
损失的推广}\label{ux9644ux5f55-bux975e-0-1-ux635fux5931ux7684ux63a8ux5e7f}

Theorem 1 对一般Bounded Loss \(\ell \in [0, B]\) 也成立，只需将 Lemma 1 中的
\(e_m\) 解释为
\(\mathbf{1}\{\ell(f_m(x), y) > \tau\}\)。核心推导不变，因为 Lemma 1
仅DependenciesConditions期望的线性性质和Noise标签的均匀性Assumptions (A4)，不Dependencies 0-1
损失的具体形式。

具体地，对回归任务（\(\mathcal{Y} \subseteq \mathbb{R}^d\)），\(K\)
的概念需重新解释。Lemma 1 中的 \(K\) 在回归场景下退化为：

\[ \mathbb{E}[C \mid \text{noise}, x] \geq 1 - \mathbb{E}[C \mid \text{clean}, x] \]

即 \(K \to \infty\) 的极限情况。此时分离间隙对任意 \(\mu_s < 1/2\)
为正。这Yes因为在连续标签空间中，专家恰好预测到Noise标签的概率为零，使NoiseDetection更容易。Theorem
1 的其余部分不变。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Changelog}\label{changelog}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1935}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2258}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2581}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3226}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Date
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Issue
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Change
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Severity
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
2026-06-28 & B1 & \textbf{Fixed A2 self-contradiction.} Updated A2
rationale to acknowledge A1 does NOT guarantee A2. Added
\(M_{\text{eff}}\) degradation formula and cross-reference to
\texttt{01\_symbol\_system.md} §12.5. & BLOCKING \\
2026-06-28 & B6 & \textbf{Added \(M \geq 1\), \(K \geq 2\) to Theorem 1
preamble.} Explicitly excludes degenerate edge cases where consensus
score is undefined or \(K-1=0\). & BLOCKING \\
2026-06-28 & B10 & \textbf{Synced DEFECT-06 (Bahadur-Rao lattice
correction)} to Theorem 1 appendix. Added cross-reference to
\texttt{06\_fixed\_point\_convergence.md} §12. & BLOCKING \\
\end{longtable}

\end{document}
