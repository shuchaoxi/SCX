% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Theorem 1: Multi-Expert Consistency Guarantees for Label Noise
Detection}\label{theorem-1-multi-expert-consistency-guarantees-for-label-noise-detection}

\begin{quote}
\textbf{Core Claim}: when  \(M\)
个at non-相交Data子集on 独立Training's专家for 某个Sample'sConsistency Score \(C(x)\)
超过阈值 \(\theta\) 时，该SampleYes标签Noise's置信度with 指数RateConvergenceto  1。

\textbf{Revision Record}: 2026-06-27 --- Lemma 3 Proof重构（新增 A6
Balanced Error DistributionAssumptions）；Chernoff 附录 KL 方toward 修正；主Theorem StatementUpdateas 
(A1)-(A6)。两个 bug all 来自验证报告。
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1 Theorem Statement}\label{ux5b9aux7406ux9648ux8ff0}

\subsubsection{1.1 Symboland 设定
(Setup)}\label{ux7b26ux53f7ux4e0eux8bbeux5b9a-setup}

Define the following objects:

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Symbol
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Meaning
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\(\mathcal{X}\) & Input space, measurable \\
\(\mathcal{Y}\) & Label space,\(|\mathcal{Y}| = K\)（classification) or
\(\mathcal{Y} \subseteq \mathbb{R}^d\)（regression) \\
\(\mathcal{D}\) & Data distribution,\((X, Y) \sim \mathcal{D}\) \\
\(f^* : \mathcal{X} \to \mathcal{Y}\) & True labeling function (unknown Oracle) \\
\(\{f_m\}_{m=1}^M\) & \(M\)
expert models,\(f_m : \mathcal{X} \to \mathcal{Y}\) \\
\(\Pi = \{s_1, \dots, s_K\}\) & Status划分，\(\mathcal{X}\) measurable partition of \\
\(\ell : \mathcal{Y} \times \mathcal{Y} \to [0, B]\) & Bounded loss function \\
\(\tau > 0\) & Expert error threshold \\
\(\theta \in (0, 1)\) & Noise detection threshold \\
\end{longtable}

\textbf{Label Noise Model}: for 于each 个Sample \((x, y^*)\)（where \(y^* = f^*(x)\)
is the true label), the observed label \(y\) is generated as follows:

\[y = \begin{cases}
y^* & \text{with probability } 1 - \eta \\
\text{Uniform}(\mathcal{Y} \setminus \{y^*\}) & \text{with probability } \eta
\end{cases}\]

where \(\eta \in (0, 1/2)\) is the global noise rate, and noise events are independent of \(x\)
and all training data.

\textbf{Consistency Score}: for Sample
\((x, y)\)，定义专家Error指示变量和Consistency Score：

\[e_m(x, y) = \mathbf{1}\{\ell(f_m(x), y) > \tau\}, \quad
C(x) = \frac{1}{M} \sum_{m=1}^M e_m(x, y)\]

\textbf{Detection Rule}: Sample \((x, y)\) is flagged as noise if and only if
\(C(x) > \theta\)。

\subsubsection{1.2 Assumptions (Assumptions)}\label{ux5047ux8bbe-assumptions}

\textbf{(A1) Disjoint Training Sets}: \(M\) 个专家at  \(M\)
个non-相交's独立同Distribution子集on Training：

\[D_m \sim \mathcal{D}^{n_m}, \quad D_m \cap D_{m'} = \varnothing, \quad D_m \perp D_{m'} \text{ for } m \neq m'\]

\textbf{(A2) CleanDataon 'sConditional Independence}: for ArbitraryCleanSample
\((x, y)\)（\(y = y^*\)），give 定 \(x\) 'sconditionsunder ，Error指示变量
\(\{e_m(x, y)\}_{m=1}^M\) YesConditional Independence's。

\emph{JustificationExplanationand 局限}: A2
Yes一种\textbf{Structure性Assumptions}，by 专家Experiment设计（Disjoint Training Sets
A1、独立初始化）which 论证，but \textbf{A1 并non-蕴含
A2}。i.e.make Training集complete non-相交，at 相似DataDistributionon Training's专家can be can at Distribution外Sampleon 产生\textbf{相关Error}（源于共享归纳偏置，non-共享TrainingData）。because 此
A2 at  SCX 流水线'sDatain \textbf{non-can be via 验检验}------for 连续输入空间
\(\mathcal{X}\)，each 个 \(x\) at has 限datasetin 最多出现一次，联合Distribution
\(P(e_1, \dots, e_M \mid x)\) non-can be 观测。 \textgreater{} \textgreater{}
\textbf{When A2 is 违反时}（实际in 常见):using 
\(M_{\text{eff}} = M/(1 + (M-1)\bar{\rho})\) 替换which has concentration inequalitiesin 's
\(M\)，where \(\bar{\rho}\)
as 专家Error指标's平all 成for 相关性。for 典型深度集成，\(\bar{\rho} \approx 0.1\)--\(0.3\)。建议at 留置验证集on 估计
\(\bar{\rho}\)，并using  \(M_{\text{eff}}\) 得to 保守Guarantee。详细Analysis参见
\texttt{01\_symbol\_system.md} §12.5。

\textbf{(A3) Bounded Loss}:
\(\ell(a, b) \in [0, B], \; \forall a, b \in \mathcal{Y}\)，where
\(B < \infty\)。

\textbf{(A4) Uniform Independent Noise}: 标签翻转事件and  \(x\) 和which has  \(D_m\)
独立。Noise标签at  \(\mathcal{Y} \setminus \{y^*\}\) on all 匀Distribution。

\textbf{(A5) StatusHomogeneous性 (Status划分充分性)}: Status划分 \(\Pi\)
make 得at each 个Status \(s\)
内，专家'sCleanDataError率近似all 匀。形式化地，存at state-level常数
\(\{\mu_s\}_{s \in \mathcal{S}}\) make 得：

\[\sup_{x \in s} \, \mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s, \quad \forall s \in \mathcal{S}\]

i.e.Status \(s\) 内CleanSample's平all 专家Error率is  \(\mu_s\) 一致Upper Bound控制。

\textbf{(A6) Balanced Error Distribution}:
专家'serror probabilityat which has ErrorCategoryon non-can 过度集in 。存at 常数
\(C_{\text{bal}} \geq 1\) make 得for ArbitraryStatus \(s\) 和 \(x \in s\)：

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\(C_{\text{bal}} = 1\)
for should complete all 匀'sErrorDistribution（each 个ErrorCategoryetc.概率）；\(C_{\text{bal}} = 2\)
Yes典型's保守Selection。此Assumptionscan be 检验and at 实践in 几乎自动满足------at non-相交CleanDataon Training's专家没has 理by will Error集in to 特定Category。

\subsubsection{1.3 Key Lemma
(Mean Separation)}\label{ux5173ux952eux5f15ux7406-ux5747ux503cux5206ux79bb}

\textbf{Lemma 1 (Mean Separation).} at Assumptions (A1)-(A5) under （A6
non-影响期望），for Arbitrary \(x \in s\)：

\textbf{(CleanSample)}
\[\mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s\]

\textbf{(NoiseSample)}
\[\mathbb{E}[C(X) \mid \text{noise}, X = x] = 1 - \frac{1}{K-1} \cdot \mathbb{E}[C(X) \mid \text{clean}, X = x] \geq 1 - \frac{\mu_s}{K-1}\]

because 此，when  \(\mu_s < \frac{K-1}{K}\) 时，存at  \(\theta\) make 得：

\[\mathbb{E}[C \mid \text{clean}] < \theta < \mathbb{E}[C \mid \text{noise}]\]

and 分离间隙as ：

\[\Delta_s(\theta) = \min\left(\theta - \mu_s, \; 1 - \frac{\mu_s}{K-1} - \theta\right)\]

when SelectionOptimal阈值
\(\theta_s^* = \frac{1}{2}\left(1 + \mu_s \cdot \frac{K-2}{K-1}\right)\)
时，分离间隙最大化（at  \(C_{\text{bal}}=1\) 's理想情况under ):

\[\Delta_s^* = \frac{1}{2}\left(1 - \mu_s \cdot \frac{K}{K-1}\right)\]

when  \(C_{\text{bal}} > 1\) 时，Optimal阈值toward  \(\mu_s\)
方toward 微调with 补偿non-all 匀误差Distribution。\(\theta_s^*\) 's精确表达式requires 求解
\(\theta - \mu_s = 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta\)。

\textbf{Proof.} CleanSample's界by  (A5) 直接give 出。for NoiseSample：

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, X = x] 
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(\ell(f_m(x), y) > \tau \;\big|\; \text{noise}, x\bigr) \\
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) \neq y \;\big|\; \text{noise}, x\bigr) \quad (\text{for  } 0\text{-}1 \text{ 损失}) \\
&= 1 - \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) = y \;\big|\; \text{noise}, x\bigr)
\end{aligned}\]

by  (A4)，Noise标签 \(y\) at  \(\mathcal{Y} \setminus \{y^*\}\)
on all 匀Distribution，and 独立于which has 专家：

\[\begin{aligned}
\mathbb{P}(f_m(x) = y \mid \text{noise}, x) 
&= \sum_{c \neq y^*} \mathbb{P}(y = c \mid \text{noise}) \cdot \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{P}(f_m(x) \neq y^* \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{E}[e_m \mid \text{clean}, x]
\end{aligned}\]

because 此：

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, x] 
&= 1 - \frac{1}{K-1} \cdot \frac{1}{M} \sum_{m=1}^M \mathbb{E}[e_m \mid \text{clean}, x] \\
&= 1 - \frac{1}{K-1} \cdot \mathbb{E}[C \mid \text{clean}, x] \\
&\geq 1 - \frac{\mu_s}{K-1} \quad \text{by  (A5)}
\end{aligned}\]

分离间隙's存at 性by  \(\mu_s < \frac{K-1}{K}\) Guarantee，此时
\(1 - \frac{\mu_s}{K-1} > \mu_s\)。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsubsection{1.4 Main Result (Main
Result)}\label{ux4e3bux5b9aux7406-main-result}

\textbf{Theorem 1 (SCX NoiseDetectionGuarantee).} 设Assumptions (A1)-(A6) 成立，and 
\(M \geq 1\)（to 少一个专家），\(K \geq 2\)（to 少两个标签Category，with Guarantee
\(K-1 > 0\)）。令 \(\rho_s = \mathbb{P}(X \in s)\)
as Status概率。for Arbitrary阈值 \(\theta\) 满足
\(\mu_s < \theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) 'sStatus
\(s\)，定义state-level分离间隙：

\[\Delta_s = \min\left(\theta - \mu_s,\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right) > 0\]

when  \(C_{\text{bal}} = 1\)（最佳情形）时，\(\Delta_s\) 恢复原始定义；when 
\(C_{\text{bal}} > 1\)
时，Noise侧's间隙收窄（because as Errorcan be can non-Yescomplete all 匀Distribution's）。

then  SCX NoiseDetection器's F1 得分满足with under lower bound：

\[\text{F1} \;\geq\; 1 - \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-2M\Delta_s^2\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-2M\Delta_s^2\bigr) \Bigr]\]

or etc.价地：

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\!\bigl(-2M\Delta_s^2\bigr)\]

where \(\eta\) as 全局Noise率。

\textbf{更紧's Chernoff 形式}: for 相同设定，make using  Chernoff 界代替
Hoeffding can be 得更紧's指数Rate：

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\bigr) \Bigr]\]

where
\(\text{KL}(p \,\|\, q) = p \log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\)。Chernoff
界at  \(\mu_s\) 接近 \(\theta\) or 
\(1 - C_{\text{bal}} \cdot \mu_s/(K-1)\) 接近 \(\theta\) 时显著优于
Hoeffding（can be 紧 2-5 倍）。

\begin{quote}
\textbf{2026-06-27 修正}：原版 Chernoff make using 了
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\)，KL 方toward has 误，and not yet 纳入
\(C_{\text{bal}}\)。现already 修正as 
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\)。
\end{quote}

\textbf{Key Corollary}: when  \(M \to \infty\) 时，for which has 满足
\(\mu_s < \frac{K-1}{K}\) 'sStatus，F1 with 指数RateConvergenceto  1：

\[\text{F1} \;=\; 1 - \mathcal{O}_P\!\left(\frac{1}{\eta} \cdot e^{-2M\Delta_{\min}^2}\right)\]

where \(\Delta_{\min} = \min_{s \in \mathcal{S}} \Delta_s\)
as 最差Status分离间隙。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2 Proof}\label{ux8bc1ux660e}

\subsubsection{2.1 False Positive Rate on Clean Data (False Positive
Rate)}\label{ux6e05ux6d01ux6570ux636eux7684ux5047ux9633ux6027ux7387-false-positive-rate}

\textbf{Lemma 2 (FPR Upper Bound).} for ArbitraryStatus \(s \in \mathcal{S}\) 满足
\(\mu_s < \theta\)，CleanSampleis 误标as Noise's概率满足：

\[\mathbb{P}(C > \theta \mid \text{clean}, X \in s) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

\textbf{Proof.} 固定 \(x \in s\)，by  (A5) 知
\(\mathbb{E}[C \mid \text{clean}, X = x] \leq \mu_s\)。by 
(A1)-(A2)，give 定 \(x\) 时 \(\{e_m\}\) Conditional Independence。by  (A3) 知
\(e_m \in [0, 1]\)。for  \(C = \frac{1}{M}\sum_m e_m\) should using  Hoeffding
non-etc.式（Pollard, 1990):

\[\mathbb{P}(C - \mathbb{E}[C] > \theta - \mu_s \mid \text{clean}, x) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

by 于 \(\theta > \mu_s\)，\((\theta - \mu_s) > 0\)。该界for Arbitrary
\(x \in s\) 成立，because 此：

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, X \in s) 
&= \mathbb{E}_{X \mid s}\bigl[ \mathbb{P}(C > \theta \mid \text{clean}, X) \bigr] \\
&\leq \sup_{x \in s} \mathbb{P}(C > \theta \mid \text{clean}, x) \\
&\leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)
\end{aligned}\]

此i.e.which requires 。\(\square\)

\subsubsection{2.2 True Positive Rate on Noisy Data (True Positive
Rate)}\label{ux566aux58f0ux6570ux636eux7684ux771fux9633ux6027ux7387-true-positive-rate}

\textbf{新增Assumptions (A6) --- Balanced Error Distribution}: for ArbitraryStatus \(s\) 和Sample
\(x \in s\)，专家'serror probabilityat which has ErrorCategoryon non-can 过度集in 。形式化地，存at 常数
\(C_{\text{bal}} \geq 1\) make 得：

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \text{where } \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\emph{Justification}：专家at non-相交'sCleanData子集on Training，没has 理by will Error集in to 特定'sErrorCategory。all 匀NoiseModelunder ，each 个ErrorCategory's概率天然约as 
\(\mu_s/(K-1)\)。\(C_{\text{bal}} = 2\)
already Yes保守Selection。此Assumptionscan be 检验：at 实际Dataon 估计
\(\max_c \mu_c(x) / (\mu_s/(K-1))\)。

\textbf{Lemma 3 (TPR lower bound).} at Assumptions (A1)-(A6) under ，for ArbitraryStatus
\(s \in \mathcal{S}\) 满足
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\)，NoiseSampleis CorrectDetection's概率满足：

\[\mathbb{P}(C > \theta \mid \text{noise}, X \in s) \geq 1 - \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)\]

\textbf{Proof}：固定 \(x \in s\)。give 定Noise标签
\(y = c\)（\(c \neq y^*\)），for 于 0-1 损失，专家 \(m\) 'sError指示as 
\(e_m = \mathbf{1}\{f_m(x) \neq c\}\)。by 于 \(f_m\) only dependence
\(D_m\)（A1），give 定 \(x\) 时 \(\{f_m(x)\}\) Conditional Independence（A2），because 此
\(\{e_m\}\) at give 定 \((x, c)\) under Conditional Independence。

each 个 \(e_m\) 's期望as 
\(\mathbb{E}[e_m \mid x, c] = 1 - \mathbb{P}(f_m(x) = c \mid x) = 1 - \mu_{c,m}(x)\)。because 此：

\[\mathbb{E}[C \mid x, c] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} \quad \text{（by  A6）}\]

by 于 \(e_m \in [0, 1]\) Conditional Independence，should using  Hoeffding non-etc.式：

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid x, c)
&= \mathbb{P}\!\left(\frac{1}{M} \sum_{m=1}^M e_m \leq \theta \;\Big|\; x, c\right) \\
&\leq \exp\!\left(-2M\left(1 - \mu_c(x) - \theta\right)^2\right) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

where Hoeffding 's gap conditions \(1 - \mu_c(x) > \theta\) by 定理Assumptions
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) 和 A6 Guarantee：
\(1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} > \theta\)。

for Noise标签 \(c\) 取平all （all 匀Distribution over \(K-1\) 类):

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid \text{noise}, x)
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(C \leq \theta \mid x, c) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

because 此
\(\mathbb{P}(C > \theta \mid \text{noise}, x) \geq 1 - \exp(-2M(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta)^2)\)。for 
\(x \in s\) 取期望i.e.得。\(\square\)

\textbf{Remark}：when  \(C_{\text{bal}} = 1\)（complete all 匀'sErrorDistribution）时，Lemma
3 恢复原始命题in 'sOptimal bound。\(C_{\text{bal}}\)
can be at 实际Dataon 估计，并using 于校准Detection阈值 \(\theta\)。

\subsubsection{2.3 F1 lower bound}\label{f1-ux4e0bux754c}

现at 组合 Lemma 2 和 Lemma 3 推导 F1 lower bound。

令Detection Ruleas  \(R(x) = \mathbf{1}\{C(x) > \theta\}\)。定义：

\[\begin{aligned}
\text{TPR}_s &= \mathbb{P}(R = 1 \mid \text{noise}, X \in s) \geq 1 - \exp(-2M(1 - \mu_s/(K-1) - \theta)^2) \\
\text{FPR}_s &= \mathbb{P}(R = 1 \mid \text{clean}, X \in s) \leq \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

by  (A4)，Noise事件and  \(X\) 独立，because 此
\(\mathbb{P}(\text{noise} \mid X \in s) = \eta\) for which has Status成立。整体
TPR 和 FPR as ：

\[\begin{aligned}
\text{TPR} &= \sum_s \rho_s \cdot \text{TPR}_s, \quad
\text{FPR} = \sum_s \rho_s \cdot \text{FPR}_s
\end{aligned}\]

F1 得分as ：

\[\begin{aligned}
\text{F1} &= \frac{2 \cdot \text{TP} }{2 \cdot \text{TP} + \text{FP} + \text{FN}} \\
&= \frac{2\eta \cdot \text{TPR}}{2\eta \cdot \text{TPR} + (1-\eta) \cdot \text{FPR} + \eta \cdot (1 - \text{TPR})} \\
&= \frac{2\eta \cdot \text{TPR}}{\eta(1 + \text{TPR}) + (1-\eta) \cdot \text{FPR}}
\end{aligned}\]

代入 TPR \(\geq 1 - \delta_1\) 和 FPR \(\leq \delta_2\)，where：

\[\begin{aligned}
\delta_1 &= \sum_s \rho_s \cdot \exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \\
\delta_2 &= \sum_s \rho_s \cdot \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

得to ：

\[\begin{aligned}
\text{F1} &\geq \frac{2\eta(1 - \delta_1)}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&= 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&\geq 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta} \quad (\text{because 分母 } \geq \eta) \\
&= 1 - \delta_1 - \frac{1-\eta}{\eta}\delta_2
\end{aligned}\]

代入 \(\delta_1, \delta_2\) 's表达式，并注意when 
\(\Delta_s = \min(\theta - \mu_s, \; 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)\)
时has  \(\exp(-2M(\theta - \mu_s)^2) \leq \exp(-2M\Delta_s^2)\) 和
\(\exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \leq \exp(-2M\Delta_s^2)\)，because 此：

\[ \text{F1} \geq 1 - \sum_s \rho_s \left[\exp(-2M\Delta_s^2) + \frac{1-\eta}{\eta}\exp(-2M\Delta_s^2)\right] = 1 - \frac{1}{\eta}\sum_s \rho_s \exp(-2M\Delta_s^2) \]

证毕。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3 Corollaries (Corollaries)}\label{ux63a8ux8bba-corollaries}

\subsubsection{3.1 Corollaries 1：Symmetric Experts (Symmetric
Experts)}\label{ux63a8ux8bba-1ux5bf9ux79f0ux4e13ux5bb6-symmetric-experts}

when which has 专家具has 相同'sCleanDataError率
\(\varepsilon\)（称as ''Symmetric Experts''）时，\(\mu_s = \varepsilon\)
for which has Status一致。此时：

\[\Delta = \frac{1}{2}\left(1 - \varepsilon \cdot \frac{K}{K-1}\right)\]

and  F1 lower bound简化as ：

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M}{2}\left(1 - \frac{\varepsilon K}{K-1}\right)^2\right)\]

when  \(K = 2\)（二分类）时，进一步简化as ：

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-2M\left(\frac{1}{2} - \varepsilon\right)^2\right)\]

\textbf{意义}: at 二分类Symmetric Experts场景under ，Detection质量complete by 专家Error率
\(\varepsilon\) and  \(1/2\) 's差距决定。\(\varepsilon\) each 远离 \(1/2\)
一个标准差（\(\approx 1/\sqrt{2M}\)），F1 lower bound提升一个指数数量级。

\subsubsection{3.2 Corollaries 2：Optimal Threshold Selection (Optimal
Threshold)}\label{ux63a8ux8bba-2ux6700ux4f18ux9608ux503cux9009ux62e9-optimal-threshold}

for give 定'sNoise率 \(\eta\)、Category数 \(K\)、专家数 \(M\) 和最大CleanError率
\(\mu_{\max} = \max_s \mu_s\)，OptimalDetection阈值 \(\theta^*\) should 满足：

\[\theta^* = \arg\max_\theta \min_s \Delta_s(\theta) = \frac{1}{2}\left(1 + \mu_{\max} \cdot \frac{K-2}{K-1}\right)\]

此时最小分离间隙as ：

\[\Delta_{\min}^* = \frac{1}{2}\left(1 - \mu_{\max} \cdot \frac{K}{K-1}\right)\]

which requires 's最少专家数（达to  F1 \(\geq 1 - \varepsilon_0\)）as ：

\[M \geq \frac{1}{2\Delta_{\min}^{*2}} \log\left(\frac{1}{\eta \varepsilon_0}\right)\]

\textbf{Proof}: 直接解
\(1 - \frac{1}{\eta}e^{-2M\Delta^2} \geq 1 - \varepsilon_0\) 得
\(e^{-2M\Delta^2} \leq \eta\varepsilon_0\)，取for 数i.e.得。if requires simultaneously控制多个Status's误差（联合界），分子in 's
\(1\) 替换as Status数 \(|\mathcal{S}|\)。\(\square\)

\subsubsection{3.3 Corollaries 3：一致can be Detection's充分conditions (Uniform
Detectability)}\label{ux63a8ux8bba-3ux4e00ux81f4ux53efux68c0ux6d4bux7684ux5145ux5206ux6761ux4ef6-uniform-detectability}

such as 果存at  \(\delta > 0\) make 得for which has Status \(s\) has 
\(\mu_s \leq \frac{K-1}{K} - \delta\)，then 令
\(\theta = \frac{1}{2}\)（固定阈值），for which has Statushas 
\(\Delta_s \geq \frac{\delta}{2}\)，and ：

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M\delta^2}{2}\right)\]

\textbf{意义}: only must each 个Status'sClean平all 专家Error率一致低于 \((K-1)/K\)（when 
\(K=2\) 时as  \(1/2\)），make using 固定阈值 \(\theta = 1/2\) i.e.can be 获得指数级别's
F1 Guarantee。

\subsubsection{3.4 Corollaries 4：has 限Sample校正 (Finite-Sample
Correction)}\label{ux63a8ux8bba-4ux6709ux9650ux6837ux672cux6821ux6b63-finite-sample-correction}

at 实践in ，\(\mu_s\) 必须Fromhas 限验证Data估计。设each 个Status \(s\) has  \(n_s\)
个Clean验证Sample。using 
\(\hat{\mu}_s = \frac{1}{M}\sum_m \hat{\varepsilon}_m(s)\) 估计
\(\mu_s\)，where \(\hat{\varepsilon}_m(s)\) is the probability that experts \(m\) at Status \(s\)
on 'svia 验Error率。

by  Hoeffding non-etc.式和联合界：for Arbitrary \(\delta_0 \in (0, 1)\)，with probability
\(\geq 1 - \delta_0\)：

\[|\hat{\mu}_s - \mu_s| \leq B \sqrt{\frac{\log(2M/\delta_0)}{2n_s}}, \quad \forall m, s\]

设校正后's阈值as 
\(\tilde{\mu}_s = \hat{\mu}_s + B\sqrt{\frac{\log(2M/\delta_0)}{2n_s}}\)（保守Upper Bound）。then 
Theorem 1 'sConclusionfor 校正后's \(\tilde{\mu}_s\) with probability
\(\geq 1 - \delta_0\) 成立，only requires at  \(\Delta_s\) in using  \(\tilde{\mu}_s\)
替代 \(\mu_s\)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4 紧致性讨论
(Tightness)}\label{ux7d27ux81f4ux6027ux8ba8ux8bba-tightness}

\subsubsection{4.1 何时紧致}\label{ux4f55ux65f6ux7d27ux81f4}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{大 M 极限}: when 
  \(M \to \infty\)，\(C \xrightarrow{\text{a.s.}} \mathbb{E}[C]\)（by 大数定律），Detection变成确定性's：\(C > \theta\)
  when and only when  \(\mathbb{E}[C] > \theta\)。此时 F1 by  \(\eta\) 和 \(\mu_s\)
  complete 决定，定理lower bound紧致。
\item
  \textbf{StatusHomogeneous性}: when  (A5)
  严格成立（Status内CleanError率complete all 匀）and 专家for 称，Hoeffding 界's常数because 子
  \(2\) YesOptimal's（by  Hoeffding lower boundGuarantee）。
\item
  \textbf{\(K \to \infty\)}: 分离间隙 \(\Delta_s \to \frac{1}{2}\)（固定
  \(\mu_s\)），Detection渐近完美。
\end{enumerate}

\subsubsection{4.2 何时松弛}\label{ux4f55ux65f6ux677eux5f1b}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{小 M}: when  \(M \leq 3\) 时，Hoeffding
  界较松弛。此时should using 精确二项式Distribution（while non-
  Hoeffding）can be 得更紧's界。via 验规then ：\(M \geq 10\) 时 Hoeffding
  界合理紧致；\(M \leq 5\) 时make using 精确二项式累积Distribution（Corollary 4.1）。
\item
  \textbf{\(\mu_s\) 接近 \(\frac{K-1}{K}\)}: when 
  \(\mu_s \uparrow (K-1)/K\) 时
  \(\Delta_s \to 0\)，指数衰减because 子消失。此时which requires 专家数
  \(M \propto 1/\Delta_s^2\)
  发散。this Yes本质性's：when 专家at 新CleanDataon 接近随机猜测时，NoiseDetection必然non-can be 靠。
\item
  \textbf{弱feature退化}:
  when Status划分No法分离Noise主导区域和Clean主导区域时，(A5) non-成立。此时
  Theorem 1 non-适using ，requires 借助交叉验证or 外部DistributionDetection（详见 Prop 6
  二层state discovery）。this Yes SCX
  框架'salready 知边界（参见\emph{弱feature失效}概念文档）。
\item
  \textbf{Noisenon-all 匀}: when  \(\eta\) dependence \(x\)
  时（\(P(\text{noise} \mid x)\) non-常数），F1 's表达式requires using 
  \(\eta_s = \mathbb{P}(\text{noise} \mid X \in s)\) 替代全局 \(\eta\)：
\end{enumerate}

\[\text{F1} \geq 1 - \frac{1}{\min_s \eta_s} \sum_s \rho_s \exp(-2M\Delta_s^2)\]

at  \(\eta_s \to 0\) 's极端Status，DetectionNo意义------该Status本身没has Noise。

\subsubsection{4.3
紧致性for 比Experiment}\label{ux7d27ux81f4ux6027ux5bf9ux6bd4ux5b9eux9a8c}

for 典型值 \(K=10\)、\(\eta=0.1\)、\(M=20\)、\(\mu_s=0.2\)，Theorem 1
give 出：

\[\Delta_s = \frac{1}{2}(1 - 0.2 \cdot 10/9) \approx 0.389, \quad \text{F1} \geq 1 - 10 \cdot e^{-2\cdot 20 \cdot 0.389^2} \approx 1 - 10 \cdot e^{-6.05} > 0.976\]

\textbf{注意}：on 述ComputationAssumptions
\(\mu_s = 0.2\)，i.e.专家at CleanDataon 'sError率as  20\%。at 实际 CIFAR-10
Experimentin （\(M=20\)，\(\eta=0.1\)），SCX-Noise 's F1 实测as 
\(0.617\)。this 个值远低于 \(0.976\) 'slower bound，Explanation CIFAR-10
Experimentin \textbf{Assumptionsnon-满足}------最can be can 'sReasonYes：(i) 专家only at  3 epoch 's
CPU Training后make using ，实际 \(\mu_s \approx 0.45\)（while non-假定's \(0.2\)），此时
\(\Delta_s \approx 0.25\)，lower bound退化as  \(\text{F1} \geq 0.18\)（and 实测
\(0.617 > 0.18\) 一致）；(ii) (A5)
StatusHomogeneous性at 低质featureunder non-严格成立。\textbf{教训}：定理lower boundfor  \(\mu_s\)
高度敏感，实践in 必须at 验证集on 准确估计 \(\mu_s\) 后again should using Formula。at  AlN v3
全 GPU Training场景in ，\(\mu_s\) can be 低to 
\(0.03\text{--}0.18\)，lower bound得with 紧致（见正文 Table 2）。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5 and  Dawid-Skene
's关系}\label{ux4e0e-dawid-skene-ux7684ux5173ux7cfb}

\subsubsection{5.1 Dawid-Skene
Model回顾}\label{dawid-skene-ux6a21ux578bux56deux987e}

Dawid 和 Skene (1979) 提出using  EM
AlgorithmFrom多标注者（专家）'s独立标注in 估计真实标签和标注者can be 靠性。核心AssumptionsYeseach 个标注者
\(m\) has 一个\textbf{全局混淆矩阵}：

\[\pi_m(c' \mid c) = \mathbb{P}(f_m(x) = c' \mid y^* = c), \quad \forall x \in \mathcal{X}\]

i.e.标注者'sErrorModenon-dependence具体Sample \(x\)------this is a全局常数。

最终's标签估计通过加权投票完成：

\[\hat{y}_{\text{DS}} = \arg\max_c \sum_{m=1}^M w_m \cdot \mathbf{1}\{f_m(x) = c\}\]

where权重 \(w_m\) by  \(\pi_m\)
'sfor 角元素（Accuracy）决定，or 更一般地，by 混淆矩阵's逆决定。

\subsubsection{5.2 SCX
's改进：Statusconditionscan be 靠性}\label{scx-ux7684ux6539ux8fdbux72b6ux6001ux6761ux4ef6ux53efux9760ux6027}

SCX will 混淆矩阵推广as \textbf{Statusconditions}形式：

\[\pi_m(c' \mid c, s) = \mathbb{P}(f_m(x) = c' \mid y^* = c, x \in s)\]

SCX 权重because 此dependence于Status：

\[w_m(x) = \frac{\exp(-\alpha \hat{R}_m(s(x)))}{\sum_{m'} \exp(-\alpha \hat{R}_{m'}(s(x)))}\]

where \(\hat{R}_m(s) = \sum_{c} \pi_m(c \mid c, s)\) as Statusconditions风险估计。

\subsubsection{5.3 SCX 严格优于 Dawid-Skene
'sconditions}\label{scx-ux4e25ux683cux4f18ux4e8e-dawid-skene-ux7684ux6761ux4ef6}

\textbf{Proposition 5.1 (SCX-DS 比较).} 令 \(R_{\text{DS}}\) as 
Dawid-Skene 加权's期望 0-1 损失，\(R_{\text{SCX}}\) as  SCX
Statusconditions加权's期望损失。then ：

\[R_{\text{SCX}} \leq R_{\text{DS}}\]

etc.号成立when and only when for each 个标注者 \(m\) 和which has Status
\(s, s' \in \mathcal{S}\)，混淆矩阵at Status间non-变：

\[\pi_m(\cdot \mid \cdot, s) = \pi_m(\cdot \mid \cdot, s'), \quad \forall s, s' \in \mathcal{S}\]

i.e.标注者can be 靠性non-dependenceSampleStatus。

\textbf{Proof.} for Arbitrary
\(x \in s\)，加权投票's贝叶斯Optimal权重YesStatusconditions权重
\(w_m^*(s)\)，它最小化点态期望损失：

\[w^*(s) = \arg\min_{w \in \Delta^{M-1}} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\]

Dawid-Skene make using 's全局权重 \(\bar{w}\) Yes常数：

\[\bar{w} = \arg\min_{w} \mathbb{E}_{X, Y^*}[\mathbb{E}[\ell(\text{vote}_w(X), Y^*) \mid X]]\]

by PointwiseOptimal性，for each 个 \(x \in s\)：

\[\mathbb{E}[\ell(\text{vote}_{w^*(s)}(x), y^*) \mid x \in s] \leq \mathbb{E}[\ell(\text{vote}_{\bar{w}}(x), y^*) \mid x \in s]\]

at StatusDistributionon 取期望i.e.得 \(R_{\text{SCX}} \leq R_{\text{DS}}\)。

etc.号conditionsby Pointwisenon-etc.式's严格性导出：only has when 全局权重also Yeseach 个Status'sOptimal权重时etc.号成立，i.e.
\(\bar{w} \in \arg\min_{w} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\)
for which has  \(s\)
成立。this must 求各Status'sOptimal权重相同，etc.价于混淆矩阵non-dependence于Status。\(\square\)

\subsubsection{5.4
at NoiseDetection语境under 's具体Difference}\label{ux5728ux566aux58f0ux68c0ux6d4bux8bedux5883ux4e0bux7684ux5177ux4f53ux5deeux5f02}

Theorem 1
'sDetection Rule建立at ''一致性''概念on ------NoiseSamplefor which has 专家all 呈现高Error。this Yes
SCX 框架's特has 优势，and  Dawid-Skene 's全局can be 靠性估计has 本质Difference：

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3611}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4722}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Dimension
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Dawid-Skene
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
SCX (Theorem 1)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
can be 靠性 & 全局常数 \(\pi_m\) & Statusconditions \(\pi_m(s)\) \\
NoiseDetection & dependence标注者一致性\emph{后验} & dependence多专家Error\emph{一致性} \\
which requires 标注 & requires must 多个(通常 \(\geq 3\))标注者 & requires must 多个专家Model \\
抗Categorynon-平衡 & 弱（全局混淆矩阵受主导Category支配） &
强（state-level估计non-受影响） \\
区分 Noise vs Hard & non-can （only give 出后验概率） & can （Consistency Score \(C\)
天然分离两者） \\
TheoryGuarantee & 渐近一致性（\(M \to \infty\)） & 指数Convergence（\(M\) 和 \(K\)
all 加速） \\
\end{longtable}

\subsubsection{5.5 Dawid-Skene 作as  SCX
's特例}\label{dawid-skene-ux4f5cux4e3a-scx-ux7684ux7279ux4f8b}

when Status划分退化to 平凡划分
\(\mathcal{S} = \{\mathcal{X}\}\)（i.e.which has Sampleat 一个Statusin ）时：

\begin{itemize}
\tightlist
\item
  SCX 权重退化as 
  \(\bar{w}_m \propto \exp(-\alpha \hat{R}_m)\)，i.e.全局常数权重
\item
  Consistency Score \(C(x)\) 退化as 全局平all Error率
\item
  Theorem 1 'sDetectionGuarantee退化as dependence
  \(\mu = \frac{1}{M}\sum_m \varepsilon_m\) 's全局Guarantee
\end{itemize}

此时 SCX 'sNoiseDetectionetc.价于 Dawid-Skene 's全局can be 靠性加权Method。this Proof了 SCX
框架's严格推广性。

\subsubsection{5.6
改进幅度's量化}\label{ux6539ux8fdbux5e45ux5ea6ux7684ux91cfux5316}

SCX 相for 于 Dawid-Skene 's改进幅度and Status间'scan be 靠性变异直接相关：

\[\Delta R = R_{\text{DS}} - R_{\text{SCX}} = \sum_{s \in \mathcal{S}} \rho_s \cdot \mathcal{D}_s\]

where
\(\mathcal{D}_s = \sum_{m=1}^M (\bar{w}_m - w_m^*(s)) \cdot R_m(s)\)
YesStatus \(s\) on make using 全局权重while non-Statusconditions权重's额外风险。

by  Proposition 3（Statusconditions权重命题）'s定理 3.1：

\[\Delta R \geq \frac{1}{\alpha} \cdot I(s; w)\]

where \(I(s; w) = \mathbb{E}_s[\text{KL}(w(s) \| \bar{w})]\)
YesStatusand 权重之间have mutual information。

\textbf{Conclusion}: SCX 严格优于 Dawid-Skene when and only when 专家can be 靠性'sStatus变异
\(I(s; w) > 0\)------this at 实践in 几乎总Yes成立。Theorem 1 'sDetectionGuaranteeonly Yes
SCX 整体框架优势at NoiseDetection任务on 's集in 体现。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{References}\label{ux53c2ux8003ux6587ux732e}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  Dawid, A. P., \& Skene, A. M. (1979). Maximum likelihood estimation of
  observer error-rates using the EM algorithm. \emph{Journal of the
  Royal Statistical Society: Series C (Applied Statistics)}, 28(1),
  20-28.
\item
  Hoeffding, W. (1963). Probability inequalities for sums of bounded
  random variables. \emph{Journal of the American Statistical
  Association}, 58(301), 13-30.
\item
  Pollard, D. (1990). Empirical Processes: Theory and Applications.
  \emph{NSF-CBMS Regional Conference Series in Probability and
  Statistics}, 2, i-86.
\item
  Bartlett, P. L., \& Mendelson, S. (2002). Rademacher and Gaussian
  complexities: Risk bounds and structural results. \emph{Journal of
  Machine Learning Research}, 3(Nov), 463-482.
\item
  SCX Framework Definitions.
  \texttt{../definitions/01\_state\_conditioned\_risk.md}.
\item
  Proposition 3: State-Conditioned Expert Weighting.
  \texttt{../propositions/03\_state\_conditioned\_weighting.md}.
\item
  Proposition 4: Compression Fidelity.
  \texttt{../propositions/04\_compression\_fidelity.md}.
\item
  Data防in 毒概念. \texttt{../../knowledge/02\_概念/Data防in 毒.md}.
\item
  Data四分类概念. \texttt{../../knowledge/02\_概念/Data四分类.md}.
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{附录 A：Chernoff
界's推导}\label{ux9644ux5f55-achernoff-ux754cux7684ux63a8ux5bfc}

Lemma 2 和 Lemma 3 can be with make using  Chernoff 界（while non-
Hoeffding）获得更紧's指数Rate。

for CleanData，令 \(\{e_m\}\) as 独立's Bernoulli 变量，all 值as 
\(\mu = \mathbb{E}[C \mid \text{clean}, x] \leq \mu_s\)。for 
\(\theta > \mu\)：

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, x) 
&= \mathbb{P}\!\left(\frac{1}{M}\sum_m e_m > \theta\right) \\
&\leq \inf_{\lambda > 0} e^{-\lambda M\theta} \mathbb{E}\!\left[e^{\lambda\sum_m e_m}\right] \\
&= \inf_{\lambda > 0} e^{-\lambda M\theta} \prod_{m=1}^M (1 - \mu_m + \mu_m e^\lambda) \\
&\leq \exp\!\left(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\right)
\end{aligned}\]

where
\(\text{KL}(p \,\|\, q) = p\log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\)，and 最后一步make using 了联合界和
\(\mu_m \leq \mu_s\)。

for NoiseData，by  Lemma 3 's修正Proof（give 定 \(c\) conditionsunder 
Hoeffding），Chernoff 形式as ：

\[\mathbb{P}(C \leq \theta \mid \text{noise}, x, c) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - \mu_c(x)\right)\right) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\right)\]

注意 KL 's方toward ：\(\text{KL}(\theta \| q)\) for should  Chernoff Upper Bound
\(\mathbb{P}(\hat{p} \leq \theta) \leq \exp(-n \cdot \text{KL}(\theta \| p))\)，where
\(\hat{p}\) Yes \(p\) 'sSampleall 值。this 里
\(p = \mathbb{E}[C \mid \text{noise}] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\)，我们
bound \(\mathbb{P}(C \leq \theta)\)（under 尾），which with Part二个ParameterYes真实期望
\(p \approx 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\)。

\begin{quote}
\textbf{2026-06-27 修正}：原版 Chernoff 附录make using 了
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\)，KL 方toward has 误。Correct形式as 
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\)------under 尾
bound in Part一个ParameterYes阈值 \(\theta\)，Part二个ParameterYes真实期望。
\end{quote}

Chernoff 界优于 Hoeffding 界's程度by with under non-etc.式刻画（Hoeffding Yes
Chernoff 's一阶近似):

\[\exp(-2M\Delta^2) \geq \exp(-M \cdot \text{KL}(\mu + \Delta \,\|\, \mu))\]

etc.号only when  \(\Delta \to 0\)
时渐近成立。at 实际Parameter范围（\(\Delta \approx 0.1\text{-}0.4\)）内，Chernoff
界can be 紧 \(2\text{-}5\) 倍。

\begin{quote}
\textbf{DEFECT-06 同步 (2026-06-28) --- Bahadur-Rao 格点修正}:
when 专家Erroras  Bernoulli Distribution（格点跨度 \(h=1\)）时，Correct's Bahadur-Rao
展开at  Chernoff 界in 引入because 子 \((1-e^{-\lambda^*})^{-1}\) 替代
\(1/\lambda^*\)。修正后's极小极大常数 \(C_{\min}^{\text{(corr)}}\)
and 原始 \(C_{\min}\) 相差约
\(5\)--\(12\%\)，极小极大Optimal性Conclusionnon-变（can be 达界and lower bound接受相同's格点修正because 子）。详细推导见
\texttt{06\_fixed\_point\_convergence.md} §12（Remark on Bahadur-Rao
Lattice Correction）。
\end{quote}

\subsection{附录 B：non- 0-1
损失's推广}\label{ux9644ux5f55-bux975e-0-1-ux635fux5931ux7684ux63a8ux5e7f}

Theorem 1 for 一般Bounded Loss \(\ell \in [0, B]\) also 成立，only requires will  Lemma 1 in 's
\(e_m\) 解释as 
\(\mathbf{1}\{\ell(f_m(x), y) > \tau\}\)。核心推导non-变，because as  Lemma 1
only dependenceconditions期望's线性性质和Noise标签'sall 匀性Assumptions (A4)，non-dependence 0-1
损失's具体形式。

具体地，for 回归任务（\(\mathcal{Y} \subseteq \mathbb{R}^d\)），\(K\)
's概念requires 重新解释。Lemma 1 in 's \(K\) at 回归场景under 退化as ：

\[ \mathbb{E}[C \mid \text{noise}, x] \geq 1 - \mathbb{E}[C \mid \text{clean}, x] \]

i.e. \(K \to \infty\) 's极限情况。此时分离间隙for Arbitrary \(\mu_s < 1/2\)
as 正。this Yesbecause as at 连续标签空间in ，专家恰好预测to Noise标签's概率as 零，make NoiseDetection更容易。Theorem
1 'sits 余部分non-变。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Changelog}\label{changelog}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1935}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2258}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2581}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3226}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Date
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Issue
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Change
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Severity
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
2026-06-28 & B1 & \textbf{Fixed A2 self-contradiction.} Updated A2
rationale to acknowledge A1 does NOT guarantee A2. Added
\(M_{\text{eff}}\) degradation formula and cross-reference to
\texttt{01\_symbol\_system.md} §12.5. & BLOCKING \\
2026-06-28 & B6 & \textbf{Added \(M \geq 1\), \(K \geq 2\) to Theorem 1
preamble.} Explicitly excludes degenerate edge cases where consensus
score is undefined or \(K-1=0\). & BLOCKING \\
2026-06-28 & B10 & \textbf{Synced DEFECT-06 (Bahadur-Rao lattice
correction)} to Theorem 1 appendix. Added cross-reference to
\texttt{06\_fixed\_point\_convergence.md} §12. & BLOCKING \\
\end{longtable}

\end{document}
