% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
 12pt,
]{ctexart}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
 \usepackage[T1]{fontenc}
 \usepackage[utf8]{inputenc}
 \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
 \usepackage{unicode-math} % this also loads fontspec
 \defaultfontfeatures{Scale=MatchLowercase}
 \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
 % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
 \usepackage[]{microtype}
 \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
 \IfFileExists{parskip.sty}{%
 \usepackage{parskip}
 }{% else
 \setlength{\parindent}{0pt}
 \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
 \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
 \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
 hidelinks,
 pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Theorem 1: Multi-Expert Consistency Guarantees for Label Noise
Detection}\label{theorem-1-multi-expert-consistency-guarantees-for-label-noise-detection}

\begin{quote}
\textbf{Core Claim}: when \(M\)
 trained on disjointdata subsets independently trained experts on a sample'sconsistency score \(C(x)\)
exceedsthreshold \(\theta\) time, shouldsampleisLabelnoise的置信degreewithexponential速率converges to 1. 

\textbf{Revision History}: 2026-06-27 --- Lemma 3 Proof重构 (newincrease A6
平衡errordistributionAssumptions); Chernoff appendix KL directioncorrection; 主Theorem Statementupdateis 
(A1)-(A6). two个 bug 均fromVerification Report. 
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1 Theorem Statement}\label{ux5b9aux7406ux9648ux8ff0}

\subsubsection{1.1 Notation and Setup
(Setup)}\label{ux7b26ux53f7ux4e0eux8bbeux5b9a-setup}

设withlowerto象: 

\begin{longtable}[]{@{}
 >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}
 >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Symbol/Concept
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Meaning
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\(\mathcal{X}\) & Input Space, may测 \\
\(\mathcal{Y}\) & Label Space, \(|\mathcal{Y}| = K\) (separate类)or
\(\mathcal{Y} \subseteq \mathbb{R}^d\) (回归) \\
\(\mathcal{D}\) & Data Distribution, \((X, Y) \sim \mathcal{D}\) \\
\(f^* : \mathcal{X} \to \mathcal{Y}\) & true标Notefunction (not知 Oracle) \\
\(\{f_m\}_{m=1}^M\) & \(M\)
个专家model, \(f_m : \mathcal{X} \to \mathcal{Y}\) \\
\(\Pi = \{s_1, \dots, s_K\}\) & Status划separate, \(\mathcal{X}\) may测separate割 \\
\(\ell : \mathcal{Y} \times \mathcal{Y} \to [0, B]\) & has 界loss function \\
\(\tau > 0\) & 专家incorrect的threshold \\
\(\theta \in (0, 1)\) & noise detectionthreshold \\
\end{longtable}

\textbf{Labelnoisemodel}: foreach个样this \((x, y^*)\) (where \(y^* = f^*(x)\)
is trueLabel), observeto的Label \(y\) generatesuch aslower: 

\[y = \begin{cases}
y^* & \text{withprobability } 1 - \eta \\
\text{Uniform}(\mathcal{Y} \setminus \{y^*\}) & \text{withprobability } \eta
\end{cases}\]

where \(\eta \in (0, 1/2)\) is globalnoise率, noise事件and \(x\)
and has trainingdataindependent. 

\textbf{consistency score}: tosample
\((x, y)\), Definition专家incorrect指示variableandconsistency score: 

\[e_m(x, y) = \mathbf{1}\{\ell(f_m(x), y) > \tau\}, \quad
C(x) = \frac{1}{M} \sum_{m=1}^M e_m(x, y)\]

\textbf{detection规then}: 样this \((x, y)\) isMarkedis noiseif and only if
\(C(x) > \theta\). 

\subsubsection{1.2 Assumptions (Assumptions)}\label{ux5047ux8bbe-assumptions}

\textbf{(A1) disjointtraining集}: \(M\) 个专家in \(M\)
个disjoint的independentsamedistributionsubsetuppertraining: 

\[D_m \sim \mathcal{D}^{n_m}, \quad D_m \cap D_{m'} = \varnothing, \quad D_m \perp D_{m'} \text{ for } m \neq m'\]

\textbf{(A2) 清洁dataupper的Conditionsindependent}: to任意清洁sample
\((x, y)\) (\(y = y^*\)), to定 \(x\) Conditionslower, incorrect指示variable
\(\{e_m(x, y)\}_{m=1}^M\) isConditionsindependent . 

\emph{reasonable性Descriptionandlimitation}: A2
is一种\textbf{structure性Assumptions}, by专家experimentdesign (disjointtraining集
A1、independentinitialization化) 论证, but\textbf{A1 并not蕴含
A2}. i.e.使training集完全disjoint, atsimilarityData Distributionuppertraining的专家mayatdistributionoutersampleupperproduces\textbf{mutualcloseincorrect} (originates from共享归纳偏置, non-共享trainingdata). becausethis
A2 in SCX 流水线的datamiddle\textbf{notmay经验检验}------tocontinuousInput Space
\(\mathcal{X}\), each个 \(x\) inhas 限data集middlemostmany出现一次, joint distribution
\(P(e_1, \dots, e_M \mid x)\) unobservable. \textgreater{} \textgreater{}
\textbf{when A2 is违反time} (实际middle常see): 用
\(M_{\text{eff}} = M/(1 + (M-1)\bar{\rho})\) replace has concentration inequality in the 
\(M\), where \(\bar{\rho}\)
is 专家incorrect指标的on average成tomutualclose性. to典型深degree集成, \(\bar{\rho} \approx 0.1\)--\(0.3\). Recommendationat留置verify集upperestimate
\(\bar{\rho}\), 并using \(M_{\text{eff}}\) 得to保守guarantee. 详细Analysis参see
\texttt{01\_symbol\_system.md} §12.5. 

\textbf{(A3) has 界loss}:
\(\ell(a, b) \in [0, B], \; \forall a, b \in \mathcal{Y}\), where
\(B < \infty\). 

\textbf{(A4) 均匀independentnoise}: Label翻转事件and \(x\) and has \(D_m\)
independent. noiseLabelin \(\mathcal{Y} \setminus \{y^*\}\) upper均匀distribution. 

\textbf{(A5) Statussame质性 (Status划separatesufficient性)}: Status划separate \(\Pi\)
makesateach个Status \(s\)
inner, 专家的清洁dataincorrect率approximate均匀. 形式化地, 存atStatus级常数
\(\{\mu_s\}_{s \in \mathcal{S}}\) makes: 

\[\sup_{x \in s} \, \mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s, \quad \forall s \in \mathcal{S}\]

i.e.Status \(s\) inner清洁sample的on average专家incorrect率is \(\mu_s\) consistentupper bound控制. 

\textbf{(A6) 平衡errordistribution}:
专家的incorrectprobabilityat has incorrectCategoryuppercannot过degree集middle. 存at常数
\(C_{\text{bal}} \geq 1\) makesto任意Status \(s\) and \(x \in s\): 

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\(C_{\text{bal}} = 1\)
toshould完全均匀的incorrectdistribution (each个incorrectCategory等probability); \(C_{\text{bal}} = 2\)
is典型的保守选择. thisAssumptionsmay检验andatpracticemiddle几乎自动满足------atdisjoint干净datauppertraining的专家nothas 理bywillincorrect集middleto特定Category. 

\subsubsection{1.3 Key Lemmas
(Mean Separation)}\label{ux5173ux952eux5f15ux7406-ux5747ux503cux5206ux79bb}

\textbf{Lemma 1 (Mean Separation).} inAssumptions (A1)-(A5) lower (A6
does not affectexpect), to任意 \(x \in s\): 

\textbf{(清洁sample)}
\[\mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s\]

\textbf{(noisesample)}
\[\mathbb{E}[C(X) \mid \text{noise}, X = x] = 1 - \frac{1}{K-1} \cdot \mathbb{E}[C(X) \mid \text{clean}, X = x] \geq 1 - \frac{\mu_s}{K-1}\]

becausethis, when \(\mu_s < \frac{K-1}{K}\) time, 存in \(\theta\) makes: 

\[\mathbb{E}[C \mid \text{clean}] < \theta < \mathbb{E}[C \mid \text{noise}]\]

andseparation gapis : 

\[\Delta_s(\theta) = \min\left(\theta - \mu_s, \; 1 - \frac{\mu_s}{K-1} - \theta\right)\]

when选择Optimal threshold
\(\theta_s^* = \frac{1}{2}\left(1 + \mu_s \cdot \frac{K-2}{K-1}\right)\)
time, separation gapmaximum化 (in \(C_{\text{bal}}=1\) 理想caselower): 

\[\Delta_s^* = \frac{1}{2}\left(1 - \mu_s \cdot \frac{K}{K-1}\right)\]

when \(C_{\text{bal}} > 1\) time, Optimal thresholdto \(\mu_s\)
direction微调with补偿non-均匀errordistribution. \(\theta_s^*\) Exact 表达式must求解
\(\theta - \mu_s = 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta\). 

\textbf{Proof.} 清洁sample的界by (A5) 直接to出. tonoisesample: 

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, X = x] 
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(\ell(f_m(x), y) > \tau \;\big|\; \text{noise}, x\bigr) \\
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) \neq y \;\big|\; \text{noise}, x\bigr) \quad (\text{to } 0\text{-}1 \text{ loss}) \\
&= 1 - \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) = y \;\big|\; \text{noise}, x\bigr)
\end{aligned}\]

by (A4), noiseLabel \(y\) in \(\mathcal{Y} \setminus \{y^*\}\)
upper均匀distribution, andis independent of has 专家: 

\[\begin{aligned}
\mathbb{P}(f_m(x) = y \mid \text{noise}, x) 
&= \sum_{c \neq y^*} \mathbb{P}(y = c \mid \text{noise}) \cdot \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{P}(f_m(x) \neq y^* \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{E}[e_m \mid \text{clean}, x]
\end{aligned}\]

becausethis: 

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, x] 
&= 1 - \frac{1}{K-1} \cdot \frac{1}{M} \sum_{m=1}^M \mathbb{E}[e_m \mid \text{clean}, x] \\
&= 1 - \frac{1}{K-1} \cdot \mathbb{E}[C \mid \text{clean}, x] \\
&\geq 1 - \frac{\mu_s}{K-1} \quad \text{by (A5)}
\end{aligned}\]

separation gap的存at性by \(\mu_s < \frac{K-1}{K}\) guarantee, thistime
\(1 - \frac{\mu_s}{K-1} > \mu_s\). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsubsection{1.4 Main Theorem (Main
Result)}\label{ux4e3bux5b9aux7406-main-result}

\textbf{Theorem 1 (SCX noise detection guarantee).} 设Assumptions (A1)-(A6) 成立, and
\(M \geq 1\) (tofew一个专家), \(K \geq 2\) (tofewtwo个LabelCategory, withguarantee
\(K-1 > 0\)). 令 \(\rho_s = \mathbb{P}(X \in s)\)
is Statusprobability. to任意threshold \(\theta\) 满足
\(\mu_s < \theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) Status
\(s\), DefinitionStatus级separation gap: 

\[\Delta_s = \min\left(\theta - \mu_s,\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right) > 0\]

when \(C_{\text{bal}} = 1\) (most佳情形)time, \(\Delta_s\) 恢复原始Definition; when
\(C_{\text{bal}} > 1\)
time, noise侧的between隙收窄 (becauseis incorrectmayis NOT 完全均匀distribution的). 

then SCX noise detection器 F1 得separate满足withlowerlower bound: 

\[\text{F1} \;\geq\; 1 - \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-2M\Delta_s^2\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-2M\Delta_s^2\bigr) \Bigr]\]

or等价地: 

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\!\bigl(-2M\Delta_s^2\bigr)\]

where \(\eta\) is globalnoise率. 

\textbf{more紧 Chernoff 形式}: tomutualsameSetup, 使using Chernoff 界代替
Hoeffding can得more紧的exponential速率: 

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\bigr) \Bigr]\]

where
\(\text{KL}(p \,\|\, q) = p \log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\). Chernoff
界in \(\mu_s\) approaches \(\theta\) or
\(1 - C_{\text{bal}} \cdot \mu_s/(K-1)\) approaches \(\theta\) time显著is superior to
Hoeffding (may紧 2-5 倍). 

\begin{quote}
\textbf{2026-06-27 correction}: 原版 Chernoff use了
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\), KL directionhas 误, andnot纳入
\(C_{\text{bal}}\). 现alreadyto 
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\). 
\end{quote}

\textbf{keyCorollary}: when \(M \to \infty\) time, to has 满足
\(\mu_s < \frac{K-1}{K}\) Status, F1 withexponential速率converges to 1: 

\[\text{F1} \;=\; 1 - \mathcal{O}_P\!\left(\frac{1}{\eta} \cdot e^{-2M\Delta_{\min}^2}\right)\]

where \(\Delta_{\min} = \min_{s \in \mathcal{S}} \Delta_s\)
is most差Statusseparation gap. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2 Proof}\label{ux8bc1ux660e}

\subsubsection{2.1 清洁data的false positive率 (False Positive
Rate)}\label{ux6e05ux6d01ux6570ux636eux7684ux5047ux9633ux6027ux7387-false-positive-rate}

\textbf{Lemma 2 (FPR upper bound).} to任意Status \(s \in \mathcal{S}\) 满足
\(\mu_s < \theta\), 清洁sampleby误标is noise的probability满足: 

\[\mathbb{P}(C > \theta \mid \text{clean}, X \in s) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

\textbf{Proof.} fixed \(x \in s\), by (A5) 知
\(\mathbb{E}[C \mid \text{clean}, X = x] \leq \mu_s\). by
(A1)-(A2), to定 \(x\) time \(\{e_m\}\) Conditionsindependent. by (A3) 知
\(e_m \in [0, 1]\). to \(C = \frac{1}{M}\sum_m e_m\) shouldusing Hoeffding
inequality (Pollard, 1990): 

\[\mathbb{P}(C - \mathbb{E}[C] > \theta - \mu_s \mid \text{clean}, x) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

byat \(\theta > \mu_s\), \((\theta - \mu_s) > 0\). should界to任意
\(x \in s\) 成立, becausethis: 

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, X \in s) 
&= \mathbb{E}_{X \mid s}\bigl[ \mathbb{P}(C > \theta \mid \text{clean}, X) \bigr] \\
&\leq \sup_{x \in s} \mathbb{P}(C > \theta \mid \text{clean}, x) \\
&\leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)
\end{aligned}\]

thisi.e. must. \(\square\)

\subsubsection{2.2 noisedata的true positive率 (True Positive
Rate)}\label{ux566aux58f0ux6570ux636eux7684ux771fux9633ux6027ux7387-true-positive-rate}

\textbf{newincreaseAssumptions (A6) --- 平衡errordistribution}: to任意Status \(s\) andsample
\(x \in s\), 专家的incorrectprobabilityat has incorrectCategoryuppercannot过degree集middle. 形式化地, 存at常数
\(C_{\text{bal}} \geq 1\) makes: 

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \text{where } \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\emph{reasonable性}: 专家atdisjoint的干净datasubsetuppertraining, nothas 理bywillincorrect集middleto特定的incorrectCategory. 均匀noisemodellower, each个incorrectCategory的probability天however约is 
\(\mu_s/(K-1)\). \(C_{\text{bal}} = 2\)
alreadyis保守选择. thisAssumptionsmay检验: at实际dataupperestimate
\(\max_c \mu_c(x) / (\mu_s/(K-1))\). 

\textbf{Lemma 3 (TPR lower bound).} inAssumptions (A1)-(A6) lower, to任意Status
\(s \in \mathcal{S}\) 满足
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\), noisesamplebycorrectdetection的probability满足: 

\[\mathbb{P}(C > \theta \mid \text{noise}, X \in s) \geq 1 - \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)\]

\textbf{Proof}: fixed \(x \in s\). to定noiseLabel
\(y = c\) (\(c \neq y^*\)), for 0-1 loss, 专家 \(m\) incorrect指示is 
\(e_m = \mathbf{1}\{f_m(x) \neq c\}\). byat \(f_m\) only-dependence
\(D_m\) (A1), to定 \(x\) time \(\{f_m(x)\}\) Conditionsindependent (A2), becausethis
\(\{e_m\}\) into定 \((x, c)\) lowerConditionsindependent. 

each个 \(e_m\) expectis 
\(\mathbb{E}[e_m \mid x, c] = 1 - \mathbb{P}(f_m(x) = c \mid x) = 1 - \mu_{c,m}(x)\). becausethis: 

\[\mathbb{E}[C \mid x, c] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} \quad \text{ (by A6)}\]

byat \(e_m \in [0, 1]\) Conditionsindependent, shouldusing Hoeffding inequality: 

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid x, c)
&= \mathbb{P}\!\left(\frac{1}{M} \sum_{m=1}^M e_m \leq \theta \;\Big|\; x, c\right) \\
&\leq \exp\!\left(-2M\left(1 - \mu_c(x) - \theta\right)^2\right) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

where Hoeffding gap Conditions \(1 - \mu_c(x) > \theta\) byTheoremAssumptions
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) and A6 guarantee: 
\(1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} > \theta\). 

tonoiseLabel \(c\) 取on average (均匀distribution over \(K-1\) 类): 

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid \text{noise}, x)
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(C \leq \theta \mid x, c) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

becausethis
\(\mathbb{P}(C > \theta \mid \text{noise}, x) \geq 1 - \exp(-2M(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta)^2)\). to
\(x \in s\) 取expecti.e.得. \(\square\)

\textbf{Note}: when \(C_{\text{bal}} = 1\) (完全均匀的incorrectdistribution)time, Lemma
3 恢复原始Proposition in the Optimal bound. \(C_{\text{bal}}\)
mayat实际dataupperestimate, 并used for校准detectionthreshold \(\theta\). 

\subsubsection{2.3 F1 lower bound}\label{f1-ux4e0bux754c}

现atcombine Lemma 2 and Lemma 3 Derivation F1 lower bound. 

令detection规thenis \(R(x) = \mathbf{1}\{C(x) > \theta\}\). Definition: 

\[\begin{aligned}
\text{TPR}_s &= \mathbb{P}(R = 1 \mid \text{noise}, X \in s) \geq 1 - \exp(-2M(1 - \mu_s/(K-1) - \theta)^2) \\
\text{FPR}_s &= \mathbb{P}(R = 1 \mid \text{clean}, X \in s) \leq \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

by (A4), noise事件and \(X\) independent, becausethis
\(\mathbb{P}(\text{noise} \mid X \in s) = \eta\) to has Status成立. whole
TPR and FPR is : 

\[\begin{aligned}
\text{TPR} &= \sum_s \rho_s \cdot \text{TPR}_s, \quad
\text{FPR} = \sum_s \rho_s \cdot \text{FPR}_s
\end{aligned}\]

F1 得separateis : 

\[\begin{aligned}
\text{F1} &= \frac{2 \cdot \text{TP} }{2 \cdot \text{TP} + \text{FP} + \text{FN}} \\
&= \frac{2\eta \cdot \text{TPR}}{2\eta \cdot \text{TPR} + (1-\eta) \cdot \text{FPR} + \eta \cdot (1 - \text{TPR})} \\
&= \frac{2\eta \cdot \text{TPR}}{\eta(1 + \text{TPR}) + (1-\eta) \cdot \text{FPR}}
\end{aligned}\]

代入 TPR \(\geq 1 - \delta_1\) and FPR \(\leq \delta_2\), where: 

\[\begin{aligned}
\delta_1 &= \sum_s \rho_s \cdot \exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \\
\delta_2 &= \sum_s \rho_s \cdot \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

得to: 

\[\begin{aligned}
\text{F1} &\geq \frac{2\eta(1 - \delta_1)}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&= 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&\geq 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta} \quad (\text{becauseseparate母 } \geq \eta) \\
&= 1 - \delta_1 - \frac{1-\eta}{\eta}\delta_2
\end{aligned}\]

代入 \(\delta_1, \delta_2\) 表达式, 并Note意when
\(\Delta_s = \min(\theta - \mu_s, \; 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)\)
timehas \(\exp(-2M(\theta - \mu_s)^2) \leq \exp(-2M\Delta_s^2)\) and
\(\exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \leq \exp(-2M\Delta_s^2)\), becausethis: 

\[ \text{F1} \geq 1 - \sum_s \rho_s \left[\exp(-2M\Delta_s^2) + \frac{1-\eta}{\eta}\exp(-2M\Delta_s^2)\right] = 1 - \frac{1}{\eta}\sum_s \rho_s \exp(-2M\Delta_s^2) \]

QED. \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3 Corollary (Corollaries)}\label{ux63a8ux8bba-corollaries}

\subsubsection{3.1 Corollary 1: to称专家 (Symmetric
Experts)}\label{ux63a8ux8bba-1ux5bf9ux79f0ux4e13ux5bb6-symmetric-experts}

when has 专家具has mutualsame的清洁dataincorrect率
\(\varepsilon\) (称is ''to称专家'')time, \(\mu_s = \varepsilon\)
to has Statusconsistent. thistime: 

\[\Delta = \frac{1}{2}\left(1 - \varepsilon \cdot \frac{K}{K-1}\right)\]

and F1 lower bound简化is : 

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M}{2}\left(1 - \frac{\varepsilon K}{K-1}\right)^2\right)\]

when \(K = 2\) (二separate类)time, 进一步简化is : 

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-2M\left(\frac{1}{2} - \varepsilon\right)^2\right)\]

\textbf{meaning}: in二separate类to称专家scenariolower, detectionquality完全by专家incorrect率
\(\varepsilon\) and \(1/2\) 差距决定. \(\varepsilon\) eachfar from \(1/2\)
一个Standard 差 (\(\approx 1/\sqrt{2M}\)), F1 lower bound提升一个exponentialquantity级. 

\subsubsection{3.2 Corollary 2: Optimal threshold选择 (Optimal
Threshold)}\label{ux63a8ux8bba-2ux6700ux4f18ux9608ux503cux9009ux62e9-optimal-threshold}

toto定的noise率 \(\eta\)、Category数 \(K\)、专家数 \(M\) andmaximum清洁incorrect率
\(\mu_{\max} = \max_s \mu_s\), Optimal detectionthreshold \(\theta^*\) should满足: 

\[\theta^* = \arg\max_\theta \min_s \Delta_s(\theta) = \frac{1}{2}\left(1 + \mu_{\max} \cdot \frac{K-2}{K-1}\right)\]

thistimeminimumseparation gapis : 

\[\Delta_{\min}^* = \frac{1}{2}\left(1 - \mu_{\max} \cdot \frac{K}{K-1}\right)\]

 must的mostfew专家数 (达to F1 \(\geq 1 - \varepsilon_0\))is : 

\[M \geq \frac{1}{2\Delta_{\min}^{*2}} \log\left(\frac{1}{\eta \varepsilon_0}\right)\]

\textbf{Proof}: 直接解
\(1 - \frac{1}{\eta}e^{-2M\Delta^2} \geq 1 - \varepsilon_0\) 得
\(e^{-2M\Delta^2} \leq \eta\varepsilon_0\), 取logarithmici.e.得. If needsametime控制many个Status的error (联combined界), separate子 in the 
\(1\) replaceis Status数 \(|\mathcal{S}|\). \(\square\)

\subsubsection{3.3 Corollary 3: consistentmaydetection的Sufficient Condition (Uniform
Detectability)}\label{ux63a8ux8bba-3ux4e00ux81f4ux53efux68c0ux6d4bux7684ux5145ux5206ux6761ux4ef6-uniform-detectability}

such as果存in \(\delta > 0\) makesto has Status \(s\) has 
\(\mu_s \leq \frac{K-1}{K} - \delta\), then令
\(\theta = \frac{1}{2}\) (fixed threshold), to has Statushas 
\(\Delta_s \geq \frac{\delta}{2}\), and: 

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M\delta^2}{2}\right)\]

\textbf{meaning}: onlymusteach个Status的清洁on average专家incorrect率consistentbelow \((K-1)/K\) (when
\(K=2\) timeis \(1/2\)), usefixed threshold \(\theta = 1/2\) i.e.may获得exponential级别的
F1 guarantee. 

\subsubsection{3.4 Corollary 4: has 限sample校currently (Finite-Sample
Correction)}\label{ux63a8ux8bba-4ux6709ux9650ux6837ux672cux6821ux6b63-finite-sample-correction}

atpracticemiddle, \(\mu_s\) mustFrom has 限verifydataestimate. 设each个Status \(s\) has \(n_s\)
个清洁verifysample. 用
\(\hat{\mu}_s = \frac{1}{M}\sum_m \hat{\varepsilon}_m(s)\) estimate
\(\mu_s\), where \(\hat{\varepsilon}_m(s)\) is专家 \(m\) inStatus \(s\)
upper的经验incorrect率. 

by Hoeffding inequalityand联combined界: to任意 \(\delta_0 \in (0, 1)\), withprobability
\(\geq 1 - \delta_0\): 

\[|\hat{\mu}_s - \mu_s| \leq B \sqrt{\frac{\log(2M/\delta_0)}{2n_s}}, \quad \forall m, s\]

设校currentlyback的thresholdis 
\(\tilde{\mu}_s = \hat{\mu}_s + B\sqrt{\frac{\log(2M/\delta_0)}{2n_s}}\) (保守upper bound). then
Theorem 1 Conclusionto校currentlypost-\(\tilde{\mu}_s\) withprobability
\(\geq 1 - \delta_0\) 成立, onlymustin \(\Delta_s\) middleusing \(\tilde{\mu}_s\)
替代 \(\mu_s\). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4 紧致性讨论
(Tightness)}\label{ux7d27ux81f4ux6027ux8ba8ux8bba-tightness}

\subsubsection{4.1 何time紧致}\label{ux4f55ux65f6ux7d27ux81f4}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{large M extremely限}: when
 \(M \to \infty\), \(C \xrightarrow{\text{a.s.}} \mathbb{E}[C]\) (bylaw of large numbers), detection变成确定性的: \(C > \theta\)
 if and only if \(\mathbb{E}[C] > \theta\). thistime F1 by \(\eta\) and \(\mu_s\)
 完全决定, Theoremlower bound紧致. 
\item
 \textbf{Statussame质性}: when (A5)
 严格成立 (Statusinner清洁incorrect率完全均匀)andexpert on称, Hoeffding 界的常数because子
 \(2\) isOptimal (by Hoeffding lower boundguarantee). 
\item
 \textbf{\(K \to \infty\)}: separation gap \(\Delta_s \to \frac{1}{2}\) (fixed 
 \(\mu_s\)), detection渐近完美. 
\end{enumerate}

\subsubsection{4.2 何time松弛}\label{ux4f55ux65f6ux677eux5f1b}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{small M}: when \(M \leq 3\) time, Hoeffding
 界relatively松弛. thistimeapplicationExact 二项式distribution (andnon-
 Hoeffding)may得more紧的界. 经验规then: \(M \geq 10\) time Hoeffding
 界reasonable紧致; \(M \leq 5\) timeuseExact 二项式累积distribution (Corollary 4.1). 
\item
 \textbf{\(\mu_s\) approaches \(\frac{K-1}{K}\)}: when
 \(\mu_s \uparrow (K-1)/K\) time
 \(\Delta_s \to 0\), exponential decaybecause子消失. thistime must专家数
 \(M \propto 1/\Delta_s^2\)
 发散. thisisthis质性的: when专家atnew清洁dataupperapproaches随机猜测time, noise detection必howevernotmay靠. 
\item
 \textbf{weakCharacteristics退化}:
 whenStatus划separateNone法separate离noise主导区域and清洁主导区域time, (A5) does not hold. thistime
 Theorem 1 not适用, must借助cross-validationorouterpart布detection (See Prop 6
 二层Statusdiscover). thisis SCX
 framework的already知boundary (参see\emph{weakCharacteristics失效}文档). 
\item
 \textbf{noisenon-均匀}: when \(\eta\) -dependence \(x\)
 time (\(P(\text{noise} \mid x)\) non-常数), F1 表达式must用
 \(\eta_s = \mathbb{P}(\text{noise} \mid X \in s)\) 替代global \(\eta\): 
\end{enumerate}

\[\text{F1} \geq 1 - \frac{1}{\min_s \eta_s} \sum_s \rho_s \exp(-2M\Delta_s^2)\]

in \(\eta_s \to 0\) extremeStatus, detectionNonemeaning------shouldStatusthis身nothas noise. 

\subsubsection{4.3
紧致性to比experiment}\label{ux7d27ux81f4ux6027ux5bf9ux6bd4ux5b9eux9a8c}

to典型值 \(K=10\)、\(\eta=0.1\)、\(M=20\)、\(\mu_s=0.2\), Theorem 1
to出: 

\[\Delta_s = \frac{1}{2}(1 - 0.2 \cdot 10/9) \approx 0.389, \quad \text{F1} \geq 1 - 10 \cdot e^{-2\cdot 20 \cdot 0.389^2} \approx 1 - 10 \cdot e^{-6.05} > 0.976\]

\textbf{Note意}: upper述computeAssumptions
\(\mu_s = 0.2\), i.e.专家at清洁dataupper的incorrect率is 20\%. at实际 CIFAR-10
experimentmiddle (\(M=20\), \(\eta=0.1\)), SCX-Noise F1 实测is 
\(0.617\). this个值远below \(0.976\) lower bound, Description CIFAR-10
experimentmiddle\textbf{Assumptionsdoes not satisfy}------mostmay的Reasonis: (i) 专家onlyin 3 epoch 
CPU trainingbackuse, 实际 \(\mu_s \approx 0.45\) (andnon-false定 \(0.2\)), thistime
\(\Delta_s \approx 0.25\), lower bound退化is \(\text{F1} \geq 0.18\) (and实测
\(0.617 > 0.18\) consistent); (ii) (A5)
Statussame质性atlow质Characteristicslowernot严格成立. \textbf{教训}: Theoremlower boundto \(\mu_s\)
highdegreesensitive, practicemiddlemustatverify集upper准确estimate \(\mu_s\) post-againapplicationFormula. in AlN v3
全 GPU trainingscenariomiddle, \(\mu_s\) canlowto
\(0.03\text{--}0.18\), lower bound得with紧致 (seecurrently文 Table 2). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5 and Dawid-Skene
的relationship}\label{ux4e0e-dawid-skene-ux7684ux5173ux7cfb}

\subsubsection{5.1 Dawid-Skene
model回顾}\label{dawid-skene-ux6a21ux578bux56deux987e}

Dawid and Skene (1979) 提出using EM
algorithmFrom many标Noteone (专家)的independent标NotemiddleestimatetrueLabeland标Noteonemay靠性. coreAssumptionsiseach个标Noteone
\(m\) has 一个\textbf{globalconfusion matrix}: 

\[\pi_m(c' \mid c) = \mathbb{P}(f_m(x) = c' \mid y^* = c), \quad \forall x \in \mathcal{X}\]

i.e.标Noteone的incorrect模式not-dependence具体样this \(x\)------thisis一个global常数. 

mostfinal的Labelestimatethroughadd权投票complete: 

\[\hat{y}_{\text{DS}} = \arg\max_c \sum_{m=1}^M w_m \cdot \mathbf{1}\{f_m(x) = c\}\]

whereweight \(w_m\) by \(\pi_m\)
的to角元素 (accuracy)决定, ormoregenerally, byconfusion matrix的逆决定. 

\subsubsection{5.2 SCX
的改进: StatusConditionsmay靠性}\label{scx-ux7684ux6539ux8fdbux72b6ux6001ux6761ux4ef6ux53efux9760ux6027}

SCX willconfusion matrix推广is \textbf{StatusConditions}形式: 

\[\pi_m(c' \mid c, s) = \mathbb{P}(f_m(x) = c' \mid y^* = c, x \in s)\]

SCX weightbecausethis-dependenceatStatus: 

\[w_m(x) = \frac{\exp(-\alpha \hat{R}_m(s(x)))}{\sum_{m'} \exp(-\alpha \hat{R}_{m'}(s(x)))}\]

where \(\hat{R}_m(s) = \sum_{c} \pi_m(c \mid c, s)\) is StatusConditionsriskestimate. 

\subsubsection{5.3 SCX 严格is superior to Dawid-Skene
的Conditions}\label{scx-ux4e25ux683cux4f18ux4e8e-dawid-skene-ux7684ux6761ux4ef6}

\textbf{Proposition 5.1 (SCX-DS comparison).} 令 \(R_{\text{DS}}\) is 
Dawid-Skene add权的expect 0-1 loss, \(R_{\text{SCX}}\) is SCX
StatusConditionsadd权的expectloss. then: 

\[R_{\text{SCX}} \leq R_{\text{DS}}\]

等号成立if and only iftoeach个标Noteone \(m\) and has Status
\(s, s' \in \mathcal{S}\), confusion matrixatStatusbetweennot变: 

\[\pi_m(\cdot \mid \cdot, s) = \pi_m(\cdot \mid \cdot, s'), \quad \forall s, s' \in \mathcal{S}\]

i.e.标Noteonemay靠性not-dependencesampleStatus. 

\textbf{Proof.} to任意
\(x \in s\), add权投票的贝叶斯Optimal weightisStatusConditionsweight
\(w_m^*(s)\), 它minimum化点态expectloss: 

\[w^*(s) = \arg\min_{w \in \Delta^{M-1}} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\]

Dawid-Skene use的globalweight \(\bar{w}\) is常数: 

\[\bar{w} = \arg\min_{w} \mathbb{E}_{X, Y^*}[\mathbb{E}[\ell(\text{vote}_w(X), Y^*) \mid X]]\]

by逐点Optimal 性, toeach个 \(x \in s\): 

\[\mathbb{E}[\ell(\text{vote}_{w^*(s)}(x), y^*) \mid x \in s] \leq \mathbb{E}[\ell(\text{vote}_{\bar{w}}(x), y^*) \mid x \in s]\]

atStatusdistributionupper取expecti.e.得 \(R_{\text{SCX}} \leq R_{\text{DS}}\). 

等号Conditionsby逐点inequality的严格性导出: onlyhas whenglobalweightalsoiseach个Status的Optimal weighttime等号成立, i.e.
\(\bar{w} \in \arg\min_{w} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\)
to has \(s\)
成立. thisrequireeachStatus的Optimal weightmutualsame, is equivalent toconfusion matrixnot-dependenceatStatus. \(\square\)

\subsubsection{5.4
atnoise detection语境lower的具体Difference}\label{ux5728ux566aux58f0ux68c0ux6d4bux8bedux5883ux4e0bux7684ux5177ux4f53ux5deeux5f02}

Theorem 1
的detection规thenestablishat''consistent性''upper------noisesampleto has 专家all呈现highincorrect. thisis
SCX framework的特has advantage, and Dawid-Skene globalmay靠性estimatehas this质Difference: 

\begin{longtable}[]{@{}
 >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.1667}}
 >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3611}}
 >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4722}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
dimension
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Dawid-Skene
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
SCX (Theorem 1)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
may靠性 & global常数 \(\pi_m\) & StatusConditions \(\pi_m(s)\) \\
noise detection & -dependence标Noteoneconsistent性\emph{back验} & -dependencemulti-expertincorrect\emph{consistent性} \\
 must标Note & needmustmany个(通常 \(\geq 3\))标Noteone & needmustmany个专家model \\
抗Categorynot平衡 & weak (globalconfusion matrix受主导Category支配) &
strong (Status级estimatenot受impact) \\
distinguish Noise vs Hard & cannot (onlyto出back验probability) & can (consistency score \(C\)
天howeverseparate离twoone) \\
theoryguarantee & 渐近consistent性 (\(M \to \infty\)) & exponentialconvergence (\(M\) and \(K\)
allaccelerates) \\
\end{longtable}

\subsubsection{5.5 Dawid-Skene 作is SCX
的特例}\label{dawid-skene-ux4f5cux4e3a-scx-ux7684ux7279ux4f8b}

whenStatus划separate退化to平凡划separate
\(\mathcal{S} = \{\mathcal{X}\}\) (i.e. has sampleat一个Statusmiddle)time: 

\begin{itemize}
\tightlist
\item
 SCX weight退化is 
 \(\bar{w}_m \propto \exp(-\alpha \hat{R}_m)\), i.e.global常数weight
\item
 consistency score \(C(x)\) 退化is globalon averageincorrect率
\item
 Theorem 1 detectionguarantee退化is -dependence
 \(\mu = \frac{1}{M}\sum_m \varepsilon_m\) globalguarantee
\end{itemize}

thistime SCX noise detectionis equivalent to Dawid-Skene globalmay靠性add权Methodology. thisproves SCX
framework的严格推广性. 

\subsubsection{5.6
改进幅degree的量化}\label{ux6539ux8fdbux5e45ux5ea6ux7684ux91cfux5316}

SCX mutualfor Dawid-Skene 改进幅degreeandStatusbetween的may靠性变differentdirectly related: 

\[\Delta R = R_{\text{DS}} - R_{\text{SCX}} = \sum_{s \in \mathcal{S}} \rho_s \cdot \mathcal{D}_s\]

where
\(\mathcal{D}_s = \sum_{m=1}^M (\bar{w}_m - w_m^*(s)) \cdot R_m(s)\)
isStatus \(s\) upperuseglobalweightandnon-StatusConditionsweight的额outerrisk. 

by Proposition 3 (StatusConditionsweightProposition)的Theorem 3.1: 

\[\Delta R \geq \frac{1}{\alpha} \cdot I(s; w)\]

where \(I(s; w) = \mathbb{E}_s[\text{KL}(w(s) \| \bar{w})]\)
isStatusandweight之between的mutualinformation. 

\textbf{Conclusion}: SCX 严格is superior to Dawid-Skene if and only if专家may靠性的Status变different
\(I(s; w) > 0\)------thisatpracticemiddle几乎totalis成立. Theorem 1 detectionguaranteeonlyis
SCX wholeframeworkadvantageatnoise detection任务upper的集middle体现. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{参考文献}\label{ux53c2ux8003ux6587ux732e}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 Dawid, A. P., \& Skene, A. M. (1979). Maximum likelihood estimation of
 observer error-rates using the EM algorithm. \emph{Journal of the
 Royal Statistical Society: Series C (Applied Statistics)}, 28(1),
 20-28.
\item
 Hoeffding, W. (1963). Probability inequalities for sums of bounded
 random variables. \emph{Journal of the American Statistical
 Association}, 58(301), 13-30.
\item
 Pollard, D. (1990). Empirical Processes: Theory and Applications.
 \emph{NSF-CBMS Regional Conference Series in Probability and
 Statistics}, 2, i-86.
\item
 Bartlett, P. L., \& Mendelson, S. (2002). Rademacher and Gaussian
 complexities: Risk bounds and structural results. \emph{Journal of
 Machine Learning Research}, 3(Nov), 463-482.
\item
 SCX Framework Definitions.
 \texttt{../definitions/01\_state\_conditioned\_risk.md}.
\item
 Proposition 3: State-Conditioned Expert Weighting.
 \texttt{../propositions/03\_state\_conditioned\_weighting.md}.
\item
 Proposition 4: Compression Fidelity.
 \texttt{../propositions/04\_compression\_fidelity.md}.
\item
 data防middle毒. \texttt{../../knowledge/02\_/data防middle毒.md}.
\item
 data四separate类. \texttt{../../knowledge/02\_/data四separate类.md}.
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{appendix A: Chernoff
界的Derivation}\label{ux9644ux5f55-achernoff-ux754cux7684ux63a8ux5bfc}

Lemma 2 and Lemma 3 canwith使using Chernoff 界 (andnon-
Hoeffding)获得more紧的exponential速率. 

to清洁data, 令 \(\{e_m\}\) is independent Bernoulli variable, meanis 
\(\mu = \mathbb{E}[C \mid \text{clean}, x] \leq \mu_s\). to
\(\theta > \mu\): 

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, x) 
&= \mathbb{P}\!\left(\frac{1}{M}\sum_m e_m > \theta\right) \\
&\leq \inf_{\lambda > 0} e^{-\lambda M\theta} \mathbb{E}\!\left[e^{\lambda\sum_m e_m}\right] \\
&= \inf_{\lambda > 0} e^{-\lambda M\theta} \prod_{m=1}^M (1 - \mu_m + \mu_m e^\lambda) \\
&\leq \exp\!\left(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\right)
\end{aligned}\]

where
\(\text{KL}(p \,\|\, q) = p\log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\), andmostback一步use了联combined界and
\(\mu_m \leq \mu_s\). 

tonoisedata, by Lemma 3 correctionProof (to定 \(c\) Conditionslower
Hoeffding), Chernoff 形式is : 

\[\mathbb{P}(C \leq \theta \mid \text{noise}, x, c) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - \mu_c(x)\right)\right) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\right)\]

Note意 KL direction: \(\text{KL}(\theta \| q)\) toshould Chernoff upper bound
\(\mathbb{P}(\hat{p} \leq \theta) \leq \exp(-n \cdot \text{KL}(\theta \| p))\), where
\(\hat{p}\) is \(p\) samplemean. this里
\(p = \mathbb{E}[C \mid \text{noise}] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\), 我们
bound \(\mathbb{P}(C \leq \theta)\) (lower尾), with第二个parameteristrueexpect
\(p \approx 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\). 

\begin{quote}
\textbf{2026-06-27 correction}: 原版 Chernoff appendixuse了
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\), KL directionhas 误. correct形式is 
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\)------lower尾
bound middle第一个parameteristhreshold \(\theta\), 第二个parameteristrueexpect. 
\end{quote}

Chernoff 界is superior to Hoeffding 界的程degreebywithlowerinequality刻画 (Hoeffding is
Chernoff 一阶approximate): 

\[\exp(-2M\Delta^2) \geq \exp(-M \cdot \text{KL}(\mu + \Delta \,\|\, \mu))\]

等号onlywhen \(\Delta \to 0\)
time渐近成立. at实际parameter范围 (\(\Delta \approx 0.1\text{-}0.4\))inner, Chernoff
界may紧 \(2\text{-}5\) 倍. 

\begin{quote}
\textbf{DEFECT-06 same步 (2026-06-28) --- Bahadur-Rao 格点correction}:
when专家incorrectis Bernoulli distribution (格点跨degree \(h=1\))time, correct Bahadur-Rao
展openin Chernoff 界middle引入because子 \((1-e^{-\lambda^*})^{-1}\) 替代
\(1/\lambda^*\). correctionback的extremelysmallextremelylarge常数 \(C_{\min}^{\text{(corr)}}\)
and原始 \(C_{\min}\) mutual差约
\(5\)--\(12\%\), extremelysmallextremelylargeOptimal 性Conclusionnot变 (may达界andlower bound接受mutualsame的格点correctionbecause子). 详细Derivationsee
\texttt{06\_fixed\_point\_convergence.md} §12 (Remark on Bahadur-Rao
Lattice Correction). 
\end{quote}

\subsection{appendix B: non-0-1
loss的推广}\label{ux9644ux5f55-bux975e-0-1-ux635fux5931ux7684ux63a8ux5e7f}

Theorem 1 to一般has 界loss \(\ell \in [0, B]\) also成立, onlymustwill Lemma 1 in the 
\(e_m\) 解释is 
\(\mathbf{1}\{\ell(f_m(x), y) > \tau\}\). coreDerivationnot变, becauseis Lemma 1
only-dependenceConditionsexpect的linear性质andnoiseLabel的均匀性Assumptions (A4), not-dependence 0-1
loss的具体形式. 

specifically, to回归任务 (\(\mathcal{Y} \subseteq \mathbb{R}^d\)), \(K\)
的must重new解释. Lemma 1 in the \(K\) in回归scenariolower退化is : 

\[ \mathbb{E}[C \mid \text{noise}, x] \geq 1 - \mathbb{E}[C \mid \text{clean}, x] \]

i.e. \(K \to \infty\) extremely限case. thistimeseparation gapto任意 \(\mu_s < 1/2\)
is currently. thisisbecauseis incontinuousLabel Spacemiddle, 专家恰goodpredictiontonoiseLabel的probabilityis 零, 使noise detectionmore容易. Theorem
1 its余partnot变. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Changelog}\label{changelog}

\begin{longtable}[]{@{}
 >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1935}}
 >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2258}}
 >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2581}}
 >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3226}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Date
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Issue
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Change
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Severity
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
2026-06-28 & B1 & \textbf{Fixed A2 self-contradiction.} Updated A2
rationale to acknowledge A1 does NOT guarantee A2. Added
\(M_{\text{eff}}\) degradation formula and cross-reference to
\texttt{01\_symbol\_system.md} §12.5. & BLOCKING \\
2026-06-28 & B6 & \textbf{Added \(M \geq 1\), \(K \geq 2\) to Theorem 1
preamble.} Explicitly excludes degenerate edge cases where consensus
score is undefined or \(K-1=0\). & BLOCKING \\
2026-06-28 & B10 & \textbf{Synced DEFECT-06 (Bahadur-Rao lattice
correction)} to Theorem 1 appendix. Added cross-reference to
\texttt{06\_fixed\_point\_convergence.md} §12. & BLOCKING \\
\end{longtable}

\end{document}
