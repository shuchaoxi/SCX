% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Theorem 1: Multi-Expert Consistency Guarantees for Label Noise
Detection}\label{theorem-1-multi-expert-consistency-guarantees-for-label-noise-detection}

\begin{quote}
\textbf{Core Claim}: when  \(M\)
at non-交Datafactor set on independent Training'sexpert for Sample'sConsistency Score \(C(x)\)
超阈value  \(\theta\) , SampleYestarget Noise's置信with number RateConvergenceto  1. 

\textbf{Revision Record}: 2026-06-27 --- Lemma 3 Proof重构(增 A6
Balanced Error DistributionAssumptions); Chernoff 附录 KL toward 修正; Theorem StatementUpdateas 
(A1)-(A6).  bug all validate 报告. 
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1 Theorem Statement}\label{ux5b9aux7406ux9648ux8ff0}

\subsubsection{1.1 Symboland 设
(Setup)}\label{ux7b26ux53f7ux4e0eux8bbeux5b9a-setup}

Define the following objects:

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.5000}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Symbol
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Meaning
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\(\mathcal{X}\) & Input space, measurable \\
\(\mathcal{Y}\) & Label space,\(|\mathcal{Y}| = K\)(classification) or
\(\mathcal{Y} \subseteq \mathbb{R}^d\)(regression) \\
\(\mathcal{D}\) & Data distribution,\((X, Y) \sim \mathcal{D}\) \\
\(f^* : \mathcal{X} \to \mathcal{Y}\) & True labeling function (unknown Oracle) \\
\(\{f_m\}_{m=1}^M\) & \(M\)
expert models,\(f_m : \mathcal{X} \to \mathcal{Y}\) \\
\(\Pi = \{s_1, \dots, s_K\}\) & Status划, \(\mathcal{X}\) measurable partition of \\
\(\ell : \mathcal{Y} \times \mathcal{Y} \to [0, B]\) & Bounded loss function \\
\(\tau > 0\) & Expert error threshold \\
\(\theta \in (0, 1)\) & Noise detection threshold \\
\end{longtable}

\textbf{Label Noise Model}: for each Sample \((x, y^*)\)(where \(y^* = f^*(x)\)
is the true label), the observed label \(y\) is generated as follows:

\[y = \begin{cases}
y^* & \text{with probability } 1 - \eta \\
\text{Uniform}(\mathcal{Y} \setminus \{y^*\}) & \text{with probability } \eta
\end{cases}\]

where \(\eta \in (0, 1/2)\) is the global noise rate, and noise events are independent of \(x\)
and all training data.

\textbf{Consistency Score}: for Sample
\((x, y)\), definition expert Error指示变quantity and Consistency Score: 

\[e_m(x, y) = \mathbf{1}\{\ell(f_m(x), y) > \tau\}, \quad
C(x) = \frac{1}{M} \sum_{m=1}^M e_m(x, y)\]

\textbf{Detection Rule}: Sample \((x, y)\) is flagged as noise if and only if
\(C(x) > \theta\). 

\subsubsection{1.2 Assumptions (Assumptions)}\label{ux5047ux8bbe-assumptions}

\textbf{(A1) Disjoint Training Sets}: \(M\) expert at  \(M\)
non-交'sindependent Distributionfactor set on Training: 

\[D_m \sim \mathcal{D}^{n_m}, \quad D_m \cap D_{m'} = \varnothing, \quad D_m \perp D_{m'} \text{ for } m \neq m'\]

\textbf{(A2) CleanDataon 'sConditional Independence}: for ArbitraryCleanSample
\((x, y)\)(\(y = y^*\)), give  \(x\) 'sconditionsunder , Error指示变quantity 
\(\{e_m(x, y)\}_{m=1}^M\) YesConditional Independence's. 

\emph{JustificationExplanationand 局限}: A2
Yesone \textbf{Structureproperty Assumptions}, by expert Experimentdesign (Disjoint Training Sets
A1, independent 初始ization )which 论证, but \textbf{A1 non-蕴含
A2}. i.e.make Trainingset complete non-交, at similar DataDistributionon Training'sexpert can be can at DistributionSampleon \textbf{correlated Error}(source 共享归纳偏置, non-共享TrainingData). because 此
A2 at  SCX Online 'sDatain \textbf{non-can be via 验检验}------for continuous 输入space 
\(\mathcal{X}\), each  \(x\) at has datasetin one , Distribution
\(P(e_1, \dots, e_M \mid x)\) non-can be 观测.  \textgreater{} \textgreater{}
\textbf{When A2 is 违反}(际in 见):using 
\(M_{\text{eff}} = M/(1 + (M-1)\bar{\rho})\) 替换which has concentration inequalitiesin 's
\(M\), where \(\bar{\rho}\)
as expert Errortarget 'sall for correlated property . for type set , \(\bar{\rho} \approx 0.1\)--\(0.3\). 建议at 留置validate set on 估计
\(\bar{\rho}\), using  \(M_{\text{eff}}\) to 保守Guarantee. 详细Analysis参见
\texttt{01\_symbol\_system.md} §12.5. 

\textbf{(A3) Bounded Loss}:
\(\ell(a, b) \in [0, B], \; \forall a, b \in \mathcal{Y}\), where
\(B < \infty\). 

\textbf{(A4) Uniform Independent Noise}: target 签翻转事item and  \(x\) and which has  \(D_m\)
independent . Noisetarget at  \(\mathcal{Y} \setminus \{y^*\}\) on all Distribution. 

\textbf{(A5) StatusHomogeneousproperty  (Status划充property )}: Status \(\Pi\)
make at each Status \(s\)
内, expert 'sCleanDataErrorrate approximate all 匀. formula ization , at state-levelnumber 
\(\{\mu_s\}_{s \in \mathcal{S}}\) make : 

\[\sup_{x \in s} \, \mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s, \quad \forall s \in \mathcal{S}\]

i.e.Status \(s\) CleanSample'sall expert Errorrate is  \(\mu_s\) one Upper Boundcontrol . 

\textbf{(A6) Balanced Error Distribution}:
expert 'serror probabilityat which has ErrorCategoryon non-can set in . at number 
\(C_{\text{bal}} \geq 1\) make for ArbitraryStatus \(s\) and  \(x \in s\): 

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\(C_{\text{bal}} = 1\)
for should complete all 匀'sErrorDistribution(each ErrorCategoryetc.概rate ); \(C_{\text{bal}} = 2\)
Yestype 's保守Selection. Assumptionscan be 检验and at in 几乎satisfies ------at non-交CleanDataon Training'sexpert has by will Errorset in to Category. 

\subsubsection{1.3 Key Lemma
(Mean Separation)}\label{ux5173ux952eux5f15ux7406-ux5747ux503cux5206ux79bb}

\textbf{Lemma 1 (Mean Separation).} at Assumptions (A1)-(A5) under (A6
non-influence 期望), for Arbitrary \(x \in s\): 

\textbf{(CleanSample)}
\[\mathbb{E}[C(X) \mid \text{clean}, X = x] \leq \mu_s\]

\textbf{(NoiseSample)}
\[\mathbb{E}[C(X) \mid \text{noise}, X = x] = 1 - \frac{1}{K-1} \cdot \mathbb{E}[C(X) \mid \text{clean}, X = x] \geq 1 - \frac{\mu_s}{K-1}\]

because 此, when  \(\mu_s < \frac{K-1}{K}\) , at  \(\theta\) make : 

\[\mathbb{E}[C \mid \text{clean}] < \theta < \mathbb{E}[C \mid \text{noise}]\]

and 离间隙as : 

\[\Delta_s(\theta) = \min\left(\theta - \mu_s, \; 1 - \frac{\mu_s}{K-1} - \theta\right)\]

when SelectionOptimalvalue 
\(\theta_s^* = \frac{1}{2}\left(1 + \mu_s \cdot \frac{K-2}{K-1}\right)\)
, 离间隙maximum ization (at  \(C_{\text{bal}}=1\) 's想况under ):

\[\Delta_s^* = \frac{1}{2}\left(1 - \mu_s \cdot \frac{K}{K-1}\right)\]

when  \(C_{\text{bal}} > 1\) , Optimalvalue toward  \(\mu_s\)
toward 微调with 补偿non-all error Distribution. \(\theta_s^*\) 'sprecise 表达formula requires 求解
\(\theta - \mu_s = 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta\). 

\textbf{Proof.} CleanSample'sbound by  (A5) direct give . for NoiseSample: 

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, X = x] 
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(\ell(f_m(x), y) > \tau \;\big|\; \text{noise}, x\bigr) \\
&= \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) \neq y \;\big|\; \text{noise}, x\bigr) \quad (\text{for  } 0\text{-}1 \text{ 损失}) \\
&= 1 - \frac{1}{M} \sum_{m=1}^M \mathbb{P}\bigl(f_m(x) = y \;\big|\; \text{noise}, x\bigr)
\end{aligned}\]

by  (A4), Noisetarget  \(y\) at  \(\mathcal{Y} \setminus \{y^*\}\)
on all Distribution, and independent which has expert : 

\[\begin{aligned}
\mathbb{P}(f_m(x) = y \mid \text{noise}, x) 
&= \sum_{c \neq y^*} \mathbb{P}(y = c \mid \text{noise}) \cdot \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(f_m(x) = c \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{P}(f_m(x) \neq y^* \mid x) \\
&= \frac{1}{K-1} \cdot \mathbb{E}[e_m \mid \text{clean}, x]
\end{aligned}\]

because 此: 

\[\begin{aligned}
\mathbb{E}[C \mid \text{noise}, x] 
&= 1 - \frac{1}{K-1} \cdot \frac{1}{M} \sum_{m=1}^M \mathbb{E}[e_m \mid \text{clean}, x] \\
&= 1 - \frac{1}{K-1} \cdot \mathbb{E}[C \mid \text{clean}, x] \\
&\geq 1 - \frac{\mu_s}{K-1} \quad \text{by  (A5)}
\end{aligned}\]

离间隙'sat property by  \(\mu_s < \frac{K-1}{K}\) Guarantee, 此
\(1 - \frac{\mu_s}{K-1} > \mu_s\). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsubsection{1.4 Main Result (Main
Result)}\label{ux4e3bux5b9aux7406-main-result}

\textbf{Theorem 1 (SCX NoiseDetectionGuarantee).} Assumptions (A1)-(A6) 立, and 
\(M \geq 1\)(to one expert ), \(K \geq 2\)(to target Category, with Guarantee
\(K-1 > 0\)).  \(\rho_s = \mathbb{P}(X \in s)\)
as Statusrate . for Arbitraryvalue  \(\theta\) satisfies 
\(\mu_s < \theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) 'sStatus
\(s\), definition state-level离间隙: 

\[\Delta_s = \min\left(\theta - \mu_s,\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right) > 0\]

when  \(C_{\text{bal}} = 1\)(最佳形), \(\Delta_s\) restore 原始definition ; when 
\(C_{\text{bal}} > 1\)
, Noise侧's间隙收窄(because as Errorcan be can non-Yescomplete all Distribution's). 

then  SCX NoiseDetectiondevice 's F1 satisfies with under lower bound: 

\[\text{F1} \;\geq\; 1 - \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-2M\Delta_s^2\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-2M\Delta_s^2\bigr) \Bigr]\]

or etc.价: 

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\!\bigl(-2M\Delta_s^2\bigr)\]

where \(\eta\) as global Noiserate . 

\textbf{更紧's Chernoff formula }: for 设, make using  Chernoff bound 代替
Hoeffding can be 更紧'snumber Rate: 

\[\text{F1} \;\geq\; 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \Bigl[ \exp\!\bigl(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\bigr) \;+\; \frac{1-\eta}{\eta} \cdot \exp\!\bigl(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\bigr) \Bigr]\]

where
\(\text{KL}(p \,\|\, q) = p \log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\). Chernoff
bound at  \(\mu_s\) 接近 \(\theta\) or 
\(1 - C_{\text{bal}} \cdot \mu_s/(K-1)\) 接近 \(\theta\) 显著优
Hoeffding(can be  2-5 倍). 

\begin{quote}
\textbf{2026-06-27 修正}: 原版 Chernoff make using 
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\), KL toward has 误, and not yet 纳入
\(C_{\text{bal}}\). already 修正as 
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\). 
\end{quote}

\textbf{Key Corollary}: when  \(M \to \infty\) , for which has satisfies 
\(\mu_s < \frac{K-1}{K}\) 'sStatus, F1 with number RateConvergenceto  1: 

\[\text{F1} \;=\; 1 - \mathcal{O}_P\!\left(\frac{1}{\eta} \cdot e^{-2M\Delta_{\min}^2}\right)\]

where \(\Delta_{\min} = \min_{s \in \mathcal{S}} \Delta_s\)
as worst Status离间隙. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2 Proof}\label{ux8bc1ux660e}

\subsubsection{2.1 False Positive Rate on Clean Data (False Positive
Rate)}\label{ux6e05ux6d01ux6570ux636eux7684ux5047ux9633ux6027ux7387-false-positive-rate}

\textbf{Lemma 2 (FPR Upper Bound).} for ArbitraryStatus \(s \in \mathcal{S}\) satisfies 
\(\mu_s < \theta\), CleanSampleis target as Noise'srate satisfies : 

\[\mathbb{P}(C > \theta \mid \text{clean}, X \in s) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

\textbf{Proof.} fixed  \(x \in s\), by  (A5) 知
\(\mathbb{E}[C \mid \text{clean}, X = x] \leq \mu_s\). by 
(A1)-(A2), give  \(x\)  \(\{e_m\}\) Conditional Independence. by  (A3) 知
\(e_m \in [0, 1]\). for  \(C = \frac{1}{M}\sum_m e_m\) should using  Hoeffding
non-etc.formula (Pollard, 1990):

\[\mathbb{P}(C - \mathbb{E}[C] > \theta - \mu_s \mid \text{clean}, x) \leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)\]

by  \(\theta > \mu_s\), \((\theta - \mu_s) > 0\). bound for Arbitrary
\(x \in s\) 立, because 此: 

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, X \in s) 
&= \mathbb{E}_{X \mid s}\bigl[ \mathbb{P}(C > \theta \mid \text{clean}, X) \bigr] \\
&\leq \sup_{x \in s} \mathbb{P}(C > \theta \mid \text{clean}, x) \\
&\leq \exp\!\bigl(-2M(\theta - \mu_s)^2\bigr)
\end{aligned}\]

此i.e.which requires . \(\square\)

\subsubsection{2.2 True Positive Rate on Noisy Data (True Positive
Rate)}\label{ux566aux58f0ux6570ux636eux7684ux771fux9633ux6027ux7387-true-positive-rate}

\textbf{增Assumptions (A6) --- Balanced Error Distribution}: for ArbitraryStatus \(s\) and Sample
\(x \in s\), expert 'serror probabilityat which has ErrorCategoryon non-can set in . formula ization , at number 
\(C_{\text{bal}} \geq 1\) make : 

\[\max_{c \neq y^*} \mu_c(x) \leq C_{\text{bal}} \cdot \frac{\mu_s}{K-1}, \quad \text{where } \mu_c(x) = \frac{1}{M} \sum_{m=1}^M \mathbb{P}(f_m(x) = c \mid x)\]

\emph{Justification}: expert at non-交'sCleanDatafactor set on Training, has by will Errorset in to 特'sErrorCategory. all NoiseModelunder , each ErrorCategory'srate 天然约as 
\(\mu_s/(K-1)\). \(C_{\text{bal}} = 2\)
already Yes保守Selection. Assumptionscan be 检验: at Dataon 估计
\(\max_c \mu_c(x) / (\mu_s/(K-1))\). 

\textbf{Lemma 3 (TPR lower bound).} at Assumptions (A1)-(A6) under , for ArbitraryStatus
\(s \in \mathcal{S}\) satisfies 
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\), NoiseSampleis CorrectDetection'srate satisfies : 

\[\mathbb{P}(C > \theta \mid \text{noise}, X \in s) \geq 1 - \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)\]

\textbf{Proof}: fixed  \(x \in s\). give Noisetarget 签
\(y = c\)(\(c \neq y^*\)), for  0-1 损失, expert  \(m\) 'sError指示as 
\(e_m = \mathbf{1}\{f_m(x) \neq c\}\). by  \(f_m\) only dependence
\(D_m\)(A1), give  \(x\)  \(\{f_m(x)\}\) Conditional Independence(A2), because 此
\(\{e_m\}\) at give  \((x, c)\) under Conditional Independence. 

each  \(e_m\) 's期望as 
\(\mathbb{E}[e_m \mid x, c] = 1 - \mathbb{P}(f_m(x) = c \mid x) = 1 - \mu_{c,m}(x)\). because 此: 

\[\mathbb{E}[C \mid x, c] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} \quad \text{(by  A6)}\]

by  \(e_m \in [0, 1]\) Conditional Independence, should using  Hoeffding non-etc.formula : 

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid x, c)
&= \mathbb{P}\!\left(\frac{1}{M} \sum_{m=1}^M e_m \leq \theta \;\Big|\; x, c\right) \\
&\leq \exp\!\left(-2M\left(1 - \mu_c(x) - \theta\right)^2\right) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

where Hoeffding 's gap conditions \(1 - \mu_c(x) > \theta\) by theorem Assumptions
\(\theta < 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\) and  A6 Guarantee: 
\(1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} > \theta\). 

for Noisetarget  \(c\) 取平all (all Distribution over \(K-1\) class ):

\[\begin{aligned}
\mathbb{P}(C \leq \theta \mid \text{noise}, x)
&= \frac{1}{K-1} \sum_{c \neq y^*} \mathbb{P}(C \leq \theta \mid x, c) \\
&\leq \exp\!\left(-2M\left(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta\right)^2\right)
\end{aligned}\]

because 此
\(\mathbb{P}(C > \theta \mid \text{noise}, x) \geq 1 - \exp(-2M(1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1} - \theta)^2)\). for 
\(x \in s\) 取期望i.e.. \(\square\)

\textbf{Remark}: when  \(C_{\text{bal}} = 1\)(complete all 匀'sErrorDistribution), Lemma
3 restore 原始命题in 'sOptimal bound. \(C_{\text{bal}}\)
can be at Dataon 估计, using 校准Detectionvalue  \(\theta\). 

\subsubsection{2.3 F1 lower bound}\label{f1-ux4e0bux754c}

at  Lemma 2 and  Lemma 3 推导 F1 lower bound. 

令Detection Ruleas  \(R(x) = \mathbf{1}\{C(x) > \theta\}\). definition : 

\[\begin{aligned}
\text{TPR}_s &= \mathbb{P}(R = 1 \mid \text{noise}, X \in s) \geq 1 - \exp(-2M(1 - \mu_s/(K-1) - \theta)^2) \\
\text{FPR}_s &= \mathbb{P}(R = 1 \mid \text{clean}, X \in s) \leq \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

by  (A4), Noiseitem and  \(X\) independent , because 此
\(\mathbb{P}(\text{noise} \mid X \in s) = \eta\) for which has Status立. 整
TPR and  FPR as : 

\[\begin{aligned}
\text{TPR} &= \sum_s \rho_s \cdot \text{TPR}_s, \quad
\text{FPR} = \sum_s \rho_s \cdot \text{FPR}_s
\end{aligned}\]

F1 as : 

\[\begin{aligned}
\text{F1} &= \frac{2 \cdot \text{TP} }{2 \cdot \text{TP} + \text{FP} + \text{FN}} \\
&= \frac{2\eta \cdot \text{TPR}}{2\eta \cdot \text{TPR} + (1-\eta) \cdot \text{FPR} + \eta \cdot (1 - \text{TPR})} \\
&= \frac{2\eta \cdot \text{TPR}}{\eta(1 + \text{TPR}) + (1-\eta) \cdot \text{FPR}}
\end{aligned}\]

代入 TPR \(\geq 1 - \delta_1\) and  FPR \(\leq \delta_2\), where: 

\[\begin{aligned}
\delta_1 &= \sum_s \rho_s \cdot \exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \\
\delta_2 &= \sum_s \rho_s \cdot \exp(-2M(\theta - \mu_s)^2)
\end{aligned}\]

to : 

\[\begin{aligned}
\text{F1} &\geq \frac{2\eta(1 - \delta_1)}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&= 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta(2 - \delta_1) + (1-\eta)\delta_2} \\
&\geq 1 - \frac{\eta\delta_1 + (1-\eta)\delta_2}{\eta} \quad (\text{because  } \geq \eta) \\
&= 1 - \delta_1 - \frac{1-\eta}{\eta}\delta_2
\end{aligned}\]

代入 \(\delta_1, \delta_2\) 's表达formula , 并注when 
\(\Delta_s = \min(\theta - \mu_s, \; 1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)\)
has  \(\exp(-2M(\theta - \mu_s)^2) \leq \exp(-2M\Delta_s^2)\) and 
\(\exp(-2M(1 - C_{\text{bal}} \cdot \mu_s/(K-1) - \theta)^2) \leq \exp(-2M\Delta_s^2)\), because 此: 

\[ \text{F1} \geq 1 - \sum_s \rho_s \left[\exp(-2M\Delta_s^2) + \frac{1-\eta}{\eta}\exp(-2M\Delta_s^2)\right] = 1 - \frac{1}{\eta}\sum_s \rho_s \exp(-2M\Delta_s^2) \]

证毕. \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3 Corollaries (Corollaries)}\label{ux63a8ux8bba-corollaries}

\subsubsection{3.1 Corollaries 1: Symmetric Experts (Symmetric
Experts)}\label{ux63a8ux8bba-1ux5bf9ux79f0ux4e13ux5bb6-symmetric-experts}

when which has expert has 'sCleanDataErrorrate 
\(\varepsilon\)(称as ''Symmetric Experts''), \(\mu_s = \varepsilon\)
for which has Statusone 致. 此: 

\[\Delta = \frac{1}{2}\left(1 - \varepsilon \cdot \frac{K}{K-1}\right)\]

and  F1 lower boundization as : 

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M}{2}\left(1 - \frac{\varepsilon K}{K-1}\right)^2\right)\]

when  \(K = 2\)(class ), one 步简ization as : 

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-2M\left(\frac{1}{2} - \varepsilon\right)^2\right)\]

\textbf{义}: at class Symmetric Expertsscenario under , Detectionquantity complete by expert Errorrate 
\(\varepsilon\) and  \(1/2\) 's差距决. \(\varepsilon\) each 远离 \(1/2\)
one standard 差(\(\approx 1/\sqrt{2M}\)), F1 lower boundboost one number number quantity level . 

\subsubsection{3.2 Corollaries 2: Optimal Threshold Selection (Optimal
Threshold)}\label{ux63a8ux8bba-2ux6700ux4f18ux9608ux503cux9009ux62e9-optimal-threshold}

for give 'sNoiserate  \(\eta\), Categorynumber  \(K\), expert number  \(M\) and maximum CleanErrorrate 
\(\mu_{\max} = \max_s \mu_s\), OptimalDetectionvalue  \(\theta^*\) should satisfies : 

\[\theta^* = \arg\max_\theta \min_s \Delta_s(\theta) = \frac{1}{2}\left(1 + \mu_{\max} \cdot \frac{K-2}{K-1}\right)\]

此minimum 离间隙as : 

\[\Delta_{\min}^* = \frac{1}{2}\left(1 - \mu_{\max} \cdot \frac{K}{K-1}\right)\]

which requires 's最少expert number (达to  F1 \(\geq 1 - \varepsilon_0\))as : 

\[M \geq \frac{1}{2\Delta_{\min}^{*2}} \log\left(\frac{1}{\eta \varepsilon_0}\right)\]

\textbf{Proof}: direct 解
\(1 - \frac{1}{\eta}e^{-2M\Delta^2} \geq 1 - \varepsilon_0\) 
\(e^{-2M\Delta^2} \leq \eta\varepsilon_0\), for number i.e.. if requires simultaneouslycontrol multiple Status'serror (联bound ), factor in 's
\(1\) 替换as Statusnumber  \(|\mathcal{S}|\). \(\square\)

\subsubsection{3.3 Corollaries 3: one can be Detection'sconditions (Uniform
Detectability)}\label{ux63a8ux8bba-3ux4e00ux81f4ux53efux68c0ux6d4bux7684ux5145ux5206ux6761ux4ef6-uniform-detectability}

such as 果存at  \(\delta > 0\) make for which has Status \(s\) has 
\(\mu_s \leq \frac{K-1}{K} - \delta\), then 令
\(\theta = \frac{1}{2}\)(fixed value ), for which has Statushas 
\(\Delta_s \geq \frac{\delta}{2}\), and : 

\[\text{F1} \geq 1 - \frac{1}{\eta} \exp\!\left(-\frac{M\delta^2}{2}\right)\]

\textbf{义}: only must each Status'sCleanall expert Errorrate one 致低 \((K-1)/K\)(when 
\(K=2\) as  \(1/2\)), make using fixed value  \(\theta = 1/2\) i.e.can be to obtain number level 别's
F1 Guarantee. 

\subsubsection{3.4 Corollaries 4: has Sample校正 (Finite-Sample
Correction)}\label{ux63a8ux8bba-4ux6709ux9650ux6837ux672cux6821ux6b63-finite-sample-correction}

at in , \(\mu_s\) 必须Fromhas validate Data估计. each Status \(s\) has  \(n_s\)
Cleanvalidate Sample. using 
\(\hat{\mu}_s = \frac{1}{M}\sum_m \hat{\varepsilon}_m(s)\) 估计
\(\mu_s\), where \(\hat{\varepsilon}_m(s)\) is the probability that experts \(m\) at Status \(s\)
on 'svia Errorrate . 

by  Hoeffding non-etc.formula and bound : for Arbitrary \(\delta_0 \in (0, 1)\), with probability
\(\geq 1 - \delta_0\): 

\[|\hat{\mu}_s - \mu_s| \leq B \sqrt{\frac{\log(2M/\delta_0)}{2n_s}}, \quad \forall m, s\]

设校正'svalue as 
\(\tilde{\mu}_s = \hat{\mu}_s + B\sqrt{\frac{\log(2M/\delta_0)}{2n_s}}\)(保守Upper Bound). then 
Theorem 1 'sConclusionfor 校正's \(\tilde{\mu}_s\) with probability
\(\geq 1 - \delta_0\) 立, only requires at  \(\Delta_s\) in using  \(\tilde{\mu}_s\)
替代 \(\mu_s\). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4 紧致property 讨论
(Tightness)}\label{ux7d27ux81f4ux6027ux8ba8ux8bba-tightness}

\subsubsection{4.1 何紧致}\label{ux4f55ux65f6ux7d27ux81f4}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{ M 极限}: when 
  \(M \to \infty\), \(C \xrightarrow{\text{a.s.}} \mathbb{E}[C]\)(by number 律), Detectiondetermine property 's: \(C > \theta\)
  when and only when  \(\mathbb{E}[C] > \theta\).  F1 by  \(\eta\) and  \(\mu_s\)
  complete 决, theorem lower bound紧致. 
\item
  \textbf{StatusHomogeneousproperty }: when  (A5)
  严格立(StatusCleanErrorrate complete all 匀)and expert for 称, Hoeffding bound 'snumber because factor 
  \(2\) YesOptimal's(by  Hoeffding lower boundGuarantee). 
\item
  \textbf{\(K \to \infty\)}: 离间隙 \(\Delta_s \to \frac{1}{2}\)(fixed 
  \(\mu_s\)), Detection渐近完美. 
\end{enumerate}

\subsubsection{4.2 何松弛}\label{ux4f55ux65f6ux677eux5f1b}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{ M}: when  \(M \leq 3\) , Hoeffding
  bound 较松弛. should using precise formula Distribution(while non-
  Hoeffding)can be 更紧'sbound . via 验规then : \(M \geq 10\)  Hoeffding
  bound 紧致; \(M \leq 5\) make using precise formula 累积Distribution(Corollary 4.1). 
\item
  \textbf{\(\mu_s\) 接近 \(\frac{K-1}{K}\)}: when 
  \(\mu_s \uparrow (K-1)/K\) 
  \(\Delta_s \to 0\), number 衰减because factor 消失. which requires expert number 
  \(M \propto 1/\Delta_s^2\)
  散. this Yesproperty 's: when expert at CleanDataon 接近随猜测, NoiseDetection必然non-can be 靠. 
\item
  \textbf{弱featureization }:
  when StatusNomethod Noise导区域and Clean导区域, (A5) non-立. 此
  Theorem 1 non-适using , requires 借助交叉validate or DistributionDetection(详见 Prop 6
  state discovery). this Yes SCX
  framework 'salready 知边bound (参见\emph{弱feature失效}概念档). 
\item
  \textbf{Noisenon-all 匀}: when  \(\eta\) dependence \(x\)
  (\(P(\text{noise} \mid x)\) non-number ), F1 's表达formula requires using 
  \(\eta_s = \mathbb{P}(\text{noise} \mid X \in s)\) 替代global  \(\eta\): 
\end{enumerate}

\[\text{F1} \geq 1 - \frac{1}{\min_s \eta_s} \sum_s \rho_s \exp(-2M\Delta_s^2)\]

at  \(\eta_s \to 0\) 's极端Status, DetectionNo义------该Statushas Noise. 

\subsubsection{4.3
紧致property for Experiment}\label{ux7d27ux81f4ux6027ux5bf9ux6bd4ux5b9eux9a8c}

for type value  \(K=10\), \(\eta=0.1\), \(M=20\), \(\mu_s=0.2\), Theorem 1
give : 

\[\Delta_s = \frac{1}{2}(1 - 0.2 \cdot 10/9) \approx 0.389, \quad \text{F1} \geq 1 - 10 \cdot e^{-2\cdot 20 \cdot 0.389^2} \approx 1 - 10 \cdot e^{-6.05} > 0.976\]

\textbf{注}: on ComputationAssumptions
\(\mu_s = 0.2\), i.e.expert at CleanDataon 'sErrorrate as  20\%. at  CIFAR-10
Experimentin (\(M=20\), \(\eta=0.1\)), SCX-Noise 's F1 as 
\(0.617\). this value 远低 \(0.976\) 'slower bound, Explanation CIFAR-10
Experimentin \textbf{Assumptionsnon-satisfies }------最can be can 'sReasonYes: (i) expert only at  3 epoch 's
CPU Trainingmake using ,  \(\mu_s \approx 0.45\)(while non-假's \(0.2\)), 此
\(\Delta_s \approx 0.25\), lower boundization as  \(\text{F1} \geq 0.18\)(and 测
\(0.617 > 0.18\) one 致); (ii) (A5)
StatusHomogeneousproperty at 低质featureunder non-严格立. \textbf{教训}: theorem lower boundfor  \(\mu_s\)
敏感, in 必须at validate set on accuracy 估计 \(\mu_s\) again should using Formula. at  AlN v3
全 GPU Trainingscenario in , \(\mu_s\) can be to 
\(0.03\text{--}0.18\), lower boundwith 紧致(见正 Table 2). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5 and  Dawid-Skene
'srelationship }\label{ux4e0e-dawid-skene-ux7684ux5173ux7cfb}

\subsubsection{5.1 Dawid-Skene
Model回顾}\label{dawid-skene-ux6a21ux578bux56deux987e}

Dawid and  Skene (1979) using  EM
AlgorithmFromtarget 注者(expert )'sindependent target in 估计真target and target 注者can be property . core AssumptionsYeseach target 注者
\(m\) has one \textbf{global 混淆矩matrix }: 

\[\pi_m(c' \mid c) = \mathbb{P}(f_m(x) = c' \mid y^* = c), \quad \forall x \in \mathcal{X}\]

i.e.target 注者'sErrorModenon-dependenceconcrete Sample \(x\)------this is aglobal number . 

最终'starget 签估计through 权投票完: 

\[\hat{y}_{\text{DS}} = \arg\max_c \sum_{m=1}^M w_m \cdot \mathbf{1}\{f_m(x) = c\}\]

where权重 \(w_m\) by  \(\pi_m\)
'sfor 角元素(Accuracy)决, or one 般, by 混淆矩matrix 's逆决. 

\subsubsection{5.2 SCX
's改: Statusconditionscan be property }\label{scx-ux7684ux6539ux8fdbux72b6ux6001ux6761ux4ef6ux53efux9760ux6027}

SCX will 混淆矩matrix generalize as \textbf{Statusconditions}formula : 

\[\pi_m(c' \mid c, s) = \mathbb{P}(f_m(x) = c' \mid y^* = c, x \in s)\]

SCX 权重because dependenceStatus: 

\[w_m(x) = \frac{\exp(-\alpha \hat{R}_m(s(x)))}{\sum_{m'} \exp(-\alpha \hat{R}_{m'}(s(x)))}\]

where \(\hat{R}_m(s) = \sum_{c} \pi_m(c \mid c, s)\) as Statusconditions风险估计. 

\subsubsection{5.3 SCX 严格优 Dawid-Skene
'sconditions}\label{scx-ux4e25ux683cux4f18ux4e8e-dawid-skene-ux7684ux6761ux4ef6}

\textbf{Proposition 5.1 (SCX-DS 较).}  \(R_{\text{DS}}\) as 
Dawid-Skene 权's期望 0-1 损失, \(R_{\text{SCX}}\) as  SCX
Statusconditions权's期望损失. then : 

\[R_{\text{SCX}} \leq R_{\text{DS}}\]

etc.号立when and only when for each target 注者 \(m\) and which has Status
\(s, s' \in \mathcal{S}\), 混淆矩matrix at Statusnon-变: 

\[\pi_m(\cdot \mid \cdot, s) = \pi_m(\cdot \mid \cdot, s'), \quad \forall s, s' \in \mathcal{S}\]

i.e.target 注者can be property non-dependenceSampleStatus. 

\textbf{Proof.} for Arbitrary
\(x \in s\), 权投票's贝叶斯Optimal权重YesStatusconditions权重
\(w_m^*(s)\), minimum ization point 态期望损失: 

\[w^*(s) = \arg\min_{w \in \Delta^{M-1}} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\]

Dawid-Skene make using 'sglobal 权重 \(\bar{w}\) Yesnumber : 

\[\bar{w} = \arg\min_{w} \mathbb{E}_{X, Y^*}[\mathbb{E}[\ell(\text{vote}_w(X), Y^*) \mid X]]\]

by PointwiseOptimalproperty , for each  \(x \in s\): 

\[\mathbb{E}[\ell(\text{vote}_{w^*(s)}(x), y^*) \mid x \in s] \leq \mathbb{E}[\ell(\text{vote}_{\bar{w}}(x), y^*) \mid x \in s]\]

at StatusDistributionon 取期望i.e. \(R_{\text{SCX}} \leq R_{\text{DS}}\). 

etc.号conditionsby Pointwisenon-etc.formula 's严格property 导: only has when global 权重also Yeseach Status'sOptimal权重etc.号立, i.e.
\(\bar{w} \in \arg\min_{w} \mathbb{E}[\ell(\text{vote}_w(x), y^*) \mid x \in s]\)
for which has  \(s\)
立. this must 求各Status'sOptimal权重, etc.价混淆矩matrix non-dependenceStatus. \(\square\)

\subsubsection{5.4
at NoiseDetection语境under 'sconcrete Difference}\label{ux5728ux566aux58f0ux68c0ux6d4bux8bedux5883ux4e0bux7684ux5177ux4f53ux5deeux5f02}

Theorem 1
'sDetection Ruleestablish at ''one property ''概念on ------NoiseSamplefor which has expert all Error. this Yes
SCX framework 'shas advantage , and  Dawid-Skene 'sglobal can be property 估计has Difference: 

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3611}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4722}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Dimension
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Dawid-Skene
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
SCX (Theorem 1)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
can be property  & global number  \(\pi_m\) & Statusconditions \(\pi_m(s)\) \\
NoiseDetection & dependencetarget 注者one property \emph{验} & dependenceexpert Error\emph{one property } \\
which requires target  & requires must multiple (通 \(\geq 3\))target 注者 & requires must multiple expert Model \\
抗Categorynon-平衡 & 弱(global 混淆矩matrix 受导Category支配) &
强(state-level估计non-受influence ) \\
区 Noise vs Hard & non-can (only give 验概rate ) & can (Consistency Score \(C\)
天然离者) \\
TheoryGuarantee & 渐近one property (\(M \to \infty\)) & number Convergence(\(M\) and  \(K\)
all 速) \\
\end{longtable}

\subsubsection{5.5 Dawid-Skene as  SCX
'se.g.}\label{dawid-skene-ux4f5cux4e3a-scx-ux7684ux7279ux4f8b}

when Status划退ization to 平凡划
\(\mathcal{S} = \{\mathcal{X}\}\)(i.e.which has Sampleat one Statusin ): 

\begin{itemize}
\tightlist
\item
  SCX 权重退ization as 
  \(\bar{w}_m \propto \exp(-\alpha \hat{R}_m)\), i.e.global number 权重
\item
  Consistency Score \(C(x)\) ization as global all Errorrate 
\item
  Theorem 1 'sDetectionGuaranteeization as dependence
  \(\mu = \frac{1}{M}\sum_m \varepsilon_m\) 'sglobal Guarantee
\end{itemize}

此 SCX 'sNoiseDetectionetc.价 Dawid-Skene 'sglobal can be property Method. this Proof SCX
framework 's严格generalize property . 

\subsubsection{5.6
改幅'squantity ization }\label{ux6539ux8fdbux5e45ux5ea6ux7684ux91cfux5316}

SCX for  Dawid-Skene 's改幅and Status间'scan be property 变异direct correlated : 

\[\Delta R = R_{\text{DS}} - R_{\text{SCX}} = \sum_{s \in \mathcal{S}} \rho_s \cdot \mathcal{D}_s\]

where
\(\mathcal{D}_s = \sum_{m=1}^M (\bar{w}_m - w_m^*(s)) \cdot R_m(s)\)
YesStatus \(s\) on make using global 权重while non-Statusconditions权重's额风险. 

by  Proposition 3(Statusconditions权重命题)'stheorem  3.1: 

\[\Delta R \geq \frac{1}{\alpha} \cdot I(s; w)\]

where \(I(s; w) = \mathbb{E}_s[\text{KL}(w(s) \| \bar{w})]\)
YesStatusand 权重间have mutual information. 

\textbf{Conclusion}: SCX 严格优 Dawid-Skene when and only when expert can be property 'sStatus变异
\(I(s; w) > 0\)------this at in 几乎总Yes立. Theorem 1 'sDetectionGuaranteeonly Yes
SCX framework advantage at NoiseDetection任务on 'sset in . 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{References}\label{ux53c2ux8003ux6587ux732e}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  Dawid, A. P., \& Skene, A. M. (1979). Maximum likelihood estimation of
  observer error-rates using the EM algorithm. \emph{Journal of the
  Royal Statistical Society: Series C (Applied Statistics)}, 28(1),
  20-28.
\item
  Hoeffding, W. (1963). Probability inequalities for sums of bounded
  random variables. \emph{Journal of the American Statistical
  Association}, 58(301), 13-30.
\item
  Pollard, D. (1990). Empirical Processes: Theory and Applications.
  \emph{NSF-CBMS Regional Conference Series in Probability and
  Statistics}, 2, i-86.
\item
  Bartlett, P. L., \& Mendelson, S. (2002). Rademacher and Gaussian
  complexities: Risk bounds and structural results. \emph{Journal of
  Machine Learning Research}, 3(Nov), 463-482.
\item
  SCX Framework Definitions.
  \texttt{../definitions/01\_state\_conditioned\_risk.md}.
\item
  Proposition 3: State-Conditioned Expert Weighting.
  \texttt{../propositions/03\_state\_conditioned\_weighting.md}.
\item
  Proposition 4: Compression Fidelity.
  \texttt{../propositions/04\_compression\_fidelity.md}.
\item
  Datain 毒概念. \texttt{../../knowledge/02\_概念/Datain 毒.md}.
\item
  Datafour class 概念. \texttt{../../knowledge/02\_概念/Datafour class .md}.
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{附录 A: Chernoff
bound 's推导}\label{ux9644ux5f55-achernoff-ux754cux7684ux63a8ux5bfc}

Lemma 2 and  Lemma 3 can be with make using  Chernoff bound (while non-
Hoeffding)to obtain 更紧'snumber Rate. 

for CleanData,  \(\{e_m\}\) as independent 's Bernoulli quantity , all value as 
\(\mu = \mathbb{E}[C \mid \text{clean}, x] \leq \mu_s\). for 
\(\theta > \mu\): 

\[\begin{aligned}
\mathbb{P}(C > \theta \mid \text{clean}, x) 
&= \mathbb{P}\!\left(\frac{1}{M}\sum_m e_m > \theta\right) \\
&\leq \inf_{\lambda > 0} e^{-\lambda M\theta} \mathbb{E}\!\left[e^{\lambda\sum_m e_m}\right] \\
&= \inf_{\lambda > 0} e^{-\lambda M\theta} \prod_{m=1}^M (1 - \mu_m + \mu_m e^\lambda) \\
&\leq \exp\!\left(-M \cdot \text{KL}(\theta \,\|\, \mu_s)\right)
\end{aligned}\]

where
\(\text{KL}(p \,\|\, q) = p\log\frac{p}{q} + (1-p)\log\frac{1-p}{1-q}\), and one make using bound and 
\(\mu_m \leq \mu_s\). 

for NoiseData, by  Lemma 3 's修正Proof(give  \(c\) conditionsunder 
Hoeffding), Chernoff formula as : 

\[\mathbb{P}(C \leq \theta \mid \text{noise}, x, c) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - \mu_c(x)\right)\right) \leq \exp\!\left(-M \cdot \text{KL}\!\left(\theta \;\Big\|\; 1 - C_{\text{bal}} \cdot \frac{\mu_s}{K-1}\right)\right)\]

注 KL 'stoward : \(\text{KL}(\theta \| q)\) for should  Chernoff Upper Bound
\(\mathbb{P}(\hat{p} \leq \theta) \leq \exp(-n \cdot \text{KL}(\theta \| p))\), where
\(\hat{p}\) Yes \(p\) 'sSampleall value . this 
\(p = \mathbb{E}[C \mid \text{noise}] = 1 - \mu_c(x) \geq 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\), 
bound \(\mathbb{P}(C \leq \theta)\)(under 尾), which with PartParameterYes真期望
\(p \approx 1 - C_{\text{bal}} \cdot \mu_s/(K-1)\). 

\begin{quote}
\textbf{2026-06-27 修正}: 原版 Chernoff 附录make using 
\(\text{KL}(1-\theta \| 1 - \mu_s/(K-1))\), KL toward has 误. Correctformula as 
\(\text{KL}(\theta \| 1 - C_{\text{bal}} \cdot \mu_s/(K-1))\)------under 尾
bound in Partone ParameterYesvalue  \(\theta\), PartParameterYes真期望. 
\end{quote}

Chernoff bound  Hoeffding bound 'sby with under non-etc.formula 刻画(Hoeffding Yes
Chernoff 'sone approximate ):

\[\exp(-2M\Delta^2) \geq \exp(-M \cdot \text{KL}(\mu + \Delta \,\|\, \mu))\]

etc.号only when  \(\Delta \to 0\)
渐近立. at Parameter范围(\(\Delta \approx 0.1\text{-}0.4\))内, Chernoff
bound can be  \(2\text{-}5\) 倍. 

\begin{quote}
\textbf{DEFECT-06  (2026-06-28) --- Bahadur-Rao point 修正}:
when expert Erroras  Bernoulli Distribution(格point  \(h=1\)), Correct's Bahadur-Rao
at  Chernoff bound in introduce because factor  \((1-e^{-\lambda^*})^{-1}\) 替代
\(1/\lambda^*\). 修正's极极number  \(C_{\min}^{\text{(corr)}}\)
and 原始 \(C_{\min}\) 差约
\(5\)--\(12\%\), 极极Optimalproperty Conclusionnon-变(can be bound and lower bound接受'spoint 修正because factor ). 详细推导见
\texttt{06\_fixed\_point\_convergence.md} §12(Remark on Bahadur-Rao
Lattice Correction). 
\end{quote}

\subsection{附录 B: non- 0-1
损失'sgeneralize }\label{ux9644ux5f55-bux975e-0-1-ux635fux5931ux7684ux63a8ux5e7f}

Theorem 1 for one Bounded Loss \(\ell \in [0, B]\) also 立, only requires will  Lemma 1 in 's
\(e_m\) explain as 
\(\mathbf{1}\{\ell(f_m(x), y) > \tau\}\). core 推导non-变, because as  Lemma 1
only dependenceconditions期望'sOnline property property and Noisetarget 签'sall property Assumptions (A4), non-dependence 0-1
损失'sconcrete formula . 

concrete , for 回归任务(\(\mathcal{Y} \subseteq \mathbb{R}^d\)), \(K\)
's概念requires re-explain . Lemma 1 in 's \(K\) at 回归scenario under ization as : 

\[ \mathbb{E}[C \mid \text{noise}, x] \geq 1 - \mathbb{E}[C \mid \text{clean}, x] \]

i.e. \(K \to \infty\) 's极限况. 此离间隙for Arbitrary \(\mu_s < 1/2\)
as 正. this Yesbecause as at continuous target space in , expert 恰预测to Noisetarget 签'srate as 零, make NoiseDetection更容易. Theorem
1 'sits part non-变. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Changelog}\label{changelog}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1935}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2258}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2581}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3226}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Date
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Issue
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Change
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Severity
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
2026-06-28 & B1 & \textbf{Fixed A2 self-contradiction.} Updated A2
rationale to acknowledge A1 does NOT guarantee A2. Added
\(M_{\text{eff}}\) degradation formula and cross-reference to
\texttt{01\_symbol\_system.md} §12.5. & BLOCKING \\
2026-06-28 & B6 & \textbf{Added \(M \geq 1\), \(K \geq 2\) to Theorem 1
preamble.} Explicitly excludes degenerate edge cases where consensus
score is undefined or \(K-1=0\). & BLOCKING \\
2026-06-28 & B10 & \textbf{Synced DEFECT-06 (Bahadur-Rao lattice
correction)} to Theorem 1 appendix. Added cross-reference to
\texttt{06\_fixed\_point\_convergence.md} §12. & BLOCKING \\
\end{longtable}

\end{document}
