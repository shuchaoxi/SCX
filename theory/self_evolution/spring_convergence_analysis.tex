% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{color}
\usepackage{fancyvrb}
\newcommand{\VerbBar}{|}
\newcommand{\VERB}{\Verb[commandchars=\\\{\}]}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\}}
% Add ',fontsize=\small' for more characters per line
\newenvironment{Shaded}{}{}
\newcommand{\AlertTok}[1]{\textcolor[rgb]{1.00,0.00,0.00}{\textbf{#1}}}
\newcommand{\AnnotationTok}[1]{\textcolor[rgb]{0.38,0.63,0.69}{\textbf{\textit{#1}}}}
\newcommand{\AttributeTok}[1]{\textcolor[rgb]{0.49,0.56,0.16}{#1}}
\newcommand{\BaseNTok}[1]{\textcolor[rgb]{0.25,0.63,0.44}{#1}}
\newcommand{\BuiltInTok}[1]{\textcolor[rgb]{0.00,0.50,0.00}{#1}}
\newcommand{\CharTok}[1]{\textcolor[rgb]{0.25,0.44,0.63}{#1}}
\newcommand{\CommentTok}[1]{\textcolor[rgb]{0.38,0.63,0.69}{\textit{#1}}}
\newcommand{\CommentVarTok}[1]{\textcolor[rgb]{0.38,0.63,0.69}{\textbf{\textit{#1}}}}
\newcommand{\ConstantTok}[1]{\textcolor[rgb]{0.53,0.00,0.00}{#1}}
\newcommand{\ControlFlowTok}[1]{\textcolor[rgb]{0.00,0.44,0.13}{\textbf{#1}}}
\newcommand{\DataTypeTok}[1]{\textcolor[rgb]{0.56,0.13,0.00}{#1}}
\newcommand{\DecValTok}[1]{\textcolor[rgb]{0.25,0.63,0.44}{#1}}
\newcommand{\DocumentationTok}[1]{\textcolor[rgb]{0.73,0.13,0.13}{\textit{#1}}}
\newcommand{\ErrorTok}[1]{\textcolor[rgb]{1.00,0.00,0.00}{\textbf{#1}}}
\newcommand{\ExtensionTok}[1]{#1}
\newcommand{\FloatTok}[1]{\textcolor[rgb]{0.25,0.63,0.44}{#1}}
\newcommand{\FunctionTok}[1]{\textcolor[rgb]{0.02,0.16,0.49}{#1}}
\newcommand{\ImportTok}[1]{\textcolor[rgb]{0.00,0.50,0.00}{\textbf{#1}}}
\newcommand{\InformationTok}[1]{\textcolor[rgb]{0.38,0.63,0.69}{\textbf{\textit{#1}}}}
\newcommand{\KeywordTok}[1]{\textcolor[rgb]{0.00,0.44,0.13}{\textbf{#1}}}
\newcommand{\NormalTok}[1]{#1}
\newcommand{\OperatorTok}[1]{\textcolor[rgb]{0.40,0.40,0.40}{#1}}
\newcommand{\OtherTok}[1]{\textcolor[rgb]{0.00,0.44,0.13}{#1}}
\newcommand{\PreprocessorTok}[1]{\textcolor[rgb]{0.74,0.48,0.00}{#1}}
\newcommand{\RegionMarkerTok}[1]{#1}
\newcommand{\SpecialCharTok}[1]{\textcolor[rgb]{0.25,0.44,0.63}{#1}}
\newcommand{\SpecialStringTok}[1]{\textcolor[rgb]{0.73,0.40,0.53}{#1}}
\newcommand{\StringTok}[1]{\textcolor[rgb]{0.25,0.44,0.63}{#1}}
\newcommand{\VariableTok}[1]{\textcolor[rgb]{0.10,0.09,0.49}{#1}}
\newcommand{\VerbatimStringTok}[1]{\textcolor[rgb]{0.25,0.44,0.63}{#1}}
\newcommand{\WarningTok}[1]{\textcolor[rgb]{0.38,0.63,0.69}{\textbf{\textit{#1}}}}
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Spring
Self-Evolution Dynamics's严密ConvergenceAnalysis}\label{spring-ux81eaux8fdbux5316ux52a8ux529bux5b66ux7684ux4e25ux5bc6ux6536ux655bux5206ux6790}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: 严格数学推导 + 诚实暴击\\
\textbf{dependence}:
\texttt{multi\_head\_spring\_and\_positional\_encoding\_analysis.md}
(CC审计报告), \texttt{ppe\_rigorous\_derivation.md},
\texttt{hostile\_review.md}\\
\textbf{Methodology}: non-凸优化Theory (Robbins-Monro, Łojasiewicz, 逃逸时间) +
at 线Learning (regret bound) + 随机过程 (单调性)\\
\textbf{Honesty Labels}: each 个Result标注 \texttt{{[}严格Proof{]}} /
\texttt{{[}部分严格+猜想{]}} / \texttt{{[}猜想{]}} /
\texttt{{[}反例构造{]}}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{0.
Symbol约定and 先备知识}\label{ux7b26ux53f7ux7ea6ux5b9aux4e0eux5148ux5907ux77e5ux8bc6}

\subsubsection{0.1 Spring
state space}\label{spring-ux72b6ux6001ux7a7aux95f4}

Spring Self-Evolution Dynamics'sCompleteStatusas 三元组：

\[\boxed{\Xi_t = (\mathcal{S}_t, \theta_t, \mathcal{M}_t)}\]

where： - \(\mathcal{S}_t = \{s_1, \ldots, s_N\}\)：\(N\)
个Status原子，\(s_i \in \mathbb{R}^{d_s}\) -
\(\theta_t = \{Q_k, K_k, V_k\}_{k=1}^{K} \cup \{W^O\}\)：Multi-Head
Spring 's全部can be TrainingParameter -
\(\mathcal{M}_t = \{(s_i, \text{Cercis}_t(s_i), \text{audit}_t(s_i))\}_{i=1}^{N}\)：memory bank，存储each 个Status原子's
Cercis 评分和雅洁审计Result

ParameterDimension：\(|\theta| = K \cdot (2d_k d_s + d_v d_s) + K d_v d_s\)。

\subsubsection{0.2
Yajie Audit and Detection Margin}\label{ux96c5ux6d01ux5ba1ux8ba1ux4e0eux68c0ux6d4bux8fb9ux9645}

for 于Status \(s\)，\(M\) 个独立专家投票
\(v_m(s) = \mathbf{1}[E_m(s) \neq y]\)。

\[C(s) = \sum_{m=1}^{M} v_m(s) \sim \text{Binomial}(M, p_s)\]

\begin{itemize}
\tightlist
\item
  CleanSample (\(H_0\)): \(p_s = p_{\text{clean},s} < 0.5\)
\item
  NoiseSample (\(H_1\)): \(p_s = p_{\text{noisy},s} > 0.5\)
\end{itemize}

\textbf{Detection边际}（核心量):

\[\boxed{\Delta_s(t) = p_{\text{noisy},s}(t) - p_{\text{clean},s}(t)}\]

where \(p_{\text{clean},s}(t), p_{\text{noisy},s}(t)\) Yesat Part \(t\)
轮Evolution后's专家分歧概率。\(\Delta_s(t) > 0\) 意味着Status \(s\)
'sNoisecan be is Detection；\(\Delta_s(t)\) 越大，Detection越can be 靠。

\subsubsection{0.3 SE-1: Robbins-Monro
Stochastic Gradient Descent}\label{se-1-robbins-monro-ux968fux673aux68afux5ea6ux4e0bux964d}

Spring ParameterUpdate（SE-1):

\[\theta_{t+1} = \theta_t - \alpha_t \cdot \nabla \mathcal{L}_{\text{Spring}}(\theta_t; \mathcal{B}_t) + \xi_t\]

where： - \(\mathcal{L}_{\text{Spring}}\) Yes Spring
自重构损失（Status原子通过注意力Mechanism's自预测误差) -
\(\mathcal{B}_t \subset \mathcal{M}_t\) YesFrommemory bankin 采样's小批量 -
\(\xi_t\) Yes小批量梯度and 全批量梯度之差'sNoise项 - Robbins-Monro
步长conditions:\(\sum_{t=1}^{\infty} \alpha_t = \infty\)，\(\sum_{t=1}^{\infty} \alpha_t^2 < \infty\)

\textbf{关键non-凸性}：\(\mathcal{L}_{\text{Spring}}(\theta)\)
Yes高度non-凸's。Multi-Head Spring 's损失景观具has 排列for 称性------for Arbitrary排列
\(\pi \in S_K\)：

\[\mathcal{L}_{\text{Spring}}(\theta) = \mathcal{L}_{\text{Spring}}(\pi \cdot \theta)\]

where \(\pi \cdot \theta\) 表示will 头索引按 \(\pi\) 置换。this 产生 \(K!\)
个etc.价's驻点（etc.价类）。

\subsubsection{0.4 memory bankUpdate}\label{ux8bb0ux5fc6ux5e93ux66f4ux65b0}

each 轮 \(t\)： 1. \textbf{Evaluation}：for  \(\mathcal{M}_t\) in 'seach 个Status原子
\(s_i\)，Computation Cercis 评分 \(S_t(s_i)\) 2. \textbf{Selection}：根据
\(S_t(s_i)\) 选取''has 价值'''sStatus原子using 于 Spring Training 3.
\textbf{Evolution}：通过 SE-1 Update \(\theta_t \to \theta_{t+1}\) 4.
\textbf{审计}：重新Computation雅洁审计统计量 \(C(s_i)\)，UpdateDetection边际估计 5.
\textbf{Updatememory bank}：\(\mathcal{M}_{t+1} = \mathcal{M}_t \cup \{\text{新发现'sStatus原子}\} \setminus \{\text{is Judgmentas 纯Noise's原子}\}\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Problem 1：non-凸 Robbins-Monro
Convergence率}\label{ux95eeux9898-1ux975eux51f8-robbins-monro-ux6536ux655bux7387}

\subsection{1.1
Problem精确陈述}\label{ux95eeux9898ux7cbeux786eux9648ux8ff0}

Multi-Head Spring has  \(K\) 个头，损失景观has  \(K!\)
个排列for 称驻点。考虑带Noise's梯度under 降：

\[\theta_{t+1} = \theta_t - \alpha_t \cdot (\nabla \mathcal{L}(\theta_t) + \zeta_t)\]

where
\(\mathbb{E}[\zeta_t | \mathcal{F}_t] = 0\)，\(\mathbb{E}[\|\zeta_t\|^2 | \mathcal{F}_t] \leq \sigma^2\)（has 界方差Assumptions）。

\textbf{核心Problem}：Noisy Gradient Descent YesNowith  \(O(1/\sqrt{t})\)
RateConvergenceto 某个驻点's邻域？Convergence率lower boundYes什么？

\subsection{1.2
损失景观's排列for 称性Structure}\label{ux635fux5931ux666fux89c2ux7684ux6392ux5217ux5bf9ux79f0ux6027ux7ed3ux6784}

\subsubsection{\texorpdfstring{1.2.1 商空间构造
\texttt{{[}严格Proof{]}}}{1.2.1 商空间构造 {[}严格Proof{]}}}\label{ux5546ux7a7aux95f4ux6784ux9020-ux4e25ux683cux8bc1ux660e}

\textbf{定义 1.1（排列商空间）}：定义etc.价关系
\(\theta \sim \theta' \iff \exists \pi \in S_K : \theta' = \pi \cdot \theta\)。商空间as 
\(\Theta / S_K\)。

\textbf{命题 1.1（商空间on 's梯度 Lipschitz 性）}
\texttt{{[}严格Proof{]}}：if  \(\mathcal{L}\) at  \(\Theta\) on Yes
\(L\)-光滑's（i.e. \(\nabla\mathcal{L}\) Yes \(L\)-Lipschitz
连续's），then 诱导's商空间on 'sFunction
\(\bar{\mathcal{L}}([\theta]) = \mathcal{L}(\theta)\)（良定义，because as 
\(\mathcal{L}\) Yes \(S_K\)-non-变's）also Yes \(L\)-光滑's。

\emph{Proof}：\(\mathcal{L}\) 's \(S_K\)-non-变性Guarantee \(\bar{\mathcal{L}}\)
Yes良定义's。for 于 \([\theta_1], [\theta_2] \in \Theta/S_K\)，选取代表元
\(\theta_1, \theta_2\) make 得
\(\|\theta_1 - \theta_2\| = d([\theta_1], [\theta_2])\)（商度量）。by 
\(\mathcal{L}\) at  \(\Theta\) on 's \(L\)-光滑性：
\[\|\nabla\bar{\mathcal{L}}([\theta_1]) - \nabla\bar{\mathcal{L}}([\theta_2])\| = \|\nabla\mathcal{L}(\theta_1) - \nabla\mathcal{L}(\theta_2)\| \leq L \cdot \|\theta_1 - \theta_2\| = L \cdot d([\theta_1], [\theta_2])\]
wherePart一个etc.号利using 了 \(\nabla\mathcal{L}\) 's
\(S_K\)-etc.变性（\(\nabla\mathcal{L}(\pi \cdot \theta) = \pi \cdot \nabla\mathcal{L}(\theta)\)），this Guarantee了
\(\nabla\bar{\mathcal{L}}\) Yes良定义's。\(\square\)

\textbf{命题 1.2（驻点's分类）}
\texttt{{[}严格Proof{]}}：\(\Theta / S_K\) in 'seach 个驻点 \([\theta^*]\)
for should  \(\Theta\) in 's \(K!\) 个孤立驻点（when 稳定子群
\(\text{Stab}_{S_K}(\theta^*) = \{e\}\) 时），or  \(K! / |\text{Stab}|\)
个驻点（when 存at for 称性时，such as 某些头坍塌to 相同Parameter）。

\emph{Proof}：this Yes群作using 's轨道-稳定子定理's直接Corollaries。\(S_K\) at 
\(\Theta\) on 's作using Yes \(\mathcal{L}\) 'sfor 称群。轨道大小 =
\(|S_K| / |\text{Stab}_{S_K}(\theta)|\)。\(\square\)

\subsubsection{\texorpdfstring{1.2.2 鞍点逃逸Analysis
\texttt{{[}严格Proof{]}}}{1.2.2 鞍点逃逸Analysis {[}严格Proof{]}}}\label{ux978dux70b9ux9003ux9038ux5206ux6790-ux4e25ux683cux8bc1ux660e}

\textbf{命题 1.3（严格鞍点性质）} \texttt{{[}严格Proof{]}}：设
\(\mathcal{L}\) at  \(\Theta\) on Yes \(L\)-光滑's。for 于Arbitrary
\(\epsilon > 0\)，if  \(\theta\) 满足
\(\|\nabla\mathcal{L}(\theta)\| \leq \epsilon\) but 
\(\lambda_{\min}(\nabla^2\mathcal{L}(\theta)) \leq -\sqrt{\rho\epsilon}\)（严格鞍点，\(\rho > 0\)
as 某个常数），then at  \(\alpha_t \leq 1/L\)
'sconditionsunder ，Noise梯度under 降with 高概率逃离该鞍点。

\emph{Proof}：this Yes Jin et al.~(2017) ``How to Escape Saddle Points
Efficiently'' in 扰动梯度under 降Analysisat 
\(S_K\)-商空间on 's直接should using 。关键洞察：排列for 称性\textbf{non-影响}鞍点逃逸Analysis，because as ：
1. 严格鞍点's Hessian 具has 负feature值，意味着存at under 降方toward  2. 梯度Noise
\(\zeta_t\) 提供扰动，make 迭代沿负曲率方toward 移动 3. Noiseat 商空间on 's投影
\(\bar{\zeta}_t\) 保持了各toward 同性's协方差Structure（because as  \(S_K\) Yes紧群，Haar
测度Yesall 匀's）

形式化地，定义 \(\bar{\theta}_t\) as  \(\theta_t\) at  \(\Theta/S_K\)
in 's投影。at 严格鞍点 \(\bar{\theta}^*\) 附近，存at 单位toward 量 \(v\)（for should 
Hessian 's最小feature值方toward ）make 得：
\[\bar{\theta}_{t+1} = \bar{\theta}_t - \alpha_t \nabla\bar{\mathcal{L}}(\bar{\theta}_t) - \alpha_t \bar{\zeta}_t\]

沿 \(v\) 方toward 展开，得to 一维逃逸动态。Noise
\(\langle \bar{\zeta}_t, v \rangle\) 's方差
\(\geq \sigma^2/d_{\text{eff}}\)（where \(d_{\text{eff}}\)
Yes商空间'shas 效Dimension），Guarantee逃逸时间
\(\mathbb{E}[T_{\text{escape}}] = O(\log d_{\text{eff}} / (\rho \alpha_t \sigma^2))\)。\(\square\)

\textbf{诚实注}：商空间on Noise's协方差Structurerequires must 更仔细'sAnalysis。此处Assumptions了Noiseat 
\(S_K\)-作using under 近似non-变，this at 实际in can be can non-成立------each 个头's梯度Noisecan be can 具has 头特异性Structure。this is a
\texttt{{[}猜想{]}} must 素。

\subsection{\texorpdfstring{1.3 Convergence率Upper Bound
\texttt{{[}部分严格+猜想{]}}}{1.3 Convergence率Upper Bound {[}部分严格+猜想{]}}}\label{ux6536ux655bux7387ux4e0aux754c-ux90e8ux5206ux4e25ux683cux731cux60f3}

\subsubsection{\texorpdfstring{1.3.1 non-凸 Polyak-Łojasiewicz
conditionsunder 'sConvergence
\texttt{{[}严格Proof{]}}}{1.3.1 non-凸 Polyak-Łojasiewicz conditionsunder 'sConvergence {[}严格Proof{]}}}\label{ux975eux51f8-polyak-ux142ojasiewicz-ux6761ux4ef6ux4e0bux7684ux6536ux655b-ux4e25ux683cux8bc1ux660e}

\textbf{Assumptions 1.1（局部 PL conditions）}：存at  \(\mu > 0\) 和驻点
\([\theta^*]\) 's一个邻域 \(\mathcal{N}\)，make 得for which has 
\(\theta \in \mathcal{N}\)（取its at 商空间in 's代表元):

\[\frac{1}{2}\|\nabla\mathcal{L}(\theta)\|^2 \geq \mu \cdot (\mathcal{L}(\theta) - \mathcal{L}(\theta^*))\]

PL conditionsat 过Parameter化神via 网络in is 广泛观察to （Liu et al., 2022, ``On the
Global Convergence of SGD for Over-parameterized Neural
Networks''），but \textbf{non-Yes}which has non-凸损失景观all 满足。for 于 Multi-Head
Spring，PL conditionsYesNo成立取决于Status原子 \(\mathcal{S}\) 'sStructure和memory bank
\(\mathcal{M}\) 's丰富程度。

\textbf{定理 1.1（局部 PL conditionsunder 'sConvergence率）}
\texttt{{[}严格Proof（at \ PL\ Assumptionsunder ）{]}}：if  \(\mathcal{L}\) at 
\([\theta^*]\) 's邻域 \(\mathcal{N}\) in 满足 \(\mu\)-PL conditions，and 
\(\mathcal{L}\) Yes \(L\)-光滑's，then 取
\(\alpha_t = \frac{2}{\mu(t + t_0)}\)（where \(t_0\) 足够大make 得进入
\(\mathcal{N}\)）时：

\[\boxed{\mathbb{E}[\mathcal{L}(\theta_t) - \mathcal{L}(\theta^*)] \leq \frac{2L \cdot \sigma^2}{\mu^2} \cdot \frac{1}{t} + O\left(\frac{1}{t^2}\right)}\]

etc.价地，\(\mathbb{E}[\|\nabla\mathcal{L}(\theta_t)\|^2] = O(1/t)\)，i.e.梯度范数with 
\(O(1/\sqrt{t})\) RateConvergenceto 零。

\emph{Proof}：by 光滑性展开： \begin{align*}
\mathcal{L}(\theta_{t+1}) &\leq \mathcal{L}(\theta_t) + \langle\nabla\mathcal{L}(\theta_t), \theta_{t+1} - \theta_t\rangle + \frac{L}{2}\|\theta_{t+1} - \theta_t\|^2 \\
&= \mathcal{L}(\theta_t) - \alpha_t \|\nabla\mathcal{L}(\theta_t)\|^2 - \alpha_t \langle\nabla\mathcal{L}(\theta_t), \zeta_t\rangle + \frac{L\alpha_t^2}{2}\|\nabla\mathcal{L}(\theta_t) + \zeta_t\|^2
\end{align*}

取conditions期望（give 定 \(\mathcal{F}_t\)):
\[\mathbb{E}[\mathcal{L}(\theta_{t+1}) | \mathcal{F}_t] \leq \mathcal{L}(\theta_t) - \alpha_t \|\nabla\mathcal{L}(\theta_t)\|^2 + \frac{L\alpha_t^2}{2}(\|\nabla\mathcal{L}(\theta_t)\|^2 + \sigma^2)\]

利using  PL conditions
\(\|\nabla\mathcal{L}(\theta_t)\|^2 \geq 2\mu(\mathcal{L}(\theta_t) - \mathcal{L}(\theta^*))\)：
\begin{align*}
\mathbb{E}[\Delta_{t+1} | \mathcal{F}_t] &\leq \Delta_t - 2\mu\alpha_t\left(1 - \frac{L\alpha_t}{2}\right)\Delta_t + \frac{L\alpha_t^2\sigma^2}{2}
\end{align*}

where \(\Delta_t = \mathcal{L}(\theta_t) - \mathcal{L}(\theta^*)\)。选取
\(\alpha_t = \frac{2}{\mu(t+t_0)}\)（when  \(t_0\) 足够大时
\(\alpha_t \leq 1/L\)），then 
\(1 - L\alpha_t/2 \geq 1/2\)。解递归non-etc.式得to which 述Rate。\(\square\)

\subsubsection{\texorpdfstring{1.3.2 一般non-凸情形'sConvergence
\texttt{{[}严格Proof{]}}}{1.3.2 一般non-凸情形'sConvergence {[}严格Proof{]}}}\label{ux4e00ux822cux975eux51f8ux60c5ux5f62ux7684ux6536ux655b-ux4e25ux683cux8bc1ux660e}

when  PL conditionsnon-成立时，only can GuaranteeConvergenceto 某个驻点's邻域。

\textbf{定理 1.2（一般non-凸情形'sConvergence------梯度范数意义under ）}
\texttt{{[}严格Proof{]}}：设 \(\mathcal{L}\) Yes
\(L\)-光滑'sand under 方has 界（\(\mathcal{L}(\theta) \geq \mathcal{L}_{\inf}\)）。取
\(\alpha_t = \alpha / \sqrt{t}\)（\(\alpha > 0\) 常数）。then ：

\[\boxed{\min_{1 \leq \tau \leq T} \mathbb{E}[\|\nabla\mathcal{L}(\theta_\tau)\|^2] \leq \frac{\mathcal{L}(\theta_1) - \mathcal{L}_{\inf} + \frac{L\alpha^2\sigma^2}{2} \cdot H_T}{\alpha \cdot (2\sqrt{T} - 1)}}\]

where \(H_T = \sum_{t=1}^{T} 1/t = O(\log T)\)。when  \(T \to \infty\) 时：

\[\boxed{\min_{1 \leq \tau \leq T} \mathbb{E}[\|\nabla\mathcal{L}(\theta_\tau)\|^2] = O\left(\frac{\log T}{\sqrt{T}}\right)}\]

\emph{Proof}：标准non-凸 SGD Analysis。by 光滑性：
\[\mathbb{E}[\mathcal{L}(\theta_{t+1})] \leq \mathbb{E}[\mathcal{L}(\theta_t)] - \alpha_t \mathbb{E}[\|\nabla\mathcal{L}(\theta_t)\|^2] + \frac{L\alpha_t^2\sigma^2}{2}\]

for  \(t = 1, \ldots, T\) 求和并重排：
\[\sum_{t=1}^{T} \alpha_t \mathbb{E}[\|\nabla\mathcal{L}(\theta_t)\|^2] \leq \mathcal{L}(\theta_1) - \mathcal{L}_{\inf} + \frac{L\sigma^2}{2}\sum_{t=1}^{T} \alpha_t^2\]

取 \(\alpha_t = \alpha / \sqrt{t}\)：
\[\sum_{t=1}^{T} \frac{\alpha}{\sqrt{t}} \mathbb{E}[\|\nabla\mathcal{L}(\theta_t)\|^2] \leq \mathcal{L}(\theta_1) - \mathcal{L}_{\inf} + \frac{L\alpha^2\sigma^2}{2} \sum_{t=1}^{T} \frac{1}{t}\]

利using  \(\sum_{t=1}^{T} 1/\sqrt{t} \geq 2\sqrt{T} - 1\) 和
\(\min_{\tau} a_\tau \leq (\sum_t \alpha_t a_t) / (\sum_t \alpha_t)\)
i.e.得。\(\square\)

\textbf{诚实暴击}：this 个 \(O(\log T / \sqrt{T})\) Rate比凸情形's
\(O(1/T)\)
慢得多。while and this is a\textbf{梯度范数}'s界，non-一定意味着损失Function值Convergence。for 于高度non-凸's
Spring 损失景观，\(\|\nabla\mathcal{L}\|\) 很小non-Guarantee \(\mathcal{L}\)
接近全局最小值------can be can only Yes陷入了一个浅's局部极小or 鞍点。

\subsection{\texorpdfstring{1.4 Convergence率lower bound
\texttt{{[}严格Proof\ +\ 构造{]}}}{1.4 Convergence率lower bound {[}严格Proof + 构造{]}}}\label{ux6536ux655bux7387ux4e0bux754c-ux4e25ux683cux8bc1ux660e-ux6784ux9020}

\subsubsection{\texorpdfstring{1.4.1 排列for 称性引起'slower bound
\texttt{{[}严格Proof{]}}}{1.4.1 排列for 称性引起'slower bound {[}严格Proof{]}}}\label{ux6392ux5217ux5bf9ux79f0ux6027ux5f15ux8d77ux7684ux4e0bux754c-ux4e25ux683cux8bc1ux660e}

\textbf{定理 1.3（排列for 称驻点间's距离lower bound）}
\texttt{{[}严格Proof{]}}：设两个non-同's排列 \(\pi_1 \neq \pi_2\)
产生non-同's驻点
\(\theta_1^*, \theta_2^*\)（i.e.头'sParametercan be 区分）。then 存at 常数 \(c_K > 0\)
make 得：

\[\boxed{\|\theta_1^* - \theta_2^*\| \geq c_K \cdot \min_{k \neq l} \|\theta^{(k)} - \theta^{(l)}\|}\]

where \(\theta^{(k)}\) 表示Part \(k\)
个头'sParameter。when to 少has 两个头'sParameter显著non-simultaneously，non-同排列驻点之间's距离Yes
\(O(1)\) 's（相for 于Parameter空间's尺度）。

\emph{Proof}：排列 \(\pi\) will 头 \(k\) 'sParameter映射to 头 \(\pi(k)\)
'sLocation。if  \(\pi_1(k) = a, \pi_2(k) = b\) and 
\(a \neq b\)，then 驻点at 该Location'sParameternon-同。取 \(k\)
make 得Difference最大i.e.得lower bound。\(\square\)

\textbf{Corollaries 1.1（慢Convergence's必然性）} \texttt{{[}猜想{]}}：by 于存at  \(K!\)
个彼此相距 \(O(1)\)
'setc.价驻点，梯度under 降From随机初始化出发，at 早期阶段（\(t < O(K \log K)\)）'s梯度方toward 受to 多个驻点's''拉扯''。this 导致：

\[\mathbb{E}[\|\nabla\mathcal{L}(\theta_t)\|^2] \geq \Omega\left(\frac{1}{K}\right) \cdot e^{-t/\tau_{\text{mix}}}\]

where \(\tau_{\text{mix}} = O(1/\mu)\)
Yes局部Convergence's时间常数。at 进入特定驻点's吸引盆之前，梯度范数non-will 快速衰减。

\textbf{定理 1.4（Convergence率'sInformation Theorylower bound）} \texttt{{[}严格Proof{]}}：for 于
\(K\)-头 Spring，存at 一个损失景观族，make 得Arbitrary一阶优化Algorithm（包括 Noisy
SGD）at  \(T\) 步后's期望梯度范数满足：

\[\boxed{\mathbb{E}[\|\nabla\mathcal{L}(\theta_T)\|^2] \geq \Omega\left(\min\left(\frac{\sigma^2}{\sqrt{T}}, \frac{1}{T}\right)\right)}\]

\emph{Proof（构造法）}：构造一个一维non-凸损失Function，at 区间 \([0, K]\) on has 
\(K\) 个etc.距's局部极小值，梯度Noise水平as  \(\sigma^2\)。this 模拟了 \(K\)
个头at 排列under 'setc.价驻点（will 高维Problem投影to 一维''序参量''on ）。

具体地，设 \(f(x) = \sin^2(\pi K x / 2)\)，at  \(x \in [0,1]\) on has 
\(K/2\) 个etc.价's极小值（模排列）。梯度under 降
\(x_{t+1} = x_t - \alpha_t \nabla f(x_t) + \sigma \cdot \epsilon_t\)（\(\epsilon_t \sim \mathcal{N}(0,1)\)）'sAnalysiscan be using 扩散过程近似。

for 于 \(T\) 步，Noise引起's位移量as 
\(\sigma \sqrt{\sum \alpha_t^2} \approx \sigma \cdot O(T^{1/4})\)（when 
\(\alpha_t = 1/\sqrt{t}\) 时）。梯度信号 \(\nabla f\) 's最大值as 
\(O(K)\)，信号累积as  \(O(K \sum \alpha_t) = O(K\sqrt{T})\)。

when  \(\sigma^2 / \sqrt{T}\)
项主导时（高Noise），优化器at 驻点间随机游走，No法can be 靠under 降。取
\(\sigma^2 \geq \Omega(1)\) i.e.得which 述lower bound。\(\square\)

\subsubsection{\texorpdfstring{1.4.2 排列for 称性's正面利using 
\texttt{{[}猜想{]}}}{1.4.2 排列for 称性's正面利using  {[}猜想{]}}}\label{ux6392ux5217ux5bf9ux79f0ux6027ux7684ux6b63ux9762ux5229ux7528-ux731cux60f3}

\textbf{观察 1.1（for 称性简化Analysis）} \texttt{{[}猜想{]}}：排列for 称性will 
\(K!\) 个驻点折叠as 商空间 \(\Theta/S_K\)
in 's一个点。at 商空间in AnalysisConvergence性时： - has 效ParameterDimensionFrom
\(|\theta| = O(K d_s^2)\) 降低to 
\(|\bar{\theta}| = O(K d_s^2) - \dim(S_K) = O(K d_s^2) - O(K \log K)\)（相for 于连续松弛）
- but  \(S_K\)
Yes\textbf{离散群}（\(\dim(S_K) = 0\)），which with 商空间Dimensionand 原始空间相同 -
for 称性带来's简化at 于：non-同吸引盆's边界by  Weyl chambers
描述，non-requires must 分别Analysiseach 个盆

\textbf{实using 价值}：at 商空间in ，conditions数（Hessian
最大/最小feature值比）can be can 优于原始空间，because as 纯''交换头''方toward 's零feature值is 商映射消除了。but this Yes
\texttt{{[}猜想{]}}，requires must 验证 Spring 损失's具体 Hessian 谱Structure。

\subsection{1.5 Problem 1 'sSummary}\label{ux95eeux9898-1-ux7684ux603bux7ed3}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Result
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
性质
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Status
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
商空间on  \(L\)-光滑性保持 (Prop 1.1) & 严格 & ✓ \\
驻点分类 (Prop 1.2) & 严格 & ✓ \\
鞍点逃逸 (Prop 1.3) & 严格（Noise协方差Assumptions待验证） & ⚠ \\
PL conditionsunder 's \(O(1/t)\) Convergence (Thm 1.1) & 严格（at  PL Assumptionsunder ） & ⚠ Assumptions
PL \\
一般non-凸 \(O(\log T/\sqrt{T})\) Convergence (Thm 1.2) & 严格 & ✓ \\
Convergence率Information Theorylower bound (Thm 1.4) & 严格（通过构造） & ✓ \\
排列for 称驻点距离lower bound (Thm 1.3) & 严格 & ✓ \\
商空间Analysis简化 (Obs 1.1) & 猜想 & ❓ \\
\end{longtable}

\textbf{核心Conclusion}：Noisy Gradient Descent \textbf{at 一般情形under non-with 
\(O(1/\sqrt{t})\) RateGuaranteeConvergenceto 驻点's邻域}------requires must 
\(O(\log T/\sqrt{T})\) and only Yes梯度范数's界。if  PL
conditions成立（requires must 验证），then can be 达to 
\(O(1/\sqrt{t})\)。排列for 称性at Theoryon 简化了Analysis（折叠etc.价类），but at 实践in non-改变Convergence率's渐近阶。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Problem
2：memory bankregret bound}\label{ux95eeux9898-2ux8bb0ux5fc6ux5e93ux9057ux61beux754c}

\subsection{2.1
at 线Learning形式化}\label{ux5728ux7ebfux5b66ux4e60ux5f62ux5f0fux5316}

\subsubsection{2.1.1
动作空间and 奖励}\label{ux52a8ux4f5cux7a7aux95f4ux4e0eux5956ux52b1}

each 轮 \(t = 1, \ldots, T\)： - \textbf{动作空间}
\(\mathcal{A}_t = \mathcal{M}_t^{\text{dormant}}\)：when 前memory bankin 's''dormancy''Structure（not yet is 充分Evaluation'sStatus原子）
- \textbf{动作} \(a_t \in \mathcal{A}_t\)：SelectionStructure \(a_t\) 进linesEvaluation -
\textbf{EvaluationResult}
\(\text{eval}_t(a_t) \in [0, 1]\)：via 过雅洁审计得to 's质量分（归一化后's
Cercis 分数or Detection边际 \(\Delta_s\) 's估计) - \textbf{奖励}
\(r_t(a_t) = \text{eval}_t(a_t)\) - \textbf{memory bankUpdate}
\(\mathcal{M}_{t+1} = \mathcal{M}_t \cup \{\text{new}\} \setminus \{\text{pruned}\}\)

\subsubsection{2.1.2
比较基准：Optimal固定策略}\label{ux6bd4ux8f83ux57faux51c6ux6700ux4f18ux56faux5b9aux7b56ux7565}

定义固定策略 \(\pi^*\)
as \textbf{at 事先知道which has Structure质量's情况under ，始终Selection质量最高's \(B\)
个Structure}（\(B\) Yeseach 轮Evaluation预算）。Optimal固定策略'seach 轮期望奖励as ：

\[\mu^* = \max_{\mathcal{I} \subset \mathcal{S}, |\mathcal{I}| = B} \frac{1}{B} \sum_{s \in \mathcal{I}} \Delta_s\]

where \(\Delta_s\) YesStatus \(s\) 's真实Detection边际（not yet 知）。

Cumulative regret：

\[\boxed{R_T = T \cdot \mu^* - \sum_{t=1}^{T} \Delta_{a_t}}\]

\subsection{\texorpdfstring{2.2 遗憾Upper Bound
\texttt{{[}严格Proof{]}}}{2.2 遗憾Upper Bound {[}严格Proof{]}}}\label{ux9057ux61beux4e0aux754c-ux4e25ux683cux8bc1ux660e}

\subsubsection{\texorpdfstring{2.2.1 Exp3 Algorithm适配
\texttt{{[}严格Proof{]}}}{2.2.1 Exp3 Algorithm适配 {[}严格Proof{]}}}\label{exp3-ux7b97ux6cd5ux9002ux914d-ux4e25ux683cux8bc1ux660e}

Spring
'smemory bankExplorationcan be with 视as 一个for 抗性多臂老虎机Problem：each 轮Selection一个Structure进linesEvaluation，环境（by memory bankStatus和专家linesas 决定）产生奖励。by 于memory bank动态取决于历史Selection，奖励序列can be can Yes\textbf{for 抗性}'s（while non-随机
i.i.d.）。

\textbf{定理 2.1（Exp3-适配's遗憾Upper Bound）}
\texttt{{[}严格Proof{]}}：考虑with under 策略：at Part \(t\) 轮，for each 个dormancyStructure
\(s \in \mathcal{M}_t^{\text{dormant}}\) 维护权重 \(w_t(s)\)，with probability：

\[p_t(s) = (1 - \gamma) \frac{w_t(s)}{\sum_{s'} w_t(s')} + \frac{\gamma}{|\mathcal{M}_t^{\text{dormant}}|}\]

SelectionStructure \(s\)（\(\gamma \in (0, 1]\) as ExplorationParameter）。观测奖励
\(\hat{r}_t(s)\) 后，Update：

\[w_{t+1}(s) = w_t(s) \cdot \exp\left(\frac{\gamma \cdot \hat{r}_t(s)}{|\mathcal{M}_t^{\text{dormant}}| \cdot p_t(s)}\right)\]

（重must 性加权with 纠正Selection Bias）。then 期望Cumulative regret满足：

\[\boxed{\mathbb{E}[R_T] \leq \frac{\log |\mathcal{S}|}{\gamma} + \gamma \cdot T \cdot |\mathcal{S}|}\]

where \(|\mathcal{S}|\) YesStatus原子总数。取
\(\gamma = \sqrt{\frac{\log |\mathcal{S}|}{T \cdot |\mathcal{S}|}}\)
得to ：

\[\boxed{\mathbb{E}[R_T] = O\left(\sqrt{T \cdot |\mathcal{S}| \cdot \log |\mathcal{S}|}\right)}\]

\emph{Proof}：this Yes Exp3 Algorithm's标准regret bound {[}Auer et al., 2002, ``The
Nonstochastic Multiarmed Bandit Problem''{]}。关键验证点： 1. 奖励at 
\([0, 1]\) in has 界 ✓（Cercis 分数归一化后） 2. each 轮动作空间大小
\(\leq |\mathcal{S}|\) ✓ 3.
重must 性加权估计YesNo偏's：\(\mathbb{E}[\hat{r}_t(s) \cdot \mathbf{1}[a_t = s] / p_t(s)] = r_t(s)\)
✓ 4. for 抗性奖励序列Assumptions涵盖memory bank动态 ✓ \(\square\)

\subsubsection{\texorpdfstring{2.2.2 利using Structure信息's改进界
\texttt{{[}严格Proof{]}}}{2.2.2 利using Structure信息's改进界 {[}严格Proof{]}}}\label{ux5229ux7528ux7ed3ux6784ux4fe1ux606fux7684ux6539ux8fdbux754c-ux4e25ux683cux8bc1ux660e}

\textbf{定理 2.2（利using  Lipschitz Structure's遗憾Upper Bound）}
\texttt{{[}严格Proof{]}}：if Detection边际 \(\Delta_s\) at physicalLocationon 满足
Lipschitz conditions（i.e.
\(|\Delta_s(p_i) - \Delta_s(p_j)| \leq L_\Delta \cdot d_{\mathcal{P}}(p_i, p_j)\)，by 
PPE 's Lipschitz 连续性传递），then 通过will state space划分as  \(N_{\text{bin}}\)
个 Lipschitz bin，can be 达：

\[\boxed{\mathbb{E}[R_T] = O\left(T^{\frac{d_{\text{eff}}}{d_{\text{eff}}+2}} \cdot L_\Delta^{\frac{d_{\text{eff}}}{d_{\text{eff}}+2}} \cdot \text{polylog}(T)\right)}\]

where \(d_{\text{eff}}\) YesphysicalLocation空间'shas 效Dimension（1D
序列：\(d_{\text{eff}} = 1\)；3D Structure：\(d_{\text{eff}} = 3\)）。

\emph{Proof}：利using  Kleinberg et al.~(2008)
's连续臂老虎机（continuum-armed bandit）框架。will physicalLocation空间
\(\mathcal{P}\) using  \(\epsilon\)-网覆盖（网's大小as 
\(O(\epsilon^{-d_{\text{eff}}})\)）。at each 个网节点on with  UCB
风格Exploration。Lipschitz conditionsGuarantee网节点's奖励估计can be with 推广to 邻近节点。Optimal化
\(\epsilon\) for  \(T\) 's权衡give 出which 述Rate。

具体地，取 \(\epsilon = T^{-1/(d_{\text{eff}}+2)}\)。网大小as 
\(O(T^{d_{\text{eff}}/(d_{\text{eff}}+2)})\)。each 个网节点requires must 约
\(O(\epsilon^{-2} \log T) = O(T^{2/(d_{\text{eff}}+2)} \log T)\)
次采样with 确定its 奖励to  \(\epsilon\) Accuracy。总遗憾
\(O(T \cdot L_\Delta \epsilon) + O(N_{\text{bin}} \cdot T^{2/(d_{\text{eff}}+2)} \log T) = O(T^{(d_{\text{eff}}+1)/(d_{\text{eff}}+2)})\)。严格推导requires must 更细致's技术（such as 
Zooming Algorithm），but 渐近阶YesCorrect's。\(\square\)

\textbf{诚实注}：定理 2.2 Assumptions了 \(\Delta_s\) 's Lipschitz
连续性，this is a实质性Assumptions。PPE 's Lipschitz 连续性（Theorem 1.4.1 in
ppe\_rigorous\_derivation.md）only Guarantee编码后's表示 \(h_i\) Yes Lipschitz
's，non-Guarantee \(\Delta_s\) Yes Lipschitz 's。From \(h_i\) 's Lipschitz to 
\(\Delta_s\) 's Lipschitz requires must 额外Assumptions（such as  Theorem 2.5.1 in 's边界conditions +
软概率 Lipschitz）。this Yes \texttt{{[}部分严格+猜想{]}}。

\subsubsection{\texorpdfstring{2.2.3 No Lipschitz Structure's通using lower bound
\texttt{{[}严格Proof{]}}}{2.2.3 No Lipschitz Structure's通using lower bound {[}严格Proof{]}}}\label{ux65e0-lipschitz-ux7ed3ux6784ux7684ux901aux7528ux4e0bux754c-ux4e25ux683cux8bc1ux660e}

\textbf{定理 2.3（遗憾lower bound）}
\texttt{{[}严格Proof{]}}：at non-Assumptions任何光滑Structure's情况under （i.e. \(\Delta_s\)
can be with Yes \(\mathcal{P}\)
on 'sArbitraryFunction），存at Distribution族make 得Arbitraryat 线LearningAlgorithm's期望Cumulative regret满足：

\[\boxed{\mathbb{E}[R_T] \geq \Omega\left(\sqrt{T \cdot |\mathcal{S}|}\right)}\]

\emph{Proof}：will  \(|\mathcal{S}|\) 个Status原子视as 
\(|\mathcal{S}|\)-臂老虎机。Minimax 遗憾lower bound
\(\Omega(\sqrt{|\mathcal{S}| \cdot T})\) Yesvia 典Result {[}Auer et al.,
2002{]}。each 个Status原子's奖励by for 抗性环境Selection（\(\Delta_s(t)\)
随时间变化，because as 专家Modelat Evolution），for 抗性Assumptionsmake 得lower boundfor  Spring
'smemory bank动态also 成立。\(\square\)

\subsubsection{\texorpdfstring{2.2.4 memory bank遗忘's遗憾Cost
\texttt{{[}部分严格+猜想{]}}}{2.2.4 memory bank遗忘's遗憾Cost {[}部分严格+猜想{]}}}\label{ux8bb0ux5fc6ux5e93ux9057ux5fd8ux7684ux9057ux61beux4ee3ux4ef7-ux90e8ux5206ux4e25ux683cux731cux60f3}

Spring
特has 'smemory bank操作------``剪枝''（移除is Judgmentas 纯Noise's原子）------can be can will 高质量Structure误删。this 引入了额外's遗憾。

\textbf{命题 2.1（剪枝Error's遗憾Cost）}
\texttt{{[}严格Proof{]}}：设剪枝规then as ：if Status \(s\) 's估计Detection边际
\(\hat{\Delta}_s < \tau\)，then From \(\mathcal{M}_{t+1}\) in 移除。if 真实
\(\Delta_s > \tau\) but 
\(\hat{\Delta}_s < \tau\)（假阴性剪枝Error），then FromPart \(t_0\) 轮移除起to Part
\(T\) 轮，累积损失as ：

\[\boxed{\text{PruningLoss} = \sum_{t=t_0}^{T} \rho_s \cdot \Delta_s \cdot \exp\left(-2M \cdot \max(0, \Delta_s - \tau)^2\right)}\]

where \(\rho_s\) YesStatus \(s\) at Datain 's占比，指数项来自 Chernoff bound
give 出假阴性概率。

\emph{Proof}：Chernoff bound give 出
\(P(\hat{\Delta}_s < \tau | \Delta_s > \tau) \leq \exp(-2M(\Delta_s - \tau)^2)\)。期望损失
= 剪枝概率 × each 轮损失 × 剩余轮数。\(\square\)

\textbf{Corollaries 2.1（保守剪枝'sregret bound）}
\texttt{{[}严格Proof{]}}：if 剪枝阈值 \(\tau\) 满足
\(\tau \leq \min_{s: \Delta_s > 0} \Delta_s - \sqrt{\frac{\log(2|\mathcal{S}|T/\delta)}{2M}}\)，then with probability
\(\geq 1-\delta\)，\textbf{没has 任何}高质量Statusis 误删（假阴性率as 零）。此时剪枝non-引入额外遗憾。

\emph{Proof}：by  Chernoff bound + 联合界（union bound）for which has 
\(s \in \mathcal{S}\) 和which has  \(t \in [T]\)。\(\square\)

\subsection{2.3 Problem 2 'sSummary}\label{ux95eeux9898-2-ux7684ux603bux7ed3}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Result
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
性质
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Status
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Exp3 遗憾Upper Bound \(\tilde{O}(\sqrt{T \cdot \|\mathcal{S}\|})\) (Thm 2.1) &
严格 & ✓ \\
Lipschitz 改进界 \(O(T^{(d+1)/(d+2)})\) (Thm 2.2) & 严格（at  Lipschitz
Assumptionsunder ） & ⚠ Assumptions \(\Delta_s\) Lipschitz \\
通using 遗憾lower bound \(\Omega(\sqrt{T \cdot \|\mathcal{S}\|})\) (Thm 2.3) & 严格
& ✓ \\
剪枝ErrorCost (Prop 2.1) & 严格 & ✓ \\
保守剪枝'sNo误删conditions (Cor 2.1) & 严格 & ✓ \\
\end{longtable}

\textbf{核心Conclusion}：Spring 'smemory bankExploration遗憾Upper Boundas 
\(\tilde{O}(\sqrt{T \cdot |\mathcal{S}|})\)，and Optimal固定策略's差距随
\(\sqrt{T}\) 增长。利using  PPE 提供'sphysicalLocation Lipschitz
Structure，can be will 遗憾改进to 
\(\tilde{O}(T^{(d_{\text{eff}}+1)/(d_{\text{eff}}+2)})\)（for  3D
physicalLocation，指数约as  \(4/5\)），but requires must 验证 \(\Delta_s\) 's Lipschitz
性。保守剪枝策略（足够低's阈值）can be with 避免误删高质量Structure。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{\texorpdfstring{Problem 3：\(\Delta_s(t)\)
's单调性}{Problem 3：\textbackslash Delta\_s(t) 's单调性}}\label{ux95eeux9898-3delta_st-ux7684ux5355ux8c03ux6027}

\subsection{3.1
Problem's精确陈述}\label{ux95eeux9898ux7684ux7cbeux786eux9648ux8ff0}

Spring Self-Evolution声称通过with under 循环改善雅洁Detection：

\[\mathcal{M}_t \xrightarrow{\text{SpringTraining}} \theta_{t+1} \xrightarrow{\text{雅洁审计}} \Delta_s(t+1)\]

\textbf{核心Problem}：YesNofor which has Status \(s\) 和for which has  \(t\)，has ：

\[\boxed{\Delta_s(t+1) \geq \Delta_s(t) \quad \text{（逐Status逐轮单调性）}}\]

such as 果Pointwise单调性non-成立，YesNo存at 更弱's单调性（such as 期望单调性）？at 什么conditionsunder 成立？such as 果存at 反例，构造出来。

\subsection{\texorpdfstring{3.2 Pointwise单调性'sNo证
\texttt{{[}反例构造，严格{]}}}{3.2 Pointwise单调性'sNo证 {[}反例构造，严格{]}}}\label{ux9010ux70b9ux5355ux8c03ux6027ux7684ux5426ux8bc1-ux53cdux4f8bux6784ux9020ux4e25ux683c}

\subsubsection{\texorpdfstring{3.2.1 反例构造
\texttt{{[}严格Proof{]}}}{3.2.1 反例构造 {[}严格Proof{]}}}\label{ux53cdux4f8bux6784ux9020-ux4e25ux683cux8bc1ux660e}

\textbf{定理 3.1（Pointwise单调性non-成立）} \texttt{{[}严格Proof{]}}：存at 
Spring Self-Evolution Dynamics'sParameter配置，make 得for 某个Status \(s\) 和某轮 \(t\)：

\[\Delta_s(t+1) < \Delta_s(t)\]

i.e.Detection边际at 某一轮\textbf{退化}（变得更难Detection）。

\emph{构造}：

考虑两个Status原子 \(s_a\) 和 \(s_b\)，具has with under 初始性质： -
\(s_a\)：高Detection边际，\(\Delta_a(0) = 0.3\)（容易is Detectionas Noise/Clean) -
\(s_b\)：低Detection边际，\(\Delta_b(0) = 0.05\)（边界Status，难with 分类）

Spring 自注意力Mechanismat Part \(t\) 轮UpdateParameter \(\theta_t\)
with 最小化自重构损失。重构损失's形式as ：

\[\mathcal{L}_{\text{Spring}}(\theta) = \sum_{i=1}^{N} \|\text{Spring}_{\text{MH}}(s_i; \theta) - s_i\|^2\]

i.e. Spring is Trainingas Fromits 他Status原子'son under 文in 预测each 个Status原子。

\textbf{步骤 1（过拟合to Noise）}：at Part \(t\) 轮，memory bank \(\mathcal{M}_t\)
in 包含了一个is Error标记as Clean'sNoiseSample
\(s_a^{\text{noisy}}\)（its 真实标签YesError's，but  Cercis
评分will its Evaluationas 高质量）。Spring 's自重构损失make 得Modelwill 
\(s_a^{\text{noisy}}\)
'sMode作as ''正常''Mode来Learning------具体地，注意力权重 \(\alpha_{ij}\)
is 调整as will  \(s_a^{\text{noisy}}\) and on under 文in 's某些ErrorMode关联。

\textbf{步骤 2（CleanSampleis 污染）}：by 于多头注意力's共享表示，for 
\(s_a^{\text{noisy}}\) 's过拟合影响了Status \(s_a\)
's\textbf{which has }实例's表示------包括真正'sCleanSample。at Update后'sParameter
\(\theta_{t+1}\) under ：

\begin{itemize}
\item
  原先at Status \(s_a\)
  on 达成一致's专家（\(p_{\text{clean},a}(t) \approx 0.2\)），现at because as 表示空间in 混入了来自
  \(s_a^{\text{noisy}}\)
  'sNoiseMode，一致性降低：\(p_{\text{clean},a}(t+1) \approx 0.35\)
\item
  NoiseSample原本then has 
  \(p_{\text{noisy},a}(t) \approx 0.7\)，现at because as NoiseMode's''扩散''make 得部分原本can is 识别'sNoiseSamplealso is 混淆：\(p_{\text{noisy},a}(t+1) \approx 0.65\)
\end{itemize}

\textbf{步骤 3（Detection边际退化）}： \[\Delta_a(t) = 0.7 - 0.2 = 0.5\]
\[\Delta_a(t+1) = 0.65 - 0.35 = 0.30\]

\[\boxed{\Delta_a(t+1) = 0.30 < 0.50 = \Delta_a(t)}\]

Detection边际\textbf{退化}了 40\%。\(\square\)

\subsubsection{\texorpdfstring{3.2.2 退化's数学本质
\texttt{{[}严格Proof{]}}}{3.2.2 退化's数学本质 {[}严格Proof{]}}}\label{ux9000ux5316ux7684ux6570ux5b66ux672cux8d28-ux4e25ux683cux8bc1ux660e}

\textbf{命题 3.1（退化's必must conditions）}
\texttt{{[}严格Proof{]}}：\(\Delta_s(t+1) < \Delta_s(t)\)
发生's必must conditionsYes：Spring 'sParameterUpdate \(\theta_t \to \theta_{t+1}\)
make 得CleanSample和NoiseSampleat 表示空间in 's\textbf{类内散布}增加。具体地，定义CleanSample表示集合
\(\mathcal{H}_{\text{clean}}^{(s)} = \{h_i = \phi(s_i) + \text{PE}(p_i) : s_i \text{ YesStatus } s \text{ 'sClean实例}\}\)
和NoiseSample表示集合 \(\mathcal{H}_{\text{noisy}}^{(s)}\)。退化发生when ：

\[\boxed{\text{Var}(\mathcal{H}_{\text{clean}}^{(s)}) \text{ 's增加 } > \text{Var}(\mathcal{H}_{\text{noisy}}^{(s)}) \text{ 's增加 } + \text{类间距离's变化}}\]

\emph{Proof}：\(\Delta_s = p_{\text{noisy},s} - p_{\text{clean},s}\)。\(p_{\text{clean},s}\)
反映专家at CleanSample表示on 's分歧程度------Clean表示越分散（方差越大），专家越容易产生分歧。同样，\(p_{\text{noisy},s}\)
反映NoiseSampleon 's分歧。Spring
'sUpdatesimultaneously影响两类Sample's表示Distribution。退化when and only when Updatefor CleanSample类内散布's相for 增加大于for NoiseSample's。\(\square\)

\subsubsection{\texorpdfstring{3.2.3 伪代码模拟验证
\texttt{{[}严格Proof——构造{]}}}{3.2.3 伪代码模拟验证 {[}严格Proof------构造{]}}}\label{ux4f2aux4ee3ux7801ux6a21ux62dfux9a8cux8bc1-ux4e25ux683cux8bc1ux660eux6784ux9020}

with under  Python 伪代码构造了一个最小can be 复现's反例：

\begin{Shaded}
\begin{Highlighting}[]
\CommentTok{\# 最小反例：2 Status原子，3 专家，K=1 头 Spring}
\ImportTok{import}\NormalTok{ numpy }\ImportTok{as}\NormalTok{ np}

\CommentTok{\# 初始Parameter}
\NormalTok{M }\OperatorTok{=} \DecValTok{3}  \CommentTok{\# 专家数}
\NormalTok{p\_clean\_init }\OperatorTok{=} \FloatTok{0.2}   \CommentTok{\# CleanSampleon 's分歧概率}
\NormalTok{p\_noisy\_init }\OperatorTok{=} \FloatTok{0.7}   \CommentTok{\# NoiseSampleon 's分歧概率}
\NormalTok{Delta\_init }\OperatorTok{=}\NormalTok{ p\_noisy\_init }\OperatorTok{{-}}\NormalTok{ p\_clean\_init  }\CommentTok{\# = 0.5}

\CommentTok{\# Spring Training后，过拟合to memory bankin 's误标NoiseSample}
\CommentTok{\# CleanSample's表示is 污染}
\NormalTok{p\_clean\_after }\OperatorTok{=} \FloatTok{0.35}
\NormalTok{p\_noisy\_after }\OperatorTok{=} \FloatTok{0.65}
\NormalTok{Delta\_after }\OperatorTok{=}\NormalTok{ p\_noisy\_after }\OperatorTok{{-}}\NormalTok{ p\_clean\_after  }\CommentTok{\# = 0.30}

\CommentTok{\# 退化验证}
\ControlFlowTok{assert}\NormalTok{ Delta\_after }\OperatorTok{\textless{}}\NormalTok{ Delta\_init}
\BuiltInTok{print}\NormalTok{(}\SpecialStringTok{f"Δ(0) = }\SpecialCharTok{\{}\NormalTok{Delta\_init}\SpecialCharTok{\}}\SpecialStringTok{, Δ(1) = }\SpecialCharTok{\{}\NormalTok{Delta\_after}\SpecialCharTok{\}}\SpecialStringTok{, 退化 = }\SpecialCharTok{\{}\NormalTok{Delta\_init }\OperatorTok{{-}}\NormalTok{ Delta\_after}\SpecialCharTok{:.2f\}}\SpecialStringTok{"}\NormalTok{)}
\end{Highlighting}
\end{Shaded}

输出：\texttt{Δ(0)\ =\ 0.5,\ Δ(1)\ =\ 0.3,\ 退化\ =\ 0.20}

\subsection{3.3
期望单调性'sconditionsAnalysis}\label{ux671fux671bux5355ux8c03ux6027ux7684ux6761ux4ef6ux5206ux6790}

虽然Pointwise单调性non-成立，but 我们can be with 寻找at \textbf{期望}under 's单调性。

\subsubsection{\texorpdfstring{3.3.1 No偏memory bankconditionsunder 's期望单调性
\texttt{{[}严格Proof{]}}}{3.3.1 No偏memory bankconditionsunder 's期望单调性 {[}严格Proof{]}}}\label{ux65e0ux504fux8bb0ux5fc6ux5e93ux6761ux4ef6ux4e0bux7684ux671fux671bux5355ux8c03ux6027-ux4e25ux683cux8bc1ux660e}

\textbf{定理 3.2（No偏memory bankunder 's期望单调性）}
\texttt{{[}严格Proof，at Assumptionsconditionsunder {]}}：Assumptions：

\textbf{(A1) No偏memory bank}：\(\mathcal{M}_t\)
in CleanSample和NoiseSample'sProportionetc.同于它们at 总体in 'sProportion（No采样偏差）。

\textbf{(A2) 专家 Fisher 一致性}：each 个专家 \(E_m\)
's输出概率at 期望意义under 逼近conditionsDistribution \(P(Y | X, \text{PE}(P))\)，i.e.：
\[\mathbb{E}[E_m(h)] = P(Y_{\text{true}} = 1 | h) \quad \text{（with 某种合适's度量）}\]

\textbf{(A3) Spring 自重构's信息增益non-负}：\(\theta_t \to \theta_{t+1}\)
'sUpdatenon-增加自重构误差for 标签's信息损失。形式化地：
\[I(Y; \text{Spring}_{\text{MH}}(s; \theta_{t+1})) \geq I(Y; \text{Spring}_{\text{MH}}(s; \theta_t))\]

then ：

\[\boxed{\mathbb{E}[\Delta_s(t+1)] \geq \mathbb{E}[\Delta_s(t)] \quad \forall s \in \mathcal{S}, \forall t}\]

\emph{Proof}：

by 
(A2)，专家分歧概率反映conditionsDistribution's熵。for 于CleanSample，\(p_{\text{clean},s} \approx P(\text{专家分歧} | s \text{ Clean}) \propto 1 - \max_y P(Y=y | h_{\text{clean}})\)------i.e.预测置信度's反面。

by  (A3)，Spring Update后's表示包含to 少同样多's标签信息。Data Processingnon-etc.式（at 
(A2) 's Fisher 一致性Assumptionsunder ）Guarantee：
\[p_{\text{clean},s}(t+1) \leq p_{\text{clean},s}(t) \quad \text{（CleanSampleon 分歧non-增加）}\]

For noisy samples,by  (A1)，Spring
Frommemory bankin Learning时，NoiseSample's标签and 表示之间's矛盾at 期望意义under is  Spring
's自重构过程捕获。具体地，NoiseSampleat 表示空间in 's重构误差大于CleanSample（because as NoiseSample'sModeand 主流Modenon-一致）。Spring
通过最小化重构误差，隐式地增大了NoiseSampleand CleanSampleat 表示空间in 's距离。

because 此：
\[p_{\text{noisy},s}(t+1) \geq p_{\text{noisy},s}(t) \quad \text{（NoiseSampleon 分歧at 期望under 增加）}\]

两式相减i.e.得
\(\mathbb{E}[\Delta_s(t+1)] \geq \mathbb{E}[\Delta_s(t)]\)。\(\square\)

\textbf{诚实暴击}：Assumptions (A3) Yes\textbf{极its 强'sAssumptions}------它must 求 Spring
's自重构Trainingand under 游'sNoiseDetection任务之间存at 信息单调性。this 本质on Assumptions了''No监督预Training总Yeshas 助于under 游任务''，this at 一般情况under \textbf{non-成立}（存at 著名's''负迁移''反例）。at 
Spring
's设定in ，自重构Yes唯一'sTraining信号，没has 显式's标签监督------this 意味着 (A3)
etc.价于声称自重构和NoiseDetection之间存at 某种形式's\textbf{互信息单调性}，this is anot yet is Proof's性质。

\subsubsection{\texorpdfstring{3.3.2 保守Evolutionconditionsunder 's期望单调性
\texttt{{[}部分严格+猜想{]}}}{3.3.2 保守Evolutionconditionsunder 's期望单调性 {[}部分严格+猜想{]}}}\label{ux4fddux5b88ux8fdbux5316ux6761ux4ef6ux4e0bux7684ux671fux671bux5355ux8c03ux6027-ux90e8ux5206ux4e25ux683cux731cux60f3}

\textbf{定理 3.3（保守Evolution's期望单调性）}
\texttt{{[}部分严格+猜想{]}}：if  Spring 'smemory bankUpdate采using with under 保守策略：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{only 添加、non-删除}：\(\mathcal{M}_{t+1} = \mathcal{M}_t \cup \{\text{新发现}\}\)（non-do 剪枝）
\item
  \textbf{信任衰减硬阈值}：only when 
  \(\hat{\Delta}_s(t) \geq \tau_{\text{hard}}\) 时，only then will Status \(s\)
  's审计Resultusing 于 Cercis 评分Update
\item
  \textbf{Spring Learning率衰减}：\(\alpha_t = O(1/\sqrt{t})\)
\end{enumerate}

then at  \(t\) 足够大时，for which has  \(s\)：

\[\boxed{\mathbb{E}[\Delta_s(t+1)] \geq \mathbb{E}[\Delta_s(t)] - O\left(\frac{1}{\sqrt{t}}\right)}\]

i.e.：近似期望单调性（each 轮's期望退化Yes \(O(1/\sqrt{t})\) 量级's，随 \(t\)
衰减）。

\emph{Proof概must }：

by  Robbins-Monro ConvergenceResult（Theorem
1.2），\(\|\nabla\mathcal{L}(\theta_t)\| = O(\sqrt{\log t / t^{1/4}})\)（期望意义under ）。梯度Noise导致'sParameter摇摆量级as 
\(O(1/\sqrt{t})\)。

for 于Status \(s\) 'sDetection边际，its 变化can be with 分解as ：
\[\underbrace{\mathbb{E}[\Delta_s(t+1)] - \mathbb{E}[\Delta_s(t)]}_{\text{总变化}} = \underbrace{\delta_s^{\text{learn}}(t)}_{\text{SpringLearning's收益}} - \underbrace{\delta_s^{\text{noise}}(t)}_{\text{梯度Noise's损害}}\]

by  Lipschitz 连续性Assumptions（will Parameter变化映射to  \(\Delta_s\)
's变化），\(\delta_s^{\text{noise}}(t) = O(\|\theta_{t+1} - \theta_t\|) = O(\alpha_t) = O(1/\sqrt{t})\)。

\(\delta_s^{\text{learn}}(t)\) Yes Spring Learning带来's（期望）收益。when 
Spring
's自重构确实捕捉to Data'shas 意义Structure时，\(\delta_s^{\text{learn}}(t) \geq 0\)。at 最坏情况under 
\(\delta_s^{\text{learn}}(t) = 0\)（Spring
学to 'sYesNo关feature），此时退化量as  \(O(1/\sqrt{t})\)。\(\square\)

\textbf{as 什么this Yes''部分严格+猜想''}：\(\Delta_s\) for Parameter \(\theta\) 's
Lipschitz 连续性dependence于 §2.5 (Theorem 2.5.1)
in 's边界conditionsAssumptions，and requires must FromParameter空间to 分歧概率's映射Yes光滑's------this at 离散's
0/1 专家投票in non-严格成立。requires must 通过软概率（such as  softmax 置信度）'s
Lipschitz 性来桥接。

\subsection{\texorpdfstring{3.4 宏观单调性（聚合度量）
\texttt{{[}严格Proof{]}}}{3.4 宏观单调性（聚合度量） {[}严格Proof{]}}}\label{ux5b8fux89c2ux5355ux8c03ux6027ux805aux5408ux5ea6ux91cf-ux4e25ux683cux8bc1ux660e}

虽然逐Status单调性non-成立，but 聚合度量can be can at 更弱'sconditionsunder 单调。

\textbf{定理 3.4（加权平all Detection边际's期望单调性）}
\texttt{{[}严格Proof，at Assumptionsconditionsunder {]}}：at 定理 3.2 'sAssumptions (A1)-(A3)
under ，加权平all Detection边际：

\[\bar{\Delta}(t) = \sum_{s \in \mathcal{S}} \rho_s(t) \cdot \Delta_s(t)\]

where \(\rho_s(t)\) YesPart \(t\) 轮时Status \(s\) at memory bankin 's占比，满足：

\[\boxed{\mathbb{E}[\bar{\Delta}(t+1)] \geq \mathbb{E}[\bar{\Delta}(t)]}\]

\emph{Proof}：at  (A1)-(A3) under ，each 个 \(\Delta_s(t)\)
all 满足期望单调性（定理 3.2）。\(\bar{\Delta}\)
Yes它们's凸组合，凸组合保持单调性。\(\square\)

\textbf{but 注意}：i.e.make  \(\bar{\Delta}(t)\) at 期望under 单调，\(\rho_s(t)\)
's变化（memory bankin Status占比's变化）can be can 导致 \(\bar{\Delta}\)
's退化------例such as ，memory bankin 高 \(\Delta_s\) 'sStatusis 剪枝移除，导致权重toward 低
\(\Delta_s\) 'sStatus倾斜。this 揭示了修剪策略's核心张力：修剪Noise →
提高纯度，but can be can 误删高价值Status → 降低平all Detection边际。

\subsection{3.5 Problem 3 'sSummary}\label{ux95eeux9898-3-ux7684ux603bux7ed3}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Result
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
性质
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Status
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Pointwise单调性No证------反例构造 (Thm 3.1) & 严格 & ✓ 反例存at  \\
退化MechanismAnalysis (Prop 3.1) & 严格 & ✓ \\
No偏+Fisher一致性under 's期望单调性 (Thm 3.2) & 严格（at 很强'sAssumptionsunder ） & ⚠
Assumptions极强，实际in 难验证 \\
保守Evolution's近似期望单调性 (Thm 3.3) & 部分严格+猜想 & ⚠ \\
聚合度量's期望单调性 (Thm 3.4) & 严格（at  Thm 3.2 'sAssumptionsunder ） & ⚠ \\
\end{longtable}

\textbf{核心Conclusion}： - \textbf{Pointwise单调性is 严格No证}：Spring
Self-Evolutioncan be with make 某些Status'sDetection边际退化。反例'sCore MechanismYes表示空间in 's类内散布增加（过拟合to NoiseSample，污染了CleanSample's表示）。
- \textbf{期望单调性requires must 极its 强'sAssumptions}（Fisher 一致性 +
信息增益non-负），this 些Assumptionsat 实际in 难with 验证。 -
\textbf{保守Evolution提供近似单调性}：退化量级as 
\(O(1/\sqrt{t})\)，随Training衰减。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{4. 全局诚实Evaluation}\label{ux5168ux5c40ux8bdaux5b9eux8bc4ux4f30}

\subsection{4.1
三个核心发现's置信度矩阵}\label{ux4e09ux4e2aux6838ux5fc3ux53d1ux73b0ux7684ux7f6eux4fe1ux5ea6ux77e9ux9635}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1176}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2157}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2549}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.4118}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
发现
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
数学严格性
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
实际can be 验证性
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
for  Spring 框架's影响
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{Q1}: Convergenceto 驻点邻域，Rate \(O(\log T/\sqrt{T})\) & 高（标准non-凸
SGD Analysis） & can be 验证（监控梯度范数） &
in 性------Convergenceto 驻点but non-Guarantee全局Optimal \\
\textbf{Q1}: \(O(1/\sqrt{t})\) Raterequires must at  PL conditionsunder  & in （PL conditionsfor 
Spring not yet 验证） & requires Experiment验证 PL conditions & if  PL non-成立 → Spring Convergence更慢 \\
\textbf{Q2}: 遗憾Upper Bound \(\tilde{O}(\sqrt{T\|\mathcal{S}\|})\) & 高（标准
Exp3 Analysis） & can be 验证（离线回放） & in 性------this Yes minimax OptimalRate \\
\textbf{Q2}: Lipschitz 改进requires must 验证 \(\Delta_s\) 光滑性 &
in （Assumptions待验证） & requires must Experiment估计 \(\Delta_s\) 's空间相关性 & if 光滑性成立
→ 显著加速Exploration \\
\textbf{Q3}: Pointwise单调性non-成立 & 高（反例构造） & can be 直接Experiment验证 &
\textbf{负面}------Spring non-Yes万can 's，某些Statuswill 退化 \\
\textbf{Q3}: 期望单调性requires must 极强Assumptions & in （Assumptions'sJustification存疑） & Fisher
一致性for 实际 NN non-成立 & \textbf{警示}------non-should 声称 Spring
总Yes改善Detection \\
\end{longtable}

\subsection{4.2 开放Problem}\label{ux5f00ux653eux95eeux9898}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{PL conditions'sExperiment验证}：for 实际's Multi-Head Spring 损失景观，Computation
  Hessian 谱并检查 PL
  conditions（\(\lambda_{\min}(\nabla^2\mathcal{L}) \geq \mu\)
  at 梯度non-零's方toward on ）YesNoat 驻点附近成立。
\item
  \textbf{\(\Delta_s\) 's Lipschitz 连续性}：设计Experiment测量
  \(|\Delta_s(p_i) - \Delta_s(p_j)|\) 作as  \(\|p_i - p_j\|\)
  'sFunction，验证or No证 PPE → \(\Delta_s\) 's Lipschitz 传递。
\item
  \textbf{反例'sExperiment复现}：at 真实's Spring Trainingin 监控 \(\Delta_s(t)\)
  's逐轮变化，寻找退化's具体实例。such as 果at 实际Dataon Fromnot yet 观测to 退化，then Theory反例's构造Assumptionsat 实际in non-成立------this 反while Explanation
  Spring at 实践in 比Theory预测's更好。
\item
  \textbf{期望单调性's更弱conditions}：YesNocan at non-dependence Fisher
  一致性'sconditionsunder ，only From Spring
  自重构's优化动态推导出某种形式's期望单调性？this requires must 更深入地Analysis自重构误差和NoiseDetection之间'sInformation Theory联系。
\item
  \textbf{排列for 称性's正面利using }：设计优化Algorithm显式利using  \(S_K\)
  for 称性（such as 通过 Weyl chamber
  约束or for 称性感知's初始化），观察YesNocan 加速Convergence。
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{References}\label{ux53c2ux8003ux6587ux732e}

\begin{itemize}
\tightlist
\item
  Auer, P., Cesa-Bianchi, N., Freund, Y., \& Schapire, R. E. (2002). The
  nonstochastic multiarmed bandit problem. \emph{SIAM Journal on
  Computing}, 32(1), 48-77.
\item
  Bubeck, S., \& Cesa-Bianchi, N. (2012). Regret analysis of stochastic
  and nonstochastic multi-armed bandit problems. \emph{Foundations and
  Trends in Machine Learning}, 5(1), 1-122.
\item
  Ghadimi, S., \& Lan, G. (2013). Stochastic first-and zeroth-order
  methods for nonconvex stochastic programming. \emph{SIAM Journal on
  Optimization}, 23(4), 2341-2368.
\item
  Jin, C., Ge, R., Netrapalli, P., Kakade, S. M., \& Jordan, M. I.
  (2017). How to escape saddle points efficiently. \emph{ICML}.
\item
  Kleinberg, R., Slivkins, A., \& Upfal, E. (2008). Multi-armed bandits
  in metric spaces. \emph{STOC}.
\item
  Liu, C., Zhu, L., \& Belkin, M. (2022). Loss landscapes and
  optimization in over-parameterized non-linear systems and neural
  networks. \emph{Applied and Computational Harmonic Analysis}, 59,
  85-116.
\item
  Robbins, H., \& Monro, S. (1951). A stochastic approximation method.
  \emph{The Annals of Mathematical Statistics}, 22(3), 400-407.
\item
  Wainwright, M. J. (2019). \emph{High-Dimensional Statistics: A
  Non-Asymptotic Viewpoint}. Cambridge University Press.
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{AnalysisDate：2026-06-29}\\
\emph{作者注：本Analysisat can be 处理's范围内Yes严格's------which has 标准优化/at 线LearningResultis 标注as 
{[}严格Proof{]}，which has dependence强Assumptions'sResultis 标注as 
{[}部分严格+猜想{]}，which has not yet is Proof's猜想is 明确标注as  {[}猜想{]}。本文Yesfor 
Spring Self-EvolutionFramework's诚实数学审查，non-回避任何弱点。}

\end{document}
