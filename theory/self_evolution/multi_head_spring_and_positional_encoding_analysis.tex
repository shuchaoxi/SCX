% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Multi-Head Spring and Physical Positional Encoding
: A Rigorous Mathematical Analysis}\label{multi-head-spring-ux4e0e-physical-positional-encoding-ux7684ux4e25ux683cux6570ux5b66ux5206ux6790}

\subsection{On the SCX
Framework Theorem Foundations: An Honest Audit}\label{ux5bf9-scx-ux6846ux67b6ux5b9aux7406ux57faux7840ux7684ux8bdaux5b9eux5ba1ux67e5}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: Complete Analysis\\
\textbf{dependence}: Theorem 1--4 (theory/theorems/), SE-1, SE-2
(theory/self\_evolution/)\\
\textbf{Methodology}: Rigorous Formal Derivation + Honest Critique (No Euphemisms)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{0.
Notation Conventions and Framework Reconstruction}\label{ux7b26ux53f7ux7ea6ux5b9aux4e0eux6846ux67b6ux91cdux6784}

Before entering the analysis, we first precisely reconstruct the mathematical foundations of the SCX framework. All definitions below are rigorously formalized from the
prompt theorem descriptions.

\subsubsection{0.1 State Crystallization (State
Crystallization）}\label{ux72b6ux6001ux7ed3ux6676state-crystallization}

Let the raw physical observation space be
\(\mathcal{X} \subset \mathbb{R}^{d_{\text{raw}}}\)（continuous physical quantities: PBE
energy surfaces, force fields, electron densities, etc.). State Crystallization is a mapping:

\[\mathcal{C}: \mathcal{X} \to \mathcal{S} = \{s_1, \ldots, s_N\}\]

where each \(s_i \in \mathbb{R}^{d_s}\)
is a\textbf{discrete state atom}'s vector representation. Unlike BPE
which is statistically driven,\(\mathcal{C}\) is physically driven: it clusters according to the natural curvature boundaries of the PBE
energy surface. Formally:

\[s_i = \mathcal{C}(x) \quad \text{where} \quad x \in \mathcal{X} \text{ and  } \nabla^2 E_{\text{PBE}}(x) \text{ at  } x \text{ 处has 一个谱间隙}\]

\subsubsection{0.2 Yajie Audit (Yajie
Audit）}\label{ux96c5ux6d01ux5ba1ux8ba1yajie-audit}

Let there be \(M\) 个独立专家Model \(\{E_1, \ldots, E_M\}\)。For a state atom \(s_i\)
and its label \(y_i\), define:

\[c_m(s_i) = \mathbf{1}[E_m(s_i) \neq y_i] \quad \text{（expert m disagrees with the given label）}\]

Consistency count:
\[C(s_i) = \sum_{m=1}^{M} c_m(s_i) \sim \text{Binomial}(M, p_{s_i})\]

where \(p_{s_i}\)
is the probability that experts''disagree with''the label. For clean samples,\(p_{s_i} = p_{\text{clean}} < 0.5\)；For noisy samples,\(p_{s_i} = p_{\text{noisy}} > 0.5\)。

\textbf{Cercis Score}: \[S(s_i) = Q(s_i) + \eta(t) \cdot N(s_i)\]

where \(Q(s_i)\) is the base quality score (based on multi-expert consistency),\(N(s_i)\)
is the novelty bonus,\(\eta(t)\) is the time-decaying exploration weight.

\subsubsection{0.3 Spring
Self-Evolution Dynamics}\label{spring-ux81eaux8fdbux5316ux52a8ux529bux5b66}

State transition: \[(S_t, \theta_t, M_t) \to (S_{t+1}, \theta_{t+1}, M_{t+1})\]

Spring 自注意力形式（单头):
\[\text{Spring}(s_i, \mathcal{S}) = \text{softmax}\left(\frac{(Q s_i)(K \mathcal{S})^T}{\sqrt{d_k}}\right) \cdot (V \mathcal{S})\]

where
\(Q, K \in \mathbb{R}^{d_k \times d_s}\)，\(V \in \mathbb{R}^{d_v \times d_s}\)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.
Core Theorems's精确Theorem Statement}\label{ux6838ux5fc3ux5b9aux7406ux7684ux7cbeux786eux5b9aux7406ux9648ux8ff0}

\subsubsection{Theorem
1（多Expert consensusNoiseDetection）}\label{theorem-1ux591aux4e13ux5bb6ux4e00ux81f4ux6027ux566aux58f0ux68c0ux6d4b}

\[\boxed{F_1 \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M \Delta_s^2\right)}\]

where： -
\(\rho_s = |\{i: \mathcal{C}(x_i) = s\}| / N_{\text{total}}\)：Status原子
\(s\) 's占比 -
\(\Delta_s = |p_{\text{clean},s} - p_{\text{noisy},s}|\)：Status \(s\)
'sDetection边际 - \(M\)：独立专家数量 - \(\eta > 0\)：归一化because 子

\textbf{Proof技术}：Chernoff bound + Hoeffding non-etc.式。for each 个Status
\(s\)，\(C(s)\) Yes两个二项Distribution's混合。标记as Noisewhen 
\(C(s) \geq \tau_s\)。Chernoff give 出：
\[P(\text{假阳性}) = P_{H_0}(C(s) \geq \tau_s) \leq \exp(-2M(\tau_s/M - p_{\text{clean},s})^2)\]
\[P(\text{假阴性}) = P_{H_1}(C(s) < \tau_s) \leq \exp(-2M(p_{\text{noisy},s} - \tau_s/M)^2)\]

选取 \(\tau_s = M \cdot (p_{\text{clean},s} + p_{\text{noisy},s})/2\)
make 两项平衡，得to  \(\Delta_s\)。

\subsubsection{Theorem
2（弱feature必然失效）}\label{theorem-2ux5f31ux7279ux5f81ux5fc5ux7136ux5931ux6548}

\[\boxed{F_{1,\text{SCX}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}}\]

where \(\delta > 0\) 刻画feature空间's信息non-足程度。

\textbf{Proof技术}：Fano non-etc.式。定义
\(\delta = \min_{f} \mathbb{E}[\mathbf{1}[f(X) \neq Y]] - \text{Bayes error}\)，i.e.Optimalcan be lines分类器and 贝叶斯Optimal之间's最小差距。Fano
give 出conditions熵and Error率's关系：
\[P_e \geq \frac{H(Y|X) - 1}{\log |\mathcal{Y}|}\]

when  \(H(Y|X)\) 较大（feature弱），任何Method（包括 SCX）'sError率lower boundby 
\(\delta\) 决定。

\subsubsection{Theorem
3（Noiseand DifficultySample'snon-can be 区分性）}\label{theorem-3ux566aux58f0ux4e0eux56f0ux96beux6837ux672cux7684ux4e0dux53efux533aux5206ux6027}

\textbf{构造性双世界Proof}：存at 两个世界
\(W_A, W_B\)，具has 观测etc.价'sDataDistribution
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)，but ：

\begin{itemize}
\tightlist
\item
  世界 \(W_A\)：Sample \((x, y)\) Yesis Error标记's（标签Noise）
\item
  世界 \(W_B\)：Sample \((x, y)\) Correct标记but 本质on Difficulty（高贝叶斯non-确定性）
\end{itemize}

没has Algorithmcan 够only 通过观测Data区分this 两个世界。

形式化：\(\forall\) 决策规then 
\(d: (\mathcal{X} \times \mathcal{Y})^n \to \{W_A, W_B\}\)，
\[\max_{W \in \{W_A, W_B\}} P_W(d(\text{data}) \neq W) \geq \frac{1}{2} - \frac{1}{2}\|P_{W_A}^{\otimes n} - P_{W_B}^{\otimes n}\|_{\text{TV}} = \frac{1}{2}\]

because as 
\(P_{W_A}^{\otimes n} = P_{W_B}^{\otimes n}\)（complete 相同's观测Distribution）。

\subsubsection{Theorem 4（精确常数 Minimax
Optimal）}\label{theorem-4ux7cbeux786eux5e38ux6570-minimax-ux6700ux4f18}

SCX 'sNoiseDetectionError率at with under 意义on 达to 精确常数Optimal：

\[\lim_{M \to \infty} -\frac{1}{M} \log P_{\text{FN}}^{(M)} = D_{\text{KL}}(P_{\text{noisy}} \| P_{\text{clean}}) \quad \text{subject to } P_{\text{FP}}^{(M)} \leq \alpha\]

and  Bahadur-Rao 展开give 出's \(O(1/\sqrt{M})\) 修正项also matching minimax lower bound。

\textbf{Proof技术}：Chernoff-Stein 引理 + Bahadur-Rao 展开。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2. 组件 1：Physical Positional
Encoding（PPE）'s数学Analysis}\label{ux7ec4ux4ef6-1physical-positional-encodingppeux7684ux6570ux5b66ux5206ux6790}

\subsubsection{2.1 形式化定义}\label{ux5f62ux5f0fux5316ux5b9aux4e49}

设each 个Status原子 \(s_i\) 带has 一个physicalLocation \(p_i \in \mathcal{P}\)，where：
- 蛋白质序列：\(\mathcal{P} = \mathbb{N}\)（残基索引) - 3D
Structure：\(\mathcal{P} = \mathbb{R}^3\)（原子坐标) -
Docking：\(\mathcal{P} = \mathbb{R}^3\)（结合口袋坐标）

\textbf{定义 1（Physical Positional Encoding）}：PPE is a映射
\[\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\]

编码后'sStatus表示： \[\boxed{h_i = \phi(s_i) + \text{PE}(p_i)}\]

where \(\phi: \mathbb{R}^{d_s} \to \mathbb{R}^{d}\)
Yesfeature映射，and 我们must 求 \(d_{\text{pe}} = d\)。

\textbf{can be 选方案（拼接+投影）}：
\[h_i = W \cdot [\phi(s_i); \text{PE}(p_i)]\]

where
\(W \in \mathbb{R}^{d \times (d + d_{\text{pe}})}\)。数学on this Yes更一般's方案，but 加法方案更简洁and at 
\(W = [I_d, I_d]\) 时退化as 加法。with under Analysiswith 加法方案as 主，Resultcan be 推广。

\subsubsection{2.2
编码Function's具体形式}\label{ux7f16ux7801ux51fdux6570ux7684ux5177ux4f53ux5f62ux5f0f}

\textbf{标量Location's正弦编码}（类比 Transformer):
\[\text{PE}(p, 2j) = \sin\left(\frac{p}{10000^{2j/d}}\right), \quad \text{PE}(p, 2j+1) = \cos\left(\frac{p}{10000^{2j/d}}\right)\]

where \(j = 0, 1, \ldots, d/2 - 1\)。

\textbf{3D Location's旋转编码}（类比 RoPE，适配physical空间):
\[\text{PE}_{\text{rot}}(\mathbf{p}) = \mathbf{R}(\mathbf{p}) \cdot \mathbf{e}_0\]

where \(\mathbf{R}(\mathbf{p}) \in SO(d)\) Yesby 坐标Parameter化's旋转矩阵。for 于
\(\mathbf{p} = (x, y, z)\)：
\[\mathbf{R}(\mathbf{p}) = \mathbf{R}_x(\omega_x x) \cdot \mathbf{R}_y(\omega_y y) \cdot \mathbf{R}_z(\omega_z z)\]

where \(\mathbf{R}_\alpha(\theta)\) Yesat  \(\alpha\) 轴on 's \(d\)
维旋转，\(\omega_x, \omega_y, \omega_z\) Yescan be Learningor 固定's频率Parameter。

\textbf{关键性质}：and  NLP
'sLocation编码non-同，physicalLocation具has \textbf{多维几何Structure}。一个良好's PPE
should 尊重physicalfor 称性：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{平移etc.变性}（for  3D
  Structure):\(\text{PE}(\mathbf{p} + \mathbf{t})\) and 
  \(\text{PE}(\mathbf{p})\) has can be 预测's关系
\item
  \textbf{旋转etc.变性}：\(\text{PE}(\mathbf{R}\mathbf{p})\)
  can be with 通过旋转编码空间来恢复
\item
  \textbf{Lipschitz
  连续性}：\(\|\text{PE}(\mathbf{p}) - \text{PE}(\mathbf{q})\| \leq L_{\text{PE}} \cdot \|\mathbf{p} - \mathbf{q}\|\)
\end{enumerate}

\subsubsection{2.3 for  Theorem 1
's影响}\label{ux5bf9-theorem-1-ux7684ux5f71ux54cd}

\textbf{核心Problem}：PPE such as 何改变Detection边际 \(\Delta_s\)？

\textbf{命题 2.1（PPE under 's修改后Detection边际）}： at  PPE
增强'sfeature空间in ，Status \(s\) 'shas 效Detection边际as ：
\[\boxed{\Delta_s^{\text{PPE}} = \Delta_s + \delta_s^{\text{PE}}}\]

where
\(\delta_s^{\text{PE}} \in [-\Delta_s, 1 - p_{\text{clean},s} - \Delta_s]\)
满足：
\[\delta_s^{\text{PE}} = \underbrace{(p_{\text{clean},s}^{\text{PPE}} - p_{\text{clean},s})}_{\text{CleanSampleon 's一致性增益}} - \underbrace{(p_{\text{noisy},s}^{\text{PPE}} - p_{\text{noisy},s})}_{\text{NoiseSampleon 's混淆增益}}\]

\emph{Proof}：at 增强空间in ，专家's预测概率变as 
\(p_{\text{clean},s}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) = y_i] | H_0]\)
和
\(p_{\text{noisy},s}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) \neq y_i] | H_1]\)。Detection边际定义as 
\(\Delta_s^{\text{PPE}} = |p_{\text{clean},s}^{\text{PPE}} - (1 - p_{\text{noisy},s}^{\text{PPE}})| = |p_{\text{clean},s}^{\text{PPE}} + p_{\text{noisy},s}^{\text{PPE}} - 1|\)。展开i.e.得。\(\square\)

\textbf{定理 2.1（PPE under 's F1 lower bound）}：
\[\boxed{F_1^{\text{PPE}} \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M (\Delta_s + \delta_s^{\text{PE}})^2\right)}\]

Chernoff-Hoeffding non-etc.式's\textbf{has 效性non-变}（指示变量仍at  \([0,1]\)
内has 界），but 常数变化。

when  \(\delta_s^{\text{PE}} > 0\) for which has  \(s\)（PPE 总Yes帮助Detection):
\[F_1^{\text{PPE}} - F_1 \geq \frac{1}{\eta} \sum_s \rho_s \left[\exp(-2M\Delta_s^2) - \exp(-2M(\Delta_s + \delta_s^{\text{PE}})^2)\right] \geq 0\]

\textbf{命题 2.2（空间局部性正then 化）}：if  PE 满足 Lipschitz
conditions（Lipschitz 常数as  \(L_{\text{PE}}\)），and 专家Model \(E_m\)
for its 输入满足 \(L_E\)-Lipschitz，then for Arbitrary两个具has physicalLocation \(p_i, p_j\)
'sStatus原子：
\[|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| \leq 2L_E \cdot L_{\text{PE}} \cdot \|p_i - p_j\|\]

\emph{Proof}：Expert consensus概率's变化受输入变化约束： \begin{align*}
|p_{\text{clean},i}^{\text{PPE}} - p_{\text{clean},j}^{\text{PPE}}|
&\leq \mathbb{E}_{E} \left|\mathbf{1}[E(h_i) = y] - \mathbf{1}[E(h_j) = y]\right| \\
&\leq \mathbb{E}_{E} \left[ L_E \cdot \|h_i - h_j\| \right] \\
&= L_E \cdot \|\text{PE}(p_i) - \text{PE}(p_j)\| \quad (\text{because as  } s_i = s_j = s) \\
&\leq L_E \cdot L_{\text{PE}} \cdot \|p_i - p_j\|
\end{align*}

同样for  \(p_{\text{noisy}}\) 成立。两者's差得to because 子 2。\(\square\)

this 意味着\textbf{physicalon 邻近'sStatus原子具has 相似'sDetection难度}------一个can be 验证's预测。

\subsubsection{2.4 for  Theorem 2
's影响}\label{ux5bf9-theorem-2-ux7684ux5f71ux54cd}

\textbf{核心Problem}：non-完美's PPE such as 何改变feature弱点's界？

\textbf{定义 2（编码non-完美度 \(\varepsilon_{\text{PE}}\)）}：
\[\boxed{\varepsilon_{\text{PE}} = I(Y; P | X) - I(Y; \text{PE}(P) | X) \geq 0}\]

where \(I(Y; P | X)\) YesphysicalLocation \(P\) at give 定原始feature \(X\) under 关于标签
\(Y\) 'sconditions互信息，while  \(I(Y; \text{PE}(P) | X)\)
Yes编码后保留's信息。\(\varepsilon_{\text{PE}}\)
量化了\textbf{Location信息at 编码过程in 's丢失}。

\textbf{定理 2.2（non-完美 PPE under 's失效Upper Bound）}：
\[\boxed{F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}}\]

\emph{Proof}：Fano non-etc.式give 出
\(P_e^{\text{PPE}} \geq \frac{H(Y|X, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\)。我们has ：
\begin{align*}
H(Y|X, \text{PE}(P)) &= H(Y|X) - I(Y; \text{PE}(P) | X) \\
&= H(Y|X) - [I(Y; P | X) - \varepsilon_{\text{PE}}] \\
&\geq H(Y|X) - I(Y; P | X) + \varepsilon_{\text{PE}}
\end{align*}

while  \(\delta\) 's定义give 出
\(H(Y|X) - 1 \geq (\delta/2) \cdot \log|\mathcal{Y}|\)（通过 Fano
's逆方toward ）。because 此：
\[H(Y|X, \text{PE}(P)) - 1 \geq \left(\frac{\delta}{2} + \frac{\varepsilon_{\text{PE}}}{C_F}\right) \cdot \log|\mathcal{Y}|\]

代入 SCX 's F1 Upper Bound表达式i.e.得。\(\square\)

\textbf{紧致性Analysis}： - when 
\(\varepsilon_{\text{PE}} = 0\)（完美编码):Upper Bound恢复as 原始 Theorem 2 -
when  \(\varepsilon_{\text{PE}} > 0\)：Upper Boundwith 
\(\sqrt{\delta + O(\varepsilon_{\text{PE}})}\) 'sRate放松 - when 
\(\varepsilon_{\text{PE}} \to I(Y; P | X)\)（complete 失败's编码，丢失了which has Location信息):Upper Bound回to complete not yet make using Location信息's情况

\textbf{Corollaries 2.1（Location信息's价值）}：such as 果physicalLocation \(P\)
本身non-携带任何关于标签 \(Y\) 's信息（i.e. \(I(Y; P | X) = 0\)），then 
\(\varepsilon_{\text{PE}} = 0\)（because as 没has 信息can be 丢失），Theorem 2
'sUpper Bound\textbf{complete non-变}。PPE at this 种情况under Yes\textbf{纯粹'sParameter浪费}。

\subsubsection{2.5 for  Theorem 3
's影响}\label{ux5bf9-theorem-3-ux7684ux5f71ux54cd}

this Yes最关键'sAnalysis。Theorem 3 'snon-can be 区分性dependence于构造两个世界 \(W_A, W_B\)
具has complete 相同's观测Distribution \(P(X, Y)\)。

\textbf{命题 2.3（固定 PPE 保持 Theorem 3）}：such as 果 PE
is a\textbf{先验固定}'sFunction（non-dependence于TrainingDataor 标签），then 两个世界in 's增强featureDistribution依然相同：
\[P_{W_A}(X, \text{PE}(P), Y) = P_{W_B}(X, \text{PE}(P), Y)\]

because 此 Theorem 3 'snon-can be 区分性\textbf{保持non-变}。PPE
没has 帮助also 没has 损害最坏情况'scan be 识别性。

\emph{Proof}：physicalLocation \(P\) by physicalStructure决定，独立于标签生成过程。because 此
\(P_{W_A}(P|X) = P_{W_B}(P|X)\)。because as  PE
Yes固定's确定性Function，\(P_{W_A}(\text{PE}(P)|X) = P_{W_B}(\text{PE}(P)|X)\)。观测Distributioncomplete 相同。\(\square\)

\textbf{命题 2.4（Learning型 PPE 破坏 Theorem 3）}：such as 果 PE 'sParameter
\(\theta_{\text{PE}}\) YesFromDatain Learning's（包括标签
\(Y\)），then at 两个世界in Learningto non-同's编码：
\[\text{PE}_{W_A} \neq \text{PE}_{W_B}\]

because as  \(W_A\) 'sNoise标签影响Learning过程。此时：
\[P_{W_A}(X, \text{PE}_{W_A}(P), Y) \neq P_{W_B}(X, \text{PE}_{W_B}(P), Y)\]

两个世界变得\textbf{can be 区分}。Theorem 3 is 破坏。

\textbf{诚实评价}：this 实际on Yes\textbf{好消息}。Theorem 3
is anon-can be can 性Result------它说at 最坏情况under ，Noise和DifficultySampleNo法区分。such as 果Learning型
PPE can 破坏定理 3
's前提conditions，意味着\textbf{Location信息提供了区分Noise和DifficultySample'scan 力}。this non-Yes框架's缺陷，while Yes框架's进步。but this 确实意味着
Theorem 3 non-again Noconditions成立------requires must 添加前提conditions''PE
Yes固定'sor Location信息non-携带标签相关信息''。

\subsubsection{2.6 for  Theorem 4
's影响}\label{ux5bf9-theorem-4-ux7684ux5f71ux54cd}

\textbf{命题 2.5（PPE non-减少Optimal误差指数）}：
\[\boxed{D_{\text{KL}}(P_{\text{noisy}}^{\text{PPE}} \| P_{\text{clean}}^{\text{PPE}}) = D_{\text{KL}}(P_{\text{noisy}} \| P_{\text{clean}}) + \Delta D_{\text{KL}}^{\text{PE}}}\]

where \(\Delta D_{\text{KL}}^{\text{PE}} \geq 0\)（non-负，by  KL
散度'sData Processingnon-etc.式Guarantee）。

\emph{Proof}：by  KL 散度's链式法then ： \begin{align*}
D_{\text{KL}}(P_{\text{noisy}}^{\text{PPE}} \| P_{\text{clean}}^{\text{PPE}})
&= D_{\text{KL}}(P_{\text{noisy}}(X, H) \| P_{\text{clean}}(X, H)) \\
&= D_{\text{KL}}(P_{\text{noisy}}(X) \| P_{\text{clean}}(X)) + \mathbb{E}_{X \sim P_{\text{noisy}}}[D_{\text{KL}}(P_{\text{noisy}}(H|X) \| P_{\text{clean}}(H|X))]
\end{align*}

because as  \(H = \phi(X) + \text{PE}(P)\)，and  PE
Yes固定's确定性Function，增强non-will 减少 KL 散度（信息处理non-etc.式）。具体地：
\[\Delta D_{\text{KL}}^{\text{PE}} = \mathbb{E}_{X \sim P_{\text{noisy}}}[D_{\text{KL}}(P_{\text{noisy}}(\text{PE}(P)|X) \| P_{\text{clean}}(\text{PE}(P)|X))] \geq 0\]

\(\square\)

PPE
\textbf{Fromnon-降低}Optimal误差指数。such as 果Location信息and 标签相关，\(\Delta D_{\text{KL}}^{\text{PE}} > 0\)，误差指数\textbf{严格更大}（Detection更准确）。such as 果Location信息No关，\(\Delta D_{\text{KL}}^{\text{PE}} = 0\)，non-变。

Bahadur-Rao 常数because  PE 引入's方差项while 修改，but 总Yes朝更紧's方toward （when  PE
has using 时）。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3. 组件 2：Multi-Head Spring
's数学Analysis}\label{ux7ec4ux4ef6-2multi-head-spring-ux7684ux6570ux5b66ux5206ux6790}

\subsubsection{3.1 形式化定义}\label{ux5f62ux5f0fux5316ux5b9aux4e49-1}

\textbf{定义 3（Multi-Head Spring）}：
\[\boxed{\text{Spring}_{\text{MH}}(s_i, \mathcal{S}) = \text{Concat}(\text{head}_1, \ldots, \text{head}_K) \cdot W^O}\]

where：
\[\text{head}_k = \text{Spring}_k(s_i, \mathcal{S}) = \text{softmax}\left(\frac{(Q_k s_i)(K_k \mathcal{S})^T}{\sqrt{d_k}}\right) \cdot (V_k \mathcal{S})\]

Parameter： - \(Q_k, K_k \in \mathbb{R}^{d_k \times d_s}\)：Part \(k\)
个头's查询/键投影 - \(V_k \in \mathbb{R}^{d_v \times d_s}\)：Part \(k\)
个头's值投影 - \(W^O \in \mathbb{R}^{K d_v \times d_s}\)：输出投影 -
\(d_k = d_s / K\)（标准多头's降维约定），or 
\(d_k = d_s\)（全维多头，each 个头操作Completestate space）

each 个头 \(k\) is 设计as 关注一个non-同'sphysicalDimension： - 头 1：键角Mode ---
关注Status原子间's角度关系 - 头 2：键长Mode --- 关注Status原子间's距离关系 -
头 3：配位环境 --- 关注Status原子's局部邻域拓扑 - 头 4：can 量梯度/力场Mode
--- 关注Status原子'scan 量面曲率

\textbf{physicalDimension约束（Structure化多头）}：as 了实现真正's专业化，each 个头's
\(Q_k, K_k\) should is 约束as only 作using 于Statustoward 量's特定子空间。if 
\(s_i = [s_i^{(1)}; s_i^{(2)}; s_i^{(3)}; s_i^{(4)}]\)
按physicalDimension分块，then ：

\[Q_k = [\mathbf{0}_{d_k \times d_{<k}} \;|\; Q_k^{(k)} \;|\; \mathbf{0}_{d_k \times d_{>k}}]\]

where \(Q_k^{(k)}\) only 作using 于Part \(k\)
个physicalDimension's子空间，its 余as 零。this \textbf{强制}专业化。

\subsubsection{\texorpdfstring{3.2 for  Theorem 1 's影响 --- has 效专家数
\(M\)}{3.2 for  Theorem 1 's影响 --- has 效专家数 M}}\label{ux5bf9-theorem-1-ux7684ux5f71ux54cd-ux6709ux6548ux4e13ux5bb6ux6570-m}

\textbf{核心Problem}：\(K\) 个头YesNoetc.价于 \(K\) 个独立专家？

\textbf{答案：No。} this is a关键's区别。

\textbf{命题 3.1（头non-Yes独立专家）}：Multi-Head Spring
's头输出at give 定Status原子集合时Yes\textbf{conditionsdependence}'s：
\[\text{Cov}(\text{head}_k(s_i), \text{head}_l(s_i) \mid \mathcal{S}) \neq 0 \quad \text{（一般情况）}\]

\emph{Proof}：which has 头共享相同's输入Status原子 \(\mathcal{S}\) 和相同's
softmax 注意力池化Structure。i.e.make 投影矩阵 \(Q_k, K_k\) non-同，注意力权重
\(\alpha_{ij}^{(k)} = \text{softmax}_j((Q_k s_i)^T (K_k s_j) / \sqrt{d_k})\)
通过共同's \(s_j\) 值耦合。because 此：
\[\text{head}_k(s_i) = \sum_j \alpha_{ij}^{(k)} \cdot V_k s_j\]
\[\text{head}_l(s_i) = \sum_j \alpha_{ij}^{(l)} \cdot V_l s_j\]

两者's协方差通过共享's \(\{s_j\}\) 项non-零。\(\square\)

\textbf{定义 4（has 效专家多样性 \(\rho_{\text{eff}}\)）}：
\[\boxed{\rho_{\text{eff}} = 1 - \frac{2}{K(K-1)} \sum_{k < l} |\text{Corr}(\text{head}_k, \text{head}_l)|}\]

where
\(\text{Corr}(\text{head}_k, \text{head}_l) = \frac{\text{Cov}(\text{head}_k, \text{head}_l)}{\sqrt{\text{Var}(\text{head}_k) \cdot \text{Var}(\text{head}_l)}}\)（逐元素平all 相关性）。

\begin{itemize}
\tightlist
\item
  \(\rho_{\text{eff}} = 1\)：which has 头complete 独立（理想情况）
\item
  \(\rho_{\text{eff}} = 0\)：which has 头complete 相关（退化as 单头）
\item
  \(\rho_{\text{eff}} < 0\)：头之间存at 负相关（for 抗性多样性）
\end{itemize}

\textbf{has 效专家数's启发式修正}：
\[M_{\text{eff}} = M \cdot (1 + \alpha \cdot \rho_{\text{eff}} \cdot (K - 1))\]

where \(\alpha \in [0, 1]\) Yes''头多样性 \(\to\) 专家多样性'''s转换系数。

\textbf{but this non-is a严格's界}。严格's Chernoff bound
requires must 处理dependence随机变量。

\textbf{定理 3.1（Multi-Head Spring under 's严格 F1 lower bound）}：

设 \(A_k(s)\) as Part \(k\) 个头for Status \(s\)
产生's''伪一致性分数''（通过头输出and 标签's一致性），then make using 
union-Chernoff Method：

\[\boxed{F_1^{\text{MH}} \geq 1 - \frac{1}{\eta} \sum_{s} \rho_s \cdot \min_{\mathcal{I} \subseteq [K]} \exp\left(-2M \cdot \tilde{\Delta}_s^2(\mathcal{I})\right)}\]

where \(\tilde{\Delta}_s(\mathcal{I})\) Yesat 头子集 \(\mathcal{I}\)
on 'shas 效边际，\(\mathcal{I}\) 选取一个最大独立子集（满足某种
\(\beta\)-混合conditions）。

at 最坏情况under （which has 头complete 相关，\(\rho_{\text{eff}} = 0\)):
\[F_1^{\text{MH}} = F_1^{\text{single}} \quad \text{（Multi-Head No增益）}\]

at 最好情况under （which has 头complete 独立，\(\rho_{\text{eff}} = 1\)，and 
\(\alpha = 1\)):
\[F_1^{\text{MH}} \geq 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M K \tilde{\Delta}_s^2\right) \quad \text{（启发式，non-严格）}\]

\textbf{诚实暴击}：严格处理头之间dependenceStructure's Chernoff bound
极its 复杂。实际on ，such as 果non-for 头之间'sdependencedo 额外Assumptions（such as  \(\beta\)-混合or 
Dobrushin conditions），Theorem 1 形式's独立指数界\textbf{non-适using 于} Multi-Head
Spring。实际can be 得's界will \textbf{松得多}（because as must using  union bound or 
Azuma-Hoeffding 处理 martingale dependence序列）。

\subsubsection{3.3 for  SE-1（Robbins-Monro
Convergence）'s影响}\label{ux5bf9-se-1robbins-monro-ux6536ux655bux7684ux5f71ux54cd}

\textbf{SE-1 's原始陈述}：Spring 'sParameter \(\theta_t\) 通过with under 方式Update：
\[\theta_{t+1} = \theta_t - \alpha_t \cdot \nabla \mathcal{L}(\theta_t) + \xi_t\]

where
\(\mathbb{E}[\xi_t | \mathcal{F}_t] = 0\)，\(\text{Var}(\xi_t | \mathcal{F}_t) \leq \sigma^2\)。at 
Robbins-Monro conditionsunder ：
\[\sum_{t=1}^{\infty} \alpha_t = \infty, \quad \sum_{t=1}^{\infty} \alpha_t^2 < \infty\]

has  \(\theta_t \xrightarrow{a.s.} \theta^*\)（at 凸性Assumptionsunder ）。

\textbf{命题 3.2（Multi-Head Spring under 'sConvergenceconditions）}：

\begin{enumerate}
\def\labelenumi{(\arabic{enumi})}
\item
  \textbf{步长conditions（\(\alpha_t\) asymptotic behavior）non-变}：conditions
  \(\sum \alpha_t = \infty\) 和 \(\sum \alpha_t^2 < \infty\)
  only dependence于步长调度，non-dependence于Parameter数量。
\item
  \textbf{but has 效Convergencemust 求更小's步长}：
  \[\boxed{\alpha_t^{\text{MH}} \leq \frac{\alpha_t^{\text{single}}}{\sqrt{K}} \cdot \frac{\sigma^2_{\text{single}}}{\sigma^2_{\text{MH}}}}\]
\end{enumerate}

where
\(\sigma^2_{\text{MH}} = \sigma^2_{\text{single}} \cdot (1 + \gamma \cdot (K - 1))\)，\(\gamma \in [0, 1]\)
Yes头间梯度干扰系数。

\emph{Proof概must }：Multi-Head Spring
's梯度方差随头数增加。each 个头's注意力权重 \(\alpha_{ij}^{(k)}\)
for 共享Status原子 \(\{s_j\}\) 'sdependence引入了交叉头梯度项。for 于损失
\(\mathcal{L} = \mathbb{E}[\|\text{Spring}_{\text{MH}}(s_i) - s_i^*\|^2]\)：
\begin{align*}
\nabla_{\theta_k} \mathcal{L} &= \mathbb{E}\left[(\text{Spring}_{\text{MH}} - s_i^*)^T W^O_{:,k} \cdot \nabla_{\theta_k} \text{head}_k\right] \\
\text{Cov}(\nabla_{\theta_k} \mathcal{L}, \nabla_{\theta_l} \mathcal{L}) &\neq 0 \quad (\text{通过共享's } W^O \text{ 和 } s_i^*)
\end{align*}

梯度Noise'shas 效方差as ：
\[\sigma^2_{\text{MH}} = \frac{1}{K} \sum_{k=1}^{K} \text{Var}(\nabla_{\theta_k} \mathcal{L}) + \frac{1}{K^2} \sum_{k \neq l} \text{Cov}(\nabla_{\theta_k} \mathcal{L}, \nabla_{\theta_l} \mathcal{L})\]

Part一项Yes \(O(1)\)（each 个头's方差and 单头相同，but 除with  \(K\)），Part二项引入
\(O((K-1)/K) \approx O(1)\)
's额外方差（when 交叉协方差non-零时）。\(\square\)

\begin{enumerate}
\def\labelenumi{(\arabic{enumi})}
\setcounter{enumi}{2}
\tightlist
\item
  \textbf{ConvergenceObjectiveFrom全局Optimal退化as 驻点}：
\end{enumerate}

Multi-Head Spring 's损失景观具has \textbf{排列for 称性}（交换 head \(k\) 和
head \(l\) 产生相同'sFunction）。this 意味着has  \(K!\)
个etc.价's驻点。损失景观高度non-凸，Robbins-Monro only can GuaranteeConvergenceto 
\(\nabla \mathcal{L} \approx 0\)（驻点），non-can Guarantee全局Optimal。

\textbf{修改后'sConvergence定理（SE-1-MH）}：

at  Robbins-Monro 步长conditions和梯度 Lipschitz 连续性's前提under ：
\[\liminf_{t \to \infty} \mathbb{E}[\|\nabla \mathcal{L}(\theta_t)\|^2] \leq \frac{C \cdot \sigma^2_{\text{MH}} \cdot \bar{\alpha}}{\text{（and  } K \text{ No关's常数）}}\]

where \(\bar{\alpha} = \limsup_{t \to \infty} \alpha_t\)。when  \(K\) 增大时
\(\sigma^2_{\text{MH}}\)
增大，Convergence's残差梯度范数also 增大。\textbf{多头make Convergence变得更差（at has 限步长under ）}。

\subsubsection{3.4
过Parameter化conditions}\label{ux8fc7ux53c2ux6570ux5316ux6761ux4ef6}

\textbf{定义 5（has 效Sample量）}：Spring 动力学通过memory bank \(M_t\)
产生Training信号。设 \(T_{\text{eff}}\)
as each 个Status原子at Self-Evolution过程in 产生'shas 效''监督信号''数量（通过 Spring
重构误差提供）。then 总has 效Sample量as 
\(N_{\text{eff}} = N \cdot T_{\text{eff}}\)。

\textbf{命题 3.3（过Parameter化临界头数）}：

for 于\textbf{标准多头}（each 头降维 \(d_k = d_s/K\)):
\[|\theta_{\text{MH}}| = \underbrace{K \cdot (2d_k d_s + d_v d_s)}_{\text{Q, K, V 投影}} + \underbrace{K d_v d_s}_{W^O} = 4 d_s^2 \quad (\text{when  } d_k = d_v = d_s/K)\]

总Parameter\textbf{non-dependence \(K\)} ------
标准设计通过降维保持了Parameter恒定。at 此情况under ，Multi-Head Spring
\textbf{non-引入额外's过Parameter化风险}。

for 于\textbf{全维多头}（each 头
\(d_k = d_s\)，各头关注non-同physical子空间but can be 访问全维):
\[|\theta_{\text{MH}}| = d_s^2 \cdot (3K + 1)\]

\textbf{定理 3.2（过拟合临界conditions）}：
\[\boxed{K_{\text{crit}} = \max\left(1, \left\lfloor \frac{N \cdot T_{\text{eff}} / d_s^2 - 1}{3} \right\rfloor \right)}\]

when  \(K > K_{\text{crit}}\)
时，ModelParameter多于has 效Training信号，\textbf{泛化误差with probability \(\geq 1 - \delta\)
to 少as }：
\[\text{GenError}(K) \geq \Omega\left(\sqrt{\frac{d_s^2 \cdot (3K + 1)}{N \cdot T_{\text{eff}}}}\right)\]

（by 标准 Rademacher 复杂度界得出）。

\textbf{数值示例}（保守Assumptions): - \(N = 500\)
Status原子，\(d_s = 64\)，\(T_{\text{eff}} = 5\) -
\(K_{\text{crit}} = \lfloor (500 \cdot 5 / 4096 - 1) / 3 \rfloor = \lfloor (0.61 - 1) / 3 \rfloor = \max(1, -0.13) = 1\)

\textbf{Conclusion}：for 于适in 'sstate space，i.e.make Yes \(K = 2\)
个全维头also can be can already via 过Parameter化。for 于标准降维多头，\(K\)
can be with 更大（because as Parameter恒定）。

\subsubsection{3.5 for  SE-2（Doob
Martingale）'s影响}\label{ux5bf9-se-2doob-martingaleux7684ux5f71ux54cd}

SE-2
声称Parameter's后验Yes鞅：\(\mathbb{E}[\theta_{t+1} | \mathcal{F}_t] = \theta_t\)。

for 于 Multi-Head
Spring，\textbf{Martingale Propertyat 联合Parameter空间on 保持}（because as 它YesBayesian update's直接后果），but ：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{边际Martingale Propertycan be can 失效}：for 于单个头'sParameter
  \(\theta_t^{(k)}\)，\(\mathbb{E}[\theta_{t+1}^{(k)} | \mathcal{F}_t] \neq \theta_t^{(k)}\)（because as Update涉and which has 头's交叉信息）。
\item
  \textbf{鞅差序列's方差增大}：
  \[\text{Var}(\theta_{t+1} - \theta_t | \mathcal{F}_t) = O(K)\]

  this 意味着Convergence's集in 度降低。Azuma-Hoeffding give 出's置信区间宽度as 
  \(O(\sqrt{K \cdot t})\) while non- \(O(\sqrt{t})\)。
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4. 组合Analysis}\label{ux7ec4ux5408ux5206ux6790}

\subsubsection{4.1 联合形式化}\label{ux8054ux5408ux5f62ux5f0fux5316}

两个组件simultaneously添加时'sComplete流水线：

\[\boxed{s_i \xrightarrow{\text{PPE}} h_i = \phi(s_i) + \text{PE}(p_i) \xrightarrow{\text{MH-Spring}} \text{Spring}_{\text{MH}}(h_i, \mathcal{H})}\]

where \(\mathcal{H} = \{h_1, \ldots, h_N\}\) Yes编码后'sStatus原子集合。

\subsubsection{4.2 Theorem 1 's联合界 ---
交叉项Analysis}\label{theorem-1-ux7684ux8054ux5408ux754c-ux4ea4ux53c9ux9879ux5206ux6790}

\textbf{命题 4.1（联合Detection边际's分解）}：
\[\boxed{\Delta_s^{\text{combined}} = \Delta_s + \delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}}\]

where交叉项 \(\delta_s^{\text{cross}}\) 满足：
\[\delta_s^{\text{cross}} = \text{Cov}_{\text{experts}}\left(\text{Benefit}_{\text{PE}}, \text{Benefit}_{\text{MH}}\right)\]

i.e. PPE 's边际收益and  Multi-Head 's边际收益at 专家间's协方差。

\textbf{展开}： \begin{align*}
F_1^{\text{combined}} &\geq 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M_{\text{eff}} \cdot (\Delta_s + \delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2\right) \\
&= 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M_{\text{eff}} \cdot \left[\Delta_s^2 + 2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + (\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2\right]\right)
\end{align*}

\textbf{超加性 vs 次加性Analysis}：

令
\(A_s = \exp(-2M_{\text{eff}}\Delta_s^2)\)（原始误差），\(B_s^{\text{PE}}, B_s^{\text{MH}}, B_s^{\text{both}}\)
分别as 单独 PPE、单独 MH、两者结合's误差。then ：

\[\log \frac{B_s^{\text{both}}}{A_s} = -2M_{\text{eff}} \cdot [2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + (\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2]\]

\[\log \frac{B_s^{\text{PE}} \cdot B_s^{\text{MH}}}{A_s^2} = -2M_{\text{eff}} \cdot [2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}}) + (\delta_s^{\text{PE}})^2 + (\delta_s^{\text{MH}})^2]\]

两者之差give 出（at for 数空间in ):
\[\text{Interaction} = -2M_{\text{eff}} \cdot [2\Delta_s \delta_s^{\text{cross}} + 2\delta_s^{\text{PE}}\delta_s^{\text{MH}} + 2\delta_s^{\text{cross}}(\delta_s^{\text{PE}} + \delta_s^{\text{MH}}) + (\delta_s^{\text{cross}})^2]\]

\textbf{超加性conditions}（组合收益 \textgreater{}
单独收益之和):交叉项as 负（减少误差），i.e.：
\[\delta_s^{\text{cross}} \cdot (2\Delta_s + 2\delta_s^{\text{PE}} + 2\delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + 2\delta_s^{\text{PE}}\delta_s^{\text{MH}} < 0\]

when  \(\delta_s^{\text{PE}} > 0\) and 
\(\delta_s^{\text{MH}} > 0\)（两者all 改善Detection）and 
\(\delta_s^{\text{cross}} > 0\)（正toward 交互）时满足：
\[\boxed{\text{Superadditive when: } \delta_s^{\text{PE}} > 0, \delta_s^{\text{MH}} > 0, \delta_s^{\text{cross}} > 0}\]

\textbf{次加性conditions}（组合收益 \textless{}
单独收益之和):when 两者之一or 交叉项as 负。

\textbf{实际判断}：Multi-Head Spring can be with 利using  PPE
提供's空间局部性来实现更has 意义's注意力Mode（例such as ，头 \(k\) 关注physicalLocation
\(p_i\) 邻域内'sStatus原子）。this 种协同大概率Yes\textbf{正toward }'s，i.e.
\(\delta_s^{\text{cross}} > 0\)。but such as 果没has 明确'sphysical约束，多头can be can 学to 虚假's跨维相关性，此时
\(\delta_s^{\text{cross}}\) can be can Yes负's。

\subsubsection{4.3 Theorem 2
's联合界}\label{theorem-2-ux7684ux8054ux5408ux754c}

feature弱点Yes次加性地恶化's（because as each 个组件all can be can 引入额外's弱点):

\[\boxed{\delta_{\text{combined}} = \delta + \delta_{\text{PE}} + \delta_{\text{MH}} + \delta_{\text{cross}} \geq \max(\delta + \delta_{\text{PE}}, \delta + \delta_{\text{MH}})}\]

where： - \(\delta_{\text{PE}} = 2\varepsilon_{\text{PE}} / C_F^2\)（来自
§2.4) -
\(\delta_{\text{MH}} \propto \frac{d_s^2 \cdot (3K + 1)}{N \cdot T_{\text{eff}}}\)（来自过Parameter化，§3.4）
- \(\delta_{\text{cross}} \geq 0\)：PPE 'snon-完美编码 + Multi-Head
's过Parameter化产生复合效should 

because 此：
\[F_{1,\text{SCX}}^{\text{combined}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta_{\text{combined}}}{2}}\]

\textbf{最坏情况（两者all 很差）}：界比单独make using 任一组件更松。

\textbf{最好情况（完美 PPE + Parameter充足's
MH）}：\(\delta_{\text{PE}} = \delta_{\text{MH}} = 0\)，界回to 原始
Theorem 2。

\subsubsection{4.4 Cercis Score
's修改}\label{cercis-score-ux7684ux4feeux6539}

\textbf{推荐修改}：
\[\boxed{S'(s_i) = Q_{\text{MH}}(h_i) + \eta(t) \cdot N(s_i) - \lambda \cdot \mathcal{R}_{\text{diversity}}}\]

where：
\[Q_{\text{MH}}(h_i) = \frac{1}{K} \sum_{k=1}^{K} w_k \cdot Q(\text{head}_k(h_i))\]

\(w_k\) Yes头can be 靠性权重（can be 通过头输出and 多数投票's一致性来估计）。

正then 化项：
\[\mathcal{R}_{\text{diversity}} = (K - 1) \cdot \left(1 - \frac{1}{K} \sum_{k=1}^{K} \left\|\text{head}_k - \frac{1}{K}\sum_{j=1}^{K} \text{head}_j\right\|^2\right)\]

惩罚头之间's过度相似（鼓励多样性），防止退化to 单头。

\(\lambda > 0\) Yes正then 化强度。when  \(K = 1\) 时
\(\mathcal{R}_{\text{diversity}} = 0\)（自动退化as 原始形式）。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5. 诚实暴击（Honest
Critique）}\label{ux8bdaux5b9eux66b4ux51fbhonest-critique}

\subsubsection{5.1
数学Guaranteeis 削弱's地方}\label{ux6570ux5b66ux4fddux8bc1ux88abux524aux5f31ux7684ux5730ux65b9}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1622}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2432}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3514}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2432}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Location
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
原始Guarantee
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
削弱后'sStatus
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
严重程度
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{Theorem 1 + MH} & Chernoff bound on i.i.d. experts &
头输出相关，i.i.d. Assumptionsnon-成立。严格 bound requires must  \(\beta\)-mixing
conditions，while 该conditionscan be can non-满足 & \textbf{高} \\
\textbf{SE-1 + MH} & Robbins-Monro → 全局Optimal（凸性under ） &
高度non-凸损失景观（\(K!\) for 称驻点）→ only can Guarantee驻点，non-can Guarantee全局Optimal &
\textbf{高} \\
\textbf{Theorem 2 + MH} & 紧致 Fano Upper Bound &
过Parameter化时界仍然成立but 变得\textbf{过松}（vacuous），实际性can can be can 远差于界
& \textbf{in } \\
\textbf{Theorem 3 + Learning型 PPE} & 最坏情况non-can be 区分 & Learning型 PE
make 得两个世界can be 区分 → \textbf{定理 3 is 破坏}（but this Yes好事） &
\textbf{in （实际on has 益）} \\
\textbf{SE-2 + MH} & 联合Martingale Property保持 & 边际Martingale Propertycan be can 失效；鞅差方差
\(O(K)\) 增大，置信区间变宽 & \textbf{in } \\
\textbf{组合 Theorem 1} & 独立 Chernoff bound & has 效 \(M_{\text{eff}}\)
Yes启发式's，non-严格。交叉项 \(\delta_s^{\text{cross}}\) 难with FromTheoryUpper Bound定
& \textbf{高} \\
\end{longtable}

\subsubsection{5.2
complete No影响's地方（加了etc.于白加）}\label{ux5b8cux5168ux65e0ux5f71ux54cdux7684ux5730ux65b9ux52a0ux4e86ux7b49ux4e8eux767dux52a0}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.3529}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.6471}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
conditions
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
as 什么No效
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{\(I(Y; P \mid X) = 0\)（Locationnon-携带标签信息）} & PPE
Yes纯Noise。Theorem 2 's界non-变。Theorem 1 's \(\Delta_s\)
non-变。Parameter全部浪费 \\
\textbf{\(\rho_{\text{eff}} = 0\)（which has 头坍塌as 单一Mode）} & Multi-Head
etc.价于单头 + \(K\) 倍冗余Parameter。Theorem 1 No增益。SE-1 Convergenceto 相同点 \\
\textbf{固定、Information Theorycan be 逆's PE} & only Yes变量替换。KL 散度non-变 → Theorem 4
non-变。No实质贡献 \\
\textbf{\(K > K_{\text{crit}}\) and No正then 化} & 过度Parameter化 → 泛化误差主导
→ 任何定理's界all 变as 空集（vacuous） \\
\textbf{physicalDimensionnon-相关（例such as 头关注physicalDimension D，but  D and 任务No关）} &
该头'shas 效 \(\Delta_s = 0\) → 纯Noise贡献 → 降低has 效
\(M_{\text{eff}}\) \\
\end{longtable}

\subsubsection{5.3
特定conditionsunder 破坏定理's情况}\label{ux7279ux5b9aux6761ux4ef6ux4e0bux7834ux574fux5b9aux7406ux7684ux60c5ux51b5}

\textbf{Theorem 3 is 破坏'sconditions（5 个conditions，按严重程度排序）}：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Learning型 PPE（最严重）}：§2.5 already Analysis。TrainingDatain 's标签Noise导致
  \(\text{PE}_{W_A} \neq \text{PE}_{W_B}\)。Result：两个世界can be 区分，Theorem
  3 'snon-can be 区分性non-成立。

  \emph{评价}：\textbf{好事}。Theorem 3
  Yes最坏情况non-can be can 性Result。such as 果Location信息can 打破this 个non-can be can 性，Explanation PPE
  has using 。
\item
  \textbf{Multi-Head Spring in 'sDimension泄漏}：if 某个头is 设计as 关注physicalDimension
  A，but 实际on 通过 softmax 's全局注意力also can 访问Dimension B
  's信息，then 头's''专业化''Yes虚假's。this can be can 导致该头学to 标签相关's虚假相关性，改变
  \(P_{\text{clean}}\) 和 \(P_{\text{noisy}}\) 's估计。
\item
  \textbf{\(K > N\)（头数超过Status原子数）}：多头's注意力ModeNo法is Data唯一确定。存at No限多个Parameter配置产生相同's
  \(\text{Spring}_{\text{MH}}(s_i)\)。Theorem 3
  's构造dependence于DataDistribution's唯一性------when Parameter化Yes欠定's，两个世界'sDistributioncan be can has 细微Difference。
\item
  \textbf{PPE Dimensionand physicalDimensionfor 齐}：such as 果 \(\text{PE}(p_i)\) 'sDimensionand 
  Multi-Head Spring
  in 某头'sphysicalDimension\textbf{正交}（i.e.编码碰巧擦除了该头requires must 's信息），then 该头's注意力Modewill 产生System性偏差。this 引入了一个
  Theorem 3 构造in not yet 考虑's自by 度。
\item
  \textbf{\(\eta(t)\) 衰减过快 + Multi-Head}：such as 果Exploration权重 \(\eta(t)\)
  衰减过快，Multi-Head Spring can be can 过早地will 某些Mode''冻结''at 次优Status。at 
  Theorem 3
  's双世界构造in ，一个世界can be can 学to 更好'sMode（because as 标签更Clean），while 另一个世界学to 差'sMode------make 两个世界can be 区分。
\end{enumerate}

\subsubsection{5.4
最诚实'sSummary}\label{ux6700ux8bdaux5b9eux7684ux603bux7ed3}

\textbf{PPE 's底线}： - \textbf{for  Theorem 1}：such as 果physicalLocationand 任务相关 →
has 正toward 贡献（\(\delta_s^{\text{PE}} > 0\)）。such as 果No关 →
没has 影响。Chernoff Structure保持。 - \textbf{for  Theorem 2}：non-完美's编码with 
\(\sqrt{\delta + O(\varepsilon_{\text{PE}})}\)
扩大Upper Bound。完美编码non-影响Upper Bound。 - \textbf{for  Theorem
3}：固定编码保持non-can be 区分性。Learning型编码\textbf{破坏}Theorem
3------but this Yes\textbf{进展}while non-退步。 - \textbf{for  Theorem
4}：non-减少误差指数。has using 时增加指数。

\textbf{Multi-Head Spring 's底线}： - \textbf{for  Theorem
1}：头\textbf{non-Yes}独立专家。严格界比 naive \(M \times K\)
松得多。头相关性'sComplete处理极its Difficulty。 - \textbf{for 
SE-1}：Convergenceto 驻点（non-Guarantee全局Optimal）。has 效步长on 限with  \(O(1/\sqrt{K})\)
缩小。Convergence更慢。 -
\textbf{过Parameter化}：for 于典型'sstate space大小，\(K_{\text{crit}}\) can be can only has 
1-3 个头。超过此数's头will 产生泛化惩罚。

\textbf{组合's最诚实评价}： - PPE
Yes\textbf{更has 价值's}组件------它at Theoryon 更清晰，影响更can be 预测，and 风险更低
- Multi-Head Spring 's\textbf{Theory风险更高}------它破坏了 Theorem 1
Optimal雅's部分（i.i.d. 专家's Chernoff bound) -
两者's\textbf{交叉项at Theoryon Yes正's}（can be can 产生超加性收益），but \textbf{at 实际in can be can as 负}（额外's复杂性引入新's失败Mode）

\textbf{一句话Summary}：PPE
is a安全's赌注，数学on 几乎纯粹has 益（除了Learning型 PPE 破坏 Theorem
3，but that Yes好事）。Multi-Head Spring
is a高风险赌注------它at 数学on 削弱了 Theorem 1 's严格性，make  SE-1
Convergence性变差，and 很has can be can 过Parameter化。组合时，PPE 's收益can be can is  Multi-Head
's复杂性部分抵消。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{6. 开放Problem}\label{ux5f00ux653eux95eeux9898}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{严格's多头 Chernoff 界}：can Noat 头'sdependenceStructureon 建立
  \(\beta\)-mixing conditions，make 得 Theorem 1 's指数界can be with 严格extensionto 多头情况？
\item
  \textbf{PPE 'sOptimal频率}：at 正弦编码in ，频率Parameter
  \(\omega_j = 10000^{-2j/d}\)
  Yesas Nature语言设计's。physicalLocation'sOptimal频率谱Yes什么？can NoFromphysicalfor 称性推导？
\item
  \textbf{头数 \(K\) 's自适should Selection}：can NoFromDatain LearningOptimal's \(K\)（类似于
  Bayesian non-parametric in 'sin 餐馆过程），at 过Parameter化之前停止？
\item
  \textbf{Theorem 3
  's修改}：such as 何at Theorem Statementin 加入''前提conditions:non-make using Datadependence'sLocation编码''来保持its 数学Complete性？
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{References（框架内部）}\label{ux53c2ux8003ux6587ux732eux6846ux67b6ux5185ux90e8}

\begin{itemize}
\tightlist
\item
  SCX Project (2026). Theorem 1-4: Multi-expert consistency, weak
  feature limits, unidentifiability, and minimax optimality.
  \texttt{theory/theorems/}
\item
  SCX Project (2026). Spring Dynamics, SE-1 (Robbins-Monro convergence),
  SE-2 (Doob martingale). \texttt{theory/self\_evolution/}
\item
  SCX Project (2026). State Crystallization: physical-driven
  discretization of continuous observables.
  \texttt{theory/propositions/}
\item
  Wainwright, M. (2019). \emph{High-Dimensional Statistics: A
  Non-Asymptotic Viewpoint}. Cambridge. （Chernoff-Hoeffding-Stein
  's标准参考）
\item
  Boucheron, S., Lugosi, G., \& Massart, P. (2013). \emph{Concentration
  Inequalities}. Oxford. （dependence变量'sconcentration inequalities）
\item
  Vaswani, A., et al.~(2017). Attention Is All You Need. \emph{NeurIPS}.
  （多头注意力's原始Formula）
\item
  Su, J., et al.~(2024). RoFormer: Enhanced Transformer with Rotary
  Position Embedding. \emph{Neurocomputing}. （RoPE 's数学基础）
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{AnalysisDate：2026-06-29}\\
\emph{作者备注：which has 定理编号指 SCX
框架内部定理，non-本文序号。本Analysis's数学处理at can be 处理's范围内Yes严格's；already 明确标注which has 近似和启发式's部分。诚实'snon-can be 处理性（such as dependence头's
Chernoff bound）already is 诚实记录。}

\end{document}
