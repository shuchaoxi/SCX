% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Multi-Head Spring and Physical Positional Encoding
: A Rigorous Mathematical Analysis}\label{multi-head-spring-ux4e0e-physical-positional-encoding-ux7684ux4e25ux683cux6570ux5b66ux5206ux6790}

\subsection{On the SCX
Framework Theorem Foundations: An Honest Audit}\label{ux5bf9-scx-ux6846ux67b6ux5b9aux7406ux57faux7840ux7684ux8bdaux5b9eux5ba1ux67e5}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: Complete Analysis\\
\textbf{dependence}: Theorem 1--4 (theory/theorems/), SE-1, SE-2
(theory/self\_evolution/)\\
\textbf{Methodology}: Rigorous Formal Derivation + Honest Critique (No Euphemisms)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{0.
Notation Conventions and Framework Reconstruction}\label{ux7b26ux53f7ux7ea6ux5b9aux4e0eux6846ux67b6ux91cdux6784}

Before entering the analysis, we first precisely reconstruct the mathematical foundations of the SCX framework. All definitions below are rigorously formalized from the
prompt theorem descriptions.

\subsubsection{0.1 State Crystallization (State
Crystallization)}\label{ux72b6ux6001ux7ed3ux6676state-crystallization}

Let the raw physical observation space be
\(\mathcal{X} \subset \mathbb{R}^{d_{\text{raw}}}\)(continuous physical quantities: PBE
energy surfaces, force fields, electron densities, etc.). State Crystallization is a mapping:

\[\mathcal{C}: \mathcal{X} \to \mathcal{S} = \{s_1, \ldots, s_N\}\]

where each \(s_i \in \mathbb{R}^{d_s}\)
is a\textbf{discrete state atom}'s vector representation. Unlike BPE
which is statistically driven,\(\mathcal{C}\) is physically driven: it clusters according to the natural curvature boundaries of the PBE
energy surface. Formally:

\[s_i = \mathcal{C}(x) \quad \text{where} \quad x \in \mathcal{X} \text{ and  } \nabla^2 E_{\text{PBE}}(x) \text{ at  } x \text{ has one 谱间隙}\]

\subsubsection{0.2 Yajie Audit (Yajie
Audit)}\label{ux96c5ux6d01ux5ba1ux8ba1yajie-audit}

Let there be \(M\) independent expert Model \(\{E_1, \ldots, E_M\}\). For a state atom \(s_i\)
and its label \(y_i\), define:

\[c_m(s_i) = \mathbf{1}[E_m(s_i) \neq y_i] \quad \text{(expert m disagrees with the given label)}\]

Consistency count:
\[C(s_i) = \sum_{m=1}^{M} c_m(s_i) \sim \text{Binomial}(M, p_{s_i})\]

where \(p_{s_i}\)
is the probability that experts''disagree with''the label. For clean samples,\(p_{s_i} = p_{\text{clean}} < 0.5\); For noisy samples,\(p_{s_i} = p_{\text{noisy}} > 0.5\). 

\textbf{Cercis Score}: \[S(s_i) = Q(s_i) + \eta(t) \cdot N(s_i)\]

where \(Q(s_i)\) is the base quality score (based on multi-expert consistency),\(N(s_i)\)
is the novelty bonus,\(\eta(t)\) is the time-decaying exploration weight.

\subsubsection{0.3 Spring
Self-Evolution Dynamics}\label{spring-ux81eaux8fdbux5316ux52a8ux529bux5b66}

State transition: \[(S_t, \theta_t, M_t) \to (S_{t+1}, \theta_{t+1}, M_{t+1})\]

Spring 注形formula (单头):
\[\text{Spring}(s_i, \mathcal{S}) = \text{softmax}\left(\frac{(Q s_i)(K \mathcal{S})^T}{\sqrt{d_k}}\right) \cdot (V \mathcal{S})\]

where
\(Q, K \in \mathbb{R}^{d_k \times d_s}\), \(V \in \mathbb{R}^{d_v \times d_s}\). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.
Core Theorems'sprecise Theorem Statement}\label{ux6838ux5fc3ux5b9aux7406ux7684ux7cbeux786eux5b9aux7406ux9648ux8ff0}

\subsubsection{Theorem
1(Expert consensusNoiseDetection)}\label{theorem-1ux591aux4e13ux5bb6ux4e00ux81f4ux6027ux566aux58f0ux68c0ux6d4b}

\[\boxed{F_1 \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M \Delta_s^2\right)}\]

where:  -
\(\rho_s = |\{i: \mathcal{C}(x_i) = s\}| / N_{\text{total}}\): Statusfactor 
\(s\) 's -
\(\Delta_s = |p_{\text{clean},s} - p_{\text{noisy},s}|\): Status \(s\)
'sDetection边际 - \(M\): independent expert number quantity  - \(\eta > 0\): one ization because factor 

\textbf{Proof技术}: Chernoff bound + Hoeffding non-etc.formula . for each Status
\(s\), \(C(s)\) YesDistribution's混. target as Noisewhen 
\(C(s) \geq \tau_s\). Chernoff give : 
\[P(\text{假阳property }) = P_{H_0}(C(s) \geq \tau_s) \leq \exp(-2M(\tau_s/M - p_{\text{clean},s})^2)\]
\[P(\text{假阴property }) = P_{H_1}(C(s) < \tau_s) \leq \exp(-2M(p_{\text{noisy},s} - \tau_s/M)^2)\]

选取 \(\tau_s = M \cdot (p_{\text{clean},s} + p_{\text{noisy},s})/2\)
make 项平衡, to  \(\Delta_s\). 

\subsubsection{Theorem
2(弱feature必然失效)}\label{theorem-2ux5f31ux7279ux5f81ux5fc5ux7136ux5931ux6548}

\[\boxed{F_{1,\text{SCX}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}}\]

where \(\delta > 0\) 刻画featurespace 's信息non-足程. 

\textbf{Proof技术}: Fano non-etc.formula . definition 
\(\delta = \min_{f} \mathbb{E}[\mathbf{1}[f(X) \neq Y]] - \text{Bayes error}\), i.e.Optimalcan be linesclass device and 贝叶斯Optimal间'sminimum 差距. Fano
give conditionsand Errorrate 'srelationship : 
\[P_e \geq \frac{H(Y|X) - 1}{\log |\mathcal{Y}|}\]

when  \(H(Y|X)\) 较(feature弱), any Method(including  SCX)'sErrorrate lower boundby 
\(\delta\) 决. 

\subsubsection{Theorem
3(Noiseand DifficultySample'snon-can be property )}\label{theorem-3ux566aux58f0ux4e0eux56f0ux96beux6837ux672cux7684ux4e0dux53efux533aux5206ux6027}

\textbf{construct property 双世bound Proof}: at bound 
\(W_A, W_B\), has 观测etc.价'sDataDistribution
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\), but : 

\begin{itemize}
\tightlist
\item
  bound  \(W_A\): Sample \((x, y)\) Yesis Errortarget 记's(target Noise)
\item
  bound  \(W_B\): Sample \((x, y)\) Correcttarget but on Difficulty(贝叶斯non-determine property )
\end{itemize}

has Algorithmcan only through 观测Datathis bound . 

形formula ization : \(\forall\) 决策规then 
\(d: (\mathcal{X} \times \mathcal{Y})^n \to \{W_A, W_B\}\), 
\[\max_{W \in \{W_A, W_B\}} P_W(d(\text{data}) \neq W) \geq \frac{1}{2} - \frac{1}{2}\|P_{W_A}^{\otimes n} - P_{W_B}^{\otimes n}\|_{\text{TV}} = \frac{1}{2}\]

because as 
\(P_{W_A}^{\otimes n} = P_{W_B}^{\otimes n}\)(complete 's观测Distribution). 

\subsubsection{Theorem 4(precise number  Minimax
Optimal)}\label{theorem-4ux7cbeux786eux5e38ux6570-minimax-ux6700ux4f18}

SCX 'sNoiseDetectionErrorrate at with under on to precise number Optimal: 

\[\lim_{M \to \infty} -\frac{1}{M} \log P_{\text{FN}}^{(M)} = D_{\text{KL}}(P_{\text{noisy}} \| P_{\text{clean}}) \quad \text{subject to } P_{\text{FP}}^{(M)} \leq \alpha\]

and  Bahadur-Rao give 's \(O(1/\sqrt{M})\) 修正项also matching minimax lower bound. 

\textbf{Proof技术}: Chernoff-Stein lemma  + Bahadur-Rao . 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2. item  1: Physical Positional
Encoding(PPE)'snumber learning Analysis}\label{ux7ec4ux4ef6-1physical-positional-encodingppeux7684ux6570ux5b66ux5206ux6790}

\subsubsection{2.1 formula ization definition }\label{ux5f62ux5f0fux5316ux5b9aux4e49}

设each Statusfactor  \(s_i\) has one physicalLocation \(p_i \in \mathcal{P}\), where: 
- 蛋白质order sequence : \(\mathcal{P} = \mathbb{N}\)(残基索引) - 3D
Structure: \(\mathcal{P} = \mathbb{R}^3\)(原factor target ) -
Docking: \(\mathcal{P} = \mathbb{R}^3\)(结口袋坐target )

\textbf{definition  1(Physical Positional Encoding)}: PPE is a映射
\[\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\]

编code 'sStatus表示:  \[\boxed{h_i = \phi(s_i) + \text{PE}(p_i)}\]

where \(\phi: \mathbb{R}^{d_s} \to \mathbb{R}^{d}\)
Yesfeature映射, and must  \(d_{\text{pe}} = d\). 

\textbf{can be 选案(拼接+投影)}: 
\[h_i = W \cdot [\phi(s_i); \text{PE}(p_i)]\]

where
\(W \in \mathbb{R}^{d \times (d + d_{\text{pe}})}\). number learning on this Yesone 般's案, but method 案更简洁and at 
\(W = [I_d, I_d]\) ization as method . with under Analysiswith method as , Resultcan be generalize . 

\subsubsection{2.2
编code Function'sconcrete formula }\label{ux7f16ux7801ux51fdux6570ux7684ux5177ux4f53ux5f62ux5f0f}

\textbf{target quantity Location's正弦编code }(class  Transformer):
\[\text{PE}(p, 2j) = \sin\left(\frac{p}{10000^{2j/d}}\right), \quad \text{PE}(p, 2j+1) = \cos\left(\frac{p}{10000^{2j/d}}\right)\]

where \(j = 0, 1, \ldots, d/2 - 1\). 

\textbf{3D Location's旋转编code }(class  RoPE, 适配physicalspace ):
\[\text{PE}_{\text{rot}}(\mathbf{p}) = \mathbf{R}(\mathbf{p}) \cdot \mathbf{e}_0\]

where \(\mathbf{R}(\mathbf{p}) \in SO(d)\) Yesby target Parameterization 's旋转矩matrix . for 
\(\mathbf{p} = (x, y, z)\): 
\[\mathbf{R}(\mathbf{p}) = \mathbf{R}_x(\omega_x x) \cdot \mathbf{R}_y(\omega_y y) \cdot \mathbf{R}_z(\omega_z z)\]

where \(\mathbf{R}_\alpha(\theta)\) Yesat  \(\alpha\) on 's \(d\)
维旋转, \(\omega_x, \omega_y, \omega_z\) Yescan be Learningor fixed 'srate Parameter. 

\textbf{key property 质}: and  NLP
'sLocationcode non-, physicalLocationhas \textbf{维几何Structure}. one 良's PPE
should 尊重physicalfor property : 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{平移etc.变property }(for  3D
  Structure):\(\text{PE}(\mathbf{p} + \mathbf{t})\) and 
  \(\text{PE}(\mathbf{p})\) has can be 预测'srelationship 
\item
  \textbf{旋转etc.变property }: \(\text{PE}(\mathbf{R}\mathbf{p})\)
  can be with through 旋转编code space restore 
\item
  \textbf{Lipschitz
  continuous property }: \(\|\text{PE}(\mathbf{p}) - \text{PE}(\mathbf{q})\| \leq L_{\text{PE}} \cdot \|\mathbf{p} - \mathbf{q}\|\)
\end{enumerate}

\subsubsection{2.3 for  Theorem 1
'sinfluence }\label{ux5bf9-theorem-1-ux7684ux5f71ux54cd}

\textbf{core Problem}: PPE such as change Detection边际 \(\Delta_s\)？

\textbf{命题 2.1(PPE under 's修改Detection边际)}:  at  PPE
增强'sfeaturespace in , Status \(s\) 'shas Detection边际as : 
\[\boxed{\Delta_s^{\text{PPE}} = \Delta_s + \delta_s^{\text{PE}}}\]

where
\(\delta_s^{\text{PE}} \in [-\Delta_s, 1 - p_{\text{clean},s} - \Delta_s]\)
satisfies : 
\[\delta_s^{\text{PE}} = \underbrace{(p_{\text{clean},s}^{\text{PPE}} - p_{\text{clean},s})}_{\text{CleanSampleon 'sone property 增益}} - \underbrace{(p_{\text{noisy},s}^{\text{PPE}} - p_{\text{noisy},s})}_{\text{NoiseSampleon 's混淆增益}}\]

\emph{Proof}: at 增强space in , expert 's预测概rate as 
\(p_{\text{clean},s}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) = y_i] | H_0]\)
and 
\(p_{\text{noisy},s}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) \neq y_i] | H_1]\). Detection边际definition as 
\(\Delta_s^{\text{PPE}} = |p_{\text{clean},s}^{\text{PPE}} - (1 - p_{\text{noisy},s}^{\text{PPE}})| = |p_{\text{clean},s}^{\text{PPE}} + p_{\text{noisy},s}^{\text{PPE}} - 1|\). i.e.. \(\square\)

\textbf{theorem  2.1(PPE under 's F1 lower bound)}: 
\[\boxed{F_1^{\text{PPE}} \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M (\Delta_s + \delta_s^{\text{PE}})^2\right)}\]

Chernoff-Hoeffding non-etc.formula 's\textbf{has property non-变}(指示变quantity at  \([0,1]\)
内has bound ), but number ization . 

when  \(\delta_s^{\text{PE}} > 0\) for which has  \(s\)(PPE Yes帮助Detection):
\[F_1^{\text{PPE}} - F_1 \geq \frac{1}{\eta} \sum_s \rho_s \left[\exp(-2M\Delta_s^2) - \exp(-2M(\Delta_s + \delta_s^{\text{PE}})^2)\right] \geq 0\]

\textbf{命题 2.2(space local property then ization )}: if  PE satisfies  Lipschitz
conditions(Lipschitz number as  \(L_{\text{PE}}\)), and expert Model \(E_m\)
for its 输入satisfies  \(L_E\)-Lipschitz, then for Arbitraryhas physicalLocation \(p_i, p_j\)
'sStatusfactor : 
\[|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| \leq 2L_E \cdot L_{\text{PE}} \cdot \|p_i - p_j\|\]

\emph{Proof}: Expert consensusrate 'sization 受输入变ization 约束:  \begin{align*}
|p_{\text{clean},i}^{\text{PPE}} - p_{\text{clean},j}^{\text{PPE}}|
&\leq \mathbb{E}_{E} \left|\mathbf{1}[E(h_i) = y] - \mathbf{1}[E(h_j) = y]\right| \\
&\leq \mathbb{E}_{E} \left[ L_E \cdot \|h_i - h_j\| \right] \\
&= L_E \cdot \|\text{PE}(p_i) - \text{PE}(p_j)\| \quad (\text{because as  } s_i = s_j = s) \\
&\leq L_E \cdot L_{\text{PE}} \cdot \|p_i - p_j\|
\end{align*}

样for  \(p_{\text{noisy}}\) 立. 者'sto because factor  2. \(\square\)

this means \textbf{physicalon 邻近'sStatusfactor has similar 'sDetection难}------one can be validate 's预测. 

\subsubsection{2.4 for  Theorem 2
'sinfluence }\label{ux5bf9-theorem-2-ux7684ux5f71ux54cd}

\textbf{core Problem}: non-完美's PPE such as change featurepoint 'sbound ？

\textbf{definition  2(编code non-完美 \(\varepsilon_{\text{PE}}\))}: 
\[\boxed{\varepsilon_{\text{PE}} = I(Y; P | X) - I(Y; \text{PE}(P) | X) \geq 0}\]

where \(I(Y; P | X)\) YesphysicalLocation \(P\) at give 原始feature \(X\) under regarding target 签
\(Y\) 'sconditions互信息, while  \(I(Y; \text{PE}(P) | X)\)
Yescode 保留's信息. \(\varepsilon_{\text{PE}}\)
quantity ization \textbf{Location信息at code in 's丢失}. 

\textbf{theorem  2.2(non-完美 PPE under 's失效Upper Bound)}: 
\[\boxed{F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}}\]

\emph{Proof}: Fano non-etc.formula give 
\(P_e^{\text{PPE}} \geq \frac{H(Y|X, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\). has : 
\begin{align*}
H(Y|X, \text{PE}(P)) &= H(Y|X) - I(Y; \text{PE}(P) | X) \\
&= H(Y|X) - [I(Y; P | X) - \varepsilon_{\text{PE}}] \\
&\geq H(Y|X) - I(Y; P | X) + \varepsilon_{\text{PE}}
\end{align*}

while  \(\delta\) 'sdefinition give 
\(H(Y|X) - 1 \geq (\delta/2) \cdot \log|\mathcal{Y}|\)(through  Fano
'stoward ). because 此: 
\[H(Y|X, \text{PE}(P)) - 1 \geq \left(\frac{\delta}{2} + \frac{\varepsilon_{\text{PE}}}{C_F}\right) \cdot \log|\mathcal{Y}|\]

代入 SCX 's F1 Upper Bound表达formula i.e.. \(\square\)

\textbf{紧致property Analysis}:  - when 
\(\varepsilon_{\text{PE}} = 0\)(完美编code ):Upper Boundrestore as 原始 Theorem 2 -
when  \(\varepsilon_{\text{PE}} > 0\): Upper Boundwith 
\(\sqrt{\delta + O(\varepsilon_{\text{PE}})}\) 'sRate放松 - when 
\(\varepsilon_{\text{PE}} \to I(Y; P | X)\)(complete 失败'scode , 丢失which has Location信息):Upper Boundto complete not yet make using Location信息's况

\textbf{Corollaries 2.1(Location信息'svalue )}: such as physicalLocation \(P\)
身non-携带any regarding target  \(Y\) 's信息(i.e. \(I(Y; P | X) = 0\)), then 
\(\varepsilon_{\text{PE}} = 0\)(because as has 信息can be 丢失), Theorem 2
'sUpper Bound\textbf{complete non-变}. PPE at this under Yes\textbf{纯粹'sParameter浪费}. 

\subsubsection{2.5 for  Theorem 3
'sinfluence }\label{ux5bf9-theorem-3-ux7684ux5f71ux54cd}

this Yeskey 'sAnalysis. Theorem 3 'snon-can be property dependenceconstruct bound  \(W_A, W_B\)
具has complete 's观测Distribution \(P(X, Y)\). 

\textbf{命题 2.3(fixed  PPE maintain  Theorem 3)}: such as  PE
is a\textbf{先验fixed }'sFunction(non-dependenceTrainingDataor target 签), then bound in 's增强featureDistribution依然: 
\[P_{W_A}(X, \text{PE}(P), Y) = P_{W_B}(X, \text{PE}(P), Y)\]

because  Theorem 3 'snon-can be property \textbf{maintain non-变}. PPE
has 帮助also has 损害最坏况'scan be 识别property . 

\emph{Proof}: physicalLocation \(P\) by physicalStructure决, independent target 签程. because 此
\(P_{W_A}(P|X) = P_{W_B}(P|X)\). because as  PE
Yesfixed 'sdetermine property Function, \(P_{W_A}(\text{PE}(P)|X) = P_{W_B}(\text{PE}(P)|X)\). 观测Distributioncomplete . \(\square\)

\textbf{命题 2.4(Learningtype  PPE break  Theorem 3)}: such as  PE 'sParameter
\(\theta_{\text{PE}}\) YesFromDatain Learning's(including target 签
\(Y\)), then at bound in Learningto non-'scode : 
\[\text{PE}_{W_A} \neq \text{PE}_{W_B}\]

because as  \(W_A\) 'sNoisetarget influence Learning程. 此: 
\[P_{W_A}(X, \text{PE}_{W_A}(P), Y) \neq P_{W_B}(X, \text{PE}_{W_B}(P), Y)\]

世bound \textbf{can be 区}. Theorem 3 is break . 

\textbf{诚评价}: this on Yes\textbf{消息}. Theorem 3
is anon-can be can property Result------at 最坏况under , Noiseand DifficultySampleNomethod 区. such as Learningtype 
PPE can break theorem  3
's前提conditions, means \textbf{Location信息provide Noiseand DifficultySample'scan }. this non-Yesframework 's缺陷, while Yesframework 's步. but this means 
Theorem 3 non-again Noconditions立------requires must 添前提conditions''PE
Yesfixed 'sor Location信息non-携带target correlated 信息''. 

\subsubsection{2.6 for  Theorem 4
'sinfluence }\label{ux5bf9-theorem-4-ux7684ux5f71ux54cd}

\textbf{命题 2.5(PPE non-decrease Optimalerror number )}: 
\[\boxed{D_{\text{KL}}(P_{\text{noisy}}^{\text{PPE}} \| P_{\text{clean}}^{\text{PPE}}) = D_{\text{KL}}(P_{\text{noisy}} \| P_{\text{clean}}) + \Delta D_{\text{KL}}^{\text{PE}}}\]

where \(\Delta D_{\text{KL}}^{\text{PE}} \geq 0\)(non-负, by  KL
散'sData Processingnon-etc.formula Guarantee). 

\emph{Proof}: by  KL 散'sformula method then :  \begin{align*}
D_{\text{KL}}(P_{\text{noisy}}^{\text{PPE}} \| P_{\text{clean}}^{\text{PPE}})
&= D_{\text{KL}}(P_{\text{noisy}}(X, H) \| P_{\text{clean}}(X, H)) \\
&= D_{\text{KL}}(P_{\text{noisy}}(X) \| P_{\text{clean}}(X)) + \mathbb{E}_{X \sim P_{\text{noisy}}}[D_{\text{KL}}(P_{\text{noisy}}(H|X) \| P_{\text{clean}}(H|X))]
\end{align*}

because as  \(H = \phi(X) + \text{PE}(P)\), and  PE
Yesfixed 'sdetermine property Function, 增强non-will decrease  KL 散(信息process non-etc.formula ). concrete : 
\[\Delta D_{\text{KL}}^{\text{PE}} = \mathbb{E}_{X \sim P_{\text{noisy}}}[D_{\text{KL}}(P_{\text{noisy}}(\text{PE}(P)|X) \| P_{\text{clean}}(\text{PE}(P)|X))] \geq 0\]

\(\square\)

PPE
\textbf{Fromnon-reduce }Optimalerror number . such as Location信息and target correlated , \(\Delta D_{\text{KL}}^{\text{PE}} > 0\), error number \textbf{严格更}(Detectionaccuracy ). such as Location信息No, \(\Delta D_{\text{KL}}^{\text{PE}} = 0\), non-变. 

Bahadur-Rao number because  PE introduce 's差项while 修改, but Yes朝更紧'stoward (when  PE
has using ). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3. item  2: Multi-Head Spring
'snumber learning Analysis}\label{ux7ec4ux4ef6-2multi-head-spring-ux7684ux6570ux5b66ux5206ux6790}

\subsubsection{3.1 formula ization definition }\label{ux5f62ux5f0fux5316ux5b9aux4e49-1}

\textbf{definition  3(Multi-Head Spring)}: 
\[\boxed{\text{Spring}_{\text{MH}}(s_i, \mathcal{S}) = \text{Concat}(\text{head}_1, \ldots, \text{head}_K) \cdot W^O}\]

where: 
\[\text{head}_k = \text{Spring}_k(s_i, \mathcal{S}) = \text{softmax}\left(\frac{(Q_k s_i)(K_k \mathcal{S})^T}{\sqrt{d_k}}\right) \cdot (V_k \mathcal{S})\]

Parameter:  - \(Q_k, K_k \in \mathbb{R}^{d_k \times d_s}\): Part \(k\)
头's查询/键投影 - \(V_k \in \mathbb{R}^{d_v \times d_s}\): Part \(k\)
头'svalue 投影 - \(W^O \in \mathbb{R}^{K d_v \times d_s}\): 输投影 -
\(d_k = d_s / K\)(standard 头's降维约), or 
\(d_k = d_s\)(全维头, each 头操Completestate space)

each  \(k\) is design as one non-'sphysicalDimension:  -  1: 键角Mode ---
注Statusfactor 间'srelationship  -  2: 键长Mode --- Statusfactor 间's距离relationship  -
头 3: 配位环境 --- Statusfactor 'slocal 邻域拓扑 -  4: can quantity 梯/场Mode
--- Statusfactor 'scan quantity rate 

\textbf{physicalDimension约束(Structureization 头)}: as implement 真正'sization , each 头's
\(Q_k, K_k\) should is 约束as only using Statustoward quantity 'sfactor space . if 
\(s_i = [s_i^{(1)}; s_i^{(2)}; s_i^{(3)}; s_i^{(4)}]\)
按physicalDimension块, then : 

\[Q_k = [\mathbf{0}_{d_k \times d_{<k}} \;|\; Q_k^{(k)} \;|\; \mathbf{0}_{d_k \times d_{>k}}]\]

where \(Q_k^{(k)}\) only using Part \(k\)
physicalDimension'sfactor space , its as 零. this \textbf{强}ization . 

\subsubsection{\texorpdfstring{3.2 for  Theorem 1 'sinfluence  --- has expert number 
\(M\)}{3.2 for  Theorem 1 'sinfluence  --- has expert number  M}}\label{ux5bf9-theorem-1-ux7684ux5f71ux54cd-ux6709ux6548ux4e13ux5bb6ux6570-m}

\textbf{core Problem}: \(K\) YesNoetc.价 \(K\) independent expert ？

\textbf{答案: No. } this is akey 's区别. 

\textbf{命题 3.1(头non-Yesindependent expert )}: Multi-Head Spring
's头输at give Statusfactor set Yes\textbf{conditionsdependence}'s: 
\[\text{Cov}(\text{head}_k(s_i), \text{head}_l(s_i) \mid \mathcal{S}) \neq 0 \quad \text{(one 般况)}\]

\emph{Proof}: which has 头共享's输入Statusfactor  \(\mathcal{S}\) and 's
softmax 注池ization Structure. i.e.make 投影矩matrix  \(Q_k, K_k\) non-, 注权重
\(\alpha_{ij}^{(k)} = \text{softmax}_j((Q_k s_i)^T (K_k s_j) / \sqrt{d_k})\)
through 共's \(s_j\) value 耦. because 此: 
\[\text{head}_k(s_i) = \sum_j \alpha_{ij}^{(k)} \cdot V_k s_j\]
\[\text{head}_l(s_i) = \sum_j \alpha_{ij}^{(l)} \cdot V_l s_j\]

者's协差through 共享's \(\{s_j\}\) non-零. \(\square\)

\textbf{definition  4(has expert property  \(\rho_{\text{eff}}\))}: 
\[\boxed{\rho_{\text{eff}} = 1 - \frac{2}{K(K-1)} \sum_{k < l} |\text{Corr}(\text{head}_k, \text{head}_l)|}\]

where
\(\text{Corr}(\text{head}_k, \text{head}_l) = \frac{\text{Cov}(\text{head}_k, \text{head}_l)}{\sqrt{\text{Var}(\text{head}_k) \cdot \text{Var}(\text{head}_l)}}\)(逐元素平all correlated property ). 

\begin{itemize}
\tightlist
\item
  \(\rho_{\text{eff}} = 1\): which has complete independent (想况)
\item
  \(\rho_{\text{eff}} = 0\): which has complete correlated (退ization as 单头)
\item
  \(\rho_{\text{eff}} < 0\): 头间存at correlated (for property property )
\end{itemize}

\textbf{has expert number 'sformula 修正}: 
\[M_{\text{eff}} = M \cdot (1 + \alpha \cdot \rho_{\text{eff}} \cdot (K - 1))\]

where \(\alpha \in [0, 1]\) Yes''头样property  \(\to\) expert property '''s转换系number . 

\textbf{but this non-is a严格'sbound }. 严格's Chernoff bound
requires must process dependence随变quantity . 

\textbf{theorem  3.1(Multi-Head Spring under 's严格 F1 lower bound)}: 

设 \(A_k(s)\) as Part \(k\) for Status \(s\)
's''伪one property number ''(through 头输and target 签'sone property ), then make using 
union-Chernoff Method: 

\[\boxed{F_1^{\text{MH}} \geq 1 - \frac{1}{\eta} \sum_{s} \rho_s \cdot \min_{\mathcal{I} \subseteq [K]} \exp\left(-2M \cdot \tilde{\Delta}_s^2(\mathcal{I})\right)}\]

where \(\tilde{\Delta}_s(\mathcal{I})\) Yesat factor set  \(\mathcal{I}\)
on 'shas 效边际, \(\mathcal{I}\) 选取one maximum independent factor set (satisfies 某
\(\beta\)-混conditions). 

at 最坏况under (which has complete correlated , \(\rho_{\text{eff}} = 0\)):
\[F_1^{\text{MH}} = F_1^{\text{single}} \quad \text{(Multi-Head No增益)}\]

at 最况under (which has complete independent , \(\rho_{\text{eff}} = 1\), and 
\(\alpha = 1\)):
\[F_1^{\text{MH}} \geq 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M K \tilde{\Delta}_s^2\right) \quad \text{(启formula , non-严格)}\]

\textbf{诚暴击}: 严格process 头间dependenceStructure's Chernoff bound
极its complexity . on , such as non-for 头间'sdependencedo Assumptions(such as  \(\beta\)-混or 
Dobrushin conditions), Theorem 1 formula 'sindependent number bound \textbf{non-适using } Multi-Head
Spring. can be 'sbound will \textbf{松}(because as must using  union bound or 
Azuma-Hoeffding process  martingale dependenceorder sequence ). 

\subsubsection{3.3 for  SE-1(Robbins-Monro
Convergence)'sinfluence }\label{ux5bf9-se-1robbins-monro-ux6536ux655bux7684ux5f71ux54cd}

\textbf{SE-1 's原始陈述}: Spring 'sParameter \(\theta_t\) through with under formula Update: 
\[\theta_{t+1} = \theta_t - \alpha_t \cdot \nabla \mathcal{L}(\theta_t) + \xi_t\]

where
\(\mathbb{E}[\xi_t | \mathcal{F}_t] = 0\), \(\text{Var}(\xi_t | \mathcal{F}_t) \leq \sigma^2\). at 
Robbins-Monro conditionsunder : 
\[\sum_{t=1}^{\infty} \alpha_t = \infty, \quad \sum_{t=1}^{\infty} \alpha_t^2 < \infty\]

has  \(\theta_t \xrightarrow{a.s.} \theta^*\)(at property Assumptionsunder ). 

\textbf{命题 3.2(Multi-Head Spring under 'sConvergenceconditions)}: 

\begin{enumerate}
\def\labelenumi{(\arabic{enumi})}
\item
  \textbf{步长conditions(\(\alpha_t\) asymptotic behavior)non-变}: conditions
  \(\sum \alpha_t = \infty\) and  \(\sum \alpha_t^2 < \infty\)
  only dependence步长调, non-dependenceParameternumber quantity . 
\item
  \textbf{but has Convergencemust 求更's步长}: 
  \[\boxed{\alpha_t^{\text{MH}} \leq \frac{\alpha_t^{\text{single}}}{\sqrt{K}} \cdot \frac{\sigma^2_{\text{single}}}{\sigma^2_{\text{MH}}}}\]
\end{enumerate}

where
\(\sigma^2_{\text{MH}} = \sigma^2_{\text{single}} \cdot (1 + \gamma \cdot (K - 1))\), \(\gamma \in [0, 1]\)
Yes头间梯干扰系number . 

\emph{Proofmust }: Multi-Head Spring
's梯差随头number increase . each 头's注权重 \(\alpha_{ij}^{(k)}\)
for 共享Statusfactor  \(\{s_j\}\) 'sdependenceintroduce 交叉头梯项. for 损失
\(\mathcal{L} = \mathbb{E}[\|\text{Spring}_{\text{MH}}(s_i) - s_i^*\|^2]\): 
\begin{align*}
\nabla_{\theta_k} \mathcal{L} &= \mathbb{E}\left[(\text{Spring}_{\text{MH}} - s_i^*)^T W^O_{:,k} \cdot \nabla_{\theta_k} \text{head}_k\right] \\
\text{Cov}(\nabla_{\theta_k} \mathcal{L}, \nabla_{\theta_l} \mathcal{L}) &\neq 0 \quad (\text{through 共享's } W^O \text{ and  } s_i^*)
\end{align*}

梯Noise'shas 效差as : 
\[\sigma^2_{\text{MH}} = \frac{1}{K} \sum_{k=1}^{K} \text{Var}(\nabla_{\theta_k} \mathcal{L}) + \frac{1}{K^2} \sum_{k \neq l} \text{Cov}(\nabla_{\theta_k} \mathcal{L}, \nabla_{\theta_l} \mathcal{L})\]

Partone Yes \(O(1)\)(each 头'sand 单头, but with  \(K\)), Partintroduce 
\(O((K-1)/K) \approx O(1)\)
's额差(when 交叉协差non-零). \(\square\)

\begin{enumerate}
\def\labelenumi{(\arabic{enumi})}
\setcounter{enumi}{2}
\tightlist
\item
  \textbf{ConvergenceObjectiveFromglobal Optimalization as point }: 
\end{enumerate}

Multi-Head Spring 's损失景观具has \textbf{排sequence for property }(交换 head \(k\) and 
head \(l\) 'sFunction). this means has  \(K!\)
etc.价'spoint . 损失景观non-凸, Robbins-Monro only can GuaranteeConvergenceto 
\(\nabla \mathcal{L} \approx 0\)(驻point ), non-can Guaranteeglobal Optimal. 

\textbf{修改'sConvergencetheorem (SE-1-MH)}: 

at  Robbins-Monro 步长conditionsand  Lipschitz continuous property 's前提under : 
\[\liminf_{t \to \infty} \mathbb{E}[\|\nabla \mathcal{L}(\theta_t)\|^2] \leq \frac{C \cdot \sigma^2_{\text{MH}} \cdot \bar{\alpha}}{\text{(and  } K \text{ No'snumber )}}\]

where \(\bar{\alpha} = \limsup_{t \to \infty} \alpha_t\). when  \(K\) 增
\(\sigma^2_{\text{MH}}\)
增, Convergence's残差梯范number also 增. \textbf{头make Convergence变更差(at has 限步长under )}. 

\subsubsection{3.4
Parameterization conditions}\label{ux8fc7ux53c2ux6570ux5316ux6761ux4ef6}

\textbf{definition  5(has Samplequantity )}: Spring learning through memory bank \(M_t\)
Training信号.  \(T_{\text{eff}}\)
as each Statusfactor at Self-Evolutionin 'shas 效''监督信号''number quantity (through  Spring
重构error provide ). then has Samplequantity as 
\(N_{\text{eff}} = N \cdot T_{\text{eff}}\). 

\textbf{命题 3.3(Parameterization bound number )}: 

for \textbf{standard 头}(each 头降维 \(d_k = d_s/K\)):
\[|\theta_{\text{MH}}| = \underbrace{K \cdot (2d_k d_s + d_v d_s)}_{\text{Q, K, V 投影}} + \underbrace{K d_v d_s}_{W^O} = 4 d_s^2 \quad (\text{when  } d_k = d_v = d_s/K)\]

总Parameter\textbf{non-dependence \(K\)} ------
standard design through 降维maintain Parameter恒. at 此况under , Multi-Head Spring
\textbf{non-introduce 额'sParameterization 风险}. 

for \textbf{全维头}(each 头
\(d_k = d_s\), 各头注non-physicalfactor space but can be 访问全维):
\[|\theta_{\text{MH}}| = d_s^2 \cdot (3K + 1)\]

\textbf{theorem  3.2(拟临bound conditions)}: 
\[\boxed{K_{\text{crit}} = \max\left(1, \left\lfloor \frac{N \cdot T_{\text{eff}} / d_s^2 - 1}{3} \right\rfloor \right)}\]

when  \(K > K_{\text{crit}}\)
, ModelParameterhas Training信号, \textbf{泛ization error with probability \(\geq 1 - \delta\)
to as }: 
\[\text{GenError}(K) \geq \Omega\left(\sqrt{\frac{d_s^2 \cdot (3K + 1)}{N \cdot T_{\text{eff}}}}\right)\]

(by standard  Rademacher complexity bound ). 

\textbf{number value e.g.}(保守Assumptions): - \(N = 500\)
Statusfactor , \(d_s = 64\), \(T_{\text{eff}} = 5\) -
\(K_{\text{crit}} = \lfloor (500 \cdot 5 / 4096 - 1) / 3 \rfloor = \lfloor (0.61 - 1) / 3 \rfloor = \max(1, -0.13) = 1\)

\textbf{Conclusion}: for in 'sstate space, i.e.make Yes \(K = 2\)
全维头also can be can already via Parameterization . for standard 降维头, \(K\)
can be with 更(because as Parameter恒). 

\subsubsection{3.5 for  SE-2(Doob
Martingale)'sinfluence }\label{ux5bf9-se-2doob-martingaleux7684ux5f71ux54cd}

SE-2
声称Parameter'sYes鞅: \(\mathbb{E}[\theta_{t+1} | \mathcal{F}_t] = \theta_t\). 

for  Multi-Head
Spring, \textbf{Martingale Propertyat Parameterspace on maintain }(because as YesBayesian update'sdirect 果), but : 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{边际Martingale Propertycan be can 失效}: for 单头'sParameter
  \(\theta_t^{(k)}\), \(\mathbb{E}[\theta_{t+1}^{(k)} | \mathcal{F}_t] \neq \theta_t^{(k)}\)(because as Updateand which has 头's交叉信息). 
\item
  \textbf{鞅差order sequence 's差增}: 
  \[\text{Var}(\theta_{t+1} - \theta_t | \mathcal{F}_t) = O(K)\]

  this means Convergence'sset in reduce . Azuma-Hoeffding give 's置信区间宽as 
  \(O(\sqrt{K \cdot t})\) while non- \(O(\sqrt{t})\). 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4. Analysis}\label{ux7ec4ux5408ux5206ux6790}

\subsubsection{4.1 联形formula ization }\label{ux8054ux5408ux5f62ux5f0fux5316}

组item simultaneously添'sCompleteOnline : 

\[\boxed{s_i \xrightarrow{\text{PPE}} h_i = \phi(s_i) + \text{PE}(p_i) \xrightarrow{\text{MH-Spring}} \text{Spring}_{\text{MH}}(h_i, \mathcal{H})}\]

where \(\mathcal{H} = \{h_1, \ldots, h_N\}\) Yescode 'sStatusfactor set . 

\subsubsection{4.2 Theorem 1 'sbound  ---
交叉项Analysis}\label{theorem-1-ux7684ux8054ux5408ux754c-ux4ea4ux53c9ux9879ux5206ux6790}

\textbf{命题 4.1(联Detection边际's解)}: 
\[\boxed{\Delta_s^{\text{combined}} = \Delta_s + \delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}}\]

where交叉项 \(\delta_s^{\text{cross}}\) satisfies : 
\[\delta_s^{\text{cross}} = \text{Cov}_{\text{experts}}\left(\text{Benefit}_{\text{PE}}, \text{Benefit}_{\text{MH}}\right)\]

i.e. PPE 's边际收益and  Multi-Head 's边际收益at expert 间's协差. 

\textbf{}:  \begin{align*}
F_1^{\text{combined}} &\geq 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M_{\text{eff}} \cdot (\Delta_s + \delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2\right) \\
&= 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M_{\text{eff}} \cdot \left[\Delta_s^2 + 2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + (\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2\right]\right)
\end{align*}

\textbf{超property  vs property Analysis}: 

令
\(A_s = \exp(-2M_{\text{eff}}\Delta_s^2)\)(原始error ), \(B_s^{\text{PE}}, B_s^{\text{MH}}, B_s^{\text{both}}\)
别as 单独 PPE, 单独 MH, 者结'serror . then : 

\[\log \frac{B_s^{\text{both}}}{A_s} = -2M_{\text{eff}} \cdot [2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + (\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2]\]

\[\log \frac{B_s^{\text{PE}} \cdot B_s^{\text{MH}}}{A_s^2} = -2M_{\text{eff}} \cdot [2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}}) + (\delta_s^{\text{PE}})^2 + (\delta_s^{\text{MH}})^2]\]

者差give (at for number space in ):
\[\text{Interaction} = -2M_{\text{eff}} \cdot [2\Delta_s \delta_s^{\text{cross}} + 2\delta_s^{\text{PE}}\delta_s^{\text{MH}} + 2\delta_s^{\text{cross}}(\delta_s^{\text{PE}} + \delta_s^{\text{MH}}) + (\delta_s^{\text{cross}})^2]\]

\textbf{超property conditions}(组收益 \textgreater{}
单独收益and ):交叉项as 负(decrease error ), i.e.: 
\[\delta_s^{\text{cross}} \cdot (2\Delta_s + 2\delta_s^{\text{PE}} + 2\delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + 2\delta_s^{\text{PE}}\delta_s^{\text{MH}} < 0\]

when  \(\delta_s^{\text{PE}} > 0\) and 
\(\delta_s^{\text{MH}} > 0\)(者all 改善Detection)and 
\(\delta_s^{\text{cross}} > 0\)(正toward 交互)satisfies : 
\[\boxed{\text{Superadditive when: } \delta_s^{\text{PE}} > 0, \delta_s^{\text{MH}} > 0, \delta_s^{\text{cross}} > 0}\]

\textbf{property conditions}(组收益 \textless{}
单独收益and ):when one or 交叉项as 负. 

\textbf{际判断}: Multi-Head Spring can be with using using  PPE
provide 'sspace local property implement has 义'sMode(e.g.such as ,  \(k\) physicalLocation
\(p_i\) 邻域内'sStatusfactor ). this 协概rate Yes\textbf{正toward }'s, i.e.
\(\delta_s^{\text{cross}} > 0\). but such as has 明确'sphysical约束, can be can learning to 虚假's跨维correlated property , 此
\(\delta_s^{\text{cross}}\) can be can Yes负's. 

\subsubsection{4.3 Theorem 2
'sbound }\label{theorem-2-ux7684ux8054ux5408ux754c}

featurepoint Yesproperty ization 's(because as each item all can be can introduce 额'spoint ):

\[\boxed{\delta_{\text{combined}} = \delta + \delta_{\text{PE}} + \delta_{\text{MH}} + \delta_{\text{cross}} \geq \max(\delta + \delta_{\text{PE}}, \delta + \delta_{\text{MH}})}\]

where:  - \(\delta_{\text{PE}} = 2\varepsilon_{\text{PE}} / C_F^2\)(
§2.4) -
\(\delta_{\text{MH}} \propto \frac{d_s^2 \cdot (3K + 1)}{N \cdot T_{\text{eff}}}\)(Parameterization , §3.4)
- \(\delta_{\text{cross}} \geq 0\): PPE 'snon-完美编code  + Multi-Head
'sParameterization 复效should 

because 此: 
\[F_{1,\text{SCX}}^{\text{combined}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta_{\text{combined}}}{2}}\]

\textbf{最坏况(者all 很差)}: bound 单独make using one item 更松. 

\textbf{最况(完美 PPE + Parameter充足's
MH)}: \(\delta_{\text{PE}} = \delta_{\text{MH}} = 0\), bound to 原始
Theorem 2. 

\subsubsection{4.4 Cercis Score
's修改}\label{cercis-score-ux7684ux4feeux6539}

\textbf{推荐修改}: 
\[\boxed{S'(s_i) = Q_{\text{MH}}(h_i) + \eta(t) \cdot N(s_i) - \lambda \cdot \mathcal{R}_{\text{diversity}}}\]

where: 
\[Q_{\text{MH}}(h_i) = \frac{1}{K} \sum_{k=1}^{K} w_k \cdot Q(\text{head}_k(h_i))\]

\(w_k\) Yescan be property 权重(can be through 头输and number 投票'sone property 估计). 

正then ization 项: 
\[\mathcal{R}_{\text{diversity}} = (K - 1) \cdot \left(1 - \frac{1}{K} \sum_{k=1}^{K} \left\|\text{head}_k - \frac{1}{K}\sum_{j=1}^{K} \text{head}_j\right\|^2\right)\]

惩罚头间'ssimilar (鼓励样property ), 防止退ization to 单头. 

\(\lambda > 0\) Yesthen ization 强. when  \(K = 1\) 
\(\mathcal{R}_{\text{diversity}} = 0\)(退ization as 原始形formula ). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5. 诚暴击(Honest
Critique)}\label{ux8bdaux5b9eux66b4ux51fbhonest-critique}

\subsubsection{5.1
number learning Guaranteeis 削弱's}\label{ux6570ux5b66ux4fddux8bc1ux88abux524aux5f31ux7684ux5730ux65b9}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1622}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2432}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3514}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2432}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Location
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
原始Guarantee
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
削弱'sStatus
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
严重程
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{Theorem 1 + MH} & Chernoff bound on i.i.d. experts &
头输correlated , i.i.d. Assumptionsnon-立. 严格 bound requires must  \(\beta\)-mixing
conditions, while conditionscan be can non-satisfies  & \textbf{} \\
\textbf{SE-1 + MH} & Robbins-Monro → global Optimal(凸property under ) &
non-凸损失景观(\(K!\) for 称驻point )→ only can Guaranteepoint , non-can Guaranteeglobal Optimal &
\textbf{} \\
\textbf{Theorem 2 + MH} & 紧致 Fano Upper Bound &
Parameterization bound 仍然立but \textbf{松}(vacuous), property can can be can 远差bound 
& \textbf{in } \\
\textbf{Theorem 3 + Learningtype  PPE} & 最坏况non-can be  & Learningtype  PE
make bound can be  → \textbf{theorem  3 is break }(but this Yes事) &
\textbf{in (际on has 益)} \\
\textbf{SE-2 + MH} & Martingale Propertymaintain  & 边际Martingale Propertycan be can 失效; 鞅差差
\(O(K)\) 增, 置信区间变宽 & \textbf{in } \\
\textbf{组 Theorem 1} & independent  Chernoff bound & has  \(M_{\text{eff}}\)
Yesformula 's, non-严格. 交叉项 \(\delta_s^{\text{cross}}\) with FromTheoryUpper Bound
& \textbf{} \\
\end{longtable}

\subsubsection{5.2
complete Noinfluence 's(etc.白)}\label{ux5b8cux5168ux65e0ux5f71ux54cdux7684ux5730ux65b9ux52a0ux4e86ux7b49ux4e8eux767dux52a0}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.3529}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.6471}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
conditions
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
as 什么No效
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{\(I(Y; P \mid X) = 0\)(Locationnon-携带target 签信息)} & PPE
YesNoise. Theorem 2 'sbound non-变. Theorem 1 's \(\Delta_s\)
non-变. Parameter全浪费 \\
\textbf{\(\rho_{\text{eff}} = 0\)(which has 头坍塌as single Mode)} & Multi-Head
etc.价单头 + \(K\) 倍冗余Parameter. Theorem 1 No增益. SE-1 Convergenceto point  \\
\textbf{fixed , Information Theorycan be 逆's PE} & only Yesquantity 替换. KL non-变 → Theorem 4
non-变. Nocontribution  \\
\textbf{\(K > K_{\text{crit}}\) and Nothen ization } & Parameterization  → ization error 导
→ any theorem 'sbound all as set (vacuous) \\
\textbf{physicalDimensionnon-correlated (e.g.such as 头注physicalDimension D, but  D and 任务No)} &
该头'shas  \(\Delta_s = 0\) → Noisecontribution  → reduce has 效
\(M_{\text{eff}}\) \\
\end{longtable}

\subsubsection{5.3
特conditionsunder break theorem 's况}\label{ux7279ux5b9aux6761ux4ef6ux4e0bux7834ux574fux5b9aux7406ux7684ux60c5ux51b5}

\textbf{Theorem 3 is break 'sconditions(5 conditions, 按严重程排order )}: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Learningtype  PPE(最严重)}: §2.5 already Analysis. TrainingDatain 'starget Noise导致
  \(\text{PE}_{W_A} \neq \text{PE}_{W_B}\). Result: bound can be 区, Theorem
  3 'snon-can be property non-立. 

  \emph{评价}: \textbf{事}. Theorem 3
  Yes最坏况non-can be can property Result. such as Location信息can 打破this non-can be can property , Explanation PPE
  has using . 
\item
  \textbf{Multi-Head Spring in 'sDimension泄漏}: if 某头is design as physicalDimension
  A, but on through  softmax 'sglobal also can 访问Dimension B
  's信息, then 头's''专ization ''Yes虚假's. this can be can 导致该头learning to target correlated 's虚假correlated property , change 
  \(P_{\text{clean}}\) and  \(P_{\text{noisy}}\) 's估计. 
\item
  \textbf{\(K > N\)(头number Statusfactor number )}: 头'sModeNomethod is Dataone determine . at Nomultiple Parameter配置's
  \(\text{Spring}_{\text{MH}}(s_i)\). Theorem 3
  'sconstruct dependenceDataDistribution'sone property ------when Parameterization Yes欠's, bound 'sDistributioncan be can has 细微Difference. 
\item
  \textbf{PPE Dimensionand physicalDimensionfor 齐}: such as  \(\text{PE}(p_i)\) 'sDimensionand 
  Multi-Head Spring
  in 某头'sphysicalDimension\textbf{正交}(i.e.编code 碰巧擦除该头requires must 's信息), then 该头'sModewill Systemproperty bias . this introduce one 
  Theorem 3 construct in not yet 考虑'sby . 
\item
  \textbf{\(\eta(t)\) 衰减快 + Multi-Head}: such as Exploration权重 \(\eta(t)\)
  衰减快, Multi-Head Spring can be can will some Mode''冻结''at Status. at 
  Theorem 3
  's双世bound construct in , one bound can be can learning to 更'sMode(because as target 签更Clean), while another bound learning to 差'sMode------make bound can be 区. 
\end{enumerate}

\subsubsection{5.4
最诚'sSummary}\label{ux6700ux8bdaux5b9eux7684ux603bux7ed3}

\textbf{PPE 'sOnline }:  - \textbf{for  Theorem 1}: such as physicalLocationand 任务correlated  →
has toward contribution (\(\delta_s^{\text{PE}} > 0\)). such as No →
has influence . Chernoff Structuremaintain .  - \textbf{for  Theorem 2}: non-完美'scode with 
\(\sqrt{\delta + O(\varepsilon_{\text{PE}})}\)
扩Upper Bound. 完美编code non-influence Upper Bound.  - \textbf{for  Theorem
3}: fixed code maintain non-can be property . Learningtype code \textbf{break }Theorem
3------but this Yes\textbf{}while non-退步.  - \textbf{for  Theorem
4}: non-decrease error number . has using increase number . 

\textbf{Multi-Head Spring 'sOnline }:  - \textbf{for  Theorem
1}: \textbf{non-Yes}independent expert . 严格bound  naive \(M \times K\)
松. correlated property 'sCompleteprocess its Difficulty.  - \textbf{for 
SE-1}: Convergenceto point (non-Guaranteeglobal Optimal). has 效步长on with  \(O(1/\sqrt{K})\)
缩. Convergence更慢.  -
\textbf{Parameterization }: for type 'sstate space, \(K_{\text{crit}}\) can be can only has 
1-3 头. 超此number 'swill ization 惩罚. 

\textbf{组's最诚评价}:  - PPE
Yes\textbf{更has value 's}item ------at Theoryon 更清晰, influence can be 预测, and 风险更低
- Multi-Head Spring 's\textbf{Theory风险更}------break  Theorem 1
Optimal雅'spart (i.i.d. expert 's Chernoff bound) -
者's\textbf{交叉项at Theoryon Yes正's}(can be can property 收益), but \textbf{at in can be can as 负}(额'scomplexity property introduce 's失败Mode)

\textbf{one 句话Summary}: PPE
is a安全's赌注, number learning on 几乎纯粹has 益(除Learningtype  PPE break  Theorem
3, but that Yes事). Multi-Head Spring
is a风险赌注------at number learning on 削弱 Theorem 1 's严格property , make  SE-1
Convergenceproperty 变差, and has can be can Parameterization . 组, PPE 's收益can be can is  Multi-Head
'scomplexity property part 抵消. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{6. Problem}\label{ux5f00ux653eux95eeux9898}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{严格's Chernoff bound }: can Noat 头'sdependenceStructureon establish 
  \(\beta\)-mixing conditions, make  Theorem 1 'snumber bound can be with 严格extensionto 头况？
\item
  \textbf{PPE 'sOptimalrate }: at 正弦编code in , rate Parameter
  \(\omega_j = 10000^{-2j/d}\)
  Yesas Nature语言design 's. physicalLocation'sOptimalrate Yes什么？can NoFromphysicalfor property 推导？
\item
  \textbf{头number  \(K\) 'sshould Selection}: can NoFromDatain LearningOptimal's \(K\)(class 似
  Bayesian non-parametric in 'sin 餐馆程), at Parameterization 前停止？
\item
  \textbf{Theorem 3
  's修改}: such as at Theorem Statementin 入''前提conditions:non-make using Datadependence'sLocationcode ''maintain its number learning Completeproperty ？
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{References(framework 内)}\label{ux53c2ux8003ux6587ux732eux6846ux67b6ux5185ux90e8}

\begin{itemize}
\tightlist
\item
  SCX Project (2026). Theorem 1-4: Multi-expert consistency, weak
  feature limits, unidentifiability, and minimax optimality.
  \texttt{theory/theorems/}
\item
  SCX Project (2026). Spring Dynamics, SE-1 (Robbins-Monro convergence),
  SE-2 (Doob martingale). \texttt{theory/self\_evolution/}
\item
  SCX Project (2026). State Crystallization: physical-driven
  discretization of continuous observables.
  \texttt{theory/propositions/}
\item
  Wainwright, M. (2019). \emph{High-Dimensional Statistics: A
  Non-Asymptotic Viewpoint}. Cambridge. (Chernoff-Hoeffding-Stein
  'sstandard 参考)
\item
  Boucheron, S., Lugosi, G., \& Massart, P. (2013). \emph{Concentration
  Inequalities}. Oxford. (dependencequantity 'sconcentration inequalities)
\item
  Vaswani, A., et al.~(2017). Attention Is All You Need. \emph{NeurIPS}.
  (头注's原始Formula)
\item
  Su, J., et al.~(2024). RoFormer: Enhanced Transformer with Rotary
  Position Embedding. \emph{Neurocomputing}. (RoPE 'snumber learning 基础)
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{AnalysisDate: 2026-06-29}\\
\emph{者备注: which has theorem 编号指 SCX
framework theorem , non-order 号. Analysis'snumber learning process at can be process 's范围内Yes严格's; already 明确target which has approximate and formula 'spart . 诚'snon-can be process property (such as dependence头's
Chernoff bound)already is 诚记录. }

\end{document}
