% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{ctexart}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Multi-Head Spring 与 Physical Positional Encoding
的严格数学Analysis}\label{multi-head-spring-ux4e0e-physical-positional-encoding-ux7684ux4e25ux683cux6570ux5b66ux5206ux6790}

\subsection{对 SCX
框架TheoremFoundation的诚实审查}\label{ux5bf9-scx-ux6846ux67b6ux5b9aux7406ux57faux7840ux7684ux8bdaux5b9eux5ba1ux67e5}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: Complete Analysis\\
\textbf{-dependence}: Theorem 1--4 (theory/theorems/), SE-1, SE-2
(theory/self\_evolution/)\\
\textbf{Methodology}: Rigorous Formal Derivation + Honest Critique (Unsparing)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{0.
Notation Convention and Framework Reconstruction}\label{ux7b26ux53f7ux7ea6ux5b9aux4e0eux6846ux67b6ux91cdux6784}

在进入Analysis之前，我们先Exact 地重构 SCX 框架的数学Foundation。以下所has Definition均From 
prompt  in the Theorem描述严格形式化。

\subsubsection{0.1 Status结晶（State
Crystallization)}\label{ux72b6ux6001ux7ed3ux6676state-crystallization}

设原始物理Observation Spaceis 
\(\mathcal{X} \subset \mathbb{R}^{d_{\text{raw}}}\)（连续物理量：PBE
能量面、力场、电子密度等)。State Crystallization 是一个映射：

\[\mathcal{C}: \mathcal{X} \to \mathcal{S} = \{s_1, \ldots, s_N\}\]

其中每个 \(s_i \in \mathbb{R}^{d_s}\)
是一个\textbf{离散Status原子}的向量表示。与 BPE
的统计驱动不同，\(\mathcal{C}\) 是物理驱动的：它根据 PBE
能量面的自然曲率边界进lines聚类。形式上has ：

\[s_i = \mathcal{C}(x) \quad \text{其中} \quad x \in \mathcal{X} \text{ 且 } \nabla^2 E_{\text{PBE}}(x) \text{ 在 } x \text{ 处has 一个谱间隙}\]

\subsubsection{0.2 Yajie审计（Yajie
Audit)}\label{ux96c5ux6d01ux5ba1ux8ba1yajie-audit}

设has  \(M\) 个独立专家模型 \(\{E_1, \ldots, E_M\}\)。对于Status原子 \(s_i\)
及其Label \(y_i\)，Definition：

\[c_m(s_i) = \mathbf{1}[E_m(s_i) \neq y_i] \quad \text{（专家 m 不同意给定Label)}\]

一致性计数：
\[C(s_i) = \sum_{m=1}^{M} c_m(s_i) \sim \text{Binomial}(M, p_{s_i})\]

其中 \(p_{s_i}\)
是专家''不同意''Label的概率。对于干净样本，\(p_{s_i} = p_{\text{clean}} < 0.5\)；对于噪声样本，\(p_{s_i} = p_{\text{noisy}} > 0.5\)。

\textbf{Cercis Score}: \[S(s_i) = Q(s_i) + \eta(t) \cdot N(s_i)\]

其中 \(Q(s_i)\) 是Foundation质量分（基于多专家一致性)，\(N(s_i)\)
是新奇性奖励，\(\eta(t)\) 是时间衰减探索权重。

\subsubsection{0.3 Spring
自进化动力学}\label{spring-ux81eaux8fdbux5316ux52a8ux529bux5b66}

Status转移： \[(S_t, \theta_t, M_t) \to (S_{t+1}, \theta_{t+1}, M_{t+1})\]

Spring 自Note意力形式（单头)：
\[\text{Spring}(s_i, \mathcal{S}) = \text{softmax}\left(\frac{(Q s_i)(K \mathcal{S})^T}{\sqrt{d_k}}\right) \cdot (V \mathcal{S})\]

其中
\(Q, K \in \mathbb{R}^{d_k \times d_s}\)，\(V \in \mathbb{R}^{d_v \times d_s}\)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.
Core Theorems的Exact Theorem Statement}\label{ux6838ux5fc3ux5b9aux7406ux7684ux7cbeux786eux5b9aux7406ux9648ux8ff0}

\subsubsection{Theorem
1（多专家一致性噪声检测)}\label{theorem-1ux591aux4e13ux5bb6ux4e00ux81f4ux6027ux566aux58f0ux68c0ux6d4b}

\[\boxed{F_1 \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M \Delta_s^2\right)}\]

其中： -
\(\rho_s = |\{i: \mathcal{C}(x_i) = s\}| / N_{\text{total}}\)：Status原子
\(s\) 的占比 -
\(\Delta_s = |p_{\text{clean},s} - p_{\text{noisy},s}|\)：Status \(s\)
的Detection Margin - \(M\)：独立专家数量 - \(\eta > 0\)：归一化因子

\textbf{Proof Technique}：Chernoff bound + Hoeffding 不等式。对每个Status
\(s\)，\(C(s)\) 是两个二项分布的混合。Markedis 噪声当
\(C(s) \geq \tau_s\)。Chernoff 给出：
\[P(\text{假阳性}) = P_{H_0}(C(s) \geq \tau_s) \leq \exp(-2M(\tau_s/M - p_{\text{clean},s})^2)\]
\[P(\text{假阴性}) = P_{H_1}(C(s) < \tau_s) \leq \exp(-2M(p_{\text{noisy},s} - \tau_s/M)^2)\]

选取 \(\tau_s = M \cdot (p_{\text{clean},s} + p_{\text{noisy},s})/2\)
使两项平衡，得到 \(\Delta_s\)。

\subsubsection{Theorem
2（弱Characteristics必然失效)}\label{theorem-2ux5f31ux7279ux5f81ux5fc5ux7136ux5931ux6548}

\[\boxed{F_{1,\text{SCX}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}}\]

其中 \(\delta > 0\) 刻画Characteristics空间的信息不足程度。

\textbf{Proof Technique}：Fano 不等式。Definition
\(\delta = \min_{f} \mathbb{E}[\mathbf{1}[f(X) \neq Y]] - \text{Bayes error}\)，即Optimal 可lines分类器与贝叶斯Optimal 之间的最小差距。Fano
给出Conditions熵与错误率的关系：
\[P_e \geq \frac{H(Y|X) - 1}{\log |\mathcal{Y}|}\]

当 \(H(Y|X)\) 较大（Characteristics弱)，任何方法（包括 SCX)的错误率下界由
\(\delta\) 决定。

\subsubsection{Theorem
3（噪声与Difficulty样本的不可区分性)}\label{theorem-3ux566aux58f0ux4e0eux56f0ux96beux6837ux672cux7684ux4e0dux53efux533aux5206ux6027}

\textbf{构造性双世界Proof}：存在两个世界
\(W_A, W_B\)，具has 观测等价的Data Distribution
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)，但：

\begin{itemize}
\tightlist
\item
  世界 \(W_A\)：样本 \((x, y)\) 是被错误Marked的（Label噪声)
\item
  世界 \(W_B\)：样本 \((x, y)\) 正确Marked但本质上Difficulty（高贝叶斯不确定性)
\end{itemize}

没has 算法能够仅通过观测data区分这两个世界。

形式化：\(\forall\) 决策规则
\(d: (\mathcal{X} \times \mathcal{Y})^n \to \{W_A, W_B\}\)，
\[\max_{W \in \{W_A, W_B\}} P_W(d(\text{data}) \neq W) \geq \frac{1}{2} - \frac{1}{2}\|P_{W_A}^{\otimes n} - P_{W_B}^{\otimes n}\|_{\text{TV}} = \frac{1}{2}\]

因is 
\(P_{W_A}^{\otimes n} = P_{W_B}^{\otimes n}\)（完全相同的观测分布)。

\subsubsection{Theorem 4（Exact 常数 Minimax
Optimal )}\label{theorem-4ux7cbeux786eux5e38ux6570-minimax-ux6700ux4f18}

SCX 的噪声检测错误率在以下意义上达到Exact 常数Optimal ：

\[\lim_{M \to \infty} -\frac{1}{M} \log P_{\text{FN}}^{(M)} = D_{\text{KL}}(P_{\text{noisy}} \| P_{\text{clean}}) \quad \text{subject to } P_{\text{FP}}^{(M)} \leq \alpha\]

且 Bahadur-Rao 展开给出的 \(O(1/\sqrt{M})\) correction term也匹配 minimax 下界。

\textbf{Proof Technique}：Chernoff-Stein Lemma + Bahadur-Rao 展开。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2. 组件 1：Physical Positional
Encoding（PPE)的数学Analysis}\label{ux7ec4ux4ef6-1physical-positional-encodingppeux7684ux6570ux5b66ux5206ux6790}

\subsubsection{2.1 Formal Definition}\label{ux5f62ux5f0fux5316ux5b9aux4e49}

设每个Status原子 \(s_i\) 带has 一个物理Location \(p_i \in \mathcal{P}\)，其中：
- 蛋白质序列：\(\mathcal{P} = \mathbb{N}\)（残基索引) - 3D
结构：\(\mathcal{P} = \mathbb{R}^3\)（原子坐标) -
Docking：\(\mathcal{P} = \mathbb{R}^3\)（结合口袋坐标)

\textbf{Definition 1（Physical Positional Encoding)}：PPE 是一个映射
\[\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\]

编码后的Status表示： \[\boxed{h_i = \phi(s_i) + \text{PE}(p_i)}\]

其中 \(\phi: \mathbb{R}^{d_s} \to \mathbb{R}^{d}\)
是Characteristics映射，且我们要求 \(d_{\text{pe}} = d\)。

\textbf{可选方案（拼接+投影)}：
\[h_i = W \cdot [\phi(s_i); \text{PE}(p_i)]\]

其中
\(W \in \mathbb{R}^{d \times (d + d_{\text{pe}})}\)。数学上这是更一般的方案，但加法方案更简洁且在
\(W = [I_d, I_d]\) 时退化is 加法。以下Analysis以加法方案is 主，结果可推广。

\subsubsection{2.2
编码函数的具体形式}\label{ux7f16ux7801ux51fdux6570ux7684ux5177ux4f53ux5f62ux5f0f}

\textbf{标量Location的正弦编码}（analogy Transformer)：
\[\text{PE}(p, 2j) = \sin\left(\frac{p}{10000^{2j/d}}\right), \quad \text{PE}(p, 2j+1) = \cos\left(\frac{p}{10000^{2j/d}}\right)\]

其中 \(j = 0, 1, \ldots, d/2 - 1\)。

\textbf{3D Location的旋转编码}（analogy RoPE，适配物理空间)：
\[\text{PE}_{\text{rot}}(\mathbf{p}) = \mathbf{R}(\mathbf{p}) \cdot \mathbf{e}_0\]

其中 \(\mathbf{R}(\mathbf{p}) \in SO(d)\) 是由坐标参数化的旋转矩阵。对于
\(\mathbf{p} = (x, y, z)\)：
\[\mathbf{R}(\mathbf{p}) = \mathbf{R}_x(\omega_x x) \cdot \mathbf{R}_y(\omega_y y) \cdot \mathbf{R}_z(\omega_z z)\]

其中 \(\mathbf{R}_\alpha(\theta)\) 是在 \(\alpha\) 轴上的 \(d\)
维旋转，\(\omega_x, \omega_y, \omega_z\) 是可学习或fixed 的频率参数。

\textbf{关键性质}：与 NLP
的Location编码不同，物理Location具has \textbf{多维几何结构}。一个良好的 PPE
应尊重物理对称性：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{平移等变性}（对 3D
  结构)：\(\text{PE}(\mathbf{p} + \mathbf{t})\) 与
  \(\text{PE}(\mathbf{p})\) has 可预测的关系
\item
  \textbf{旋转等变性}：\(\text{PE}(\mathbf{R}\mathbf{p})\)
  可以通过旋转编码空间来恢复
\item
  \textbf{Lipschitz
  连续性}：\(\|\text{PE}(\mathbf{p}) - \text{PE}(\mathbf{q})\| \leq L_{\text{PE}} \cdot \|\mathbf{p} - \mathbf{q}\|\)
\end{enumerate}

\subsubsection{2.3 对 Theorem 1
的影响}\label{ux5bf9-theorem-1-ux7684ux5f71ux54cd}

\textbf{Core Problem}：PPE 如何改变Detection Margin \(\Delta_s\)？

\textbf{Proposition 2.1（PPE 下的修改后Detection Margin)}： 在 PPE
增强的Characteristics空间中，Status \(s\) 的has 效Detection Marginis ：
\[\boxed{\Delta_s^{\text{PPE}} = \Delta_s + \delta_s^{\text{PE}}}\]

其中
\(\delta_s^{\text{PE}} \in [-\Delta_s, 1 - p_{\text{clean},s} - \Delta_s]\)
满足：
\[\delta_s^{\text{PE}} = \underbrace{(p_{\text{clean},s}^{\text{PPE}} - p_{\text{clean},s})}_{\text{干净样本上的一致性增益}} - \underbrace{(p_{\text{noisy},s}^{\text{PPE}} - p_{\text{noisy},s})}_{\text{噪声样本上的混淆增益}}\]

\emph{Proof}：在增强空间中，专家的预测概率变is 
\(p_{\text{clean},s}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) = y_i] | H_0]\)
和
\(p_{\text{noisy},s}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) \neq y_i] | H_1]\)。Detection MarginDefinitionis 
\(\Delta_s^{\text{PPE}} = |p_{\text{clean},s}^{\text{PPE}} - (1 - p_{\text{noisy},s}^{\text{PPE}})| = |p_{\text{clean},s}^{\text{PPE}} + p_{\text{noisy},s}^{\text{PPE}} - 1|\)。展开即得。\(\square\)

\textbf{Theorem 2.1（PPE 下的 F1 下界)}：
\[\boxed{F_1^{\text{PPE}} \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M (\Delta_s + \delta_s^{\text{PE}})^2\right)}\]

Chernoff-Hoeffding 不等式的\textbf{has 效性不变}（指示变量仍在 \([0,1]\)
内has 界)，但常数变化。

当 \(\delta_s^{\text{PE}} > 0\) 对所has  \(s\)（PPE 总是帮助检测)：
\[F_1^{\text{PPE}} - F_1 \geq \frac{1}{\eta} \sum_s \rho_s \left[\exp(-2M\Delta_s^2) - \exp(-2M(\Delta_s + \delta_s^{\text{PE}})^2)\right] \geq 0\]

\textbf{Proposition 2.2（空间局部性正则化)}：If  PE 满足 Lipschitz
Conditions（Lipschitz 常数is  \(L_{\text{PE}}\))，且专家模型 \(E_m\)
对其输入满足 \(L_E\)-Lipschitz，则对任意两个具has 物理Location \(p_i, p_j\)
的Status原子：
\[|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| \leq 2L_E \cdot L_{\text{PE}} \cdot \|p_i - p_j\|\]

\emph{Proof}：专家一致性概率的变化受输入变化约束： \begin{align*}
|p_{\text{clean},i}^{\text{PPE}} - p_{\text{clean},j}^{\text{PPE}}|
&\leq \mathbb{E}_{E} \left|\mathbf{1}[E(h_i) = y] - \mathbf{1}[E(h_j) = y]\right| \\
&\leq \mathbb{E}_{E} \left[ L_E \cdot \|h_i - h_j\| \right] \\
&= L_E \cdot \|\text{PE}(p_i) - \text{PE}(p_j)\| \quad (\text{因is  } s_i = s_j = s) \\
&\leq L_E \cdot L_{\text{PE}} \cdot \|p_i - p_j\|
\end{align*}

同样对 \(p_{\text{noisy}}\) 成立。两者的差得到因子 2。\(\square\)

这意味着\textbf{物理上邻近的Status原子具has 相似的检测难度}------一个可验证的预测。

\subsubsection{2.4 对 Theorem 2
的影响}\label{ux5bf9-theorem-2-ux7684ux5f71ux54cd}

\textbf{Core Problem}：不完美的 PPE 如何改变Characteristics弱点的界？

\textbf{Definition 2（编码不完美度 \(\varepsilon_{\text{PE}}\))}：
\[\boxed{\varepsilon_{\text{PE}} = I(Y; P | X) - I(Y; \text{PE}(P) | X) \geq 0}\]

其中 \(I(Y; P | X)\) 是物理Location \(P\) 在给定原始Characteristics \(X\) 下关于Label
\(Y\) 的Conditions互信息，而 \(I(Y; \text{PE}(P) | X)\)
是编码后保留的信息。\(\varepsilon_{\text{PE}}\)
量化了\textbf{Location信息在编码过程 in the 丢失}。

\textbf{Theorem 2.2（不完美 PPE 下的失效upper bound)}：
\[\boxed{F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}}\]

\emph{Proof}：Fano 不等式给出
\(P_e^{\text{PPE}} \geq \frac{H(Y|X, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\)。我们has ：
\begin{align*}
H(Y|X, \text{PE}(P)) &= H(Y|X) - I(Y; \text{PE}(P) | X) \\
&= H(Y|X) - [I(Y; P | X) - \varepsilon_{\text{PE}}] \\
&\geq H(Y|X) - I(Y; P | X) + \varepsilon_{\text{PE}}
\end{align*}

而 \(\delta\) 的Definition给出
\(H(Y|X) - 1 \geq (\delta/2) \cdot \log|\mathcal{Y}|\)（通过 Fano
的逆方向)。因此：
\[H(Y|X, \text{PE}(P)) - 1 \geq \left(\frac{\delta}{2} + \frac{\varepsilon_{\text{PE}}}{C_F}\right) \cdot \log|\mathcal{Y}|\]

代入 SCX 的 F1 upper bound表达式即得。\(\square\)

\textbf{紧致性Analysis}： - 当
\(\varepsilon_{\text{PE}} = 0\)（完美编码)：upper bound恢复is 原始 Theorem 2 -
当 \(\varepsilon_{\text{PE}} > 0\)：upper bound以
\(\sqrt{\delta + O(\varepsilon_{\text{PE}})}\) 的速率放松 - 当
\(\varepsilon_{\text{PE}} \to I(Y; P | X)\)（完全失败的编码，丢失了所has Location信息)：upper bound回到完全未使用Location信息的情况

\textbf{Corollary 2.1（Location信息的价值)}：如果物理Location \(P\)
本身不携带任何关于Label \(Y\) 的信息（即 \(I(Y; P | X) = 0\))，则
\(\varepsilon_{\text{PE}} = 0\)（因is 没has 信息可丢失)，Theorem 2
的upper bound\textbf{完全不变}。PPE 在这种情况下是\textbf{纯粹的参数浪费}。

\subsubsection{2.5 对 Theorem 3
的影响}\label{ux5bf9-theorem-3-ux7684ux5f71ux54cd}

这是最关键的Analysis。Theorem 3 的不可区分性-dependence于构造两个世界 \(W_A, W_B\)
具has 完全相同的观测分布 \(P(X, Y)\)。

\textbf{Proposition 2.3（fixed  PPE 保持 Theorem 3)}：如果 PE
是一个\textbf{先验fixed }的函数（不-dependence于训练data或Label)，则两个世界 in the 增强Characteristics分布依然相同：
\[P_{W_A}(X, \text{PE}(P), Y) = P_{W_B}(X, \text{PE}(P), Y)\]

因此 Theorem 3 的不可区分性\textbf{保持不变}。PPE
没has 帮助也没has 损害最坏情况的可识别性。

\emph{Proof}：物理Location \(P\) 由物理结构决定，独立于Label生成过程。因此
\(P_{W_A}(P|X) = P_{W_B}(P|X)\)。因is  PE
是fixed 的确定性函数，\(P_{W_A}(\text{PE}(P)|X) = P_{W_B}(\text{PE}(P)|X)\)。观测分布完全相同。\(\square\)

\textbf{Proposition 2.4（learnable  PPE breaking  Theorem 3)}：如果 PE 的参数
\(\theta_{\text{PE}}\) 是From data中学习的（包括Label
\(Y\))，则在两个世界中学习到不同的编码：
\[\text{PE}_{W_A} \neq \text{PE}_{W_B}\]

因is  \(W_A\) 的噪声Label影响学习过程。此时：
\[P_{W_A}(X, \text{PE}_{W_A}(P), Y) \neq P_{W_B}(X, \text{PE}_{W_B}(P), Y)\]

两个世界变得\textbf{可区分}。Theorem 3 被breaking 。

\textbf{诚实评价}：这实际上是\textbf{好消息}。Theorem 3
是一个不可能性结果------它说在最坏情况下，噪声和Difficulty样本None法区分。如果learnable 
PPE 能breaking Theorem 3
的前提Conditions，意味着\textbf{Location信息提供了区分噪声和Difficulty样本的能力}。这is NOT 框架的缺陷，而是框架的进步。但这确实意味着
Theorem 3 不再NoneConditions成立------需要添加前提Conditions''PE
是fixed 的或Location信息不携带Label相关信息''。

\subsubsection{2.6 对 Theorem 4
的影响}\label{ux5bf9-theorem-4-ux7684ux5f71ux54cd}

\textbf{Proposition 2.5（PPE 不减少Optimal 误差指数)}：
\[\boxed{D_{\text{KL}}(P_{\text{noisy}}^{\text{PPE}} \| P_{\text{clean}}^{\text{PPE}}) = D_{\text{KL}}(P_{\text{noisy}} \| P_{\text{clean}}) + \Delta D_{\text{KL}}^{\text{PE}}}\]

其中 \(\Delta D_{\text{KL}}^{\text{PE}} \geq 0\)（非负，由 KL
散度的data处理不等式保证)。

\emph{Proof}：由 KL 散度的链式法则： \begin{align*}
D_{\text{KL}}(P_{\text{noisy}}^{\text{PPE}} \| P_{\text{clean}}^{\text{PPE}})
&= D_{\text{KL}}(P_{\text{noisy}}(X, H) \| P_{\text{clean}}(X, H)) \\
&= D_{\text{KL}}(P_{\text{noisy}}(X) \| P_{\text{clean}}(X)) + \mathbb{E}_{X \sim P_{\text{noisy}}}[D_{\text{KL}}(P_{\text{noisy}}(H|X) \| P_{\text{clean}}(H|X))]
\end{align*}

因is  \(H = \phi(X) + \text{PE}(P)\)，且 PE
是fixed 的确定性函数，增强不会减少 KL 散度（信息处理不等式)。具体地：
\[\Delta D_{\text{KL}}^{\text{PE}} = \mathbb{E}_{X \sim P_{\text{noisy}}}[D_{\text{KL}}(P_{\text{noisy}}(\text{PE}(P)|X) \| P_{\text{clean}}(\text{PE}(P)|X))] \geq 0\]

\(\square\)

PPE
\textbf{From 不降低}Optimal 误差指数。如果Location信息与Label相关，\(\Delta D_{\text{KL}}^{\text{PE}} > 0\)，误差指数\textbf{严格更大}（检测更准确)。如果Location信息None关，\(\Delta D_{\text{KL}}^{\text{PE}} = 0\)，不变。

Bahadur-Rao 常数因 PE 引入的方差项而修改，但总是朝更紧的方向（当 PE
has 用时)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3. 组件 2：Multi-Head Spring
的数学Analysis}\label{ux7ec4ux4ef6-2multi-head-spring-ux7684ux6570ux5b66ux5206ux6790}

\subsubsection{3.1 Formal Definition}\label{ux5f62ux5f0fux5316ux5b9aux4e49-1}

\textbf{Definition 3（Multi-Head Spring)}：
\[\boxed{\text{Spring}_{\text{MH}}(s_i, \mathcal{S}) = \text{Concat}(\text{head}_1, \ldots, \text{head}_K) \cdot W^O}\]

其中：
\[\text{head}_k = \text{Spring}_k(s_i, \mathcal{S}) = \text{softmax}\left(\frac{(Q_k s_i)(K_k \mathcal{S})^T}{\sqrt{d_k}}\right) \cdot (V_k \mathcal{S})\]

参数： - \(Q_k, K_k \in \mathbb{R}^{d_k \times d_s}\)：第 \(k\)
个头的查询/键投影 - \(V_k \in \mathbb{R}^{d_v \times d_s}\)：第 \(k\)
个头的值投影 - \(W^O \in \mathbb{R}^{K d_v \times d_s}\)：输出投影 -
\(d_k = d_s / K\)（Standard 多头的降维约定)，或
\(d_k = d_s\)（全维多头，每个头操作CompleteState Space)

每个头 \(k\) 被设计is 关Note一个不同的物理维度： - 头 1：键角模式 ---
关NoteStatus原子间的角度关系 - 头 2：键长模式 --- 关NoteStatus原子间的距离关系 -
头 3：配位环境 --- 关NoteStatus原子的局部邻域拓扑 - 头 4：能量梯度/力场模式
--- 关NoteStatus原子的能量面曲率

\textbf{物理维度约束（结构化多头)}：is 了实现真正的专业化，每个头的
\(Q_k, K_k\) 应被约束is 仅作用于Status向量的特定子空间。If 
\(s_i = [s_i^{(1)}; s_i^{(2)}; s_i^{(3)}; s_i^{(4)}]\)
按物理维度分块，则：

\[Q_k = [\mathbf{0}_{d_k \times d_{<k}} \;|\; Q_k^{(k)} \;|\; \mathbf{0}_{d_k \times d_{>k}}]\]

其中 \(Q_k^{(k)}\) 仅作用于第 \(k\)
个物理维度的子空间，其余is 零。这\textbf{强制}专业化。

\subsubsection{\texorpdfstring{3.2 对 Theorem 1 的影响 --- has 效专家数
\(M\)}{3.2 对 Theorem 1 的影响 --- has 效专家数 M}}\label{ux5bf9-theorem-1-ux7684ux5f71ux54cd-ux6709ux6548ux4e13ux5bb6ux6570-m}

\textbf{Core Problem}：\(K\) 个头是否等价于 \(K\) 个独立专家？

\textbf{答案：否。} 这是一个关键的区别。

\textbf{Proposition 3.1（头is NOT 独立专家)}：Multi-Head Spring
的头输出在给定Status原子集合时是\textbf{Conditions-dependence}的：
\[\text{Cov}(\text{head}_k(s_i), \text{head}_l(s_i) \mid \mathcal{S}) \neq 0 \quad \text{（一般情况)}\]

\emph{Proof}：所has 头共享相同的输入Status原子 \(\mathcal{S}\) 和相同的
softmax Note意力池化结构。即使投影矩阵 \(Q_k, K_k\) 不同，Note意力权重
\(\alpha_{ij}^{(k)} = \text{softmax}_j((Q_k s_i)^T (K_k s_j) / \sqrt{d_k})\)
通过共同的 \(s_j\) 值coupled 。因此：
\[\text{head}_k(s_i) = \sum_j \alpha_{ij}^{(k)} \cdot V_k s_j\]
\[\text{head}_l(s_i) = \sum_j \alpha_{ij}^{(l)} \cdot V_l s_j\]

两者的协方差通过共享的 \(\{s_j\}\) 项非零。\(\square\)

\textbf{Definition 4（has 效专家多样性 \(\rho_{\text{eff}}\))}：
\[\boxed{\rho_{\text{eff}} = 1 - \frac{2}{K(K-1)} \sum_{k < l} |\text{Corr}(\text{head}_k, \text{head}_l)|}\]

其中
\(\text{Corr}(\text{head}_k, \text{head}_l) = \frac{\text{Cov}(\text{head}_k, \text{head}_l)}{\sqrt{\text{Var}(\text{head}_k) \cdot \text{Var}(\text{head}_l)}}\)（逐元素平均相关性)。

\begin{itemize}
\tightlist
\item
  \(\rho_{\text{eff}} = 1\)：所has 头完全独立（理想情况)
\item
  \(\rho_{\text{eff}} = 0\)：所has 头完全相关（退化is 单头)
\item
  \(\rho_{\text{eff}} < 0\)：头之间存在负相关（对抗性多样性)
\end{itemize}

\textbf{has 效专家数的启发式修正}：
\[M_{\text{eff}} = M \cdot (1 + \alpha \cdot \rho_{\text{eff}} \cdot (K - 1))\]

其中 \(\alpha \in [0, 1]\) 是''头多样性 \(\to\) 专家多样性''的转换系数。

\textbf{但这is NOT 一个严格的界}。严格的 Chernoff bound
需要处理-dependence随机变量。

\textbf{Theorem 3.1（Multi-Head Spring 下的严格 F1 下界)}：

设 \(A_k(s)\) is 第 \(k\) 个头对Status \(s\)
产生的''伪一致性分数''（通过头输出与Label的一致性)，则使用
union-Chernoff 方法：

\[\boxed{F_1^{\text{MH}} \geq 1 - \frac{1}{\eta} \sum_{s} \rho_s \cdot \min_{\mathcal{I} \subseteq [K]} \exp\left(-2M \cdot \tilde{\Delta}_s^2(\mathcal{I})\right)}\]

其中 \(\tilde{\Delta}_s(\mathcal{I})\) 是在头子集 \(\mathcal{I}\)
上的has 效边际，\(\mathcal{I}\) 选取一个最大独立子集（满足某种
\(\beta\)-混合Conditions)。

在最坏情况下（所has 头完全相关，\(\rho_{\text{eff}} = 0\))：
\[F_1^{\text{MH}} = F_1^{\text{single}} \quad \text{（Multi-Head None增益)}\]

在最好情况下（所has 头完全独立，\(\rho_{\text{eff}} = 1\)，且
\(\alpha = 1\))：
\[F_1^{\text{MH}} \geq 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M K \tilde{\Delta}_s^2\right) \quad \text{（启发式，非严格)}\]

\textbf{Honest Critique}：严格处理头之间-dependence结构的 Chernoff bound
极其复杂。实际上，如果不对头之间的-dependence做额外Assumptions（如 \(\beta\)-混合或
Dobrushin Conditions)，Theorem 1 形式的独立指数界\textbf{不适用于} Multi-Head
Spring。实际可得的界会\textbf{松得多}（因is 要用 union bound 或
Azuma-Hoeffding 处理 martingale -dependence序列)。

\subsubsection{3.3 对 SE-1（Robbins-Monro
收敛)的影响}\label{ux5bf9-se-1robbins-monro-ux6536ux655bux7684ux5f71ux54cd}

\textbf{SE-1 的原始陈述}：Spring 的参数 \(\theta_t\) 通过以下方式更新：
\[\theta_{t+1} = \theta_t - \alpha_t \cdot \nabla \mathcal{L}(\theta_t) + \xi_t\]

其中
\(\mathbb{E}[\xi_t | \mathcal{F}_t] = 0\)，\(\text{Var}(\xi_t | \mathcal{F}_t) \leq \sigma^2\)。在
Robbins-Monro Conditions下：
\[\sum_{t=1}^{\infty} \alpha_t = \infty, \quad \sum_{t=1}^{\infty} \alpha_t^2 < \infty\]

has  \(\theta_t \xrightarrow{a.s.} \theta^*\)（在凸性Assumptions下)。

\textbf{Proposition 3.2（Multi-Head Spring 下的收敛Conditions)}：

\begin{enumerate}
\def\labelenumi{(\arabic{enumi})}
\item
  \textbf{步长Conditions（\(\alpha_t\) 的渐近linesis )不变}：Conditions
  \(\sum \alpha_t = \infty\) 和 \(\sum \alpha_t^2 < \infty\)
  仅-dependence于步长调度，不-dependence于参数数量。
\item
  \textbf{但has 效收敛要求更小的步长}：
  \[\boxed{\alpha_t^{\text{MH}} \leq \frac{\alpha_t^{\text{single}}}{\sqrt{K}} \cdot \frac{\sigma^2_{\text{single}}}{\sigma^2_{\text{MH}}}}\]
\end{enumerate}

其中
\(\sigma^2_{\text{MH}} = \sigma^2_{\text{single}} \cdot (1 + \gamma \cdot (K - 1))\)，\(\gamma \in [0, 1]\)
是头间梯度干扰系数。

\emph{Proof概要}：Multi-Head Spring
的梯度方差随头数增加。每个头的Note意力权重 \(\alpha_{ij}^{(k)}\)
对共享Status原子 \(\{s_j\}\) 的-dependence引入了交叉头梯度项。对于损失
\(\mathcal{L} = \mathbb{E}[\|\text{Spring}_{\text{MH}}(s_i) - s_i^*\|^2]\)：
\begin{align*}
\nabla_{\theta_k} \mathcal{L} &= \mathbb{E}\left[(\text{Spring}_{\text{MH}} - s_i^*)^T W^O_{:,k} \cdot \nabla_{\theta_k} \text{head}_k\right] \\
\text{Cov}(\nabla_{\theta_k} \mathcal{L}, \nabla_{\theta_l} \mathcal{L}) &\neq 0 \quad (\text{通过共享的 } W^O \text{ 和 } s_i^*)
\end{align*}

梯度噪声的has 效方差is ：
\[\sigma^2_{\text{MH}} = \frac{1}{K} \sum_{k=1}^{K} \text{Var}(\nabla_{\theta_k} \mathcal{L}) + \frac{1}{K^2} \sum_{k \neq l} \text{Cov}(\nabla_{\theta_k} \mathcal{L}, \nabla_{\theta_l} \mathcal{L})\]

第一项是 \(O(1)\)（每个头的方差与单头相同，但除以 \(K\))，第二项引入
\(O((K-1)/K) \approx O(1)\)
的额外方差（当交叉协方差非零时)。\(\square\)

\begin{enumerate}
\def\labelenumi{(\arabic{enumi})}
\setcounter{enumi}{2}
\tightlist
\item
  \textbf{收敛目标From 全局Optimal 退化is 驻点}：
\end{enumerate}

Multi-Head Spring 的损失景观具has \textbf{排列对称性}（交换 head \(k\) 和
head \(l\) 产生相同的函数)。这意味着has  \(K!\)
个等价的驻点。损失景观高度非凸，Robbins-Monro 只能保证收敛到
\(\nabla \mathcal{L} \approx 0\)（驻点)，不能保证全局Optimal 。

\textbf{修改后的收敛Theorem（SE-1-MH)}：

在 Robbins-Monro 步长Conditions和梯度 Lipschitz 连续性的前提下：
\[\liminf_{t \to \infty} \mathbb{E}[\|\nabla \mathcal{L}(\theta_t)\|^2] \leq \frac{C \cdot \sigma^2_{\text{MH}} \cdot \bar{\alpha}}{\text{（与 } K \text{ None关的常数)}}\]

其中 \(\bar{\alpha} = \limsup_{t \to \infty} \alpha_t\)。当 \(K\) 增大时
\(\sigma^2_{\text{MH}}\)
增大，收敛的残差梯度范数也增大。\textbf{多头使收敛变得更差（在has 限步长下)}。

\subsubsection{3.4
过参数化Conditions}\label{ux8fc7ux53c2ux6570ux5316ux6761ux4ef6}

\textbf{Definition 5（has 效样本量)}：Spring 动力学通过记忆库 \(M_t\)
产生训练信号。设 \(T_{\text{eff}}\)
is 每个Status原子在自进化过程中产生的has 效''监督信号''数量（通过 Spring
重构误差提供)。则总has 效样本量is 
\(N_{\text{eff}} = N \cdot T_{\text{eff}}\)。

\textbf{Proposition 3.3（过参数化临界头数)}：

对于\textbf{Standard 多头}（每头降维 \(d_k = d_s/K\))：
\[|\theta_{\text{MH}}| = \underbrace{K \cdot (2d_k d_s + d_v d_s)}_{\text{Q, K, V 投影}} + \underbrace{K d_v d_s}_{W^O} = 4 d_s^2 \quad (\text{当 } d_k = d_v = d_s/K)\]

总参数\textbf{不-dependence \(K\)} ------
Standard 设计通过降维保持了参数恒定。在此情况下，Multi-Head Spring
\textbf{不引入额外的过参数化风险}。

对于\textbf{全维多头}（每头
\(d_k = d_s\)，各头关Note不同物理子空间但可访问全维)：
\[|\theta_{\text{MH}}| = d_s^2 \cdot (3K + 1)\]

\textbf{Theorem 3.2（过拟合临界Conditions)}：
\[\boxed{K_{\text{crit}} = \max\left(1, \left\lfloor \frac{N \cdot T_{\text{eff}} / d_s^2 - 1}{3} \right\rfloor \right)}\]

当 \(K > K_{\text{crit}}\)
时，模型参数多于has 效训练信号，\textbf{泛化误差以概率 \(\geq 1 - \delta\)
to少is }：
\[\text{GenError}(K) \geq \Omega\left(\sqrt{\frac{d_s^2 \cdot (3K + 1)}{N \cdot T_{\text{eff}}}}\right)\]

（由Standard  Rademacher 复杂度界得出)。

\textbf{数值示例}（保守Assumptions)： - \(N = 500\)
Status原子，\(d_s = 64\)，\(T_{\text{eff}} = 5\) -
\(K_{\text{crit}} = \lfloor (500 \cdot 5 / 4096 - 1) / 3 \rfloor = \lfloor (0.61 - 1) / 3 \rfloor = \max(1, -0.13) = 1\)

\textbf{Conclusion}：对于适 in the State Space，即使是 \(K = 2\)
个全维头也可能已经过参数化。对于Standard 降维多头，\(K\)
可以更大（因is 参数恒定)。

\subsubsection{3.5 对 SE-2（Doob
Martingale)的影响}\label{ux5bf9-se-2doob-martingaleux7684ux5f71ux54cd}

SE-2
声称参数的后验是鞅：\(\mathbb{E}[\theta_{t+1} | \mathcal{F}_t] = \theta_t\)。

对于 Multi-Head
Spring，\textbf{鞅性质在联合参数空间上保持}（因is 它是贝叶斯更新的直接后果)，但：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{边际鞅性质可能失效}：对于单个头的参数
  \(\theta_t^{(k)}\)，\(\mathbb{E}[\theta_{t+1}^{(k)} | \mathcal{F}_t] \neq \theta_t^{(k)}\)（因is 更新涉及所has 头的交叉信息)。
\item
  \textbf{鞅差序列的方差增大}：
  \[\text{Var}(\theta_{t+1} - \theta_t | \mathcal{F}_t) = O(K)\]

  这意味着收敛的集中度降低。Azuma-Hoeffding 给出的置信区间宽度is 
  \(O(\sqrt{K \cdot t})\) 而非 \(O(\sqrt{t})\)。
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4. 组合Analysis}\label{ux7ec4ux5408ux5206ux6790}

\subsubsection{4.1 联合形式化}\label{ux8054ux5408ux5f62ux5f0fux5316}

两个组件同时添加时的Complete流水线：

\[\boxed{s_i \xrightarrow{\text{PPE}} h_i = \phi(s_i) + \text{PE}(p_i) \xrightarrow{\text{MH-Spring}} \text{Spring}_{\text{MH}}(h_i, \mathcal{H})}\]

其中 \(\mathcal{H} = \{h_1, \ldots, h_N\}\) 是编码后的Status原子集合。

\subsubsection{4.2 Theorem 1 的联合界 ---
交叉项Analysis}\label{theorem-1-ux7684ux8054ux5408ux754c-ux4ea4ux53c9ux9879ux5206ux6790}

\textbf{Proposition 4.1（联合Detection Margin的分解)}：
\[\boxed{\Delta_s^{\text{combined}} = \Delta_s + \delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}}\]

其中交叉项 \(\delta_s^{\text{cross}}\) 满足：
\[\delta_s^{\text{cross}} = \text{Cov}_{\text{experts}}\left(\text{Benefit}_{\text{PE}}, \text{Benefit}_{\text{MH}}\right)\]

即 PPE 的边际收益与 Multi-Head 的边际收益在专家间的协方差。

\textbf{展开}： \begin{align*}
F_1^{\text{combined}} &\geq 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M_{\text{eff}} \cdot (\Delta_s + \delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2\right) \\
&= 1 - \frac{1}{\eta} \sum_s \rho_s \cdot \exp\left(-2M_{\text{eff}} \cdot \left[\Delta_s^2 + 2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + (\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2\right]\right)
\end{align*}

\textbf{超加性 vs 次加性Analysis}：

令
\(A_s = \exp(-2M_{\text{eff}}\Delta_s^2)\)（原始误差)，\(B_s^{\text{PE}}, B_s^{\text{MH}}, B_s^{\text{both}}\)
分别is 单独 PPE、单独 MH、两者结合的误差。则：

\[\log \frac{B_s^{\text{both}}}{A_s} = -2M_{\text{eff}} \cdot [2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + (\delta_s^{\text{PE}} + \delta_s^{\text{MH}} + \delta_s^{\text{cross}})^2]\]

\[\log \frac{B_s^{\text{PE}} \cdot B_s^{\text{MH}}}{A_s^2} = -2M_{\text{eff}} \cdot [2\Delta_s(\delta_s^{\text{PE}} + \delta_s^{\text{MH}}) + (\delta_s^{\text{PE}})^2 + (\delta_s^{\text{MH}})^2]\]

两者之差给出（在对数空间中)：
\[\text{Interaction} = -2M_{\text{eff}} \cdot [2\Delta_s \delta_s^{\text{cross}} + 2\delta_s^{\text{PE}}\delta_s^{\text{MH}} + 2\delta_s^{\text{cross}}(\delta_s^{\text{PE}} + \delta_s^{\text{MH}}) + (\delta_s^{\text{cross}})^2]\]

\textbf{超加性Conditions}（组合收益 \textgreater{}
单独收益之和)：交叉项is 负（减少误差)，即：
\[\delta_s^{\text{cross}} \cdot (2\Delta_s + 2\delta_s^{\text{PE}} + 2\delta_s^{\text{MH}} + \delta_s^{\text{cross}}) + 2\delta_s^{\text{PE}}\delta_s^{\text{MH}} < 0\]

当 \(\delta_s^{\text{PE}} > 0\) 且
\(\delta_s^{\text{MH}} > 0\)（两者都改善检测)且
\(\delta_s^{\text{cross}} > 0\)（正向交互)时满足：
\[\boxed{\text{Superadditive when: } \delta_s^{\text{PE}} > 0, \delta_s^{\text{MH}} > 0, \delta_s^{\text{cross}} > 0}\]

\textbf{次加性Conditions}（组合收益 \textless{}
单独收益之和)：当两者之一或交叉项is 负。

\textbf{实际Judge }：Multi-Head Spring 可以利用 PPE
提供的空间局部性来实现更has 意义的Note意力模式（例如，头 \(k\) 关Note物理Location
\(p_i\) 邻域内的Status原子)。这种协同大概率是\textbf{正向}的，即
\(\delta_s^{\text{cross}} > 0\)。但如果没has Explicit 物理约束，多头可能学到虚假的跨维相关性，此时
\(\delta_s^{\text{cross}}\) 可能是负的。

\subsubsection{4.3 Theorem 2
的联合界}\label{theorem-2-ux7684ux8054ux5408ux754c}

Characteristics弱点是次加性地恶化的（因is 每个组件都可能引入额外的弱点)：

\[\boxed{\delta_{\text{combined}} = \delta + \delta_{\text{PE}} + \delta_{\text{MH}} + \delta_{\text{cross}} \geq \max(\delta + \delta_{\text{PE}}, \delta + \delta_{\text{MH}})}\]

其中： - \(\delta_{\text{PE}} = 2\varepsilon_{\text{PE}} / C_F^2\)（来自
§2.4) -
\(\delta_{\text{MH}} \propto \frac{d_s^2 \cdot (3K + 1)}{N \cdot T_{\text{eff}}}\)（来自过参数化，§3.4)
- \(\delta_{\text{cross}} \geq 0\)：PPE 的不完美编码 + Multi-Head
的过参数化产生复合效应

因此：
\[F_{1,\text{SCX}}^{\text{combined}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta_{\text{combined}}}{2}}\]

\textbf{最坏情况（两者都很差)}：界比单独使用任一组件更松。

\textbf{最好情况（完美 PPE + 参数充足的
MH)}：\(\delta_{\text{PE}} = \delta_{\text{MH}} = 0\)，界回到原始
Theorem 2。

\subsubsection{4.4 Cercis Score
的修改}\label{cercis-score-ux7684ux4feeux6539}

\textbf{推荐修改}：
\[\boxed{S'(s_i) = Q_{\text{MH}}(h_i) + \eta(t) \cdot N(s_i) - \lambda \cdot \mathcal{R}_{\text{diversity}}}\]

其中：
\[Q_{\text{MH}}(h_i) = \frac{1}{K} \sum_{k=1}^{K} w_k \cdot Q(\text{head}_k(h_i))\]

\(w_k\) 是头可靠性权重（可通过头输出与多数投票的一致性来估计)。

正则化项：
\[\mathcal{R}_{\text{diversity}} = (K - 1) \cdot \left(1 - \frac{1}{K} \sum_{k=1}^{K} \left\|\text{head}_k - \frac{1}{K}\sum_{j=1}^{K} \text{head}_j\right\|^2\right)\]

惩罚头之间的过度相似（鼓励多样性)，防止退化到单头。

\(\lambda > 0\) 是正则化强度。当 \(K = 1\) 时
\(\mathcal{R}_{\text{diversity}} = 0\)（自动退化is 原始形式)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{5. Honest Critique（Honest
Critique)}\label{ux8bdaux5b9eux66b4ux51fbhonest-critique}

\subsubsection{5.1
数学保证被削弱的地方}\label{ux6570ux5b66ux4fddux8bc1ux88abux524aux5f31ux7684ux5730ux65b9}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1622}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2432}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.3514}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2432}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Location
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
原始保证
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
削弱后的Status
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
严重程度
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{Theorem 1 + MH} & Chernoff bound on i.i.d. experts &
头输出相关，i.i.d. Assumptionsdoes not hold。严格 bound 需要 \(\beta\)-mixing
Conditions，而该Conditions可能不满足 & \textbf{高} \\
\textbf{SE-1 + MH} & Robbins-Monro → 全局Optimal （凸性下) &
高度非凸损失景观（\(K!\) 对称驻点)→ 只能保证驻点，不能保证全局Optimal  &
\textbf{高} \\
\textbf{Theorem 2 + MH} & 紧致 Fano upper bound &
过参数化时界仍然成立但变得\textbf{过松}（vacuous)，实际性能可能远差于界
& \textbf{中} \\
\textbf{Theorem 3 + learnable  PPE} & 最坏情况不可区分 & learnable  PE
使得两个世界可区分 → \textbf{Theorem 3 被breaking }（但这是好事) &
\textbf{中（实际上has 益)} \\
\textbf{SE-2 + MH} & 联合鞅性质保持 & 边际鞅性质可能失效；鞅差方差
\(O(K)\) 增大，置信区间变宽 & \textbf{中} \\
\textbf{组合 Theorem 1} & 独立 Chernoff bound & has 效 \(M_{\text{eff}}\)
是启发式的，非严格。交叉项 \(\delta_s^{\text{cross}}\) 难以From 理论upper bound定
& \textbf{高} \\
\end{longtable}

\subsubsection{5.2
完全None影响的地方（加了等于白加)}\label{ux5b8cux5168ux65e0ux5f71ux54cdux7684ux5730ux65b9ux52a0ux4e86ux7b49ux4e8eux767dux52a0}

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.3529}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.6471}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Conditions
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
is 什么None效
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{\(I(Y; P \mid X) = 0\)（Location不携带Label信息)} & PPE
是纯噪声。Theorem 2 的界不变。Theorem 1 的 \(\Delta_s\)
不变。参数全部浪费 \\
\textbf{\(\rho_{\text{eff}} = 0\)（所has 头坍塌is 单一模式)} & Multi-Head
等价于单头 + \(K\) 倍冗余参数。Theorem 1 None增益。SE-1 收敛到相同点 \\
\textbf{fixed 、信息论可逆的 PE} & 仅是变量替换。KL 散度不变 → Theorem 4
不变。None实质贡献 \\
\textbf{\(K > K_{\text{crit}}\) 且None正则化} & 过度参数化 → 泛化误差主导
→ 任何Theorem的界都变is 空集（vacuous) \\
\textbf{物理维度不相关（例如头关Note物理维度 D，但 D 与任务None关)} &
该头的has 效 \(\Delta_s = 0\) → 纯噪声贡献 → 降低has 效
\(M_{\text{eff}}\) \\
\end{longtable}

\subsubsection{5.3
特定Conditions下breaking Theorem的情况}\label{ux7279ux5b9aux6761ux4ef6ux4e0bux7834ux574fux5b9aux7406ux7684ux60c5ux51b5}

\textbf{Theorem 3 被breaking 的Conditions（5 个Conditions，按严重程度排序)}：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{learnable  PPE（最严重)}：§2.5 已Analysis。训练data in the Label噪声导致
  \(\text{PE}_{W_A} \neq \text{PE}_{W_B}\)。结果：两个世界可区分，Theorem
  3 的不可区分性does not hold。

  \emph{评价}：\textbf{好事}。Theorem 3
  是最坏情况不可能性结果。如果Location信息能打破这个不可能性，Description PPE
  has 用。
\item
  \textbf{Multi-Head Spring  in the 维度泄漏}：If 某个头被设计is 关Note物理维度
  A，但实际上通过 softmax 的全局Note意力也能访问维度 B
  的信息，则头的''专业化''是虚假的。这可能导致该头学到Label相关的虚假相关性，改变
  \(P_{\text{clean}}\) 和 \(P_{\text{noisy}}\) 的估计。
\item
  \textbf{\(K > N\)（头数超过Status原子数)}：多头的Note意力模式None法被data唯一确定。存在None限多个参数配置产生相同的
  \(\text{Spring}_{\text{MH}}(s_i)\)。Theorem 3
  的构造-dependence于Data Distribution的唯一性------当参数化是欠定的，两个世界的分布可能has 细微Difference。
\item
  \textbf{PPE 维度与物理维度对齐}：如果 \(\text{PE}(p_i)\) 的维度与
  Multi-Head Spring
  中某头的物理维度\textbf{正交}（即编码碰巧擦除了该头需要的信息)，则该头的Note意力模式会产生系统性偏差。这引入了一个
  Theorem 3 构造中未考虑的自由度。
\item
  \textbf{\(\eta(t)\) 衰减过快 + Multi-Head}：如果探索权重 \(\eta(t)\)
  衰减过快，Multi-Head Spring 可能过早地将某些模式''冻结''在次优Status。在
  Theorem 3
  的双世界构造中，一个世界可能学到更好的模式（因is Label更干净)，而另一个世界学到差的模式------使两个世界可区分。
\end{enumerate}

\subsubsection{5.4
最诚实的Summary}\label{ux6700ux8bdaux5b9eux7684ux603bux7ed3}

\textbf{PPE 的底线}： - \textbf{对 Theorem 1}：如果物理Location与任务相关 →
has 正向贡献（\(\delta_s^{\text{PE}} > 0\))。如果None关 →
没has 影响。Chernoff 结构保持。 - \textbf{对 Theorem 2}：不完美的编码以
\(\sqrt{\delta + O(\varepsilon_{\text{PE}})}\)
扩大upper bound。完美编码不影响upper bound。 - \textbf{对 Theorem
3}：fixed 编码preserves unidentifiability。learnable 编码\textbf{breaking }Theorem
3------但这是\textbf{进展}而非退步。 - \textbf{对 Theorem
4}：不减少误差指数。has 用时增加指数。

\textbf{Multi-Head Spring 的底线}： - \textbf{对 Theorem
1}：头\textbf{is NOT }独立专家。严格界比 naive \(M \times K\)
松得多。头相关性的Complete处理极其Difficulty。 - \textbf{对
SE-1}：收敛到驻点（不保证全局Optimal )。has 效步长上限以 \(O(1/\sqrt{K})\)
缩小。收敛更慢。 -
\textbf{过参数化}：对于典型的State Space大小，\(K_{\text{crit}}\) 可能只has 
1-3 个头。超过此数的头会产生泛化惩罚。

\textbf{组合的最诚实评价}： - PPE
是\textbf{更has 价值的}组件------它在理论上更清晰，影响更可预测，且风险更低
- Multi-Head Spring 的\textbf{理论风险更高}------它breaking 了 Theorem 1
Optimal 雅的部分（i.i.d. 专家的 Chernoff bound) -
两者的\textbf{交叉项在理论上是正的}（可能产生超加性收益)，但\textbf{在实际中可能is 负}（额外的复杂性引入新的失败模式)

\textbf{一句话Summary}：PPE
是一个安全的赌Note，数学上几乎纯粹has 益（除了learnable  PPE breaking  Theorem
3，但那是好事)。Multi-Head Spring
是一个高风险赌Note------它在数学上削弱了 Theorem 1 的严格性，使 SE-1
收敛性变差，且很has 可能过参数化。组合时，PPE 的收益可能被 Multi-Head
的复杂性部分抵消。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{6. open problems}\label{ux5f00ux653eux95eeux9898}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{严格的多头 Chernoff 界}：能否在头的-dependence结构上建立
  \(\beta\)-mixing Conditions，使得 Theorem 1 的指数界可以严格Extension到多头情况？
\item
  \textbf{PPE 的Optimal 频率}：在正弦编码中，频率参数
  \(\omega_j = 10000^{-2j/d}\)
  是is 自然语言设计的。物理Location的Optimal 频率谱是什么？能否From 物理对称性Derivation？
\item
  \textbf{头数 \(K\) 的自适应选择}：能否From data中学习Optimal 的 \(K\)（类似于
  Bayesian non-parametric  in the 中餐馆过程)，在过参数化之前停止？
\item
  \textbf{Theorem 3
  的修改}：如何在Theorem Statement中加入''前提Conditions：不使用data-dependence的Location编码''来保持其数学Complete性？
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{参考文献（框架内部)}\label{ux53c2ux8003ux6587ux732eux6846ux67b6ux5185ux90e8}

\begin{itemize}
\tightlist
\item
  SCX Project (2026). Theorem 1-4: Multi-expert consistency, weak
  feature limits, unidentifiability, and minimax optimality.
  \texttt{theory/theorems/}
\item
  SCX Project (2026). Spring Dynamics, SE-1 (Robbins-Monro convergence),
  SE-2 (Doob martingale). \texttt{theory/self\_evolution/}
\item
  SCX Project (2026). State Crystallization: Physics-driven
  discretization of continuous observables.
  \texttt{theory/propositions/}
\item
  Wainwright, M. (2019). \emph{High-Dimensional Statistics: A
  Non-Asymptotic Viewpoint}. Cambridge. （Chernoff-Hoeffding-Stein
  的Standard 参考)
\item
  Boucheron, S., Lugosi, G., \& Massart, P. (2013). \emph{Concentration
  Inequalities}. Oxford. （-dependence变量的集中不等式)
\item
  Vaswani, A., et al.~(2017). Attention Is All You Need. \emph{NeurIPS}.
  （多头Note意力的原始Formula)
\item
  Su, J., et al.~(2024). RoFormer: Enhanced Transformer with Rotary
  Position Embedding. \emph{Neurocomputing}. （RoPE 的数学Foundation)
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{Analysis日期：2026-06-29}\\
\emph{作者备Note：所has TheoremNumber指 SCX
框架内部Theorem，非本文序号。本Analysis的数学处理在可处理的范围内是严格的；已明确标Note所has 近似和启发式的部分。诚实的不可处理性（如-dependence头的
Chernoff bound)已被诚实记录。}

\end{document}
