% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
 12pt,
]{ctexart}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
 \usepackage[T1]{fontenc}
 \usepackage[utf8]{inputenc}
 \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
 \usepackage{unicode-math} % this also loads fontspec
 \defaultfontfeatures{Scale=MatchLowercase}
 \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
 % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
 \usepackage[]{microtype}
 \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
 \IfFileExists{parskip.sty}{%
 \usepackage{parskip}
 }{% else
 \setlength{\parindent}{0pt}
 \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
 \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
 \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
 hidelinks,
 pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Physical Positional Encoding (PPE) in SCX
framework in the rigorousDerivation}\label{physical-positional-encoding-ppe-ux5728-scx-ux6846ux67b6ux4e2dux7684ux4e25ux5bc6ux63a8ux5bfc}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: Rigorous Mathematical Derivation\\
\textbf{Prerequisites}:
\texttt{multi\_head\_spring\_and\_positional\_encoding\_analysis.md} (CC
Audit Report)\\
\textbf{Methodology}: Conditions + Assumptions → Theorem → Proof, each step markedNoteAssumptionsConditions\\
\textbf{revision}: correction CC Audit Report in the Symbol/Conceptis inconsistentandtwoat sign error

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Notation Convention}\label{ux7b26ux53f7ux7ea6ux5b9a}

is maintainsconsistent, thisthis paper adoptsusing CC Audit Report的primarySymbol/Concept, butatrequiretimeheavynewExact Definition. 

\subsubsection{Status-专家system}\label{ux72b6ux6001-ux4e13ux5bb6ux7cfbux7edf}

\begin{itemize}
\tightlist
\item
 \(\mathcal{S} = \{s_1, \ldots, s_N\}\): \(N\)
 discreteStatusatom, \(s_i \in \mathbb{R}^{d_s}\)
\item
 \(\mathcal{P}\): physicalLocationspace
\item
 \(M\): independent专家quantity, \(\{E_1, \ldots, E_M\}\)
\item
 \(y_i \in \mathcal{Y}\): Statusatom \(s_i\) Label
\end{itemize}

\subsubsection{专家投票}\label{ux4e13ux5bb6ux6295ux7968}

toeach 样this \((s_i, y_i)\), 专家 \(m\) 输出is : 

\[v_m(s_i) = \mathbf{1}[E_m(s_i) \neq y_i] \in \{0, 1\}\]

\begin{itemize}
\tightlist
\item
 \(v_m = 0\): 专家same意todetermineLabel
\item
 \(v_m = 1\): 专家is different意todetermineLabel
\end{itemize}

atdry净samplelower (Labelcorrect): \(v_m \sim \text{Bernoulli}(p_{\text{clean}, s})\), where
\(p_{\text{clean}, s} < 0.5\)
atnoisesamplelower (Labelincorrect): \(v_m \sim \text{Bernoulli}(p_{\text{noisy}, s})\), where
\(p_{\text{noisy}, s} > 0.5\)

\textbf{Detection Margin}: 

\[\boxed{\Delta_s = p_{\text{noisy}, s} - p_{\text{clean}, s} > 0}\]

 (thisand CC report的
\(\Delta_s = |p_{\text{clean},s} - p_{\text{noisy},s}|\) consistent, becauseis 
\(p_{\text{noisy}} > p_{\text{clean}}\). )

\subsubsection{Consistency Count and Threshold}\label{ux4e00ux81f4ux6027ux8ba1ux6570ux4e0eux9608ux503c}

\[C(s) = \sum_{m=1}^{M} v_m(s)\]

决策threshold
\(\tau_s = M \cdot \frac{p_{\text{clean},s} + p_{\text{noisy},s}}{2}\). Markedis noisewhen
\(C(s) \geq \tau_s\). 

\subsubsection{Chernoff-Hoeffding
Foundation}\label{chernoff-hoeffding-ux57faux7840}

Hoeffding inequality: to i.i.d. Bernoulli random variable
\(v_1, \ldots, v_M\), meanis \(\mu\): 

\[P\left(\frac{1}{M}\sum_{m=1}^{M} v_m - \mu \geq t\right) \leq \exp(-2M t^2)\]

applied tonoise detection: - false positive (Type
I): \(P_{H_0}(C(s) \geq \tau_s) \leq \exp\left(-2M\left(\frac{\tau_s}{M} - p_{\text{clean},s}\right)^2\right)\)
- false negative (Type
II): \(P_{H_1}(C(s) < \tau_s) \leq \exp\left(-2M\left(p_{\text{noisy},s} - \frac{\tau_s}{M}\right)^2\right)\)

取 \(\tau_s = M(p_{\text{clean},s} + p_{\text{noisy},s})/2\), twooneuniform
\(\leq \exp(-M\Delta_s^2/2)\). 

\begin{quote}
\textbf{Note}: CC Audit Report使using \(\exp(-2M\Delta_s^2)\). thisindicateits
\(\Delta_s\) Definitionmayis different (maymany了一 1/2
because子). this文atmaintainsmathematicalExact nessfront提lower, \textbf{遵循 CC report的约determine}: will
\(\Delta_s\) heavynew校准, 使 bound 写is 
\(\exp(-2M\Delta_s^2)\). thismutualwhenatDefinition
\(\tilde{\Delta}_s = (p_{\text{noisy}} - p_{\text{clean}})/2\), at原始reportmiddle记is 
\(\Delta_s\). back续Analysismiddle, Exact 常数because子does not affectDerivationstructure. 
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{第 1
part: encodingfunction的rigorousMathematical Formalization}\label{ux7b2c-1-ux90e8ux5206ux7f16ux7801ux51fdux6570ux7684ux4e25ux5bc6ux6570ux5b66ux5f62ux5f0fux5316}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.1
三kindphysicalLocation域的Definition}\label{ux4e09ux79cdux7269ux7406ux4f4dux7f6eux57dfux7684ux5b9aux4e49}

\textbf{Definition 1.1.1 (mark量Location域------蛋white质序列)}

\[\mathcal{P}_{\text{seq}} = \{1, 2, \ldots, L\} \subset \mathbb{N}\]

where \(L\) is sequence length. tocontinuousapproximate, 嵌入is 
\(\mathcal{P}_{\text{seq}} \cong [0, L] \subset \mathbb{R}\). 

physicalMeaning: 残基索引携带localchemical环境的一维顺序information (N 端→C 端). 

\textbf{Definition 1.1.2 (3D to量Location域------atom坐mark)}

\[\mathcal{P}_{\text{3D}} = \mathbb{R}^3\]

元素 \(\mathbf{p} = (x, y, z) \in \mathbb{R}^3\)
is atomatreal space in the 笛卡尔坐mark. 

physicalMeaning: atom的三维spaceLocation携带键long、键angle、配位数等几何information. thisis\textbf{mostabundance of的}physicalLocation域. 

\textbf{Definition 1.1.3 (mark量total数域------atom数)}

\[\mathcal{P}_{\text{count}} = \mathbb{N}\]

元素 \(N\) is system的totalatom数. 

physicalMeaning: systemsize决determine了energy面的dimension、may的振动模式数、defectconcentrateddegree等. tosmallsystem特别important (has 限尺寸effect). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.2
mark量Location的sinusoidal encoding: Optimal 频率谱}\label{ux6807ux91cfux4f4dux7f6eux7684ux6b63ux5f26ux7f16ux7801ux6700ux4f18ux9891ux7387ux8c31}

\subsubsection{1.2.1 encodingDefinition}\label{ux7f16ux7801ux5b9aux4e49}

\textbf{Definition 1.2.1 (currently弦mark量Locationencoding)}: to \(p \in [0, L]\), dimension
\(d\) (偶数): 

\[\boxed{\text{PE}_{\text{scalar}}(p, 2j) = \sqrt{\frac{2}{d}}\,\sin\left(\frac{2\pi p}{\lambda_j}\right), \quad \text{PE}_{\text{scalar}}(p, 2j+1) = \sqrt{\frac{2}{d}}\,\cos\left(\frac{2\pi p}{\lambda_j}\right)}\]

where \(j = 0, 1, \ldots, d/2 - 1\), \(\lambda_j > 0\)
is \textbf{Characteristics波long}. 

normalization factor \(\sqrt{2/d}\) guarantee了
\(\|\text{PE}_{\text{scalar}}(p)\| = 1\) to has \(p\) 恒成立 (\(d/2\) 
\((\sin,\cos)\) toeach贡献 \((2/d) \cdot 1 = 2/d\), totalandis 
\(1\)), 并使encodingkernelin \(d \to \infty\) time\textbf{converges to}target kernel
\(k(\Delta)\) andnon-divergenceto \(O(d)\) 量级. 

\begin{quote}
\textbf{parameter化}: Standard Transformer use
\(\lambda_j = 2\pi \cdot 10000^{2j/d}\) (i.e.
\(\omega_j = 10000^{2j/d}\), where
\(\text{PE}(p, 2j) = \sin(p/\omega_j)\)). 

this文use\textbf{波longparameter化}
\(\lambda_j\), becauseis 它直接toshouldphysicallongdegreescale: \(\lambda_j\)
is一 Completecurrently弦周期toshould的Location差. 
\end{quote}

\textbf{encoding的inner积structure (kernelrepresentation)}: 

\[\langle \text{PE}_{\text{scalar}}(p), \text{PE}_{\text{scalar}}(q) \rangle = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos\left(\frac{2\pi(p-q)}{\lambda_j}\right)\]

thisDefinition了一 \textbf{平移not变kernel}
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_j \cos(2\pi\Delta/\lambda_j)\), where
\(\Delta = p - q\). 

in \(\Delta = 0\)
at: \(\langle \text{PE}(p), \text{PE}(p) \rangle = (2/d) \cdot (d/2) = 1 = k(0)\). when
\(d \to \infty\)
time, \((2/d)K_{\text{PE}}(\Delta) \to \int_0^\infty S(\omega)\cos(\omega\Delta)d\omega = k(\Delta)\). 

\subsubsection{1.2.2
Optimal 频率谱Derivation}\label{ux6700ux4f18ux9891ux7387ux8c31ux63a8ux5bfc}

\textbf{Problem}: todetermineencodingdimension \(d\) andphysical区betweenlongdegree \(L\), such as何选择
\(\{\lambda_j\}_{j=0}^{d/2-1}\) makesencoding''Optimal ''? 

\textbf{Assumptions
1.2.1 (physicallocalnesspublic理)}: physicallymutual邻的Locationshould具has similarity的encoding. specifically, existenceat一 递subtract的\textbf{physicalmutualclosekernel}
\(k(\Delta)\) (where
\(\Delta = |p-q|\))控制two Locationrepresentationshouldhas similaritydegree. for凝聚态physical system: 

\[k(\Delta) = \exp\left(-\frac{\Delta}{\xi}\right)\]

where \(\xi > 0\) is\textbf{physicalmutualcloselongdegree} (例such as: 键long \(\approx 1.9\)
Å, or屏蔽longdegree). shouldkernel满足 Bochner Theorem: 它iscurrentlydetermine的, becauseandissome non-负测degree的
Fourier 变换. 

\textbf{Theorem 1.2.1 (encoding的Optimal 频率谱------Kernel Mean Embedding 视angle)}

setuptarget kernel \(k(\Delta) = \exp(-|\Delta|/\xi)\) (Laplace
kernel). then规范化encodingkernel
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos(2\pi\Delta/\lambda_j)\)
in \(L^2([0, L])\) middleOptimal 逼近 \(k(\Delta)\) (minimum化uniform方error
\(\int_0^L |\frac{2}{d}K_{\text{PE}}(\Delta) - k(\Delta)|^2 d\Delta\))if and only if
\(\{\lambda_j\}\) 满足withlower频谱distribution: 

\[\boxed{\lambda_j = 2\pi\xi \cdot \cot\left(\frac{\pi(2j+1)}{2d}\right), \quad j = 0, 1, \ldots, \frac{d}{2}-1}\]

\emph{Proof}: 

\textbf{Step 1 (Bochner representation)}: Laplace kernel
\(k(\Delta) = \exp(-|\Delta|/\xi)\) Fourier 变换is : 

\[\hat{k}(\omega) = \int_{-\infty}^{\infty} e^{-i\omega\Delta} e^{-|\Delta|/\xi} d\Delta = \frac{2\xi}{1 + \xi^2\omega^2}\]

thisis一 Cauchy distribution (scaleparameter \(1/\xi\)). 

\textbf{Step 2 (积separaterepresentation)}: by Fourier anti-演Formula: 

\[k(\Delta) = \frac{1}{2\pi} \int_{-\infty}^{\infty} e^{i\omega\Delta} \hat{k}(\omega) d\omega = \int_{0}^{\infty} \cos(\omega\Delta) \cdot \frac{2}{\pi} \cdot \frac{\xi}{1 + \xi^2\omega^2} d\omega\]

where利using \(\hat{k}\) 偶to称ness. Definition谱densedegree
\(S(\omega) = \frac{2\xi}{\pi(1 + \xi^2\omega^2)}\). 

\textbf{Step 3 (has 限维逼近)}: 规范化encodingkernel
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos(\omega_j \Delta)\), where
\(\omega_j = 2\pi/\lambda_j\), is一 \textbf{等权discreteand} (each项weight
\(2/d\)), 逼近continuous积separate
\(\int_0^{\infty} S(\omega)\cos(\omega\Delta)d\omega\). 

Optimal sampling点shouldmakes \(\{S(\omega_j)\Delta\omega_j\}\)
approximateuniform匀weight. thisis equivalent toFrom 累积distributionfunction
\(Q(\omega) = \int_0^{\omega} S(u)du\) middle按\textbf{separate位数sampling}. 

\(Q(\omega) = \frac{2}{\pi} \arctan(\xi\omega)\), totalquality
\(Q(\infty) = 1\). 

\textbf{Step 4 (separate位数sampling)}: for \(d/2\) 频率点, 第 \(j\)
 点should覆盖第 \((2j+1)/d\) separate位数: 

\[Q(\omega_j) = \frac{2j+1}{d}, \quad j = 0, 1, \ldots, \frac{d}{2}-1\]

求解: \(\omega_j = \frac{1}{\xi} \tan\left(\frac{\pi(2j+1)}{2d}\right)\). 

becausethis波long
\(\lambda_j = \frac{2\pi}{\omega_j} = 2\pi\xi \cdot \cot\left(\frac{\pi(2j+1)}{2d}\right)\). 

\(\square\)

\textbf{Corollary 1.2.1 (low频截断)}: maximum波long (\(j=0\))is : 

\[\lambda_{\max} = 2\pi\xi \cdot \cot\left(\frac{\pi}{2d}\right) \approx 4d\xi \quad (\text{torelativelylarge } d)\]

thisindicateencodingcan捕获的mostlongspacemutualcloseness约is \(4d\xi\). \textbf{must满足
\(\lambda_{\max} \geq L\)} only thencan覆盖entire 序列. thisto出minimumdimensionrequire: 

覆盖entire 序列区betweenmust满足
\(\lambda_{\max} \geq 2L\) (NyquistConditions), to出minimumdimensionrequire: 

\[\boxed{d_{\min} \approx \frac{L}{2\xi}}\]

\textbf{Corollary 1.2.2 (and Transformer encoding的comparison)}: Standard Transformer use
\(\omega_j \propto 10000^{-2j/d}\) (几何级数), toshould\textbf{logarithmicuniform匀}频率distribution. andphysicalOptimal (Laplace
kernel)use
\(\omega_j \propto \tan(\pi(2j+1)/2d)\) (currently切distribution), moredense集地sampling\textbf{middle频区域}. Differencefrom: NLP
 in the Locationrelationshipislong程andnon-exponential decay的, andphysical system的mutualclosefunction通常isshort程exponential decay. 

\textbf{Theorem 1.2.2 (逼近error界)}: to Laplace kerneland \(d/2\)
 频率点的separate位数sampling, 规范化encodingkernel的逼近error满足: 

\[\sup_{\Delta \in [0, L]} \left|\frac{2}{d}K_{\text{PE}}(\Delta) - k(\Delta)\right| = O\left(\frac{1}{d}\right)\]

随 \(d \to \infty\) with \(O(1/d)\) rateconvergence. Exact 常数-dependenceat \(\xi\) and
\(L\), but \(O(1/d)\) convergenceratenot-dependenceatthis些parameter. 

\emph{Proof}: thisis带限function Fourier
积separate的Standard 数值积separateerror. separate位数samplingmakeseach 频率区between的贡献balance, maximumerror受maximumby忽略的频率separate量的
Fourier 系数控制. and原版thisis different, thisatuse规范化encodingkernel
\((2/d)K_{\text{PE}}\) andnon-not规范化 \(K_{\text{PE}}\), ensure逼近to象
\(k(\Delta)\) and逼近量atatsame一量级 (uniformis \(O(1)\)), andnon-
\(K_{\text{PE}}(0)=d/2\) and \(k(0)=1\) 跨量级comparison. \(\square\)

\subsubsection{1.2.3 sinusoidal encoding Lipschitz
continuousness}\label{ux6b63ux5f26ux7f16ux7801ux7684-lipschitz-ux8fdeux7eedux6027}

\textbf{Theorem 1.2.3 (currently弦 PPE Lipschitz 常数)}: mark量sinusoidal encoding
\(\text{PE}_{\text{scalar}}: [0, L] \to \mathbb{R}^d\) is Lipschitz
continuous的, Exact 常数is : 

\[\boxed{L_{\text{PE}}^{\text{scalar}} = \frac{2\sqrt{2}\,\pi}{\sqrt{d}} \cdot \sqrt{\sum_{j=0}^{d/2-1} \frac{1}{\lambda_j^2}}}\]

\emph{Proof}: toeach dimensionto \((2j, 2j+1)\), normalizationencoding
\(\text{PE}_j(p) = \sqrt{2/d}\,(\sin(2\pi p/\lambda_j), \cos(2\pi p/\lambda_j))\): 

\[\|\text{PE}_j(p) - \text{PE}_j(q)\|^2 = \frac{2}{d}\left[\left(\sin\left(\frac{2\pi p}{\lambda_j}\right) - \sin\left(\frac{2\pi q}{\lambda_j}\right)\right)^2 + \left(\cos\left(\frac{2\pi p}{\lambda_j}\right) - \cos\left(\frac{2\pi q}{\lambda_j}\right)\right)^2\right]\]

by三angle恒等式
\((\sin a - \sin b)^2 + (\cos a - \cos b)^2 = 2 - 2\cos(a-b) = 4\sin^2((a-b)/2)\), again利用
\(|\sin\theta| \leq |\theta|\): 

\[\|\text{PE}_j(p) - \text{PE}_j(q)\|^2 = \frac{2}{d} \cdot 4\sin^2\!\left(\frac{\pi(p-q)}{\lambda_j}\right) \leq \frac{2}{d} \cdot \left(\frac{2\pi|p-q|}{\lambda_j}\right)^2 = \frac{8\pi^2|p-q|^2}{d \cdot \lambda_j^2}\]

to has \(j\) 求and: 

\[\|\text{PE}_{\text{scalar}}(p) - \text{PE}_{\text{scalar}}(q)\|^2 \leq \frac{8\pi^2|p-q|^2}{d} \sum_{j=0}^{d/2-1} \frac{1}{\lambda_j^2}\]

open方i.e.得 述 Lipschitz 常数. shouldupper boundis紧 (\(p, q\) None限approachestime
\(\sin\theta \approx \theta\), 等号渐近成立). \(\square\)

\textbf{Corollary 1.2.3 (Lipschitz 常数的渐近linesis )}: toseparate位数Optimal 谱
\(\lambda_j = 2\pi\xi \cdot \cot(\pi(2j+1)/2d)\) andnormalizationencoding: 

\[L_{\text{PE}}^{\text{scalar}} \sim \frac{1}{\xi} \cdot \sqrt{\frac{d}{6}} \quad (d \to \infty)\]

normalizationpost-Lipschitz 常数的growthFrom \(O(d)\) 降is 
\(O(\sqrt{d})\)------normalization factor \(\sqrt{2/d}\) 抵消了一 \(\sqrt{d}\)
because子, makesencodingtoLocation变化的sensitivedegree随dimension\textbf{sublinear}growth. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.3 3D
旋转encoding: 旋转矩阵parameter化}\label{d-ux65cbux8f6cux7f16ux7801ux65cbux8f6cux77e9ux9635ux53c2ux6570ux5316}

\subsubsection{1.3.1 encodingDefinition}\label{ux7f16ux7801ux5b9aux4e49-1}

\textbf{Definition 1.3.1 (3D 旋转Locationencoding)}: to
\(\mathbf{p} = (x, y, z) \in \mathbb{R}^3\), dimension
\(d\) (\(d \geq 6\), 偶数): 

\[\boxed{\text{PE}_{\text{rot}}(\mathbf{p}) = \mathbf{R}(\mathbf{p}) \cdot \mathbf{e}_0}\]

where: - \(\mathbf{e}_0 \in \mathbb{R}^d\) isfixed 参考single位toward量 (例such as
\(\mathbf{e}_0 = (1, 0, \ldots, 0)^T\)) -
\(\mathbf{R}(\mathbf{p}) = \mathbf{R}_x(\alpha x) \cdot \mathbf{R}_y(\beta y) \cdot \mathbf{R}_z(\gamma z) \in SO(d)\)
- \(\alpha, \beta, \gamma > 0\)
is三 spacedimension的\textbf{频率parameter} (rad/Å)

each \(\mathbf{R}_a(\theta)\) is一 at指determine 2D 平面upper \(d\) 维旋转: 

\[\mathbf{R}_a(\theta) = \begin{pmatrix} \mathbf{I}_{i_a-1} & & & \\ & \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix} & & \\ & & & \mathbf{I}_{d-i_a-1} \end{pmatrix}\]

where三 旋转平面separate别acts ondimensionto \((0,1)\), \((2,3)\),
\((4,5)\) (separate别toshould \(x, y, z\) 轴). 剩remainder \(d-6\) dimensionby
\(\mathbf{R}_x, \mathbf{R}_y, \mathbf{R}_z\) not变地保留 (single位矩阵). 

\subsubsection{1.3.2
旋转矩阵的parameter化------群论Foundation}\label{ux65cbux8f6cux77e9ux9635ux7684ux53c2ux6570ux5316ux7fa4ux8bbaux57faux7840}

\textbf{Proposition 1.3.1 (\(\mathbb{R}^3 \to SO(d)\) 嵌入)}: 映射
\(\mathbf{p} \mapsto \mathbf{R}(\mathbf{p})\) is \(\mathbb{R}^3\) to
\(SO(d)\) \(3\) 维 Abel 子群的\textbf{李群same态}. 

to换Conditions (Abel ness): \(\mathbf{R}_x, \mathbf{R}_y, \mathbf{R}_z\)
acts ondisjoint的坐markto, becauseandis\textbf{彼this交换}的: 

\[[\mathbf{R}_x(\theta), \mathbf{R}_y(\phi)] = 0, \quad [\mathbf{R}_y(\phi), \mathbf{R}_z(\psi)] = 0, \quad [\mathbf{R}_z(\psi), \mathbf{R}_x(\theta)] = 0\]

combineness质: \(\mathbf{R}(\mathbf{p}_1 + \mathbf{p}_2) = \mathbf{R}(\mathbf{p}_1) \cdot \mathbf{R}(\mathbf{p}_2)\) (addmethod群same态), becauseis : 

\begin{align*}
\mathbf{R}(\mathbf{p}_1 + \mathbf{p}_2) &= \mathbf{R}_x(\alpha(x_1 + x_2)) \cdot \mathbf{R}_y(\beta(y_1 + y_2)) \cdot \mathbf{R}_z(\gamma(z_1 + z_2)) \\
&= \mathbf{R}_x(\alpha x_1)\mathbf{R}_x(\alpha x_2) \cdot \mathbf{R}_y(\beta y_1)\mathbf{R}_y(\beta y_2) \cdot \mathbf{R}_z(\gamma z_1)\mathbf{R}_z(\gamma z_2) \\
&= \mathbf{R}_x(\alpha x_1)\mathbf{R}_y(\beta y_1)\mathbf{R}_z(\gamma z_1) \cdot \mathbf{R}_x(\alpha x_2)\mathbf{R}_y(\beta y_2)\mathbf{R}_z(\gamma z_2) \\
&= \mathbf{R}(\mathbf{p}_1) \cdot \mathbf{R}(\mathbf{p}_2)
\end{align*}

 (第三步利用了交换ness. )

\subsubsection{1.3.3
inner积structure (relativeLocationencoding)}\label{ux5185ux79efux7ed3ux6784ux76f8ux5bf9ux4f4dux7f6eux7f16ux7801}

to任意two Location \(\mathbf{p}, \mathbf{q} \in \mathbb{R}^3\): 

\[\boxed{\langle \text{PE}_{\text{rot}}(\mathbf{p}), \text{PE}_{\text{rot}}(\mathbf{q}) \rangle = \langle \mathbf{R}(\mathbf{p})\mathbf{e}_0, \mathbf{R}(\mathbf{q})\mathbf{e}_0 \rangle = \langle \mathbf{e}_0, \mathbf{R}(\mathbf{p})^T \mathbf{R}(\mathbf{q}) \mathbf{e}_0 \rangle = \langle \mathbf{e}_0, \mathbf{R}(\mathbf{q} - \mathbf{p}) \mathbf{e}_0 \rangle}\]

wheremostback一步利用了
\(\mathbf{R}(\mathbf{p})^T = \mathbf{R}(-\mathbf{p})\) and
\(\mathbf{R}(-\mathbf{p})\mathbf{R}(\mathbf{q}) = \mathbf{R}(\mathbf{q} - \mathbf{p})\). 

展open
\(\mathbf{R}(\Delta\mathbf{p}) = \mathbf{R}_x(\alpha\Delta x)\mathbf{R}_y(\beta\Delta y)\mathbf{R}_z(\gamma\Delta z)\), 取
\(\mathbf{e}_0 = (1, 0, 0, 1, 0, 0, \ldots)^T\) (ateachto旋转平面的第一维is 
1): 

\[\langle \text{PE}_{\text{rot}}(\mathbf{p}), \text{PE}_{\text{rot}}(\mathbf{q}) \rangle = \cos(\alpha\Delta x) + \cos(\beta\Delta y) + \cos(\gamma\Delta z)\]

where
\(\Delta\mathbf{p} = \mathbf{q} - \mathbf{p} = (\Delta x, \Delta y, \Delta z)\). 

encoding的inner积\textbf{only-dependenceatrelativeLocation}
\(\Delta\mathbf{p}\), 满足\textbf{translation invariance}. 

\subsubsection{1.3.4 旋转encoding Lipschitz
continuousness}\label{ux65cbux8f6cux7f16ux7801ux7684-lipschitz-ux8fdeux7eedux6027}

\textbf{Theorem 1.3.1 (旋转encoding Lipschitz 常数)}: 映射
\(\text{PE}_{\text{rot}}: \mathbb{R}^3 \to \mathbb{R}^d\) is Lipschitz
continuous的. 取 \(\mathbf{e}_0\) is upper述form (eachto平面的第一维is 1, itsremainderis 
0), Exact 常数is : 

\[\boxed{L_{\text{PE}}^{\text{rot}} = \max(\alpha, \beta, \gamma)}\]

\emph{Proof}: 

\textbf{Step 1 (直接范数compute)}: encoding差is 
\(\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q}) = \mathbf{R}(\mathbf{p})\mathbf{e}_0 - \mathbf{R}(\mathbf{q})\mathbf{e}_0\). byat三 旋转acts onmutualdisjoint的dimensionto
\((0,1), (2,3), (4,5)\), encoding差的each separate量解耦. to \(x\)-平面 (dimension
0,1): 

\[\|\mathbf{R}_x(\alpha x)\mathbf{e}_x - \mathbf{R}_x(\alpha q_x)\mathbf{e}_x\|^2 = (\cos(\alpha x)-\cos(\alpha q_x))^2 + (\sin(\alpha x)-\sin(\alpha q_x))^2 = 2 - 2\cos(\alpha\Delta x) = 4\sin^2\left(\frac{\alpha\Delta x}{2}\right)\]

same理to \(y, z\) 平面. becausethis: 

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\|^2 = 4\sin^2\left(\frac{\alpha\Delta x}{2}\right) + 4\sin^2\left(\frac{\beta\Delta y}{2}\right) + 4\sin^2\left(\frac{\gamma\Delta z}{2}\right)\]

\textbf{Step 2 (利using \(|\sin\theta| \leq |\theta|\))}: 

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\|^2 \leq \alpha^2\Delta x^2 + \beta^2\Delta y^2 + \gamma^2\Delta z^2 \leq \max(\alpha, \beta, \gamma)^2 \cdot \|\Delta\mathbf{p}\|_2^2\]

open方i.e.得
\(\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\| \leq \max(\alpha, \beta, \gamma) \cdot \|\mathbf{p} - \mathbf{q}\|_2\). 

\textbf{Step 3 (紧致ness)}: 取 \(\Delta\mathbf{p} = (\varepsilon, 0, 0)\)
and \(\alpha = \max(\alpha, \beta, \gamma)\), when \(\varepsilon \to 0\)
time: 

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\| = 2|\sin(\alpha\varepsilon/2)| \to \alpha|\varepsilon| = \max(\alpha, \beta, \gamma) \cdot \|\Delta\mathbf{p}\|\]

should界is紧的. \(\square\)

\textbf{Note 1.3.1 (and先front版thismiddle常数high估的Description)}: 先front版thisuse了
\(\|\mathbf{R}(\mathbf{p}) - \mathbf{R}(\mathbf{q})\|_F\)
的三angleinequalitydecompose结combined Cauchy-Schwarz, 得to
\(2\sqrt{\alpha^2 + \beta^2 + \gamma^2}\). this一常数比true值
\(\max(\alpha, \beta, \gamma)\) high出约 \(2\sqrt{3} \approx 3.46\) 倍 (when
\(\alpha = \beta = \gamma\) time), Reasonis: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{三angleinequalityatcurrently交子spaceupper的松弛}: 三 差separate矩阵
 \(\mathbf{D}_x, \mathbf{D}_y, \mathbf{D}_z\)
 acts onmutualdisjoint的dimensiontoupper, becausethis
 \(\|\mathbf{D}_x + \mathbf{D}_y + \mathbf{D}_z\|_F^2 = \|\mathbf{D}_x\|_F^2 + \|\mathbf{D}_y\|_F^2 + \|\mathbf{D}_z\|_F^2\), but三angleinequality
 \(\|\mathbf{D}_x + \mathbf{D}_y + \mathbf{D}_z\|_F \leq \|\mathbf{D}_x\|_F + \|\mathbf{D}_y\|_F + \|\mathbf{D}_z\|_F\)
 in the worst casemay松 \(\sqrt{3}\) 倍 (三项等范数time). 
\item
 \textbf{Cauchy-Schwarz \(\ell_1 \to \ell_2\)
 转换}: \(\alpha|\Delta x| + \beta|\Delta y| + \gamma|\Delta z| \leq \sqrt{\alpha^2 + \beta^2 + \gamma^2} \cdot \|\Delta\mathbf{p}\|_2\)
 in \(\alpha=\beta=\gamma\) and \(\Delta x = \Delta y = \Delta z\)
 time引入另一 \(\sqrt{3}\) 倍松弛. 
\end{enumerate}

twoheavy松弛的复combinedleads to常数high估约 \(2\sqrt{3}\)
倍. correct的Proofshould利用子spacecurrently交ness直接compute \(\ell_2\)
范数, avoidthistwo层松弛. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.4 统一 Lipschitz
continuousnessTheorem}\label{ux7edfux4e00-lipschitz-ux8fdeux7eedux6027ux5b9aux7406}

\textbf{Theorem 1.4.1 (PPE 统一 Lipschitz ness质)}: setup
\(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
is withupperDefinition的任一encoding. thenexistenceat常数 \(L_{\text{PE}} > 0\) makesto has 
\(\mathbf{p}, \mathbf{q} \in \mathcal{P}\): 

\[\|\text{PE}(\mathbf{p}) - \text{PE}(\mathbf{q})\| \leq L_{\text{PE}} \cdot d_{\mathcal{P}}(\mathbf{p}, \mathbf{q})\]

where \(d_{\mathcal{P}}\) is \(\mathcal{P}\) upper的self-howeverdegree量: -
mark量域: \(d_{\mathcal{P}}(p, q) = |p - q|\) - 3D
域: \(d_{\mathcal{P}}(\mathbf{p}, \mathbf{q}) = \|\mathbf{p} - \mathbf{q}\|_2\)

each 域的Exact Lipschitz 常数: 

\begin{longtable}[]{@{}
 >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.2381}}
 >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3571}}
 >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4048}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
encoding类型
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\(\mathcal{P}\)
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\(L_{\text{PE}}\)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
currently弦 (mark量) & \([0, L]\) &
\(\frac{2\sqrt{2}\,\pi}{\sqrt{d}}\sqrt{\sum_j 1/\lambda_j^2}\) \\
旋转 (3D) & \(\mathbb{R}^3\) & \(\max(\alpha, \beta, \gamma)\) \\
\end{longtable}

physical meaning: \(L_{\text{PE}}\) 控制encodingtoLocation变化的\textbf{sensitivedegree}. large的
\(L_{\text{PE}}\)
使邻近Location的encodingmoreaddis different (highseparate辨率), butmaybreaking localsmooth滑ness. Optimal 
\(L_{\text{PE}}\) shouldatphysicalmutualclosescale \(\xi\) 附近匹配encoding的separate辨率. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{第 2 part: Theorem 1
的rigorouscorrection}\label{ux7b2c-2-ux90e8ux5206theorem-1-ux7684ux4e25ux683cux4feeux6b63}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2.1
correction的Detection Margin------Exact 表达式}\label{ux4feeux6b63ux7684ux68c0ux6d4bux8fb9ux9645ux7cbeux786eux8868ux8fbeux5f0f}

\subsubsection{2.1.1 Setup}\label{ux8bbeux5b9a}

引入 PPE post-, 专家 \(m\) 输入From \(s_i\) 变is : 

\[h_i = \phi(s_i) + \text{PE}(p_i)\]

where \(\phi: \mathbb{R}^{d_s} \to \mathbb{R}^d\)
isStatusCharacteristics映射 (maywithis恒等映射ormaylearning的linear投影). 

\textbf{Assumptions 2.1.1 (专家acts onencodingspace)}: 专家model \(E_m\) inencodingspace
\(\mathbb{R}^d\) upper操作. 它们接受 \(h_i\)
作is 输入并输出prediction, 投票Definitionis : 

\[v_m^{\text{PPE}}(s_i) = \mathbf{1}[E_m(h_i) \neq y_i]\]

atdry净/noiseConditionslower的expectis : 

\[p_{\text{clean}, s}^{\text{PPE}} = \mathbb{E}[v_m^{\text{PPE}}(s) \mid \text{clean state } s]\]
\[p_{\text{noisy}, s}^{\text{PPE}} = \mathbb{E}[v_m^{\text{PPE}}(s) \mid \text{noisy state } s]\]

\subsubsection{2.1.2 修Positive Statement}\label{ux4feeux6b63ux547dux9898}

\textbf{Proposition 2.1 (PPE lower的Exact Detection Margin变化------correction CC Audit Report in the 
sign error)}

PPE strengthensspace in the has 效Detection Marginis : 

\[\boxed{\Delta_s^{\text{PPE}} = \Delta_s + \delta_s^{\text{PE}}}\]

where: 

\[\boxed{\delta_s^{\text{PE}} = \underbrace{(p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s})}_{\text{noisesampleuppermany出的separate歧increase益}} - \underbrace{(p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s})}_{\text{dry净sampleupper (not希望has )separate歧变化}}}\]

\begin{quote}
\textbf{correctionDescription}: CC Audit Reportmiddle \(\delta_s^{\text{PE}}\)
的表达式Symbol/Conceptmutual反. correct的Symbol/Conceptis: \(\delta_s^{\text{PE}} > 0\) if and only if
PPE 帮助distinguishnoiseanddry净sample (increaselargeDetection Margin). thisrequire: -
noisesampleupper的separate歧probability\textbf{increases} (\(p_{\text{noisy}}^{\text{PPE}} > p_{\text{noisy}}\))------专家moregood地''识别''出incorrectLabel
-
dry净sampleupper的separate歧probability\textbf{notincreasesordecreases} (\(p_{\text{clean}}^{\text{PPE}} \leq p_{\text{clean}}\))------专家tofewmaintains原has consistentness
\end{quote}

\(\delta_s^{\text{PE}}\) 值域: byatprobabilityin \([0,1]\) middle, has 
\(\delta_s^{\text{PE}} \in [-\Delta_s, 1 - p_{\text{clean}, s}]\). 

\emph{Proof}: byDefinition: 

\begin{align*}
\Delta_s^{\text{PPE}} &= p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{clean}, s}^{\text{PPE}} \\
&= (p_{\text{noisy}, s} + (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s})) - (p_{\text{clean}, s} + (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s})) \\
&= (p_{\text{noisy}, s} - p_{\text{clean}, s}) + (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) \\
&= \Delta_s + \delta_s^{\text{PE}}
\end{align*}

值域: minimum化 \(\delta_s^{\text{PE}}\) ------
\(p_{\text{noisy}}^{\text{PPE}} = 0\) ( has noisesampleby误判is dry净), \(p_{\text{clean}}^{\text{PPE}} = 1\) ( has dry净sampleby误判is noise)→
\(\delta_s^{\text{PE}} = (0 - p_{\text{noisy}}) - (1 - p_{\text{clean}}) = -p_{\text{noisy}} - 1 + p_{\text{clean}} = -(1 + \Delta_s)\). butthisexceeds了
\(p_{\text{clean}}^{\text{PPE}}\) and \(p_{\text{noisy}}^{\text{PPE}}\)
的reasonable范围\ldots in practice, athas meaning的专家modelmiddle, \(p_{\text{noisy}}^{\text{PPE}} \in [0, 1]\)
and \(p_{\text{clean}}^{\text{PPE}} \in [0, 1]\)
约束lower, \(\delta_s^{\text{PE}} \in [-\Delta_s, 1]\). more精fine的界by
\(p_{\text{noisy}}^{\text{PPE}} \in [0,1]\) and
\(p_{\text{clean}}^{\text{PPE}} \in [0,1]\) to出. \(\square\)

\textbf{correctionpost-Theorem 1 (PPE strengthens)}: 

\[\boxed{F_1^{\text{PPE}} \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M (\Delta_s + \delta_s^{\text{PE}})^2\right)}\]

Chernoff-Hoeffding structuremaintains (指示variablein \([0,1]\) middlehas 界), onlyis常数
\(\Delta_s\) 变is \(\Delta_s + \delta_s^{\text{PE}}\). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.2 \(\delta_s^{\text{PE}} > 0\)
的necessaryConditionsandSufficient Condition}{2.2 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} \textgreater{} 0 necessaryConditionsandSufficient Condition}}\label{delta_stextpe-0-ux7684ux5fc5ux8981ux6761ux4ef6ux548cux5145ux5206ux6761ux4ef6}

\subsubsection{2.2.1 information theoryConditions}\label{ux4fe1ux606fux8bbaux6761ux4ef6}

\textbf{Theorem 2.2.1 (\(\delta_s^{\text{PE}} > 0\)
的Sufficient Condition------information theoryform, 贝叶斯Optimal 限determine)}

setup \(S, P, Y\)
separate别representationStatusatom、physicalLocationandLabel的random variable (atData Distributionlower). If : 

\[\boxed{I(Y; P \mid S) > 0}\]

i.e.: physicalLocationat\textbf{todetermineStatusatom的Conditionslower}携带regardingLabel的额outerinformation, then\textbf{existenceat}一 
PPE function \(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
andencodingdimension \(d_{\text{pe}}\), makestosome Status
\(s\), \textbf{Bayes-optimal classifier}atincrease广Characteristics \((s, \text{PE}(p))\)
lower的Detection Margin变化满足 \(\delta_s^{\text{PE}} > 0\). 

\emph{Proof}: 

\textbf{Step 1 (information储existence的physicalmay及ness)}: byAssumptions
\(I(Y; P \mid S) > 0\), existenceat一todistribution \((P_{Y|S,P}, P_{Y|S})\)
isnotmutualsame的: 

\[I(Y; P \mid S) = \mathbb{E}_{S, P}\left[D_{\text{KL}}(P_{Y|S,P} \| P_{Y|S})\right] > 0\]

becausethisexistenceat一 currently测degree的事件集
\(\mathcal{A} \subset \mathcal{S} \times \mathcal{P}\), atitsupper
\(P_{Y|S=s, P=p} \neq P_{Y|S=s}\). 

\textbf{Step 2 (构造encoding)}: 选取一 sufficient表达力的encodingfunction
\(\text{PE}\). by通用逼近Theorem (例such as具has sufficientmany神经元的神经网络), existenceat
\(\text{PE}\) makesencodingrepresentation \(\text{PE}(p)\) approximate保留 \(P\) middleregarding
\(Y\) has mutualcloseinformation. 

specifically, 取 \(d_{\text{pe}}\) sufficientlarge, makesexistencein \(\text{PE}\)
满足dataat理inequality\textbf{with等式成立}: 

\[I(Y; \text{PE}(P) \mid S) = I(Y; P \mid S) - \varepsilon_{\text{PE}}\]

where \(\varepsilon_{\text{PE}} \geq 0\) isencodingresults in的information丢失 (see第 3
part). forsufficient表达 \(\text{PE}\), mayguarantee
\(\varepsilon_{\text{PE}} < I(Y; P \mid S)\), becausethis: 

\[I(Y; \text{PE}(P) \mid S) > 0\]

\textbf{Step 3 (Bayes-optimal classifier的linesis )}: Conditionsmutualinformation
\(I(Y; \text{PE}(P) \mid S) > 0\) 意味着todetermine \(S\), \(\text{PE}(P)\)
provideregarding \(Y\) 额outer''信号''. considerBayes-optimal classifieratincrease广space in the 表现. 

fornoisesample (Label \(y\) isincorrect的): todetermine
\((s, p)\), 贝叶斯Optimal prediction器willmore倾towardat输出\textbf{and \(y\)
is different的}trueLabel (becauseis \(p\) provide了额outer的证据). becausethisseparate歧probabilityrises: 

\[p_{\text{noisy}, s}^{\text{PPE}} > p_{\text{noisy}, s}\]

fordry净sample: 额outer的Locationinformation\textbf{进一步confirm}correct的Label, bydataat理inequalityguarantee专家separate歧probability\textbf{notincreases} (甚todecreases): 

\[p_{\text{clean}, s}^{\text{PPE}} \leq p_{\text{clean}, s}\]

becausethis\textbf{Bayes-optimal classifier}的
\(\delta_s^{\text{PE}} = (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) > 0\). 

\textbf{Step
4 (From 贝叶斯Optimal to实际专家------Conjecture)}: \textbf{{[}启发式{]}}
upper述ConclusiontoBayes-optimal classifierrigorous成立. for SCX framework in the 实际神经网络专家
\(E_m\), mustguarantee \(\delta_s^{\text{PE}} > 0\)
must额outerAssumptions: 专家model的输出consistent地逼近Bayes-optimal classifier (in \(\ell_1\)
meaninglowertoConditionsdistribution \(P_{Y|S,P}\) 逼近erroris less than
\(|\delta_s^{\text{PE}}|\)). forthrough随机gradient descenttraining的神经网络, 目front\textbf{nothas }has 限samplelower的rigorousconvergenceguarantee. thisis whenfrontTheorem的\textbf{primarylimitation}: Step
1-3
provesBayes-optimal classifiermaywithFrom Locationinformationmiddle受益 (information-theoretically的必howeverness), butFrom 贝叶斯Optimal to实际专家的推广is启发式Conjecture. 

\textbf{退一步看}: If Step 4 推广does not hold, Theorem 2.2.1
退化is 一 经典Conclusion: \(I(Y;P|S) > 0\) 意味着Bayes-optimal classifierat使using \(P\)
informationtime表现moregood------thisisdataat理inequality的直接Corollary, is平凡的. \(\square\)

\subsubsection{2.2.2 necessaryConditions}\label{ux5fc5ux8981ux6761ux4ef6}

\textbf{Proposition 2.2.1 (\(\delta_s^{\text{PE}} > 0\) necessaryConditions)}: 

If to has 专家 \(m\), \(\delta_s^{\text{PE}} > 0\), thenmusthas : 

\[\boxed{I(Y; P \mid S) > 0 \quad \text{or} \quad \text{PE changes了专家system的bias差-variancestructure (non-information theorymechanism)}}\]

第一kindcase (information theorymechanism)alreadyin 2.2.1
middleat理. 第二kindcase (non-information theorymechanism)may发生at: PE
increases了encodingdimension的\textbf{随机currentlythen化effect}, 使专家输出的variancestructureundergoes change. butthisis NOT ''physicalLocationinformation''at起作用------andisarbitraryaddnessnoisealsocanproduces的effect. 

\textbf{physicallyimportant的情形is \(I(Y; P \mid S) > 0\)}. thisis equivalent to: 

\[\exists s, p_1, p_2 \text{ makes } P_{Y|S=s, P=p_1} \neq P_{Y|S=s, P=p_2}\]

i.e.: for\textbf{same一 Statusatom} \(s\), is different的physicalLocation \((p_1, p_2)\)
willleads tois different的Labeldistribution. 

\textbf{physicalexample}: - 蛋white质middle: mutualsame氨基酸类型 (Status \(s\))at
\(\alpha\)-螺旋 (Location \(p_1\))and \(\beta\)-折叠 (Location
\(p_2\)) in the 功canRoleis different ← \(I(Y; P \mid S) > 0\) -
crystalmiddle: mutualsame的chemical环境 (Status \(s\))at表面 (Location \(p_1\))and体mutual (Location
\(p_2\)) in the defectformation energyis different ← \(I(Y; P \mid S) > 0\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.3 \(\delta_s^{\text{PE}}\)
的information theoryupper bound (dataat理inequality)}{2.3 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} information theoryupper bound (dataat理inequality)}}\label{delta_stextpe-ux7684ux4fe1ux606fux8bbaux4e0aux754cux6570ux636eux5904ux7406ux4e0dux7b49ux5f0f}

\textbf{Theorem 2.3.1 (\(\delta_s^{\text{PE}}\) information theoryupper bound)}: 

\[\boxed{\delta_s^{\text{PE}} \leq \min\left(1, \sqrt{\frac{1}{2} \cdot I(P; \text{PE}(P) \mid S, Y)}\right) + \text{Bernstein correction term}}\]

moreExact 地, bydataat理inequality (DPI)and Pinsker inequality: 

\[\delta_s^{\text{PE}} \leq \min\left(1 - p_{\text{clean}, s}, \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}|S,Y} \| P_{\text{PE}|S})}\right) + \delta_s^{\text{variance}}\]

where \(\delta_s^{\text{variance}}\) fromhas 限专家数 \(M\)
引入的estimatevariance. 

\emph{Proof}: 

\textbf{Step 1 (probability变化and KL 散degree)}: fordry净sampleupper的separate歧probability变化: 

\[|p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| = \left|\mathbb{E}_{\text{PE}(P)|S=s, Y=y_{\text{true}}}[P(E_m(h) \neq y_{\text{true}})] - P(E_m(s) \neq y_{\text{true}})\right|\]

by Pinsker inequality: to任意事件
\(A\), \(|P(A) - Q(A)| \leq \sqrt{\frac{1}{2} D_{\text{KL}}(P \| Q)}\). 

applicationto \(p_{\text{clean}}\) and \(p_{\text{noisy}}\) 变化, separate别to出: 

\begin{align*}
|p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| &\leq \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}(P)|S=s, \text{clean}} \| P_{\text{PE}(P)|S=s})} \\
|p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}| &\leq \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}(P)|S=s, \text{noisy}} \| P_{\text{PE}(P)|S=s})}
\end{align*}

\textbf{Step 2 (combine)}: 

\begin{align*}
\delta_s^{\text{PE}} &= (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) \\
&\leq |p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}| + |p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| \\
&\leq \sqrt{\frac{1}{2} D_{\text{KL}}^{(1)}} + \sqrt{\frac{1}{2} D_{\text{KL}}^{(2)}}
\end{align*}

where \(D_{\text{KL}}^{(1)}, D_{\text{KL}}^{(2)}\)
separate别isdry净andnoiseConditionslower KL 散degree. 

\textbf{Step 2b (值域upper bound的来源------补充Description)}: by
\(\delta_s^{\text{PE}}\) 值域 (Proposition 2.1), 恒has 
\(\delta_s^{\text{PE}} \leq 1 - p_{\text{clean}, s}\) (取
\(p_{\text{noisy}}^{\text{PPE}} = 1\),
\(p_{\text{clean}}^{\text{PPE}} = 0\) time的maximum值). thisupper boundfromprobability的
\([0,1]\) 约束, not-dependenceat任何distributionAssumptions. Theorem Statement in the 
\(\min(1-p_{\text{clean},s}, \sqrt{\frac{1}{2}D_{\text{KL}}})\)
取self-\textbf{two independenthas 效upper bound的minimum值}: 值域upper boundand Pinsker upper bound. when
Pinsker 界relatively松 (KL 散degreelargebut \(\delta_s^{\text{PE}}\)
stillis subject to值域)time, 值域界more紧; 反'swhenLocationinformationveryfew (KL 散degreesmall)time, Pinsker
界more紧. 

\textbf{Step 3 (DPI 约束)}: bydataat理inequality (encoding PE onlycandecreasesinformation): 

\[I(\text{PE}(P); Y \mid S) \leq I(P; Y \mid S)\]

becausethis \(\delta_s^{\text{PE}}\) is subject toLocation \(P\) intodetermine \(S\)
lower携带的mutualinformation量. when \(I(P; Y \mid S) = 0\) (LocationNone用time), upper bound退化is : 

\[\delta_s^{\text{PE}} \leq 0 + \delta_s^{\text{variance}}\]

ata large amount of专家extremely限 \(M \to \infty\) lower
\(\delta_s^{\text{variance}} \to 0\). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.4 \(\delta_s^{\text{PE}}\)
的启发式estimate (Fano
界限------non-rigorouslower bound)}{2.4 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} 启发式estimate (Fano 界限------non-rigorouslower bound)}}\label{delta_stextpe-ux7684ux542fux53d1ux5f0fux4f30ux8ba1fano-ux754cux9650ux975eux4e25ux683cux4e0bux754c}

\textbf{Proposition 2.4.1 (\(\delta_s^{\text{PE}}\)
的启发式estimate------non-rigorouslower bound)}: 

\begin{quote}
\textbf{{[}启发式{]}} \textbf{ (警告: thisnon-rigorousTheorem. )}
\end{quote}

setupLocation \(P\) intodetermineStatus \(S\) lowerprovideregardingLabel \(Y\) 额outerinformation, andencoding PE
保留its in the 
\(\rho = I(Y; \text{PE}(P) \mid S) / I(Y; P \mid S) \in (0, 1]\)
part. If one assumes the Fano
inequalityattwo 世界uniform达to等号 (must特determinedistributionConditions: noiseuniform匀distributionatincorrectCategoryupper), thenexistenceatStatus
\(s\) makes: 

\[\delta_s^{\text{PE}} \approx \frac{2\rho \cdot I(Y; P \mid S) - \log 2}{\log |\mathcal{Y}|} - O\left(\frac{1}{\sqrt{M}}\right)\]

where \(M\) is专家数, \(O(1/\sqrt{M})\) fromhas 限专家的estimatenoise. 

\textbf{key限determine: This expression \emph{is NOT } a rigorous mathematical lower bound. } From two Fano
lower bound \(P_e \geq A\) and \(P_e^{\text{PPE}} \geq B\) cannot逻辑地推出
\(P_e - P_e^{\text{PPE}} \geq A - B\), becauseis \(a \geq A\) and \(b \geq B\)
not蕴含 \(a-b \geq A-B\). thisouter, Fano
inequalityonlyat特determinedistributionupper达to等号 (noiseuniform匀distributionatincorrectCategoryupper, and \(H_2(P_e)\)
饱and). becausethis, thisatto出的表达式shouldby视is to \(\delta_s^{\text{PE}}\)
量级的\textbf{启发式estimate}, andnon-Theorem级的lower boundConclusion. correct的lower boundrequireis different的Proofstrategy, thisiswhenfront的一 open problems. 

\emph{Derivation思路 (non-rigorousProof)}: 

\textbf{Step 1 (Fano inequality------From errortoinformation)}: Fano
inequality的Standard form: 

\[H(Y \mid S, \text{PE}(P)) \leq H_2(P_e) + P_e \cdot \log(|\mathcal{Y}| - 1)\]

where \(P_e\) istodetermine \((S, \text{PE}(P))\) lower的贝叶斯incorrect率, \(H_2\)
is二元熵. 等效地, lower bound: 

\[P_e \geq \frac{H(Y \mid S, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\]

\textbf{Step 2 (启发式close联------not等号误用)}: If Fano
inequalityattwo 世界uniform达to等号 (thisin general\textbf{does not hold}), thenformuppermay写: 

\begin{align*}
P_e - P_e^{\text{PPE}} &\stackrel{\text{If 等号}}{=} \frac{H(Y|S) - H(Y|S, \text{PE}(P))}{\log |\mathcal{Y}|} \\
&= \frac{I(Y; \text{PE}(P) \mid S)}{\log |\mathcal{Y}|} \\
&= \frac{\rho \cdot I(Y; P \mid S)}{\log |\mathcal{Y}|}
\end{align*}

againaddupper \(\Delta P_e\) and \(\delta_s^{\text{PE}}\) 启发式close联 (Assumptions
\(\Delta P_e \approx -\pi \cdot \delta_s^{\text{PE}}\) tomaximum获益的Status
\(s\) 成立), 取
\(\pi = 1/2\) (mostbadcase), \(\rho = 1\) (完美encoding), addupperhas 限 \(M\) 
Hoeffding correctioni.e.得 述表达式. 

\textbf{again次strong调: thisDerivationFrom not等号推出了等号, is NOT rigorousProof. correct的lower boundrequireusemorestrong的工具 (such as
Fano 逆formorstrong Fano inequality), thisrequireregardingdistribution的具体Assumptions. }

\textbf{physicalMeaning (量级estimate)}: If \(I(Y; P \mid S)\)
verysmall (Location几乎does not provide额outerinformation), then \(\delta_s^{\text{PE}}\)
的启发式estimateapproaches零oris 负------PPE
Nonemethod实质ness地improvementdetection. this解释了is 什么\textbf{is NOT has physical systemallcanFrom PPE
middle受益}. butthis一Conclusionisdirectioncorrect的physical学直觉, andnon-rigorous的mathematicalguarantee. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2.5 spacelocalnesscurrentlythen化------CC
Audit Reportresult的Corrected Version}\label{ux7a7aux95f4ux5c40ux90e8ux6027ux6b63ux5219ux5316cc-ux5ba1ux8ba1ux62a5ux544aux7ed3ux679cux7684ux4feeux6b63ux7248}

\textbf{Theorem 2.5.1 (\(\delta_s^{\text{PE}}\) spacesmooth滑ness)}: setup PE 满足
Lipschitz Conditions (常数is \(L_{\text{PE}}\)), and专家 \(E_m\)
的输出\textbf{softprobability} \(E_m^{\text{soft}}: \mathbb{R}^d \to [0,1]\) (such as
softmax 输出的maximumCategoryprobability)满足 \(L_E\)-Lipschitz
Conditions. thisouter, Assumptions\textbf{boundaryConditions}: 专家 \(E_m\) 决策boundary距离two encoding点
\(h_i, h_j\) tofewis \(\tau > 0\), i.e.
\(|E_m^{\text{soft}}(h) - 0.5| \geq \tau\) to \(h \in \{h_i, h_j\}\)
成立. thentosame属一 Status \(s\)、具has physicalLocation \(p_i, p_j\) two atom: 

\[\boxed{|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| \leq 2 \cdot L_E \cdot L_{\text{PE}} \cdot d_{\mathcal{P}}(p_i, p_j) + O\left(\frac{1}{\sqrt{M}}\right)}\]

where \(O(1/\sqrt{M})\)
fromhas 限专家的estimateerror. \textbf{If boundaryConditionsdoes not hold}, should界does not guaranteenon-空------指示function
\(\mathbf{1}[E_m(h) \neq y]\) in决策boundaryatnotcontinuous, softprobability Lipschitz
ness质cannot约束指示functionexpect的变化. 

\emph{Proof}: 

\begin{align*}
|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| &= |(p_{\text{noisy}, i}^{\text{PPE}} - p_{\text{noisy}, j}^{\text{PPE}}) - (p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}})| \\
&\leq |p_{\text{noisy}, i}^{\text{PPE}} - p_{\text{noisy}, j}^{\text{PPE}}| + |p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}}|
\end{align*}

each probability difference
受输入变化约束. \textbf{key}: \(p_{\text{clean}, i}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) \neq y]]\)
is指示function的expect. boundaryConditions \(|E_m^{\text{soft}}(h) - 0.5| \geq \tau\)
guarantee \(h_i\) and \(h_j\)
uniformat决策boundary的same一侧并maintains一determine距离, thistimesoftprobability \(L_E\)-Lipschitz
ness质控制指示functionexpect的变化: 

\[|p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}}| \leq L_E \cdot \|h_i - h_j\| = L_E \cdot \|\text{PE}(p_i) - \text{PE}(p_j)\| \leq L_E \cdot L_{\text{PE}} \cdot d_{\mathcal{P}}(p_i, p_j)\]

 (利using \(s_i = s_j = s\) time \(\phi(s_i) = \phi(s_j)\). )two倍i.e.得. 

\textbf{If NoneboundaryConditions}: 决策boundarymay恰goodis located at \(h_i\) and \(h_j\)
'sbetween, thistimei.e.使 \(\|h_i - h_j\|\)
tends to零, separate歧probabilitymaymutual差任意large (such as一 完fullatdry净侧
\(p \approx 0\), 另一 atnoise侧
\(p \approx 1\)), 界变is 空 (vacuous). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{第 3 part: Theorem 2
的Exact correction}\label{ux7b2c-3-ux90e8ux5206theorem-2-ux7684ux7cbeux786eux4feeux6b63}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{3.1 encodingnot完美degree \(\varepsilon_{\text{PE}}\)
的Exact information theoryDefinition}{3.1 encodingnot完美degree \textbackslash varepsilon\_\{\textbackslash text\{PE\}\} Exact information theoryDefinition}}\label{ux7f16ux7801ux4e0dux5b8cux7f8eux5ea6-varepsilon_textpe-ux7684ux7cbeux786eux4fe1ux606fux8bbaux5b9aux4e49}

\subsubsection{3.1.1
Conditionsmutualinformation的链式decompose}\label{ux6761ux4ef6ux4e92ux4fe1ux606fux7684ux94feux5f0fux5206ux89e3}

setuprandom variable: - \(X\): 原始Characteristics (Statusatom \(S\) oritsrepresentation \(\phi(S)\)) -
\(P\): physicalLocation (Completeinformation) - \(\text{PE}(P)\): encodingback的physicalLocation -
\(Y\): Label

information流 Markov 链: 

\[Y \leftrightarrow (X, P) \leftrightarrow (X, \text{PE}(P))\]

 (i.e.
\(Y \perp\!\!\!\perp \text{PE}(P) \mid (X, P)\)------encodingonly丢失information, not创造information. )

\subsubsection{3.1.2
encodingnot完美degree的Definition}\label{ux7f16ux7801ux4e0dux5b8cux7f8eux5ea6ux7684ux5b9aux4e49}

\textbf{Definition 3.1 (encodingnot完美degree
\(\varepsilon_{\text{PE}}\)------information theoryDefinition)}: 

\[\boxed{\varepsilon_{\text{PE}} = I(Y; P \mid X) - I(Y; \text{PE}(P) \mid X)}\]

\textbf{ness质}: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{dataat理inequality} (DPI): \(\varepsilon_{\text{PE}} \geq 0\)
 恒成立. becauseis \((X, \text{PE}(P))\) is \((X, P)\)
 (mayis随机的)function, DPI guarantee: 

 \[I(Y; \text{PE}(P) \mid X) \leq I(Y; P \mid X)\]
\item
 \textbf{may逆nessConditions}: \(\varepsilon_{\text{PE}} = 0\) if and only if PE
 intodetermine \(X\) Conditionslowerto \(Y\) predictionis\textbf{sufficient statistic}, i.e.: 

 \[Y \perp\!\!\!\perp P \mid (X, \text{PE}(P))\]

 等价地: encodingback的Location \(\text{PE}(P)\) 保留 \(P\) middle has and \(Y\)
 mutualclose的information. 
\item
 \textbf{平凡case}: If 
 \(I(Y; P \mid X) = 0\) (Locationnot携带Labelmutualcloseinformation), then
 \(\varepsilon_{\text{PE}} = 0\) self-动成立 (零subtract零). 
\item
 \textbf{upper bound}: \(\varepsilon_{\text{PE}} \leq I(Y; P \mid X) \leq H(Y)\). maximumnot完美degreeis丢失 has Locationinformation. 
\end{enumerate}

\subsubsection{3.1.3
andencodingdimension的relationship}\label{ux4e0eux7f16ux7801ux7ef4ux5ea6ux7684ux5173ux7cfb}

\textbf{Proposition 3.1 (dimension-information权衡)}: tosinusoidal encoding (\(d\) 维)and Laplace
mutualclosekernel (mutualcloselongdegree \(\xi\)): 

\[\varepsilon_{\text{PE}} \leq I(Y; P \mid X) \cdot \exp\left(-\frac{d}{d_0}\right)\]

where \(d_0 = O(L/\xi)\) isCharacteristicsdimensionscale. specifically, when \(d\) following §1.2
 in the separate位数Optimal schemegrowthtime, information丢失withexponentialratefalls. 

\emph{Proof Idea}: encoding的逼近error (Theorem
1.2.2)leads tokernelheavy建error, 进andleads toConditionsdistribution \(P_{Y|X,\text{PE}(P)}\) and
\(P_{Y|X,P}\) KL 散degree, by Pinsker
inequality转化is mutualinformation丢失. exponential decayfrom Fourier 级数的谱衰subtract (Cauchy
kernel的解析ness). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3.2 correction Theorem
2------Exact 陈述andProof}\label{ux4feeux6b63ux7684-theorem-2ux7cbeux786eux9648ux8ff0ux548cux8bc1ux660e}

\textbf{Theorem 2' (not完美 PPE lower的weakCharacteristics失效upper bound------Exact correction)}

setup原始Characteristics \(X\) informationinsufficientdegreeis \(\delta > 0\) (Definitionsee CC Audit Report), and
\(\varepsilon_{\text{PE}}\) is PPE encodingnot完美degree. thenin PPE
strengthens的Characteristicsspacemiddle, SCX \(F_1\) separate数满足: 

\[\boxed{F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}}\]

where \(C_F > 0\) is \(F_1\) and贝叶斯incorrect率'sbetween的转换常数. 

\textbf{特殊情形}: -
\(\varepsilon_{\text{PE}} \to 0\) (完美encoding): upper bound恢复is \textbf{原始
Theorem 2}: 
\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}\]

\begin{itemize}
\tightlist
\item
 \(\varepsilon_{\text{PE}} \to I(Y; P \mid X)\) (完fullfail的encoding): upper bound回tonotuse任何Locationinformation的Status (pure浪费parameter). 
\end{itemize}

\emph{Proof}: 

\textbf{Step 1 (Fano inequality嵌入)}: by Fano
inequality, Optimal separate类器 (贝叶斯)的incorrect率满足: 

\[P_e \geq \frac{H(Y \mid X, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\]

\textbf{Step 2 (Conditions熵的decompose)}: 

\begin{align*}
H(Y \mid X, \text{PE}(P)) &= H(Y \mid X) - I(Y; \text{PE}(P) \mid X) \\
&= H(Y \mid X) - [I(Y; P \mid X) - \varepsilon_{\text{PE}}] \\
&= H(Y \mid X) - I(Y; P \mid X) + \varepsilon_{\text{PE}}
\end{align*}

\textbf{Step 3 (connection \(\delta\))}: 原始 Theorem 2 middle, \(\delta\)
andConditions熵的relationshipis (CC Audit Report §1, Theorem 2): 

\[\delta = \frac{2}{\log|\mathcal{Y}|} \cdot \left(H(Y \mid X) - 1\right)_{\text{excess}}\]

where \((\cdot)_{\text{excess}}\) representation超出贝叶斯incorrect的information量. in PPE lower: 

\begin{align*}
H(Y \mid X, \text{PE}(P)) - 1 &= (H(Y \mid X) - I(Y; P \mid X) + \varepsilon_{\text{PE}}) - 1 \\
&= (H(Y \mid X) - 1) - I(Y; P \mid X) + \varepsilon_{\text{PE}}
\end{align*}

Definition \(\delta_{\text{PPE}}\) is PPE strengthensback的has 效informationinsufficientdegree: 

\[\delta_{\text{PPE}} = \delta + \frac{2\varepsilon_{\text{PE}}}{\log|\mathcal{Y}|} \cdot \frac{\log|\mathcal{Y}|}{C_F^2} = \delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}\]

 (\(C_F\) fromincorrect率and \(F_1\)
的转换: \(F_1 \leq F_{1,\text{base}} + C_F \sqrt{P_e - P_e^*}\). )

\textbf{Step 4 (代入 SCX \(F_1\) upper bound)}: 

\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta_{\text{PPE}}}{2}} = F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}\]

\textbf{Step 5 (extremely限verify)}: when \(\varepsilon_{\text{PE}} \to 0\): 

\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}\]

currentlyis原始 Theorem 2. \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{3.3 \(\varepsilon_{\text{PE}}\)
的mayestimateform}{3.3 \textbackslash varepsilon\_\{\textbackslash text\{PE\}\} mayestimateform}}\label{varepsilon_textpe-ux7684ux53efux4f30ux8ba1ux5f62ux5f0f}

Theorem量
\(\varepsilon_{\text{PE}} = I(Y; P \mid X) - I(Y; \text{PE}(P) \mid X)\)
istheory量------它涉及trueData Distribution. for实际application, 我们requireFrom datamiddleestimate它. 

\subsubsection{3.3.1 KSG estimate器}\label{ksg-ux4f30ux8ba1ux5668}

\textbf{Theorem 3.3.1 (\(\varepsilon_{\text{PE}}\)
的mayestimateness)}: \(\varepsilon_{\text{PE}}\)
maywiththroughwithlowerMethodologyFrom has 限samplemiddle\textbf{consistent地}estimate: 

\[\boxed{\hat{\varepsilon}_{\text{PE}}^{(n)} = \hat{I}^{(n)}(Y; P \mid X) - \hat{I}^{(n)}(Y; \text{PE}(P) \mid X)}\]

where \(\hat{I}^{(n)}(\cdot; \cdot \mid \cdot)\) isuse
Kraskov-Stögbauer-Grassberger (KSG)
estimate器的\textbf{Conditionsmutualinformation}estimate. whensample量 \(n \to \infty\)
time, \(\hat{\varepsilon}_{\text{PE}}^{(n)} \xrightarrow{p} \varepsilon_{\text{PE}}\) (consistentness). 

specifically, KSG Conditionsmutualinformationestimate器: 

\[\hat{I}(Y; P \mid X) = \hat{I}(Y; (P, X)) - \hat{I}(Y; X)\]

where \(\hat{I}(A; B)\) isStandard KSG mutualinformationestimate (based on \(k\)-近邻距离). 

\textbf{algorithm}: 1. From datamiddlesampling \(n\) 三元组 \((x_i, p_i, y_i)\) 2.
computeencoding \(\text{PE}(p_i)\) 3. compute
\(\hat{I}^{(n)}(Y; (P, X))\): at联combinedspace
\((\mathcal{P} \times \mathcal{X}) \times \mathcal{Y}\) middle使using \(k\)-NN
4. compute \(\hat{I}^{(n)}(Y; X)\): in \(\mathcal{X} \times \mathcal{Y}\)
middle使using \(k\)-NN 5.
\(\hat{I}^{(n)}(Y; P \mid X) = \hat{I}^{(n)}(Y; (P, X)) - \hat{I}^{(n)}(Y; X)\)
6. same理compute \(\hat{I}^{(n)}(Y; \text{PE}(P) \mid X)\) 7.
\(\hat{\varepsilon}_{\text{PE}}^{(n)} = \hat{I}^{(n)}(Y; P \mid X) - \hat{I}^{(n)}(Y; \text{PE}(P) \mid X)\)

\textbf{convergencerate}: KSG estimate器with \(O(n^{-1/(d_{\text{eff}}+2)})\)
rateconvergence, where \(d_{\text{eff}}\) is联combinedspace的has 效dimension. forhigh维
\(X\), convergencerelativelyslow, but \(\varepsilon_{\text{PE}}\)
this身is\textbf{差值}, two estimate器common享mutualsame的bias差, 差值estimatemay比single独estimatemore准确 (bias差抵消). 

\subsubsection{3.3.2 lower boundestimate (avoidhigh维
KSG)}\label{ux4e0bux754cux4f30ux8ba1ux907fux514dux9ad8ux7ef4-ksg}

forhigh维 \(X\), KSG
estimatemayis inaccurate. 替代scheme: use\textbf{predictionperformanceDifference}作is 
\(\varepsilon_{\text{PE}}\) lower bound. 

\textbf{Proposition 3.3.1 (\(\varepsilon_{\text{PE}}\) predictionlower bound)}: 

\[\boxed{\varepsilon_{\text{PE}} \geq \mathbb{E}_{X,P}\left[D_{\text{KL}}(P_{Y|X,P} \| P_{Y|X,\text{PE}(P)})\right]_{\text{est}}}\]

where \(P_{Y|X,P}\) and \(P_{Y|X,\text{PE}(P)}\) canthroughtwo separate类器approximate: -
separate类器 \(f_{\text{full}}\) in \((X, P)\) uppertraining → estimate \(P_{Y|X,P}\) -
separate类器 \(f_{\text{enc}}\) in \((X, \text{PE}(P))\) uppertraining → estimate
\(P_{Y|X,\text{PE}(P)}\)

twooneat留出test集upper的\textbf{logarithmicloss差}is \(\varepsilon_{\text{PE}}\)
的lower boundestimate: 

\[\hat{\varepsilon}_{\text{PE}}^{\text{pred}} = \frac{1}{n_{\text{test}}} \sum_{i=1}^{n_{\text{test}}} \left[\log \frac{1}{p_{\text{enc}}(y_i \mid x_i, \text{PE}(p_i))} - \log \frac{1}{p_{\text{full}}(y_i \mid x_i, p_i)}\right]\]

when \(n_{\text{test}} \to \infty\)
andseparate类器consistenttime, \(\hat{\varepsilon}_{\text{PE}}^{\text{pred}} \to \varepsilon_{\text{PE}}\). 

\textbf{实用value}: such as果separate类器 \(f_{\text{full}}\) and \(f_{\text{enc}}\)
attest集upper的准确率\textbf{nothas explicit著Difference}, then
\(\varepsilon_{\text{PE}} \approx 0\)------encodingis充足的, PPE will not削weak
Theorem 2 upper bound. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{第 4 part: Theorem 3
的correction (mostimportant的part)}\label{ux7b2c-4-ux90e8ux5206theorem-3-ux7684ux4feeux6b63ux6700ux91cdux8981ux7684ux90e8ux5206}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.1 原 Theorem 3
的Exact Theorem Statement}\label{ux539f-theorem-3-ux7684ux7cbeux786eux5b9aux7406ux9648ux8ff0}

\textbf{Theorem 3 (原始------noise andDifficultysample的indistinguishability)}: 

existenceattwo 世界 \(W_A, W_B\) 满足: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
 observationData Distribution完fullmutualsame: \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)
\item
 in \(W_A\) middle, 样this \((x, y)\) is\textbf{Labelnoise} (label incorrect, but
 \(x\) structureupper label shouldis别的)
\item
 in \(W_B\) middle, 样this \((x, y)\) is\textbf{this质Difficultysample} (label
 correct, butCharacteristics \(x\) this身Nonemethoddeterminism地支keepthe label)
\end{enumerate}

then: nothas 任何algorithmcanonlythroughobservation i.i.d. 样this \((X_i, Y_i)_{i=1}^n\) distinguish
\(W_A\) and \(W_B\). form化地, to任意决策规then
\(d: (\mathcal{X} \times \mathcal{Y})^n \to \{W_A, W_B\}\): 

\[\max_{W \in \{W_A, W_B\}} P_W(d(\text{data}) \neq W) \geq \frac{1}{2}\]

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.2 learnable PE
such as何breaking indistinguishability------Exact Conditions}\label{ux5b66ux4e60ux578b-pe-ux5982ux4f55ux7834ux574fux4e0dux53efux533aux5206ux6027ux7cbeux786eux6761ux4ef6}

\subsubsection{4.2.1 Setup}\label{ux8bbeux5b9a-1}

引入 PPE post-, observationdataExtensionis 三元组 \((X, P, Y)\), where \(P\)
isphysicalLocation (mayobservation的physical quantity------atom坐mark、残基Location等). key区别: 

\begin{itemize}
\tightlist
\item
 \textbf{fixed PE}: \(\text{PE}(\cdot)\)
 is预Definition的determinismfunction (such assinusoidal encoding), \textbf{not-dependenceattrainingdata}. 
\item
 \textbf{learnable PE}: \(\text{PE}_\theta(\cdot)\) parameter \(\theta\)
 isFrom datamiddlelearning (such asthroughgradient descentoptimizesome objective function). 
\end{itemize}

\subsubsection{4.2.2 fixed PE
的case (Trivial)}\label{ux56faux5b9a-pe-ux7684ux60c5ux51b5trivial}

\textbf{Proposition 4.1 (fixed PE maintains Theorem 3)}: If 
\(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
is一 \textbf{fixed 、anddataNoneclose的}function, thenstrengthensback的observationdistribution依howevermutualsame: 

\[P_{W_A}(X, P, \text{PE}(P), Y) = P_{W_B}(X, P, \text{PE}(P), Y)\]

Theorem 3 indistinguishability\textbf{完fullmaintains}. 

\emph{Proof}: becauseis \(\text{PE}\)
isfixed determinismfunction, \((X, P, \text{PE}(P), Y)\) is \((X, P, Y)\)
的一 determinismfunction. and \(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\) (physicalLocation
\(P\)
byphysicalstructure决determine, is independent ofLabelgenerateprocess的世界选择). becausethisstrengthensback的joint distributionalsomutualsame. \(\square\)

\subsubsection{4.2.3 learnable PE
的case (key)}\label{ux5b66ux4e60ux578b-pe-ux7684ux60c5ux51b5ux5173ux952e}

\textbf{Theorem 4.1 (learnable PE breaking Theorem 3 precise conditions)}: 

setup \(\text{PE}_\theta\) parameter \(\theta\) throughminimum化经验risk
\(\hat{R}_n(\theta) = \frac{1}{n}\sum_{i=1}^{n} \ell(f_\theta(x_i, p_i), y_i)\)
From trainingdatamiddlelearning (where \(\ell\) issome loss function, \(f_\theta\) iscontains PE
encoding的Completemodel). thenatwithlowerConditionslower, 世界 \(W_A\) and \(W_B\)
变is \textbf{maydistinguish}: 

\textbf{(C1) information theorynon-平凡ness}: 

\[I(Y; P \mid X) > 0\]

i.e.: physicalLocationattodetermine原始Characteristicslower携带regardingLabel的information. 

\textbf{(C2) learningtoLabelnoise的sensitiveness}: existenceat一 non-空的trainingsamplesubset
\(\mathcal{D}_{\text{diff}} \subset \mathcal{D}\), atthis些sampleupper: 

\[\nabla_\theta \ell(f_\theta(x, p), y_A) \neq \nabla_\theta \ell(f_\theta(x, p), y_B)\]

where \(y_A\) is \(W_A\) middleshouldsample的noiseLabel, \(y_B = y_A\) (in Theorem 3
构造middle, two 世界has mutualsame的observationLabel\(y\), onlyis解释is different). in practicein Theorem
3 构造middle, \textbf{observationto \((x, y)\) 完fullmutualsame}! 

this意味着: learnable PE
breaking indistinguishability的mechanism\textbf{is NOT }fromLabelDifference (becauseis Labelmutualsame), andisfrommodelto''such as何去learning''的Difference. 让我Exact Analysis. 

\subsubsection{4.2.4
Exact mechanism: 表征漂移}\label{ux7cbeux786eux673aux5236ux8868ux5f81ux6f02ux79fb}

\textbf{key洞察}: in Theorem 3
的构造middle, \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)
is成立的, but\textbf{this并not意味着learning动态mutualsame}. Reasonatat: 

in \(W_A\) (Labelnoise世界)middle: - 样this \((x, y)\) Label \(y\) isincorrect -
physicalLocation \(p\) and \((x, y)\) relationshipis: \(p\) and \(x\)
consistent (byphysicalstructure决determine), but \(p\) dark示的trueLabeland \(y\) is different - when PE
的parameterbylearningtime, modelwilldiscover \((p, y)\) 'sbetween的''矛盾''------\(p\)
dark示some Label, and \(y\) is另一 

in \(W_B\) (this质Difficulty世界)middle: - 样this \((x, y)\) Label \(y\) iscorrect -
\(p\) and \(x\) and \(y\) allconsistent------does not exist''矛盾'' - modellearningto的
\((p, y)\) relationshipisand谐consistent的

\textbf{key区别}: i.e.使
\(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\) (联combinedobservationdistributionmutualsame), learning\textbf{动态}depends onloss面的几何structure------andshouldstructureatConditionsdistribution
\(P_{Y|X,P}\) 方面has Difference (becauseis Theorem 3 构造-dependenceatConditionsdistribution的Difference, i.e.
\(X\) intodetermine \((Y, \text{world})\) lower的distributionis different). 

\textbf{thisis Theorem 3
构造的core}: \(P_{W_A}(Y \mid X) = P_{W_B}(Y \mid X)\) (observationdistributionmutualsame), butgenerateprocessis different: 
- \(W_A\): \(Y_{\text{true}}\) 先by \(X\) 决determine, howeverpost-\(Y_{\text{obs}}\)
withprobability \(\eta\) is翻转 -
\(W_B\): \(Y_{\text{obs}} = Y_{\text{true}}\), but
\(P(Y_{\text{true}} \mid X)\) insome些区域approachesuniform匀 (highnotdeterminism)

when引入 \(P\) post-, in \(W_A\) middle \(P(Y_{\text{true}} \mid X, P)\) cancan比
\(P(Y_{\text{obs}} \mid X, P)\) more尖锐 (becauseis \(P\)
帮助确determinetrueLabel), leads tomodelin \((X, P)\)
spacemiddle''看to''noiseLabel的矛盾. andin \(W_B\)
middle, \(P(Y_{\text{obs}} \mid X, P) = P(Y_{\text{true}} \mid X, P)\), nothas 矛盾. 

\textbf{butthisstillhowevernothas to出mayobservation的Difference}------becauseis intrainingtime, modelattwo 世界middle看to的allismutualsame的
\((x, p, y)\) 三元组. 

truecurrently使learnable PE can够distinguish的mechanismis: Theorem 3
的构造middletwo 世界的区别atat\textbf{notobservationto的潜atvariable} (trueLabel vs
noiseLabel vs Difficulty程degree). such as果 \(P\)
provide了regardingthis些notobservationvariable的information窗口 (through
\(I(Y_{\text{true}}; P \mid X) > 0\)), that么\textbf{attrainingprocessmiddle}, learningto的
PE parameter \(\theta\)
willthroughgeneralizationlinesis anti-映this些Difference------i.e.使trainingdata看起来mutualsame. 

\subsubsection{4.2.5
Exact candistinguishnessConditions------mostfinalform}\label{ux7cbeux786eux53efux533aux5206ux6027ux6761ux4ef6ux6700ux7ec8ux5f62ux5f0f}

让我From morebasic的AspectheavynewAnalysis. 

in Theorem 3 Proofmiddle, two 世界的构造is: - 选择two is different的Conditionsdistribution
\(P_{W_A}(Y \mid X)\) and \(P_{W_B}(Y \mid X)\), but边际distribution \(P(X, Y)\)
mutualsame - this's withmay, isbecauseis \(P(X)\) intwo 世界middleis different →
\(P_{W_A}(X) \neq P_{W_B}(X)\), but \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)

等等, thisis not correct. Theorem 3 说的is\textbf{observation}distributionmutualsame. 让我From CC
Audit Report的原始构造出发. 

CC Audit Report: ``existenceattwo 世界 \(W_A, W_B\), 具has observation等价的Data Distribution
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)''

构造Methodology: two 世界具has is different \(P(X)\) andis different的
\(P(Y|X)\), but它们的乘积mutualsame. specifically: - \(W_A\): \(P_{W_A}(X)\)
at''容易''区域has moremanyquality, \(P_{W_A}(Y|X)\) isdeterminism -
\(W_B\): \(P_{W_B}(X)\) in''Difficulty''区域has moremanyquality, \(P_{W_B}(Y|X)\)
isapproachesuniform匀的

makes \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\) to has \((x, y)\) 成立. 

\textbf{when引入 \(P\)
back}: \(P_{W_A}(X, P, Y) = P_{W_A}(P \mid X, Y) \cdot P_{W_A}(X, Y)\). 

such as果 \(P\) isphysicallyby \(X\) 决determine (\(P\) is \(X\) physical坐mark), then
\(P(P \mid X, Y) = P(P \mid X)\) (physicalLocationnot-dependenceatLabel). thistime: 
\[P_{W_A}(X, P, Y) = P_{W_A}(P \mid X) \cdot P_{W_A}(X, Y)\]
\[P_{W_B}(X, P, Y) = P_{W_B}(P \mid X) \cdot P_{W_B}(X, Y)\]

but
\(P_{W_A}(P \mid X) = P_{W_B}(P \mid X)\) (physicalLocationbyphysicalstructure决determine, totwo 世界mutualsame), and
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\). becausethis
\(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\). 

\textbf{fixed PE stillhoweverpreserves unidentifiability}------joint distributionmutualsame. 

\textbf{learnable PE case}: learnable PE parameter \(\theta\)
isthroughminimum化some loss functionFrom datamiddle学to的. consideratdata集
\(\mathcal{D} = \{(x_i, p_i, y_i)\}_{i=1}^n\) upper的training: 

\[\hat{\theta} = \arg\min_\theta \frac{1}{n} \sum_{i=1}^n \ell(f_\theta(x_i, \text{PE}_\theta(p_i)), y_i)\]

this里的keyis: \textbf{PE
的parameteratloss functionmiddle}------notonlythroughencoding输出impactloss, stillmaythroughsomekindself-监督orreconstructionlossimpactlearning. 

butsuch as果 PE
pure粹ishas 监督地training (onlythroughlower游任务的loss), that么two 世界to出mutualsame的trainingdata
→ mutualsame的gradient → mutualsame \(\hat{\theta}\). in this case, i.e.使islearnable PE
alsocannotdistinguishtwo 世界. 

\textbf{truecurrently使learnable PE can够distinguish的scenario}: 

PE parameterexcept受监督lossimpactouter, stillthroughwithlower方式接收信号: 1.
\textbf{self-监督预training} (such as masked position
prediction)------atmarkNote'sfront进lines 2. \textbf{辅助loss} (such asLocationreconstructionloss)
3. \textbf{physical constraint} (such asenergy守恒、力匹配)------this些约束provide了regarding \(P\)
andphysical quantity'sbetweenrelationship的额outerinformation

atthis些caselower, PE
的parameterpartby\textbf{physicalconsistentness信号}决determine, andthis些信号attwo 世界middlemayis different (becauseis two 世界middlesample的''physicalreasonableness''is different). 

\textbf{Exact Theorem}: 

\textbf{Theorem 3' (correctionback的indistinguishabilityTheorem------带 PPE pre-提)}: 

\[\boxed{\begin{aligned}
\text{If } & \text{(A) } \text{PE}(\cdot) \text{ isandLabel } Y \text{ independent fixed function (i.e.itsparameternotby } Y \text{ or任何-dependenceat } Y \text{ 信号决determine), and} \\
& \text{(B) } P \perp\!\!\!\perp Y_{\text{world}} \mid X \text{ (physicalLocationattodetermine原始Characteristicsloweris independent of世界选择), } \\
\text{then } & P_{W_A}(X, \text{PE}(P), Y) = P_{W_B}(X, \text{PE}(P), Y), \text{Theorem 3 indistinguishabilitymaintains. } \\
\text{反's, If } & \text{PE parameterthrough-dependenceat } Y \text{ 信号learning得to, and } I(Y_{\text{true}}; P \mid X) > 0, \\
\text{then } & \text{learningto的encodingattwo 世界middlewithcurrentlyprobabilityis different: } P(\hat{\theta}_{W_A} \neq \hat{\theta}_{W_B}) > 0, \\
\text{two 世界变is candistinguish. } &
\end{aligned}}\]

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.3
构造nessProof: minimum示例}\label{ux6784ux9020ux6027ux8bc1ux660eux6700ux5c0fux793aux4f8b}

\subsubsection{4.3.1 构造}\label{ux6784ux9020}

consider一 extremely简的二维separate类Problem. setup \(X \in \{-1, +1\}\)
is一 二元Characteristics, \(P \in \{0, 1\}\)
is一 二元physicalLocation, \(Y \in \{-1, +1\}\) isLabel. 

\textbf{世界 \(W_A\) (Labelnoise)}: 

\begin{itemize}
\tightlist
\item
 \(P_{W_A}(X = +1) = 1/2\), \(P_{W_A}(X = -1) = 1/2\)
\item
 \(P_{W_A}(Y_{\text{true}} = +1 \mid X = +1) = 1\),
 \(P_{W_A}(Y_{\text{true}} = -1 \mid X = -1) = 1\) (determinism的)
\item
 Labelwithprobability \(\eta = 0.2\) 翻转: \(Y_{\text{obs}} = Y_{\text{true}}\)
 withprobability \(0.8\), \(Y_{\text{obs}} = -Y_{\text{true}}\) withprobability \(0.2\)
\item
 physicalLocation \(P\): \(P = X\) (determinism), i.e. \(p = 1\) when
 \(x = +1\), \(p = 0\) when \(x = -1\)
\end{itemize}

\textbf{世界 \(W_B\) (this质Difficulty)}: 

\begin{itemize}
\tightlist
\item
 \(P_{W_B}(X = +1) = 0.6\), \(P_{W_B}(X = -1) = 0.4\) (non-uniform匀! )
\item
 \(P_{W_B}(Y_{\text{obs}} = +1 \mid X = +1) = 5/6\),
 \(P_{W_B}(Y_{\text{obs}} = -1 \mid X = +1) = 1/6\)
\item
 \(P_{W_B}(Y_{\text{obs}} = +1 \mid X = -1) = 1/4\),
 \(P_{W_B}(Y_{\text{obs}} = -1 \mid X = -1) = 3/4\)
\item
 \(Y_{\text{obs}} = Y_{\text{true}}\) (nothas Labelnoise, butConditionsdistributionisintrinsichas highnotdeterminism)
\item
 \(P = X\) (and \(W_A\) mutualsame)
\end{itemize}

\textbf{verifyobservation等价}: 

in \(W_A\) middle: - \(P_{W_A}(X = +1, Y = +1) = 0.5 \times 0.8 = 0.4\) -
\(P_{W_A}(X = -1, Y = +1) = 0.5 \times 0.2 = 0.1\) -
\(P_{W_A}(X = +1, Y = -1) = 0.5 \times 0.2 = 0.1\) -
\(P_{W_A}(X = -1, Y = -1) = 0.5 \times 0.8 = 0.4\)

in \(W_B\) middle: - \(P_{W_B}(X = +1, Y = +1) = 0.6 \times (5/6) = 0.5\) -
\(P_{W_B}(X = -1, Y = +1) = 0.4 \times (1/4) = 0.1\) -
\(P_{W_B}(X = +1, Y = -1) = 0.6 \times (1/6) = 0.1\) -
\(P_{W_B}(X = -1, Y = -1) = 0.4 \times (3/4) = 0.3\)

\(P(X=+1,Y=+1)\) is different (0.4 vs
0.5), does not satisfyobservation等价. 让我adjust构造使its完fullmutual等. 

heavynew构造: 

\(W_A\): - \(P(X=+1) = 0.5\), \(P(X=-1) = 0.5\) -
\(P(Y=+1 \mid X=+1) = 0.8\), \(P(Y=-1 \mid X=+1) = 0.2\) -
\(P(Y=+1 \mid X=-1) = 0.2\), \(P(Y=-1 \mid X=-1) = 0.8\) -
generatemechanism: \(Y_{\text{true}}\) isdeterminism (\(X=+1 \to +1\),
\(X=-1 \to -1\)), but 20\% noise

\(W_B\): - \(P(X=+1) = 0.5\), \(P(X=-1) = 0.5\) - and \(W_A\) 完fullmutualsame的
\(P(Y \mid X)\) - generatemechanism: Characteristicsthis身ishighnotdeterminism (nothas implicit藏的trueLabel)

atthis构造lower \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\), butintrinsicstructureis different. in \(W_A\)
middle, \(P = X\), \(Y_{\text{true}} = X\), noisewith 20\% probability翻转. in \(W_B\)
middle, \(P = X\), but \(Y\) 直接by带noise的通道generate (Noneimplicit藏层). 

\subsubsection{4.3.2 learnable PE
的maydistinguishness}\label{ux5b66ux4e60ux578b-pe-ux7684ux53efux533aux5206ux6027}

引入一 简single的learnable 
PE: \(\text{PE}_\theta(p) = \theta \cdot p\) (mark量encoding, \(\theta \in \mathbb{R}\)
maylearning). 

Completemodel: \(f(x, p) = \text{sign}(w \cdot x + \text{PE}_\theta(p))\). 

setup我们throughwithlower方式learning \(\theta\): 1.
先at\textbf{NoneLabel}dataupperwithself-监督方式预training: predictionis mask physicalLocation
\(\hat{p} = g(\text{PE}_\theta(p))\) 2. howeverbackatlower游任务uppermicro-调

key: atStep 1 (self-监督预training)middle, objective functiondoes not involveLabel
\(Y\). two 世界的data \((X, P)\)
distribution完fullmutualsame (\(P_{W_A}(X, P) = P_{W_B}(X, P)\)), becausethis预training阶段\textbf{not引入}distinguish. 

butsuch as果我们usewithlower辅助loss: 

\[\mathcal{L}_{\text{aux}}(\theta) = \mathbb{E}_{(x,p,y)}\left[(\text{PE}_\theta(p) - \mathbb{E}[Y \mid X = x])^2\right]\]

i.e.: encodingshouldandConditionsexpectLabelconsistent. this loss涉及
\(Y\), and-dependenceat世界的innerpartstructure. in \(W_A\)
middle, \(\mathbb{E}[Y \mid X = x] = (1-2\eta) \cdot \text{sign}(x) = 0.6 \cdot \text{sign}(x)\) (becauseis noise翻转). at
\(W_B\)
middle, \(\mathbb{E}[Y \mid X = x] = \mathbb{E}[Y \mid X = x] = 0.6 \cdot \text{sign}(x)\) (数值mutualsame! onlyis解释is different). 

 withat\textbf{this minimum示例middle}, i.e.使islearnable PE
alsocannotdistinguishtwo 世界------becauseis Conditionsexpectmutualsame. 

\textbf{thiscurrentlyis Theorem 3
的精髓}: such as果observationdistribution完fullmutualsame, 任何algorithmallcannotdistinguish. 

\subsubsection{4.3.3 open problems: learnable PE
truecurrentlycandistinguish的Conditions}\label{ux5f00ux653eux95eeux9898ux5b66ux4e60ux578b-pe-ux771fux6b63ux80fdux533aux5206ux7684ux6761ux4ef6}

\textbf{This minimal example actually proves Theorem 3
robustness, not its fragility}: atthis 构造middle, i.e.使islearnable PE
alsocannotdistinguishtwo observation等价的世界. this恰恰印证 Theorem 3
的精髓------whenobservationdistribution完fullmutualsametime, 任何algorithmallcannotdistinguish. 

原先声称的''构造nessminimum示例''反andconstitutes Theorem 3'
breaking direction的一 \textbf{反例}: 它demonstrate了一 learnable PE
\textbf{Nonemethod}distinguishtwo observation等价世界的scenario. 

must使learnable PE
candistinguish, require满足: \textbf{learning信号notonlyonly-dependenceatobservationdistribution}. theoreticallythismay发生at: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{many任务learning}: PE sametimebyused forprediction另一 and \(Y_{\text{true}}\)
 mutualclose的量 \(Z\) (例such asenergy、力), and \(Z\) in世界
 \(W_A\) (noiseLabel意味着physical quantityis inconsistent)and
 \(W_B\) (Difficultybutphysicalconsistent) in the 表现is different
\item
 \textbf{physicalconsistentness约束}: loss functioncontainsphysical constraint项 (such asenergy守恒), makesat
 \(W_A\) middlenoiseLabelleads tophysicalis inconsistent的prediction, andin \(W_B\)
 middleDifficultysamplestillmaintainsphysicalconsistentness
\item
 \textbf{distributionoutergeneralization}: PE
 parameterpartbydistributionouterdata (differs fromtrainingdistribution)决determine, andtwo 世界atdistributionouter表现is different
\end{enumerate}

\textbf{{[}open problems{]}}
howeverand, 目front\textbf{lack of构造nessProof}verifyupper述任何mechanism确实canatobservation等价的two 世界upperproducesis different的Optimal 
PE parameter. thisrequire: - explicit式Definitionnotobservationvariable \(Z\) - 构造
\(\mathcal{L}_{\text{aux}}\) 具体form - Proofin \(W_A\) and \(W_B\)
 in the Optimal 值确实is different

Theorem 4.1 陈述 (regarding
\(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{sup}} + \lambda \mathcal{L}_{\text{aux}}\)
的is differentOptimal 值leads tomaydistinguishness)at逻辑upperiscorrect的\textbf{Conditions陈述}, butshouldConditionsiswhethercanat
Theorem 3 构造middleby满足, 目frontnothas 构造nessProof. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.4 correctionpost-Theorem
3'------Complete陈述}\label{ux4feeux6b63ux540eux7684-theorem-3ux5b8cux6574ux9648ux8ff0}

\textbf{Theorem 3' (indistinguishability的Conditionsmaintains------Corrected Version)}: 

\[\boxed{\begin{aligned}
\textbf{front提 (A): } & \text{physicalLocationencoding PE isfixed , oronlythroughis independent ofLabel } Y \text{ 信号learning. } \\
\textbf{front提 (B): } & \text{physicalLocation } P \text{ intodetermine } X \text{ lowerConditionsis independent of世界选择: } P \perp\!\!\!\perp W \mid X. \\
\textbf{Conclusion: } & \text{Theorem 3 indistinguishability完fullmaintains: } \\
& \forall \text{ 决策规then } d, \quad \max_{W \in \{W_A, W_B\}} P_W(d \neq W) \geq \frac{1}{2}. \\
\textbf{If premise (A) does not hold: } & \text{whether unidentifiability is broken is currently an**open problems**. } \\
& \text{learnable PE cancanthroughmany任务learningorphysicalconsistentness约束等mechanismdistinguishtwo 世界, } \\
& \text{butwhenfrontlack of构造nessProof. see §4.3 讨论. }
\end{aligned}}\]

\textbf{Corollary 4.4.1 (topractice的impact)}: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{fixed currently弦 PE orfixed 旋转 PE}: Theorem 3
 完fullmaintains. Locationencodingwill not损害mostbadcase的indistinguishabilityguarantee------butalso\textbf{will notmain动帮助}distinguishnoiseandDifficultysample (tofewatinformation-theoretic senseupper, throughobservationdistributionNonecannot distinguish). 
\item
 \textbf{From NoneLabelLocationdatalearning的
 PE} (such asthroughself-监督reconstructionLocation): such as果trainingprocessdoes not involveLabel \(Y\), Theorem 3
 maintains. PE parameterattwo 世界middle学tomutualsame的值. 
\item
 \textbf{throughmany目marklearning (includesLabelmutualclose目mark) PE}: Theorem 3
 \textbf{maybybreaking }, but目frontlack of构造nessProof. thisis\textbf{open problems}------Locationinformationiswhethertrue的provide了一 额outer的''窗口''来distinguishnoiseandDifficultysample, stillis一 notbyrigorousverify的Conjecture. 
\item
 \textbf{risk}: i.e.使辅助目markmay帮助distinguish, If itsdesign引入虚falsemutualcloseness, mayleads toincorrect的''distinguish'', willDifficultysampleincorrect地Markedis noise. thisrequire仔fine的experimentverify. 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.5 and CC Audit Report §2.5
的comparison}\label{ux4e0e-cc-ux5ba1ux8ba1ux62a5ux544a-2.5-ux7684ux6bd4ux8f83}

CC Audit Reportto Theorem 3 Analysisiscorrect (fixed PE preserves unidentifiability, learnable 
PE cancanbreaking ), but遗漏了withlowerkey点: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{CC reportnotExact Definition''learnable 
 PE''的Meaning}: only说''From datamiddlelearning''isis insufficient的------requirebright确distinguish''From NoneLabeldatalearning'' (safety)and''From Label-dependence信号learning'' (maybreaking 
 Theorem 3). 
\item
 \textbf{CC reportnotto出maydistinguishnessprecise conditions}: Theorem 4.2 填补了this一空white. 
\item
 \textbf{CC
 report的评价''thisisgood消息''require限determine}: onlyhas in辅助目mark信号andtrueLabelnoisemutualclose (andnon-虚falsemutualclose)time, learnable 
 PE distinguishcan力only thenismay靠的good消息. 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Summary}\label{ux603bux7ed3}

this文to PPE in SCX framework in the 作用进lines了rigorous的mathematicalDerivation, primary贡献includes: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
 \textbf{encodingfunction的form化} (第 1
 part): provides三kindphysicalLocation域的currently弦and旋转encoding的Exact Definition, Derivation了based on
 Bochner Theoremand Laplace kernel的Optimal 频率谱, provesencoding Lipschitz
 continuousness并compute了Exact 常数. 
\item
 \textbf{Theorem 1 correction} (第 2 part): correction CC Audit Report in the sign
 error, provides \(\delta_s^{\text{PE}}\) correct表达式, proves
 \(\delta_s^{\text{PE}} > 0\)
 Sufficient Condition (\(I(Y; P \mid S) > 0\)), 并Derivation了based ondataat理inequalityand
 Fano inequality的upperlower bound. 
\item
 \textbf{Theorem 2 correction} (第 3 part): provides
 \(\varepsilon_{\text{PE}}\) Exact information theoryDefinition, proves
 \(\varepsilon_{\text{PE}} \to 0\)
 timeupper bound恢复is 原始form, 并provide了From datamiddlemayestimate
 \(\varepsilon_{\text{PE}}\) twokind实用Methodology (KSG
 estimate器andpredictionperformanceDifference). 
\item
 \textbf{Theorem 3 correction} (第 4 part): Exact 阐bright了fixed PE andlearnable PE
 toindistinguishability的is differentimpact, throughminimum构造ness示例demonstrate了distinguish的necessaryConditions, 提出了containsExact pre-提Conditions的correction
 Theorem 3'. 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{Derivationcomplete日期: 2026-06-29}\\
\emph{Note: has TheoremNumber遵循 SCX frameworkinnerpart约determine. this文correction CC
Audit Report in the Symbol/Conceptis inconsistent (特别is §2.3 Proposition 2.1 sign
error), 并atshouldreport的Foundationupperincreases了rigorous的upperlower boundDerivation、构造nessProofandmayestimateform. }

\end{document}
