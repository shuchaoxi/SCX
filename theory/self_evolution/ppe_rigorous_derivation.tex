% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Physical Positional Encoding (PPE) at  SCX
framework in 's严密推导}\label{physical-positional-encoding-ppe-ux5728-scx-ux6846ux67b6ux4e2dux7684ux4e25ux5bc6ux63a8ux5bfc}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: 严格number learning 推导\\
\textbf{Prerequisite Reading}:
\texttt{multi\_head\_spring\_and\_positional\_encoding\_analysis.md}(CC
Audit Report)\\
\textbf{Methodology}: conditions + Assumptions → theorem  → Proof, each one target Assumptionsconditions\\
\textbf{修订}: 修正 CC 审计报告in 'sSymbolnon-one and  sign error

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Symbol约}\label{ux7b26ux53f7ux7ea6ux5b9a}

as maintain one 致, using  CC 审计报告'smust Symbol, but at requires must re-精determine 义. 

\subsubsection{Status-expert System}\label{ux72b6ux6001-ux4e13ux5bb6ux7cfbux7edf}

\begin{itemize}
\tightlist
\item
  \(\mathcal{S} = \{s_1, \ldots, s_N\}\): \(N\)
  discrete state atom, \(s_i \in \mathbb{R}^{d_s}\)
\item
  \(\mathcal{P}\): physicalLocationspace 
\item
  \(M\): independent expert number quantity , \(\{E_1, \ldots, E_M\}\)
\item
  \(y_i \in \mathcal{Y}\): Statusfactor  \(s_i\) 'starget 签
\end{itemize}

\subsubsection{expert 投票}\label{ux4e13ux5bb6ux6295ux7968}

for each Sample \((s_i, y_i)\), expert  \(m\) 'sas : 

\[v_m(s_i) = \mathbf{1}[E_m(s_i) \neq y_i] \in \{0, 1\}\]

\begin{itemize}
\tightlist
\item
  \(v_m = 0\): expert give target 签
\item
  \(v_m = 1\): expert disagree withgive target 签
\end{itemize}

at CleanSampleunder (target Correct):\(v_m \sim \text{Bernoulli}(p_{\text{clean}, s})\), where
\(p_{\text{clean}, s} < 0.5\)
at NoiseSampleunder (target Error):\(v_m \sim \text{Bernoulli}(p_{\text{noisy}, s})\), where
\(p_{\text{noisy}, s} > 0.5\)

\textbf{Detection边际}: 

\[\boxed{\Delta_s = p_{\text{noisy}, s} - p_{\text{clean}, s} > 0}\]

(this and  CC 报告's
\(\Delta_s = |p_{\text{clean},s} - p_{\text{noisy},s}|\) one 致, because as 
\(p_{\text{noisy}} > p_{\text{clean}}\). )

\subsubsection{one property number and value }\label{ux4e00ux81f4ux6027ux8ba1ux6570ux4e0eux9608ux503c}

\[C(s) = \sum_{m=1}^{M} v_m(s)\]

决策阈value 
\(\tau_s = M \cdot \frac{p_{\text{clean},s} + p_{\text{noisy},s}}{2}\). target as Noisewhen 
\(C(s) \geq \tau_s\). 

\subsubsection{Chernoff-Hoeffding
基础}\label{chernoff-hoeffding-ux57faux7840}

Hoeffding non-etc.formula : for  i.i.d. Bernoulli 随变quantity 
\(v_1, \ldots, v_M\), all value as  \(\mu\): 

\[P\left(\frac{1}{M}\sum_{m=1}^{M} v_m - \mu \geq t\right) \leq \exp(-2M t^2)\]

should using NoiseDetection:  - 假阳property (Type
I):\(P_{H_0}(C(s) \geq \tau_s) \leq \exp\left(-2M\left(\frac{\tau_s}{M} - p_{\text{clean},s}\right)^2\right)\)
- 假阴property (Type
II):\(P_{H_1}(C(s) < \tau_s) \leq \exp\left(-2M\left(p_{\text{noisy},s} - \frac{\tau_s}{M}\right)^2\right)\)

取 \(\tau_s = M(p_{\text{clean},s} + p_{\text{noisy},s})/2\), all 
\(\leq \exp(-M\Delta_s^2/2)\). 

\begin{quote}
\textbf{注}: CC 审计报告make using  \(\exp(-2M\Delta_s^2)\). this 表明its 
\(\Delta_s\) 'sdefinition can be can non-(can be can one  1/2
because factor ). at maintain number learning precise property 's前提under , \textbf{遵循 CC 报告's约}: will 
\(\Delta_s\) re-校准, make  bound as 
\(\exp(-2M\Delta_s^2)\). this when definition 
\(\tilde{\Delta}_s = (p_{\text{noisy}} - p_{\text{clean}})/2\), at 原始报告in as 
\(\Delta_s\). Analysisin , precise 'snumber because factor non-influence 推导Structure. 
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 1
part : code Function's严密number learning formula ization }\label{ux7b2c-1-ux90e8ux5206ux7f16ux7801ux51fdux6570ux7684ux4e25ux5bc6ux6570ux5b66ux5f62ux5f0fux5316}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.1
physicalLocation域'sdefinition }\label{ux4e09ux79cdux7269ux7406ux4f4dux7f6eux57dfux7684ux5b9aux4e49}

\textbf{definition  1.1.1(target quantity Location域------蛋白质order sequence )}

\[\mathcal{P}_{\text{seq}} = \{1, 2, \ldots, L\} \subset \mathbb{N}\]

where \(L\) as order sequence 长. for continuous approximate , 嵌入as 
\(\mathcal{P}_{\text{seq}} \cong [0, L] \subset \mathbb{R}\). 

physicalMeaning: 残基索引携带local Chemistry环境'sone 维顺order 信息(N 端→C 端). 

\textbf{definition  1.1.2(3D toward quantity Location域------原factor target )}

\[\mathcal{P}_{\text{3D}} = \mathbb{R}^3\]

元素 \(\mathbf{p} = (x, y, z) \in \mathbb{R}^3\)
as factor at space in 's笛卡尔坐target . 

physicalMeaning: factor 'sspace Location携带键长, 键角, 配位number etc.几何信息. this Yes\textbf{最丰富's}physicalLocation域. 

\textbf{definition  1.1.3(target quantity number 域------原factor number )}

\[\mathcal{P}_{\text{count}} = \mathbb{N}\]

元素 \(N\) as System's总原factor number . 

physicalMeaning: Systemcan quantity 'sDimension, can be can 'sModenumber , 缺陷浓etc.. for Systemespecially must (has 限尺寸效should ). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.2
target quantity Location's正弦编code : Optimalrate 谱}\label{ux6807ux91cfux4f4dux7f6eux7684ux6b63ux5f26ux7f16ux7801ux6700ux4f18ux9891ux7387ux8c31}

\subsubsection{1.2.1 code definition }\label{ux7f16ux7801ux5b9aux4e49}

\textbf{definition  1.2.1(正弦target quantity Locationcode )}: for  \(p \in [0, L]\), Dimension
\(d\)(偶number ):

\[\boxed{\text{PE}_{\text{scalar}}(p, 2j) = \sqrt{\frac{2}{d}}\,\sin\left(\frac{2\pi p}{\lambda_j}\right), \quad \text{PE}_{\text{scalar}}(p, 2j+1) = \sqrt{\frac{2}{d}}\,\cos\left(\frac{2\pi p}{\lambda_j}\right)}\]

where \(j = 0, 1, \ldots, d/2 - 1\), \(\lambda_j > 0\)
as \textbf{feature波长}. 

归one ization because factor  \(\sqrt{2/d}\) Guarantee
\(\|\text{PE}_{\text{scalar}}(p)\| = 1\) for which has  \(p\) 恒立(\(d/2\) 
\((\sin,\cos)\) for contribution  \((2/d) \cdot 1 = 2/d\), and as 
\(1\)), make code at  \(d \to \infty\) \textbf{Convergenceto }Objective核
\(k(\Delta)\) rather than diverging to \(O(d)\) quantity level . 

\begin{quote}
\textbf{Parameterization }: standard  Transformer make using 
\(\lambda_j = 2\pi \cdot 10000^{2j/d}\)(i.e.
\(\omega_j = 10000^{2j/d}\), where
\(\text{PE}(p, 2j) = \sin(p/\omega_j)\)). 

make using \textbf{波长Parameterization }
\(\lambda_j\), because as direct for should physicalscale : \(\lambda_j\)
is aComplete正弦周期for should 'sLocation差. 
\end{quote}

\textbf{编code 's内积Structure(核表示)}: 

\[\langle \text{PE}_{\text{scalar}}(p), \text{PE}_{\text{scalar}}(q) \rangle = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos\left(\frac{2\pi(p-q)}{\lambda_j}\right)\]

this definition one \textbf{平移non-变核}
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_j \cos(2\pi\Delta/\lambda_j)\), where
\(\Delta = p - q\). 

at  \(\Delta = 0\)
处: \(\langle \text{PE}(p), \text{PE}(p) \rangle = (2/d) \cdot (d/2) = 1 = k(0)\). when 
\(d \to \infty\)
, \((2/d)K_{\text{PE}}(\Delta) \to \int_0^\infty S(\omega)\cos(\omega\Delta)d\omega = k(\Delta)\). 

\subsubsection{1.2.2
Optimalrate 谱推导}\label{ux6700ux4f18ux9891ux7387ux8c31ux63a8ux5bfc}

\textbf{Problem}: give code Dimension \(d\) and physical区间长 \(L\), such as Selection
\(\{\lambda_j\}_{j=0}^{d/2-1}\) make code ''Optimal''？

\textbf{Assumptions
1.2.1(physicallocal property 公)}: physicalon 邻'sLocationshould has similar 'scode . concrete , at one 递减's\textbf{physicalcorrelated 核}
\(k(\Delta)\)(where
\(\Delta = |p-q|\))control Location表示should has 'ssimilar . for 凝聚态physicalSystem: 

\[k(\Delta) = \exp\left(-\frac{\Delta}{\xi}\right)\]

where \(\xi > 0\) Yes\textbf{physicalcorrelated 长}(e.g.such as : 键长 \(\approx 1.9\)
Å, or 屏蔽长). 该核satisfies  Bochner theorem : Yes正's, because while Yesnon-负测's
Fourier 变换. 

\textbf{theorem  1.2.1(编code 'sOptimalrate 谱------Kernel Mean Embedding 视角)}

设Objective \(k(\Delta) = \exp(-|\Delta|/\xi)\)(Laplace
核). then 规范ization code 核
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos(2\pi\Delta/\lambda_j)\)
at  \(L^2([0, L])\) in Optimal逼近 \(k(\Delta)\)(minimum ization all error 
\(\int_0^L |\frac{2}{d}K_{\text{PE}}(\Delta) - k(\Delta)|^2 d\Delta\))when and only when 
\(\{\lambda_j\}\) satisfies with under 频谱Distribution: 

\[\boxed{\lambda_j = 2\pi\xi \cdot \cot\left(\frac{\pi(2j+1)}{2d}\right), \quad j = 0, 1, \ldots, \frac{d}{2}-1}\]

\emph{Proof}: 

\textbf{步骤 1(Bochner 表示)}: Laplace 核
\(k(\Delta) = \exp(-|\Delta|/\xi)\) 's Fourier 变换as : 

\[\hat{k}(\omega) = \int_{-\infty}^{\infty} e^{-i\omega\Delta} e^{-|\Delta|/\xi} d\Delta = \frac{2\xi}{1 + \xi^2\omega^2}\]

this is a Cauchy Distribution(scale Parameter \(1/\xi\)). 

\textbf{步骤 2(积表示)}: by  Fourier 反演Formula: 

\[k(\Delta) = \frac{1}{2\pi} \int_{-\infty}^{\infty} e^{i\omega\Delta} \hat{k}(\omega) d\omega = \int_{0}^{\infty} \cos(\omega\Delta) \cdot \frac{2}{\pi} \cdot \frac{\xi}{1 + \xi^2\omega^2} d\omega\]

whereusing using  \(\hat{k}\) 'sfor property . definition 谱密
\(S(\omega) = \frac{2\xi}{\pi(1 + \xi^2\omega^2)}\). 

\textbf{步骤 3(has 限维逼近)}: 规范ization code 核
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos(\omega_j \Delta)\), where
\(\omega_j = 2\pi/\lambda_j\), is a\textbf{etc.权discrete and }(each 项权重
\(2/d\)), 逼近continuous 积
\(\int_0^{\infty} S(\omega)\cos(\omega\Delta)d\omega\). 

Optimal采样point should make  \(\{S(\omega_j)\Delta\omega_j\}\)
approximate all 匀权重. this etc.价From累积DistributionFunction
\(Q(\omega) = \int_0^{\omega} S(u)du\) in \textbf{位number 采样}. 

\(Q(\omega) = \frac{2}{\pi} \arctan(\xi\omega)\), 总质quantity 
\(Q(\infty) = 1\). 

\textbf{步骤 4(位number 采样)}: for  \(d/2\) rate point , Part \(j\)
point should 覆盖Part \((2j+1)/d\) number : 

\[Q(\omega_j) = \frac{2j+1}{d}, \quad j = 0, 1, \ldots, \frac{d}{2}-1\]

求解: \(\omega_j = \frac{1}{\xi} \tan\left(\frac{\pi(2j+1)}{2d}\right)\). 

because 此波长
\(\lambda_j = \frac{2\pi}{\omega_j} = 2\pi\xi \cdot \cot\left(\frac{\pi(2j+1)}{2d}\right)\). 

\(\square\)

\textbf{Corollaries 1.2.1(低频截断)}: maximum 波长(\(j=0\))as : 

\[\lambda_{\max} = 2\pi\xi \cdot \cot\left(\frac{\pi}{2d}\right) \approx 4d\xi \quad (\text{for  } d)\]

this 表明编code can 捕获's最长space correlated property as  \(4d\xi\). \textbf{必须satisfies 
\(\lambda_{\max} \geq L\)} only then can 覆盖whole order sequence . this give minimum Dimensionmust 求: 

覆盖whole order sequence 区间requires satisfies 
\(\lambda_{\max} \geq 2L\)(Nyquistconditions), give minimum Dimensionmust 求: 

\[\boxed{d_{\min} \approx \frac{L}{2\xi}}\]

\textbf{Corollaries 1.2.2(and  Transformer code 's较)}: standard  Transformer make using 
\(\omega_j \propto 10000^{-2j/d}\)(几何level number ), for should \textbf{for number all 匀}rate Distribution. while physicalOptimal(Laplace
核)make using 
\(\omega_j \propto \tan(\pi(2j+1)/2d)\)(正切Distribution), 更密set 采样\textbf{in 频区域}. Difference: NLP
in 'sLocationrelationship Yes长程while non-指number 衰减's, while physicalSystem'scorrelated FunctionYes短程指number 衰减. 

\textbf{theorem  1.2.2(逼近error bound )}: for  Laplace and  \(d/2\)
频rate point 'snumber 采样, 规范ization code 核's逼近error satisfies : 

\[\sup_{\Delta \in [0, L]} \left|\frac{2}{d}K_{\text{PE}}(\Delta) - k(\Delta)\right| = O\left(\frac{1}{d}\right)\]

As \(d \to \infty\) with  \(O(1/d)\) RateConvergence. precise number dependence \(\xi\) and 
\(L\), but  \(O(1/d)\) 'sConvergenceRatenon-dependencethis Parameter. 

\emph{Proof}: this Yes带限Function Fourier
积'sstandard number value error . number 采样make each rate 区间'scontribution 平衡, maximum error maximum is 忽略'srate quantity 's
Fourier number control . and 原版non-, 此处make using 规范ization code 核
\((2/d)K_{\text{PE}}\) while non-not yet 规范ization 's \(K_{\text{PE}}\), ensure 逼近for 象
\(k(\Delta)\) and 逼近quantity one quantity level (all as  \(O(1)\)), while non-
\(K_{\text{PE}}(0)=d/2\) and  \(k(0)=1\) 'squantity level 较. \(\square\)

\subsubsection{1.2.3 正弦编code 's Lipschitz
continuous property }\label{ux6b63ux5f26ux7f16ux7801ux7684-lipschitz-ux8fdeux7eedux6027}

\textbf{theorem  1.2.3(正弦 PPE 's Lipschitz number )}: target quantity 正弦编code 
\(\text{PE}_{\text{scalar}}: [0, L] \to \mathbb{R}^d\) Yes Lipschitz
continuous 's, precise number as : 

\[\boxed{L_{\text{PE}}^{\text{scalar}} = \frac{2\sqrt{2}\,\pi}{\sqrt{d}} \cdot \sqrt{\sum_{j=0}^{d/2-1} \frac{1}{\lambda_j^2}}}\]

\emph{Proof}: for each Dimensionfor  \((2j, 2j+1)\), one ization code 
\(\text{PE}_j(p) = \sqrt{2/d}\,(\sin(2\pi p/\lambda_j), \cos(2\pi p/\lambda_j))\): 

\[\|\text{PE}_j(p) - \text{PE}_j(q)\|^2 = \frac{2}{d}\left[\left(\sin\left(\frac{2\pi p}{\lambda_j}\right) - \sin\left(\frac{2\pi q}{\lambda_j}\right)\right)^2 + \left(\cos\left(\frac{2\pi p}{\lambda_j}\right) - \cos\left(\frac{2\pi q}{\lambda_j}\right)\right)^2\right]\]

by 角恒etc.formula 
\((\sin a - \sin b)^2 + (\cos a - \cos b)^2 = 2 - 2\cos(a-b) = 4\sin^2((a-b)/2)\), again using using 
\(|\sin\theta| \leq |\theta|\): 

\[\|\text{PE}_j(p) - \text{PE}_j(q)\|^2 = \frac{2}{d} \cdot 4\sin^2\!\left(\frac{\pi(p-q)}{\lambda_j}\right) \leq \frac{2}{d} \cdot \left(\frac{2\pi|p-q|}{\lambda_j}\right)^2 = \frac{8\pi^2|p-q|^2}{d \cdot \lambda_j^2}\]

for which has  \(j\) and : 

\[\|\text{PE}_{\text{scalar}}(p) - \text{PE}_{\text{scalar}}(q)\|^2 \leq \frac{8\pi^2|p-q|^2}{d} \sum_{j=0}^{d/2-1} \frac{1}{\lambda_j^2}\]

i.e.which  Lipschitz number . Upper BoundYes紧's(\(p, q\) No限接近
\(\sin\theta \approx \theta\), etc.号渐近立). \(\square\)

\textbf{Corollaries 1.2.3(Lipschitz number asymptotic behavior)}: for number Optimal谱
\(\lambda_j = 2\pi\xi \cdot \cot(\pi(2j+1)/2d)\) and one ization code : 

\[L_{\text{PE}}^{\text{scalar}} \sim \frac{1}{\xi} \cdot \sqrt{\frac{d}{6}} \quad (d \to \infty)\]

归one ization  Lipschitz number 's增长From \(O(d)\) to
\(O(\sqrt{d})\)------归one ization because factor  \(\sqrt{2/d}\) 抵消one  \(\sqrt{d}\)
because factor , make code for Locationization 's敏感随Dimension\textbf{亚Online property }增长. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.3 3D
旋转编code : 旋转矩matrix Parameterization }\label{d-ux65cbux8f6cux7f16ux7801ux65cbux8f6cux77e9ux9635ux53c2ux6570ux5316}

\subsubsection{1.3.1 code definition }\label{ux7f16ux7801ux5b9aux4e49-1}

\textbf{definition  1.3.1(3D 旋转Locationcode )}: for 
\(\mathbf{p} = (x, y, z) \in \mathbb{R}^3\), Dimension
\(d\)(\(d \geq 6\), number ):

\[\boxed{\text{PE}_{\text{rot}}(\mathbf{p}) = \mathbf{R}(\mathbf{p}) \cdot \mathbf{e}_0}\]

where:  - \(\mathbf{e}_0 \in \mathbb{R}^d\) Yesfixed 's参考单位toward quantity (e.g.such as 
\(\mathbf{e}_0 = (1, 0, \ldots, 0)^T\)) -
\(\mathbf{R}(\mathbf{p}) = \mathbf{R}_x(\alpha x) \cdot \mathbf{R}_y(\beta y) \cdot \mathbf{R}_z(\gamma z) \in SO(d)\)
- \(\alpha, \beta, \gamma > 0\)
Yesspace Dimension's\textbf{频rate Parameter}(rad/Å)

each  \(\mathbf{R}_a(\theta)\) is aat  2D on 's \(d\) 维旋转: 

\[\mathbf{R}_a(\theta) = \begin{pmatrix} \mathbf{I}_{i_a-1} & & & \\ & \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix} & & \\ & & & \mathbf{I}_{d-i_a-1} \end{pmatrix}\]

where旋转平别using Dimensionfor  \((0,1)\), \((2,3)\),
\((4,5)\)(别for should  \(x, y, z\) 轴). 剩余's \(d-6\) Dimensionis 
\(\mathbf{R}_x, \mathbf{R}_y, \mathbf{R}_z\) non-变保留(单位矩matrix ). 

\subsubsection{1.3.2
旋转矩matrix 'sParameterization ------group 论基础}\label{ux65cbux8f6cux77e9ux9635ux7684ux53c2ux6570ux5316ux7fa4ux8bbaux57faux7840}

\textbf{命题 1.3.1(\(\mathbb{R}^3 \to SO(d)\) 's嵌入)}: 映射
\(\mathbf{p} \mapsto \mathbf{R}(\mathbf{p})\) Yes \(\mathbb{R}^3\) to 
\(SO(d)\) 's \(3\)  Abel factor group 's\textbf{李group 态}. 

for conditions(Abel property ):\(\mathbf{R}_x, \mathbf{R}_y, \mathbf{R}_z\)
using non-交'starget for , because while Yes\textbf{彼此交换}'s: 

\[[\mathbf{R}_x(\theta), \mathbf{R}_y(\phi)] = 0, \quad [\mathbf{R}_y(\phi), \mathbf{R}_z(\psi)] = 0, \quad [\mathbf{R}_z(\psi), \mathbf{R}_x(\theta)] = 0\]

组property 质: \(\mathbf{R}(\mathbf{p}_1 + \mathbf{p}_2) = \mathbf{R}(\mathbf{p}_1) \cdot \mathbf{R}(\mathbf{p}_2)\)(method group 态), because as : 

\begin{align*}
\mathbf{R}(\mathbf{p}_1 + \mathbf{p}_2) &= \mathbf{R}_x(\alpha(x_1 + x_2)) \cdot \mathbf{R}_y(\beta(y_1 + y_2)) \cdot \mathbf{R}_z(\gamma(z_1 + z_2)) \\
&= \mathbf{R}_x(\alpha x_1)\mathbf{R}_x(\alpha x_2) \cdot \mathbf{R}_y(\beta y_1)\mathbf{R}_y(\beta y_2) \cdot \mathbf{R}_z(\gamma z_1)\mathbf{R}_z(\gamma z_2) \\
&= \mathbf{R}_x(\alpha x_1)\mathbf{R}_y(\beta y_1)\mathbf{R}_z(\gamma z_1) \cdot \mathbf{R}_x(\alpha x_2)\mathbf{R}_y(\beta y_2)\mathbf{R}_z(\gamma z_2) \\
&= \mathbf{R}(\mathbf{p}_1) \cdot \mathbf{R}(\mathbf{p}_2)
\end{align*}

(Partusing using 交换property . )

\subsubsection{1.3.3
内积Structure(for Locationcode )}\label{ux5185ux79efux7ed3ux6784ux76f8ux5bf9ux4f4dux7f6eux7f16ux7801}

for ArbitraryLocation \(\mathbf{p}, \mathbf{q} \in \mathbb{R}^3\): 

\[\boxed{\langle \text{PE}_{\text{rot}}(\mathbf{p}), \text{PE}_{\text{rot}}(\mathbf{q}) \rangle = \langle \mathbf{R}(\mathbf{p})\mathbf{e}_0, \mathbf{R}(\mathbf{q})\mathbf{e}_0 \rangle = \langle \mathbf{e}_0, \mathbf{R}(\mathbf{p})^T \mathbf{R}(\mathbf{q}) \mathbf{e}_0 \rangle = \langle \mathbf{e}_0, \mathbf{R}(\mathbf{q} - \mathbf{p}) \mathbf{e}_0 \rangle}\]

whereone using using 
\(\mathbf{R}(\mathbf{p})^T = \mathbf{R}(-\mathbf{p})\) and 
\(\mathbf{R}(-\mathbf{p})\mathbf{R}(\mathbf{q}) = \mathbf{R}(\mathbf{q} - \mathbf{p})\). 


\(\mathbf{R}(\Delta\mathbf{p}) = \mathbf{R}_x(\alpha\Delta x)\mathbf{R}_y(\beta\Delta y)\mathbf{R}_z(\gamma\Delta z)\), 取
\(\mathbf{e}_0 = (1, 0, 0, 1, 0, 0, \ldots)^T\)(at each for 旋转平'sPartone as 
1):

\[\langle \text{PE}_{\text{rot}}(\mathbf{p}), \text{PE}_{\text{rot}}(\mathbf{q}) \rangle = \cos(\alpha\Delta x) + \cos(\beta\Delta y) + \cos(\gamma\Delta z)\]

where
\(\Delta\mathbf{p} = \mathbf{q} - \mathbf{p} = (\Delta x, \Delta y, \Delta z)\). 

编code 's内积\textbf{only dependencefor Location}
\(\Delta\mathbf{p}\), satisfies \textbf{平移non-变property }. 

\subsubsection{1.3.4 旋转编code 's Lipschitz
continuous property }\label{ux65cbux8f6cux7f16ux7801ux7684-lipschitz-ux8fdeux7eedux6027}

\textbf{theorem  1.3.1(旋转编code 's Lipschitz number )}: 映射
\(\text{PE}_{\text{rot}}: \mathbb{R}^3 \to \mathbb{R}^d\) Yes Lipschitz
continuous 's.  \(\mathbf{e}_0\) as on 述形formula (each for 平'sPartone as  1, its as 
0), precise number as : 

\[\boxed{L_{\text{PE}}^{\text{rot}} = \max(\alpha, \beta, \gamma)}\]

\emph{Proof}: 

\textbf{步骤 1(direct number Computation)}: code as 
\(\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q}) = \mathbf{R}(\mathbf{p})\mathbf{e}_0 - \mathbf{R}(\mathbf{q})\mathbf{e}_0\). by 旋转using non-交'sDimensionfor 
\((0,1), (2,3), (4,5)\), code 差'squantity 解耦. for  \(x\)-平(Dimension
0,1):

\[\|\mathbf{R}_x(\alpha x)\mathbf{e}_x - \mathbf{R}_x(\alpha q_x)\mathbf{e}_x\|^2 = (\cos(\alpha x)-\cos(\alpha q_x))^2 + (\sin(\alpha x)-\sin(\alpha q_x))^2 = 2 - 2\cos(\alpha\Delta x) = 4\sin^2\left(\frac{\alpha\Delta x}{2}\right)\]

for  \(y, z\) 平. because 此: 

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\|^2 = 4\sin^2\left(\frac{\alpha\Delta x}{2}\right) + 4\sin^2\left(\frac{\beta\Delta y}{2}\right) + 4\sin^2\left(\frac{\gamma\Delta z}{2}\right)\]

\textbf{步骤 2(using using  \(|\sin\theta| \leq |\theta|\))}: 

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\|^2 \leq \alpha^2\Delta x^2 + \beta^2\Delta y^2 + \gamma^2\Delta z^2 \leq \max(\alpha, \beta, \gamma)^2 \cdot \|\Delta\mathbf{p}\|_2^2\]

i.e.
\(\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\| \leq \max(\alpha, \beta, \gamma) \cdot \|\mathbf{p} - \mathbf{q}\|_2\). 

\textbf{步骤 3(紧致property )}:  \(\Delta\mathbf{p} = (\varepsilon, 0, 0)\)
and  \(\alpha = \max(\alpha, \beta, \gamma)\), when  \(\varepsilon \to 0\)
: 

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\| = 2|\sin(\alpha\varepsilon/2)| \to \alpha|\varepsilon| = \max(\alpha, \beta, \gamma) \cdot \|\Delta\mathbf{p}\|\]

该bound Yes紧's. \(\square\)

\textbf{注 1.3.1(and 先前版in number 估'sExplanation)}: 先前版make using 
\(\|\mathbf{R}(\mathbf{p}) - \mathbf{R}(\mathbf{q})\|_F\)
'snon-etc.formula 解结 Cauchy-Schwarz, to 
\(2\sqrt{\alpha^2 + \beta^2 + \gamma^2}\). this one number value 
\(\max(\alpha, \beta, \gamma)\)  \(2\sqrt{3} \approx 3.46\) 倍(when 
\(\alpha = \beta = \gamma\) ), ReasonYes: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{角non-etc.formula at 正交factor space on 's松弛}: 差矩matrix 
  \(\mathbf{D}_x, \mathbf{D}_y, \mathbf{D}_z\)
  using non-交'sDimensionfor on , because 此
  \(\|\mathbf{D}_x + \mathbf{D}_y + \mathbf{D}_z\|_F^2 = \|\mathbf{D}_x\|_F^2 + \|\mathbf{D}_y\|_F^2 + \|\mathbf{D}_z\|_F^2\), but non-etc.formula 
  \(\|\mathbf{D}_x + \mathbf{D}_y + \mathbf{D}_z\|_F \leq \|\mathbf{D}_x\|_F + \|\mathbf{D}_y\|_F + \|\mathbf{D}_z\|_F\)
  at 最坏况under can be  \(\sqrt{3}\) 倍(项etc.范number ). 
\item
  \textbf{Cauchy-Schwarz 's \(\ell_1 \to \ell_2\)
  转换}: \(\alpha|\Delta x| + \beta|\Delta y| + \gamma|\Delta z| \leq \sqrt{\alpha^2 + \beta^2 + \gamma^2} \cdot \|\Delta\mathbf{p}\|_2\)
  at  \(\alpha=\beta=\gamma\) and  \(\Delta x = \Delta y = \Delta z\)
  introduce another  \(\sqrt{3}\) 倍松弛. 
\end{enumerate}

重松弛's复导致number 估约 \(2\sqrt{3}\)
倍. Correct'sProofshould using using factor space 正交property direct Computation \(\ell_2\)
范number , 避免this 层松弛. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.4 one  Lipschitz
continuous property theorem }\label{ux7edfux4e00-lipschitz-ux8fdeux7eedux6027ux5b9aux7406}

\textbf{theorem  1.4.1(PPE 'sone  Lipschitz property 质)}: 设
\(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
as with on definition 'sone code . then at number  \(L_{\text{PE}} > 0\) make for which has 
\(\mathbf{p}, \mathbf{q} \in \mathcal{P}\): 

\[\|\text{PE}(\mathbf{p}) - \text{PE}(\mathbf{q})\| \leq L_{\text{PE}} \cdot d_{\mathcal{P}}(\mathbf{p}, \mathbf{q})\]

where \(d_{\mathcal{P}}\) Yes \(\mathcal{P}\) on 'sNaturequantity :  -
target quantity 域: \(d_{\mathcal{P}}(p, q) = |p - q|\) - 3D
域: \(d_{\mathcal{P}}(\mathbf{p}, \mathbf{q}) = \|\mathbf{p} - \mathbf{q}\|_2\)

各域'sprecise  Lipschitz number : 

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.2381}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3571}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4048}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
编code class type 
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\(\mathcal{P}\)
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\(L_{\text{PE}}\)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
正弦(target quantity ) & \([0, L]\) &
\(\frac{2\sqrt{2}\,\pi}{\sqrt{d}}\sqrt{\sum_j 1/\lambda_j^2}\) \\
旋转(3D) & \(\mathbb{R}^3\) & \(\max(\alpha, \beta, \gamma)\) \\
\end{longtable}

physical义: \(L_{\text{PE}}\) control code for Locationization 's\textbf{敏感}. 's
\(L_{\text{PE}}\)
make 邻近Location'scode non-(辨rate ), but can be can break local 光滑property . Optimal
\(L_{\text{PE}}\) should at physicalcorrelated scale  \(\xi\) 附近matchingcode 'srate . 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 2 part : Theorem 1
's严格修正}\label{ux7b2c-2-ux90e8ux5206theorem-1-ux7684ux4e25ux683cux4feeux6b63}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2.1
修正'sDetection边际------precise 表达formula }\label{ux4feeux6b63ux7684ux68c0ux6d4bux8fb9ux9645ux7cbeux786eux8868ux8fbeux5f0f}

\subsubsection{2.1.1 设}\label{ux8bbeux5b9a}

introduce  PPE , expert  \(m\) 's输入From \(s_i\) as : 

\[h_i = \phi(s_i) + \text{PE}(p_i)\]

where \(\phi: \mathbb{R}^{d_s} \to \mathbb{R}^d\)
YesStatusfeature映射(can be with Yesetc.映射or can be Learning'sOnline property 投影). 

\textbf{Assumptions 2.1.1(expert using code space )}: expert Model \(E_m\) at code space 
\(\mathbb{R}^d\) on 操. 接受 \(h_i\)
as 输入并输预测, 投票definition as : 

\[v_m^{\text{PPE}}(s_i) = \mathbf{1}[E_m(h_i) \neq y_i]\]

at Clean/Noiseconditionsunder 's期望as : 

\[p_{\text{clean}, s}^{\text{PPE}} = \mathbb{E}[v_m^{\text{PPE}}(s) \mid \text{clean state } s]\]
\[p_{\text{noisy}, s}^{\text{PPE}} = \mathbb{E}[v_m^{\text{PPE}}(s) \mid \text{noisy state } s]\]

\subsubsection{2.1.2 修正命题}\label{ux4feeux6b63ux547dux9898}

\textbf{命题 2.1(PPE under 'sprecise Detection边际变ization ------修正 CC 审计报告in 's
sign error)}

PPE 增强space in 'shas Detection边际as : 

\[\boxed{\Delta_s^{\text{PPE}} = \Delta_s + \delta_s^{\text{PE}}}\]

where: 

\[\boxed{\delta_s^{\text{PE}} = \underbrace{(p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s})}_{\text{NoiseSampleon 's歧增益}} - \underbrace{(p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s})}_{\text{CleanSampleon 's(non-希望has 's)歧变ization }}}\]

\begin{quote}
\textbf{修正Explanation}: CC 审计报告in  \(\delta_s^{\text{PE}}\)
's表达formula Symbol反. Correct'sSymbolYes: \(\delta_s^{\text{PE}} > 0\) when and only when 
PPE 帮助区Noiseand CleanSample(增Detection边际). this must 求:  -
NoiseSampleon 's歧概rate \textbf{increase }(\(p_{\text{noisy}}^{\text{PPE}} > p_{\text{noisy}}\))------expert 更''识别''Errortarget 签
-
CleanSampleon 's歧概rate \textbf{non-increase or decrease }(\(p_{\text{clean}}^{\text{PPE}} \leq p_{\text{clean}}\))------expert to maintain has 'sone property 
\end{quote}

\(\delta_s^{\text{PE}}\) 'svalue 域: by rate at  \([0,1]\) in , has 
\(\delta_s^{\text{PE}} \in [-\Delta_s, 1 - p_{\text{clean}, s}]\). 

\emph{Proof}: by definition : 

\begin{align*}
\Delta_s^{\text{PPE}} &= p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{clean}, s}^{\text{PPE}} \\
&= (p_{\text{noisy}, s} + (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s})) - (p_{\text{clean}, s} + (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s})) \\
&= (p_{\text{noisy}, s} - p_{\text{clean}, s}) + (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) \\
&= \Delta_s + \delta_s^{\text{PE}}
\end{align*}

value 域: minimum ization  \(\delta_s^{\text{PE}}\) ------
\(p_{\text{noisy}}^{\text{PPE}} = 0\)(which has NoiseSampleis 误判as Clean), \(p_{\text{clean}}^{\text{PPE}} = 1\)(which has CleanSampleis 误判as Noise)→
\(\delta_s^{\text{PE}} = (0 - p_{\text{noisy}}) - (1 - p_{\text{clean}}) = -p_{\text{noisy}} - 1 + p_{\text{clean}} = -(1 + \Delta_s)\). but this 超
\(p_{\text{clean}}^{\text{PPE}}\) and  \(p_{\text{noisy}}^{\text{PPE}}\)
's范围\ldots on , at has 义'sexpert Modelin , \(p_{\text{noisy}}^{\text{PPE}} \in [0, 1]\)
and  \(p_{\text{clean}}^{\text{PPE}} \in [0, 1]\)
约束under , \(\delta_s^{\text{PE}} \in [-\Delta_s, 1]\). 更精细'sbound by 
\(p_{\text{noisy}}^{\text{PPE}} \in [0,1]\) and 
\(p_{\text{clean}}^{\text{PPE}} \in [0,1]\) give . \(\square\)

\textbf{修正's Theorem 1(PPE 增强)}: 

\[\boxed{F_1^{\text{PPE}} \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M (\Delta_s + \delta_s^{\text{PE}})^2\right)}\]

Chernoff-Hoeffding Structuremaintain (指示变quantity at  \([0,1]\) in has bound ), only Yesnumber 
\(\Delta_s\) as  \(\Delta_s + \delta_s^{\text{PE}}\). 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.2 \(\delta_s^{\text{PE}} > 0\)
'smust conditionsand conditions}{2.2 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} \textgreater{} 0 'smust conditionsand conditions}}\label{delta_stextpe-0-ux7684ux5fc5ux8981ux6761ux4ef6ux548cux5145ux5206ux6761ux4ef6}

\subsubsection{2.2.1 Information Theoryconditions}\label{ux4fe1ux606fux8bbaux6761ux4ef6}

\textbf{theorem  2.2.1(\(\delta_s^{\text{PE}} > 0\)
'sconditions------Information Theoryformula , 贝叶斯Optimal限)}

设 \(S, P, Y\)
别表示Statusfactor , physicalLocationand target 签's随变quantity (at DataDistributionunder ). if : 

\[\boxed{I(Y; P \mid S) > 0}\]

i.e.: physicalLocationat \textbf{give Statusfactor 'sconditionsunder }携带regarding target 签's额信息, then \textbf{存at }one 
PPE Function \(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
and code Dimension \(d_{\text{pe}}\), make for Status
\(s\), \textbf{贝叶斯Optimalclass device }at 增广feature \((s, \text{PE}(p))\)
under 'sDetection边际变ization satisfies  \(\delta_s^{\text{PE}} > 0\). 

\emph{Proof}: 

\textbf{步骤 1(信息储存'sphysicalcan be and property )}: by Assumptions
\(I(Y; P \mid S) > 0\), at one for Distribution \((P_{Y|S,P}, P_{Y|S})\)
Yesnon-'s: 

\[I(Y; P \mid S) = \mathbb{E}_{S, P}\left[D_{\text{KL}}(P_{Y|S,P} \| P_{Y|S})\right] > 0\]

because 此存at one 正测'sitem set 
\(\mathcal{A} \subset \mathcal{S} \times \mathcal{P}\), at its on 
\(P_{Y|S=s, P=p} \neq P_{Y|S=s}\). 

\textbf{步骤 2(construct code )}: 选取one 足够表达'scode Function
\(\text{PE}\). by using 逼近theorem (e.g.such as has 足够神via 元'svia network ), at 
\(\text{PE}\) make code 表示 \(\text{PE}(p)\) approximate 保留 \(P\) in regarding 
\(Y\) 'swhich has correlated 信息. 

concrete ,  \(d_{\text{pe}}\) 足够, make at  \(\text{PE}\)
satisfies Data Processingnon-etc.formula \textbf{with etc.formula 立}: 

\[I(Y; \text{PE}(P) \mid S) = I(Y; P \mid S) - \varepsilon_{\text{PE}}\]

where \(\varepsilon_{\text{PE}} \geq 0\) Yescode 造's信息丢失(见Part 3
part ). for 充表达's \(\text{PE}\), can be Guarantee
\(\varepsilon_{\text{PE}} < I(Y; P \mid S)\), because 此: 

\[I(Y; \text{PE}(P) \mid S) > 0\]

\textbf{步骤 3(贝叶斯Optimalclass device 'slinesas )}: conditions互信息
\(I(Y; \text{PE}(P) \mid S) > 0\) means give  \(S\), \(\text{PE}(P)\)
provide regarding  \(Y\) 's额''信号''. 考虑贝叶斯Optimalclass device at 增广space in 's表. 

for NoiseSample(target  \(y\) YesError's):give 
\((s, p)\), 贝叶斯Optimal预测device will 更倾toward \textbf{and  \(y\)
non-'s}target 签(because as  \(p\) provide 额'sevidence ). because 此歧概rate on 升: 

\[p_{\text{noisy}, s}^{\text{PPE}} > p_{\text{noisy}, s}\]

for CleanSample: 额'sLocation信息\textbf{one confirm }Correct'starget 签, by Data Processingnon-etc.formula Guaranteeexpert 歧概rate \textbf{non-increase }(甚to decrease ):

\[p_{\text{clean}, s}^{\text{PPE}} \leq p_{\text{clean}, s}\]

because \textbf{贝叶斯Optimalclass device }'s
\(\delta_s^{\text{PE}} = (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) > 0\). 

\textbf{步骤
4(From贝叶斯Optimalto expert ------猜想)}: \textbf{{[}formula {]}}
on Conclusionfor 贝叶斯Optimalclass device 严格立. for  SCX framework in 's际神via network expert 
\(E_m\), must Guarantee \(\delta_s^{\text{PE}} > 0\)
requires Assumptions: expert Model'sone 致逼近贝叶斯Optimalclass device (at  \(\ell_1\)
义under for conditionsDistribution \(P_{Y|S,P}\) 's逼近error 
\(|\delta_s^{\text{PE}}|\)). for through Stochastic Gradient DescentTraining'svia network , Purpose: \textbf{has }has Sampleunder 's严格Convergence Guarantee. as when theorem 's\textbf{must 局限}: 步骤
1-3
Proof贝叶斯Optimalclass device can be with FromLocation信息in 受益(Information Theoryon 's必然property ), but From贝叶斯Optimalto expert 'sgeneralize Yesformula 猜想. 

\textbf{退one 步看}: if 步骤 4 'sgeneralize non-立, theorem  2.2.1
退ization as one via Conclusion: \(I(Y;P|S) > 0\) means 贝叶斯Optimalclass device at make using  \(P\)
信息表更------this YesData Processingnon-etc.formula 'sdirect Corollaries, Yes平凡's. \(\square\)

\subsubsection{2.2.2 must conditions}\label{ux5fc5ux8981ux6761ux4ef6}

\textbf{命题 2.2.1(\(\delta_s^{\text{PE}} > 0\) 'smust conditions)}: 

if for which has expert  \(m\), \(\delta_s^{\text{PE}} > 0\), then 必须has : 

\[\boxed{I(Y; P \mid S) > 0 \quad \text{or } \quad \text{PE change expert System'sbias -差Structure(non-Information TheoryMechanism)}}\]

Partone 况(Information TheoryMechanism)already at  2.2.1
in process . Part况(non-Information TheoryMechanism)can be can at : PE
increase code Dimension's\textbf{随正then ization should }, make expert 输'sStructureization . but this non-Yes''physicalLocation信息''at using ------while YesArbitrary'sproperty Noisealso can 'sshould . 

\textbf{physicalon must 'sYes \(I(Y; P \mid S) > 0\)}. this etc.价: 

\[\exists s, p_1, p_2 \text{ make  } P_{Y|S=s, P=p_1} \neq P_{Y|S=s, P=p_2}\]

i.e.: for \textbf{one Statusfactor } \(s\), non-'sphysicalLocation \((p_1, p_2)\)
will 导致non-'starget Distribution. 

\textbf{physicale.g.}:  - 蛋白质in : 氨基酸class type (Status \(s\))at 
\(\alpha\)-螺旋(Location \(p_1\))and  \(\beta\)-折叠(Location
\(p_2\))in 'scan Rolenon- ← \(I(Y; P \mid S) > 0\) -
晶in : 'sChemistry环境(Status \(s\))at Surface(Location \(p_1\))and (Location
\(p_2\))in 's缺陷form can non- ← \(I(Y; P \mid S) > 0\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.3 \(\delta_s^{\text{PE}}\)
'sInformation TheoryUpper Bound(Data Processingnon-etc.formula )}{2.3 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} 'sInformation TheoryUpper Bound(Data Processingnon-etc.formula )}}\label{delta_stextpe-ux7684ux4fe1ux606fux8bbaux4e0aux754cux6570ux636eux5904ux7406ux4e0dux7b49ux5f0f}

\textbf{theorem  2.3.1(\(\delta_s^{\text{PE}}\) 'sInformation TheoryUpper Bound)}: 

\[\boxed{\delta_s^{\text{PE}} \leq \min\left(1, \sqrt{\frac{1}{2} \cdot I(P; \text{PE}(P) \mid S, Y)}\right) + \text{Bernstein 修正项}}\]

更precise , by Data Processingnon-etc.formula (DPI)and  Pinsker non-etc.formula : 

\[\delta_s^{\text{PE}} \leq \min\left(1 - p_{\text{clean}, s}, \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}|S,Y} \| P_{\text{PE}|S})}\right) + \delta_s^{\text{variance}}\]

where \(\delta_s^{\text{variance}}\) has expert number  \(M\)
introduce 's估计差. 

\emph{Proof}: 

\textbf{步骤 1(概rate ization and  KL 散)}: for CleanSampleon 's歧概rate ization : 

\[|p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| = \left|\mathbb{E}_{\text{PE}(P)|S=s, Y=y_{\text{true}}}[P(E_m(h) \neq y_{\text{true}})] - P(E_m(s) \neq y_{\text{true}})\right|\]

by  Pinsker non-etc.formula : for Arbitraryitem 
\(A\), \(|P(A) - Q(A)| \leq \sqrt{\frac{1}{2} D_{\text{KL}}(P \| Q)}\). 

should using to  \(p_{\text{clean}}\) and  \(p_{\text{noisy}}\) 'sization , give : 

\begin{align*}
|p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| &\leq \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}(P)|S=s, \text{clean}} \| P_{\text{PE}(P)|S=s})} \\
|p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}| &\leq \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}(P)|S=s, \text{noisy}} \| P_{\text{PE}(P)|S=s})}
\end{align*}

\textbf{步骤 2(组)}: 

\begin{align*}
\delta_s^{\text{PE}} &= (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) \\
&\leq |p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}| + |p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| \\
&\leq \sqrt{\frac{1}{2} D_{\text{KL}}^{(1)}} + \sqrt{\frac{1}{2} D_{\text{KL}}^{(2)}}
\end{align*}

where \(D_{\text{KL}}^{(1)}, D_{\text{KL}}^{(2)}\)
别YesCleanand Noiseconditionsunder 's KL 散. 

\textbf{步骤 2b(value Upper Bound'ssource ------补充Explanation)}: by 
\(\delta_s^{\text{PE}}\) 'svalue 域(命题 2.1), has 
\(\delta_s^{\text{PE}} \leq 1 - p_{\text{clean}, s}\)(取
\(p_{\text{noisy}}^{\text{PPE}} = 1\),
\(p_{\text{clean}}^{\text{PPE}} = 0\) 'smaximum value ). Upper Boundrate 's
\([0,1]\) 约束, non-dependenceany DistributionAssumptions. Theorem Statementin 's
\(\min(1-p_{\text{clean},s}, \sqrt{\frac{1}{2}D_{\text{KL}}})\)
取\textbf{independent has Upper Bound'sminimum value }: value Upper Boundand  Pinsker Upper Bound. when 
Pinsker bound 较松(KL but  \(\delta_s^{\text{PE}}\)
仍受限value 域), value bound 更紧; when Location信息很少(KL 散), Pinsker
bound 更紧. 

\textbf{步骤 3(DPI 约束)}: by Data Processingnon-etc.formula (编code  PE only can decrease 信息):

\[I(\text{PE}(P); Y \mid S) \leq I(P; Y \mid S)\]

because  \(\delta_s^{\text{PE}}\) 受限Location \(P\) at give  \(S\)
under 携带have mutual informationquantity . when  \(I(P; Y \mid S) = 0\)(LocationNousing ), Upper Boundization as : 

\[\delta_s^{\text{PE}} \leq 0 + \delta_s^{\text{variance}}\]

at quantity expert 极限 \(M \to \infty\) under 
\(\delta_s^{\text{variance}} \to 0\). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.4 \(\delta_s^{\text{PE}}\)
'sformula 估计(Fano
bound 限------non-严格lower bound)}{2.4 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} 'sformula 估计(Fano bound 限------non-严格lower bound)}}\label{delta_stextpe-ux7684ux542fux53d1ux5f0fux4f30ux8ba1fano-ux754cux9650ux975eux4e25ux683cux4e0bux754c}

\textbf{命题 2.4.1(\(\delta_s^{\text{PE}}\)
'sformula 估计------non-严格lower bound)}: 

\begin{quote}
\textbf{{[}formula {]}} \textbf{(警告: non-严格theorem . )}
\end{quote}

设Location \(P\) at give Status \(S\) under provide regarding target  \(Y\) 's额信息, and code  PE
保留where's
\(\rho = I(Y; \text{PE}(P) \mid S) / I(Y; P \mid S) \in (0, 1]\)
part . if Assumptions Fano
non-etc.formula at bound all to etc.号(requires Distributionconditions:Noiseall Distributionat ErrorCategoryon ), then at Status
\(s\) make : 

\[\delta_s^{\text{PE}} \approx \frac{2\rho \cdot I(Y; P \mid S) - \log 2}{\log |\mathcal{Y}|} - O\left(\frac{1}{\sqrt{M}}\right)\]

where \(M\) is the probability that expertsnumber , \(O(1/\sqrt{M})\) has expert 's估计Noise. 

\textbf{key 限: 此表达formula  \emph{non-Yes} 严格'snumber learning lower bound. } From Fano
lower bound \(P_e \geq A\) and  \(P_e^{\text{PPE}} \geq B\) non-can 逻辑推
\(P_e - P_e^{\text{PPE}} \geq A - B\), because as  \(a \geq A\) and  \(b \geq B\)
non-蕴含 \(a-b \geq A-B\). 此, Fano
non-etc.formula only at Distributionon to etc.号(Noiseall Distributionat ErrorCategoryon , and  \(H_2(P_e)\)
饱and ). because 此, 此处give 's表达formula should is as for  \(\delta_s^{\text{PE}}\)
quantity level 's\textbf{启formula 估计}, while non-theorem level 'slower boundConclusion. Correct'slower boundrequires must non-'sProofstrategy , this Yeswhen 前'sone Problem. 

\emph{推导思路(non-严格Proof)}: 

\textbf{步骤 1(Fano non-etc.formula ------Fromerror to 信息)}: Fano
non-etc.formula 'sstandard formula : 

\[H(Y \mid S, \text{PE}(P)) \leq H_2(P_e) + P_e \cdot \log(|\mathcal{Y}| - 1)\]

where \(P_e\) Yesgive  \((S, \text{PE}(P))\) under 's贝叶斯Errorrate , \(H_2\)
Yes元熵. etc.效, lower bound: 

\[P_e \geq \frac{H(Y \mid S, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\]

\textbf{步骤 2(启formula 联------non-etc.号误using )}: if  Fano
non-etc.formula at bound all to etc.号(this at one 般况under \textbf{non-立}), then formula on can be 写: 

\begin{align*}
P_e - P_e^{\text{PPE}} &\stackrel{\text{if etc.号}}{=} \frac{H(Y|S) - H(Y|S, \text{PE}(P))}{\log |\mathcal{Y}|} \\
&= \frac{I(Y; \text{PE}(P) \mid S)}{\log |\mathcal{Y}|} \\
&= \frac{\rho \cdot I(Y; P \mid S)}{\log |\mathcal{Y}|}
\end{align*}

again on  \(\Delta P_e\) and  \(\delta_s^{\text{PE}}\) 'sformula 联(Assumptions
\(\Delta P_e \approx -\pi \cdot \delta_s^{\text{PE}}\) for maximum 获益'sStatus
\(s\) 立), 取
\(\pi = 1/2\)(最坏况), \(\rho = 1\)(完美编code ), on has  \(M\) 's
Hoeffding 修正i.e.which 述表达formula . 

\textbf{again 强调: 此推导Fromnon-etc.号推etc.号, non-Yes严格Proof. Correct'slower boundrequires must make using 更强's具(such as 
Fano 's逆形formula or  Fano non-etc.formula ), this requires must regarding Distribution'sconcrete Assumptions. }

\textbf{physicalMeaning(quantity level 估计)}: if  \(I(Y; P \mid S)\)
很(Location几乎non-provide 额信息), then  \(\delta_s^{\text{PE}}\)
'sformula 估计接近零or as 负------PPE
Nomethod property 改善Detection. this explain as 什么\textbf{non-Yeswhich has physicalSystemall can From PPE
in 受益}. but this one ConclusionYestoward Correct'sphysicallearning 直觉, while non-严格'snumber learning Guarantee. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2.5 space local property then ization ------CC
审计报告Result's修正版}\label{ux7a7aux95f4ux5c40ux90e8ux6027ux6b63ux5219ux5316cc-ux5ba1ux8ba1ux62a5ux544aux7ed3ux679cux7684ux4feeux6b63ux7248}

\textbf{theorem  2.5.1(\(\delta_s^{\text{PE}}\) 'sspace 光滑property )}:  PE satisfies 
Lipschitz conditions(number as  \(L_{\text{PE}}\)), and expert  \(E_m\)
's\textbf{软概rate } \(E_m^{\text{soft}}: \mathbb{R}^d \to [0,1]\)(such as 
softmax 输'smaximum Categoryrate )satisfies  \(L_E\)-Lipschitz
conditions. 此, Assumptions\textbf{边bound conditions}: expert  \(E_m\) 's决策边bound 距离编code point 
\(h_i, h_j\) to as  \(\tau > 0\), i.e.
\(|E_m^{\text{soft}}(h) - 0.5| \geq \tau\) for  \(h \in \{h_i, h_j\}\)
立. then for one Status \(s\), has physicalLocation \(p_i, p_j\) 'sfactor : 

\[\boxed{|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| \leq 2 \cdot L_E \cdot L_{\text{PE}} \cdot d_{\mathcal{P}}(p_i, p_j) + O\left(\frac{1}{\sqrt{M}}\right)}\]

where \(O(1/\sqrt{M})\)
has expert 's估计error . \textbf{if bound conditionsnon-立}, bound non-Guaranteenon-空------指示Function
\(\mathbf{1}[E_m(h) \neq y]\) at 决策边bound non-continuous , 软概rate 's Lipschitz
property non-can 约束指示Function期望'sization . 

\emph{Proof}: 

\begin{align*}
|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| &= |(p_{\text{noisy}, i}^{\text{PPE}} - p_{\text{noisy}, j}^{\text{PPE}}) - (p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}})| \\
&\leq |p_{\text{noisy}, i}^{\text{PPE}} - p_{\text{noisy}, j}^{\text{PPE}}| + |p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}}|
\end{align*}

each  probability difference
受输入变ization 约束. \textbf{key }: \(p_{\text{clean}, i}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) \neq y]]\)
Yes指示Function's期望. bound conditions \(|E_m^{\text{soft}}(h) - 0.5| \geq \tau\)
Guarantee \(h_i\) and  \(h_j\)
all at 决策边bound 'sone 侧并maintain one 距离, 此软概rate 's \(L_E\)-Lipschitz
property control 指示Function期望'sization : 

\[|p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}}| \leq L_E \cdot \|h_i - h_j\| = L_E \cdot \|\text{PE}(p_i) - \text{PE}(p_j)\| \leq L_E \cdot L_{\text{PE}} \cdot d_{\mathcal{P}}(p_i, p_j)\]

(using using  \(s_i = s_j = s\)  \(\phi(s_i) = \phi(s_j)\). )倍i.e.. 

\textbf{if Nobound conditions}: 决策边bound can be can 恰位 \(h_i\) and  \(h_j\)
间, i.e.make  \(\|h_i - h_j\|\)
趋零, 歧概rate can be Arbitrary(such as one complete at Clean侧
\(p \approx 0\), another at Noise侧
\(p \approx 1\)), bound as 空(vacuous). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 3 part : Theorem 2
'sprecise 修正}\label{ux7b2c-3-ux90e8ux5206theorem-2-ux7684ux7cbeux786eux4feeux6b63}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{3.1 code non-完美 \(\varepsilon_{\text{PE}}\)
'sprecise Information Theorydefinition }{3.1 code non-完美 \textbackslash varepsilon\_\{\textbackslash text\{PE\}\} 'sprecise Information Theorydefinition }}\label{ux7f16ux7801ux4e0dux5b8cux7f8eux5ea6-varepsilon_textpe-ux7684ux7cbeux786eux4fe1ux606fux8bbaux5b9aux4e49}

\subsubsection{3.1.1
conditions互信息'sformula 解}\label{ux6761ux4ef6ux4e92ux4fe1ux606fux7684ux94feux5f0fux5206ux89e3}

设随变quantity :  - \(X\): 原始feature(Statusfactor  \(S\) or its 表示 \(\phi(S)\)) -
\(P\): physicalLocation(Complete信息) - \(\text{PE}(P)\): code 'sphysicalLocation -
\(Y\): target 签

信息流's Markov 链: 

\[Y \leftrightarrow (X, P) \leftrightarrow (X, \text{PE}(P))\]

(i.e.
\(Y \perp\!\!\!\perp \text{PE}(P) \mid (X, P)\)------编code only 丢失信息, non-创造信息. )

\subsubsection{3.1.2
编code non-完美'sdefinition }\label{ux7f16ux7801ux4e0dux5b8cux7f8eux5ea6ux7684ux5b9aux4e49}

\textbf{definition  3.1(编code non-完美
\(\varepsilon_{\text{PE}}\)------Information Theorydefinition )}: 

\[\boxed{\varepsilon_{\text{PE}} = I(Y; P \mid X) - I(Y; \text{PE}(P) \mid X)}\]

\textbf{property 质}: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Data Processingnon-etc.formula }(DPI):\(\varepsilon_{\text{PE}} \geq 0\)
  恒立. because as  \((X, \text{PE}(P))\) Yes \((X, P)\)
  's(can be can Yes随's)Function, DPI Guarantee: 

  \[I(Y; \text{PE}(P) \mid X) \leq I(Y; P \mid X)\]
\item
  \textbf{can be property conditions}: \(\varepsilon_{\text{PE}} = 0\) when and only when  PE
  at give  \(X\) 'sconditionsunder for  \(Y\) 's预测Yes\textbf{充统计quantity }, i.e.: 

  \[Y \perp\!\!\!\perp P \mid (X, \text{PE}(P))\]

  etc.价: code 'sLocation \(\text{PE}(P)\) 保留 \(P\) in which has and  \(Y\)
  correlated 's信息. 
\item
  \textbf{平凡况}: if 
  \(I(Y; P \mid X) = 0\)(Locationnon-携带target correlated 信息), then 
  \(\varepsilon_{\text{PE}} = 0\) 立(零减零). 
\item
  \textbf{Upper Bound}: \(\varepsilon_{\text{PE}} \leq I(Y; P \mid X) \leq H(Y)\). maximum non-完美Yes丢失which has Location信息. 
\end{enumerate}

\subsubsection{3.1.3
and code Dimension'srelationship }\label{ux4e0eux7f16ux7801ux7ef4ux5ea6ux7684ux5173ux7cfb}

\textbf{命题 3.1(Dimension-信息权衡)}: for 正弦编code (\(d\) 维)and  Laplace
correlated 核(correlated  \(\xi\)):

\[\varepsilon_{\text{PE}} \leq I(Y; P \mid X) \cdot \exp\left(-\frac{d}{d_0}\right)\]

where \(d_0 = O(L/\xi)\) YesfeatureDimensionscale . concrete , when  \(d\) 按照 §1.2
in 'snumber Optimal案增长, 信息丢失with number Rateunder 降. 

\emph{Proof思路}: code 's逼近error (Theorem
1.2.2)导致核重建error , while 导致conditionsDistribution \(P_{Y|X,\text{PE}(P)}\) and 
\(P_{Y|X,P}\) 's KL 散, by  Pinsker
non-etc.formula ization as 互信息丢失. number 衰减 Fourier level number 's谱衰减(Cauchy
核's解析property ). \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3.2 修正's Theorem
2------precise 陈述and Proof}\label{ux4feeux6b63ux7684-theorem-2ux7cbeux786eux9648ux8ff0ux548cux8bc1ux660e}

\textbf{Theorem 2'(non-完美 PPE under 'sfeature失效Upper Bound------precise correction)}

设原始feature \(X\) 's信息non-足as  \(\delta > 0\)(definition  CC Audit Report), and 
\(\varepsilon_{\text{PE}}\) Yes PPE 'scode non-完美. then at  PPE
增强'sfeaturespace in , SCX 's \(F_1\) number satisfies : 

\[\boxed{F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}}\]

where \(C_F > 0\) Yes \(F_1\) and 贝叶斯Errorrate 间's转换number . 

\textbf{special 形}:  -
\(\varepsilon_{\text{PE}} \to 0\)(完美编code ):Upper Boundrestore as  \textbf{原始
Theorem 2}: 
\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}\]

\begin{itemize}
\tightlist
\item
  \(\varepsilon_{\text{PE}} \to I(Y; P \mid X)\)(complete 失败'scode ):Upper Boundto not yet make using any Location信息'sStatus(纯浪费Parameter). 
\end{itemize}

\emph{Proof}: 

\textbf{步骤 1(Fano non-etc.formula 嵌入)}: by  Fano
non-etc.formula , Optimalclass device (贝叶斯)'sErrorrate satisfies : 

\[P_e \geq \frac{H(Y \mid X, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\]

\textbf{步骤 2(conditions熵's解)}: 

\begin{align*}
H(Y \mid X, \text{PE}(P)) &= H(Y \mid X) - I(Y; \text{PE}(P) \mid X) \\
&= H(Y \mid X) - [I(Y; P \mid X) - \varepsilon_{\text{PE}}] \\
&= H(Y \mid X) - I(Y; P \mid X) + \varepsilon_{\text{PE}}
\end{align*}

\textbf{步骤 3(联系 \(\delta\))}: 原始 Theorem 2 in , \(\delta\)
and conditions熵'srelationship as (CC 审计报告 §1, Theorem 2):

\[\delta = \frac{2}{\log|\mathcal{Y}|} \cdot \left(H(Y \mid X) - 1\right)_{\text{excess}}\]

where \((\cdot)_{\text{excess}}\) 表示超贝叶斯Error's信息quantity . at  PPE under : 

\begin{align*}
H(Y \mid X, \text{PE}(P)) - 1 &= (H(Y \mid X) - I(Y; P \mid X) + \varepsilon_{\text{PE}}) - 1 \\
&= (H(Y \mid X) - 1) - I(Y; P \mid X) + \varepsilon_{\text{PE}}
\end{align*}

definition  \(\delta_{\text{PPE}}\) as  PPE 增强'shas 效信息non-足: 

\[\delta_{\text{PPE}} = \delta + \frac{2\varepsilon_{\text{PE}}}{\log|\mathcal{Y}|} \cdot \frac{\log|\mathcal{Y}|}{C_F^2} = \delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}\]

(\(C_F\) Errorrate and  \(F_1\)
's转换: \(F_1 \leq F_{1,\text{base}} + C_F \sqrt{P_e - P_e^*}\). )

\textbf{步骤 4(代入 SCX 's \(F_1\) Upper Bound)}: 

\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta_{\text{PPE}}}{2}} = F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}\]

\textbf{步骤 5(极限validate )}: when  \(\varepsilon_{\text{PE}} \to 0\): 

\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}\]

正Yes原始 Theorem 2. \(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{3.3 \(\varepsilon_{\text{PE}}\)
'scan be 估计形formula }{3.3 \textbackslash varepsilon\_\{\textbackslash text\{PE\}\} 'scan be 估计形formula }}\label{varepsilon_textpe-ux7684ux53efux4f30ux8ba1ux5f62ux5f0f}

theorem quantity 
\(\varepsilon_{\text{PE}} = I(Y; P \mid X) - I(Y; \text{PE}(P) \mid X)\)
YesTheoryquantity ------涉and DataDistribution. for should using , requires must FromDatain 估计. 

\subsubsection{3.3.1 KSG 估计device }\label{ksg-ux4f30ux8ba1ux5668}

\textbf{theorem  3.3.1(\(\varepsilon_{\text{PE}}\)
'scan be 估计property )}: \(\varepsilon_{\text{PE}}\)
can be with through with under MethodFromhas Samplein \textbf{one 致}估计: 

\[\boxed{\hat{\varepsilon}_{\text{PE}}^{(n)} = \hat{I}^{(n)}(Y; P \mid X) - \hat{I}^{(n)}(Y; \text{PE}(P) \mid X)}\]

where \(\hat{I}^{(n)}(\cdot; \cdot \mid \cdot)\) Yesmake using 
Kraskov-Stögbauer-Grassberger (KSG)
估计device 's\textbf{conditions互信息}估计. when Samplequantity  \(n \to \infty\)
, \(\hat{\varepsilon}_{\text{PE}}^{(n)} \xrightarrow{p} \varepsilon_{\text{PE}}\)(one property ). 

concrete , KSG conditions互信息估计device : 

\[\hat{I}(Y; P \mid X) = \hat{I}(Y; (P, X)) - \hat{I}(Y; X)\]

where \(\hat{I}(A; B)\) Yesstandard 's KSG 互信息估计(based on  \(k\)-近邻距离). 

\textbf{Algorithm}:  1. FromDatain 采样 \(n\) 元组 \((x_i, p_i, y_i)\) 2.
Computationcode  \(\text{PE}(p_i)\) 3. Computation
\(\hat{I}^{(n)}(Y; (P, X))\): at space 
\((\mathcal{P} \times \mathcal{X}) \times \mathcal{Y}\) in make using  \(k\)-NN
4. Computation \(\hat{I}^{(n)}(Y; X)\): at  \(\mathcal{X} \times \mathcal{Y}\)
in make using  \(k\)-NN 5.
\(\hat{I}^{(n)}(Y; P \mid X) = \hat{I}^{(n)}(Y; (P, X)) - \hat{I}^{(n)}(Y; X)\)
6. Computation \(\hat{I}^{(n)}(Y; \text{PE}(P) \mid X)\) 7.
\(\hat{\varepsilon}_{\text{PE}}^{(n)} = \hat{I}^{(n)}(Y; P \mid X) - \hat{I}^{(n)}(Y; \text{PE}(P) \mid X)\)

\textbf{ConvergenceRate}: KSG 估计device with  \(O(n^{-1/(d_{\text{eff}}+2)})\)
RateConvergence, where \(d_{\text{eff}}\) Yesspace 'shas Dimension. for 维
\(X\), Convergence较慢, but  \(\varepsilon_{\text{PE}}\)
身Yes\textbf{差value }, 估计device 共享'sbias , value 估计can be can 单独估计更accuracy (bias 抵消). 

\subsubsection{3.3.2 lower bound估计(避免维
KSG)}\label{ux4e0bux754cux4f30ux8ba1ux907fux514dux9ad8ux7ef4-ksg}

for  \(X\), KSG
估计can be can non-accuracy . 替代案: make using \textbf{预测property can Difference}as 
\(\varepsilon_{\text{PE}}\) 'slower bound. 

\textbf{命题 3.3.1(\(\varepsilon_{\text{PE}}\) 's预测lower bound)}: 

\[\boxed{\varepsilon_{\text{PE}} \geq \mathbb{E}_{X,P}\left[D_{\text{KL}}(P_{Y|X,P} \| P_{Y|X,\text{PE}(P)})\right]_{\text{est}}}\]

where \(P_{Y|X,P}\) and  \(P_{Y|X,\text{PE}(P)}\) can be through class device approximate :  -
class device  \(f_{\text{full}}\) at  \((X, P)\) on Training → 估计 \(P_{Y|X,P}\) -
class device  \(f_{\text{enc}}\) at  \((X, \text{PE}(P))\) on Training → 估计
\(P_{Y|X,\text{PE}(P)}\)

者at Testset on 's\textbf{for number 损失差}Yes \(\varepsilon_{\text{PE}}\)
'slower bound估计: 

\[\hat{\varepsilon}_{\text{PE}}^{\text{pred}} = \frac{1}{n_{\text{test}}} \sum_{i=1}^{n_{\text{test}}} \left[\log \frac{1}{p_{\text{enc}}(y_i \mid x_i, \text{PE}(p_i))} - \log \frac{1}{p_{\text{full}}(y_i \mid x_i, p_i)}\right]\]

when  \(n_{\text{test}} \to \infty\)
and class device one 致, \(\hat{\varepsilon}_{\text{PE}}^{\text{pred}} \to \varepsilon_{\text{PE}}\). 

\textbf{using value }: such as class device  \(f_{\text{full}}\) and  \(f_{\text{enc}}\)
at Testset on 'saccuracy rate \textbf{has 显著Difference}, then 
\(\varepsilon_{\text{PE}} \approx 0\)------编code Yes充足's, PPE non-will 削弱
Theorem 2 'sUpper Bound. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 4 part : Theorem 3
's修正(最重must 'spart )}\label{ux7b2c-4-ux90e8ux5206theorem-3-ux7684ux4feeux6b63ux6700ux91cdux8981ux7684ux90e8ux5206}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.1  Theorem 3
'sprecise Theorem Statement}\label{ux539f-theorem-3-ux7684ux7cbeux786eux5b9aux7406ux9648ux8ff0}

\textbf{Theorem 3(原始------Noiseand DifficultySample'snon-can be property )}: 

存at bound  \(W_A, W_B\) satisfies : 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  观测DataDistributioncomplete : \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)
\item
  at  \(W_A\) in , Sample \((x, y)\) Yes\textbf{target Noise}(label Error, but 
  \(x\) Structureon  label should Yes别's)
\item
  at  \(W_B\) in , Sample \((x, y)\) Yes\textbf{质DifficultySample}(label
  Correct, but feature \(x\) Nomethod determine property 支持该 label)
\end{enumerate}

then : has any Algorithmcan only through 观测 i.i.d. Sample \((X_i, Y_i)_{i=1}^n\) 区
\(W_A\) and  \(W_B\). formula ization , for Arbitrary决策规then 
\(d: (\mathcal{X} \times \mathcal{Y})^n \to \{W_A, W_B\}\): 

\[\max_{W \in \{W_A, W_B\}} P_W(d(\text{data}) \neq W) \geq \frac{1}{2}\]

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.2 Learningtype  PE
such as break non-can be property ------precise conditions}\label{ux5b66ux4e60ux578b-pe-ux5982ux4f55ux7834ux574fux4e0dux53efux533aux5206ux6027ux7cbeux786eux6761ux4ef6}

\subsubsection{4.2.1 设}\label{ux8bbeux5b9a-1}

introduce  PPE , 观测Dataextensionas 元组 \((X, P, Y)\), where \(P\)
YesphysicalLocation(can be 观测'sPhysical Quantity------原factor target , 残基Locationetc.). key 区别: 

\begin{itemize}
\tightlist
\item
  \textbf{fixed  PE}: \(\text{PE}(\cdot)\)
  Yesdefinition 'sdetermine property Function(such as 正弦编code ), \textbf{non-dependenceTrainingData}. 
\item
  \textbf{Learningtype  PE}: \(\text{PE}_\theta(\cdot)\) 'sParameter \(\theta\)
  YesFromDatain Learning's(such as through under 降优ization ObjectiveFunction). 
\end{itemize}

\subsubsection{4.2.2 fixed  PE
's况(Trivial)}\label{ux56faux5b9a-pe-ux7684ux60c5ux51b5trivial}

\textbf{命题 4.1(fixed  PE maintain  Theorem 3)}: if 
\(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
is a\textbf{fixed 's, and DataNo's}Function, then 增强's观测Distribution依然: 

\[P_{W_A}(X, P, \text{PE}(P), Y) = P_{W_B}(X, P, \text{PE}(P), Y)\]

theorem  3 'snon-can be property \textbf{complete maintain }. 

\emph{Proof}: because as  \(\text{PE}\)
Yesfixed 'sdetermine property Function, \((X, P, \text{PE}(P), Y)\) Yes \((X, P, Y)\)
'sone determine property Function. while  \(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\)(physicalLocation
\(P\)
by physicalStructure决, independent target 签程'sbound Selection). because 此增强'sDistributionalso . \(\square\)

\subsubsection{4.2.3 Learningtype  PE
's况(key )}\label{ux5b66ux4e60ux578b-pe-ux7684ux60c5ux51b5ux5173ux952e}

\textbf{theorem  4.1(Learningtype  PE break  Theorem 3 'sprecise conditions)}: 

设 \(\text{PE}_\theta\) 'sParameter \(\theta\) through minimum ization via 验风险
\(\hat{R}_n(\theta) = \frac{1}{n}\sum_{i=1}^{n} \ell(f_\theta(x_i, p_i), y_i)\)
FromTrainingDatain Learning(where \(\ell\) Yes某损失Function, \(f_\theta\) Yes包含 PE
编code 'sCompleteModel). then at with under conditionsunder , bound  \(W_A\) and  \(W_B\)
变as \textbf{can be 区}: 

\textbf{(C1) Information Theorynon-平凡property }: 

\[I(Y; P \mid X) > 0\]

i.e.: physicalLocationat give 原始featureunder 携带regarding target 签's信息. 

\textbf{(C2) Learningfor target Noise's敏感property }: at one non-空'sTrainingSamplefactor set 
\(\mathcal{D}_{\text{diff}} \subset \mathcal{D}\), at this Sampleon : 

\[\nabla_\theta \ell(f_\theta(x, p), y_A) \neq \nabla_\theta \ell(f_\theta(x, p), y_B)\]

where \(y_A\) Yes \(W_A\) in Sample'sNoisetarget 签, \(y_B = y_A\)(at  Theorem 3
construct in , bound has 's观测target \(y\), only Yesexplain non-). on at  Theorem
3 'sconstruct in , \textbf{观测to 's \((x, y)\) complete }！

this means : Learningtype  PE
break non-can be property 'sMechanism\textbf{non-Yes}target Difference(because as target 签), while YesModelfor ''such as Learning'''sDifference. let precise Analysis. 

\subsubsection{4.2.4
precise Mechanism: 表征漂移}\label{ux7cbeux786eux673aux5236ux8868ux5f81ux6f02ux79fb}

\textbf{key 洞察}: at  Theorem 3
'sconstruct in , \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)
Yes立's, but \textbf{this non-means Learningdynamic }. Reasonat : 

at  \(W_A\)(target Noisebound )in :  - Sample \((x, y)\) 'starget  \(y\) YesError's -
physicalLocation \(p\) and  \((x, y)\) 'srelationship Yes: \(p\) and  \(x\)
one 致(by physicalStructure决), but  \(p\) 暗示'starget and  \(y\) non- - when  PE
'sParameteris Learning, Modelwill  \((p, y)\) 间's''矛盾''------\(p\)
暗示某target 签, while  \(y\) Yesanother 

at  \(W_B\)(质Difficultybound )in :  - Sample \((x, y)\) 'starget  \(y\) YesCorrect's -
\(p\) and  \(x\) and  \(y\) all one 致------non-存at ''矛盾'' - ModelLearningto 's
\((p, y)\) relationship Yesand one 致's

\textbf{key 区别}: i.e.make 
\(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\)(联观测Distribution), Learning\textbf{dynamic }取决损失's几何Structure------while Structureat conditionsDistribution
\(P_{Y|X,P}\) has Difference(because as  Theorem 3 'sconstruct dependenceconditionsDistribution'sDifference, i.e.
\(X\) at give  \((Y, \text{world})\) under 'sDistributionnon-). 

\textbf{this Yes Theorem 3
construct 'score }: \(P_{W_A}(Y \mid X) = P_{W_B}(Y \mid X)\)(观测Distribution), but non-: 
- \(W_A\): \(Y_{\text{true}}\) by  \(X\) 决,  \(Y_{\text{obs}}\)
with probability \(\eta\) is 翻转 -
\(W_B\): \(Y_{\text{obs}} = Y_{\text{true}}\), but 
\(P(Y_{\text{true}} \mid X)\) at some 区域接近all 匀(non-determine property )

when introduce  \(P\) , at  \(W_A\) in  \(P(Y_{\text{true}} \mid X, P)\) can be can 
\(P(Y_{\text{obs}} \mid X, P)\) 更尖锐(because as  \(P\)
帮助determine target 签), 导致Modelat  \((X, P)\)
space in ''看to ''Noisetarget 签's矛盾. while at  \(W_B\)
in , \(P(Y_{\text{obs}} \mid X, P) = P(Y_{\text{true}} \mid X, P)\), has 矛盾. 

\textbf{but this 仍然has give can be 观测'sDifference}------because as at Training, Modelat bound in to 'sall Yes's
\((x, p, y)\) 元组. 

真正make Learningtype  PE can 够区'sMechanismYes: theorem  3
'sconstruct in bound 's区别at \textbf{not yet 观测to 'sat quantity }(真target  vs
Noisetarget  vs Difficulty程). such as  \(P\)
provide regarding this not yet 观测变quantity 's信息窗口(through 
\(I(Y_{\text{true}}; P \mid X) > 0\)), that \textbf{at Trainingin }, Learningto 's
PE Parameter \(\theta\)
will through ization linesas 反映this Difference------i.e.make TrainingData看. 

\subsubsection{4.2.5
precise can be property conditions------最终形formula }\label{ux7cbeux786eux53efux533aux5206ux6027ux6761ux4ef6ux6700ux7ec8ux5f62ux5f0f}

let Frombasic 'slevel re-Analysis. 

at  Theorem 3 'sProofin , bound 'sconstruct Yes:  - Selectionnon-'sconditionsDistribution
\(P_{W_A}(Y \mid X)\) and  \(P_{W_B}(Y \mid X)\), but 边际Distribution \(P(X, Y)\)
 - this which with can be can , Yesbecause as  \(P(X)\) at bound in non- →
\(P_{W_A}(X) \neq P_{W_B}(X)\), but  \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)

etc.etc., this non-for . Theorem 3 'sYes\textbf{观测}Distribution. let From CC
审计报告's原始construct . 

CC 审计报告: ``存at bound  \(W_A, W_B\), has 观测etc.价'sDataDistribution
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)''

construct Method: bound has non-'s \(P(X)\) and non-'s
\(P(Y|X)\), but 's乘积. concrete :  - \(W_A\): \(P_{W_A}(X)\)
at ''容易''区域has 更质quantity , \(P_{W_A}(Y|X)\) Yesdetermine property 's -
\(W_B\): \(P_{W_B}(X)\) at ''Difficulty''区域has 更质quantity , \(P_{W_B}(Y|X)\)
Yes接近all 匀's

make  \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\) for which has  \((x, y)\) 立. 

\textbf{when introduce  \(P\)
}: \(P_{W_A}(X, P, Y) = P_{W_A}(P \mid X, Y) \cdot P_{W_A}(X, Y)\). 

such as  \(P\) Yesphysicalon by  \(X\) 决's(\(P\) Yes \(X\) 'sphysicaltarget ), then 
\(P(P \mid X, Y) = P(P \mid X)\)(physicalLocationnon-dependencetarget 签). 此: 
\[P_{W_A}(X, P, Y) = P_{W_A}(P \mid X) \cdot P_{W_A}(X, Y)\]
\[P_{W_B}(X, P, Y) = P_{W_B}(P \mid X) \cdot P_{W_B}(X, Y)\]

but 
\(P_{W_A}(P \mid X) = P_{W_B}(P \mid X)\)(physicalLocationby physicalStructure决, for bound ), and 
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\). because 此
\(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\). 

\textbf{fixed  PE 仍然maintain non-can be property }------联Distribution. 

\textbf{Learningtype  PE 's况}: Learningtype  PE 'sParameter \(\theta\)
Yesthrough minimum ization 某损失FunctionFromDatain learning to 's. 考虑at dataset
\(\mathcal{D} = \{(x_i, p_i, y_i)\}_{i=1}^n\) on 'sTraining: 

\[\hat{\theta} = \arg\min_\theta \frac{1}{n} \sum_{i=1}^n \ell(f_\theta(x_i, \text{PE}_\theta(p_i)), y_i)\]

this 'skey Yes: \textbf{PE
'sParameterat 损失Functionin }------non-only through code influence 损失, also can be can through 某监督or 重构损失influence Learning. 

but such as  PE
纯粹Yeshas 监督Training's(only through under 游任务's损失), that 么世bound give 'sTrainingData
→ 's → 's \(\hat{\theta}\). at this under , i.e.make YesLearningtype  PE
also non-can 区世bound . 

\textbf{真正make Learningtype  PE can 够区'sscenario }: 

PE 'sParameter除受监督损失influence , also through with under formula 接收信号:  1.
\textbf{监督预Training}(such as  masked position
prediction)------at target 注前lines 2. \textbf{辅助损失}(such as Location重构损失)
3. \textbf{physical约束}(such as can quantity 守恒, matching)------this 约束provide regarding  \(P\)
and Physical Quantityrelationship 's额信息

at this under , PE
'sParameterpart by \textbf{physicalone property 's信号}决, while this 信号at bound in can be can non-(because as bound in Sample's''physicalJustification''non-). 

\textbf{precise theorem }: 

\textbf{Theorem 3'(修正'snon-can be property theorem ------带 PPE 前提)}: 

\[\boxed{\begin{aligned}
\text{if  } & \text{(A) } \text{PE}(\cdot) \text{ Yesand target  } Y \text{ independent 'sfixed Function(i.e.its Parameternon-by  } Y \text{ or any dependence } Y \text{ 's信号决), and } \\
& \text{(B) } P \perp\!\!\!\perp Y_{\text{world}} \mid X \text{(physicalLocationat give 原始featureunder independent bound Selection), } \\
\text{then  } & P_{W_A}(X, \text{PE}(P), Y) = P_{W_B}(X, \text{PE}(P), Y), \text{Theorem 3 'snon-can be property maintain . } \\
\text{反, if  } & \text{PE 'sParameterthrough dependence } Y \text{ 's信号Learningto , and  } I(Y_{\text{true}}; P \mid X) > 0, \\
\text{then  } & \text{Learningto 'scode at bound in with 正概rate non-: } P(\hat{\theta}_{W_A} \neq \hat{\theta}_{W_B}) > 0, \\
\text{世bound as can be 区. } &
\end{aligned}}\]

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.3
construct property Proof: minimum e.g.}\label{ux6784ux9020ux6027ux8bc1ux660eux6700ux5c0fux793aux4f8b}

\subsubsection{4.3.1 construct }\label{ux6784ux9020}

考虑one 极简'sclass Problem.  \(X \in \{-1, +1\}\)
is afeature, \(P \in \{0, 1\}\)
is aphysicalLocation, \(Y \in \{-1, +1\}\) Yestarget 签. 

\textbf{世bound  \(W_A\)(target Noise)}: 

\begin{itemize}
\tightlist
\item
  \(P_{W_A}(X = +1) = 1/2\), \(P_{W_A}(X = -1) = 1/2\)
\item
  \(P_{W_A}(Y_{\text{true}} = +1 \mid X = +1) = 1\),
  \(P_{W_A}(Y_{\text{true}} = -1 \mid X = -1) = 1\)(determine property 's)
\item
  target with probability \(\eta = 0.2\) 翻转: \(Y_{\text{obs}} = Y_{\text{true}}\)
  with probability \(0.8\), \(Y_{\text{obs}} = -Y_{\text{true}}\) with probability \(0.2\)
\item
  physicalLocation \(P\): \(P = X\)(determine property ), i.e. \(p = 1\) when 
  \(x = +1\), \(p = 0\) when  \(x = -1\)
\end{itemize}

\textbf{世bound  \(W_B\)(质Difficulty)}: 

\begin{itemize}
\tightlist
\item
  \(P_{W_B}(X = +1) = 0.6\), \(P_{W_B}(X = -1) = 0.4\)(non-all 匀！)
\item
  \(P_{W_B}(Y_{\text{obs}} = +1 \mid X = +1) = 5/6\),
  \(P_{W_B}(Y_{\text{obs}} = -1 \mid X = +1) = 1/6\)
\item
  \(P_{W_B}(Y_{\text{obs}} = +1 \mid X = -1) = 1/4\),
  \(P_{W_B}(Y_{\text{obs}} = -1 \mid X = -1) = 3/4\)
\item
  \(Y_{\text{obs}} = Y_{\text{true}}\)(has target Noise, but conditionsDistributionYeshas 'snon-determine property )
\item
  \(P = X\)(and  \(W_A\) )
\end{itemize}

\textbf{validate 观测etc.价}: 

at  \(W_A\) in :  - \(P_{W_A}(X = +1, Y = +1) = 0.5 \times 0.8 = 0.4\) -
\(P_{W_A}(X = -1, Y = +1) = 0.5 \times 0.2 = 0.1\) -
\(P_{W_A}(X = +1, Y = -1) = 0.5 \times 0.2 = 0.1\) -
\(P_{W_A}(X = -1, Y = -1) = 0.5 \times 0.8 = 0.4\)

at  \(W_B\) in :  - \(P_{W_B}(X = +1, Y = +1) = 0.6 \times (5/6) = 0.5\) -
\(P_{W_B}(X = -1, Y = +1) = 0.4 \times (1/4) = 0.1\) -
\(P_{W_B}(X = +1, Y = -1) = 0.6 \times (1/6) = 0.1\) -
\(P_{W_B}(X = -1, Y = -1) = 0.4 \times (3/4) = 0.3\)

\(P(X=+1,Y=+1)\) non-(0.4 vs
0.5), non-satisfies 观测etc.价. let 调整construct make its complete etc.. 

re-construct : 

\(W_A\):  - \(P(X=+1) = 0.5\), \(P(X=-1) = 0.5\) -
\(P(Y=+1 \mid X=+1) = 0.8\), \(P(Y=-1 \mid X=+1) = 0.2\) -
\(P(Y=+1 \mid X=-1) = 0.2\), \(P(Y=-1 \mid X=-1) = 0.8\) -
Mechanism: \(Y_{\text{true}}\) Yesdetermine property 's (\(X=+1 \to +1\),
\(X=-1 \to -1\)), but  20\% Noise

\(W_B\):  - \(P(X=+1) = 0.5\), \(P(X=-1) = 0.5\) - and  \(W_A\) complete 's
\(P(Y \mid X)\) - Mechanism: featureYesnon-determine property 's(has 隐藏'starget 签)

at construct under  \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\), but at Structurenon-. at  \(W_A\)
in , \(P = X\), \(Y_{\text{true}} = X\), Noisewith  20\% rate 翻转. at  \(W_B\)
in , \(P = X\), but  \(Y\) direct by Noise's通道(No隐藏层). 

\subsubsection{4.3.2 Learningtype  PE
'scan be property }\label{ux5b66ux4e60ux578b-pe-ux7684ux53efux533aux5206ux6027}

introduce one easy'sLearningtype 
PE: \(\text{PE}_\theta(p) = \theta \cdot p\)(target quantity code , \(\theta \in \mathbb{R}\)
can be Learning). 

CompleteModel: \(f(x, p) = \text{sign}(w \cdot x + \text{PE}_\theta(p))\). 

设through with under formula Learning \(\theta\):  1.
先at \textbf{Notarget 签}Dataon with 监督formula Training: 预测is  mask 'sphysicalLocation
\(\hat{p} = g(\text{PE}_\theta(p))\) 2. at under 游任务on 微调

key : at 步骤 1(监督预Training)in , ObjectiveFunctionnon-涉and target 签
\(Y\). bound 'sData \((X, P)\)
Distributioncomplete (\(P_{W_A}(X, P) = P_{W_B}(X, P)\)), because 此预Training阶段\textbf{non-introduce }区. 

but such as make using with under 辅助损失: 

\[\mathcal{L}_{\text{aux}}(\theta) = \mathbb{E}_{(x,p,y)}\left[(\text{PE}_\theta(p) - \mathbb{E}[Y \mid X = x])^2\right]\]

i.e.: code should and conditions期望target one 致. this 损失涉and 
\(Y\), and dependencebound 'sStructure. at  \(W_A\)
in , \(\mathbb{E}[Y \mid X = x] = (1-2\eta) \cdot \text{sign}(x) = 0.6 \cdot \text{sign}(x)\)(because as Noise翻转). at 
\(W_B\)
in , \(\mathbb{E}[Y \mid X = x] = \mathbb{E}[Y \mid X = x] = 0.6 \cdot \text{sign}(x)\)(number value ！only Yesexplain non-). 

which with at \textbf{this minimum e.g.in }, i.e.make YesLearningtype  PE
also non-can 区世bound ------because as conditions期望. 

\textbf{this Yes Theorem 3
's精髓}: such as 果观测Distributioncomplete , any Algorithmall non-can 区. 

\subsubsection{4.3.3 Problem: Learningtype  PE
真正can 区'sconditions}\label{ux5f00ux653eux95eeux9898ux5b66ux4e60ux578b-pe-ux771fux6b63ux80fdux533aux5206ux7684ux6761ux4ef6}

\textbf{此minimum e.g.际on Proof Theorem 3
's鲁棒property , while non-its 脆弱property }: at this construct in , i.e.make YesLearningtype  PE
also non-can 区观测etc.价'sbound . this 恰恰印证 Theorem 3
's精髓------when 观测Distributioncomplete simultaneously, any Algorithmall non-can 区. 

原先声称's''construct property minimum e.g.''反while  Theorem 3'
break toward 'sone \textbf{反e.g.}: one Learningtype  PE
\textbf{Nomethod }区观测etc.价世bound 'sscenario . 

must make Learningtype  PE
can 够区, requires must satisfies : \textbf{Learning信号non-only only dependence观测Distribution}. Theoryon this can be can at : 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{任务Learning}: PE simultaneouslyis using 预测another and  \(Y_{\text{true}}\)
  correlated 'squantity  \(Z\)(e.g.such as can quantity , ), and  \(Z\) at bound 
  \(W_A\)(Noisetarget means Physical Quantitynon-one 致)and 
  \(W_B\)(Difficultybut physicalone 致)in 'snon-
\item
  \textbf{physicalone property 约束}: 损失Function包含physical约束项(such as can quantity 守恒), make at 
  \(W_A\) in Noisetarget 签导致physicalnon-one 致's预测, while at  \(W_B\)
  in DifficultySamplemaintain physicalone property 
\item
  \textbf{Distributionization }: PE
  'sParameterpart by DistributionData(non-training distribution)决, and bound at Distributionnon-
\end{enumerate}

\textbf{{[}Problem{]}}
然while , Purpose: \textbf{缺乏construct property Proof}validate on any Mechanismcan at 观测etc.价'sbound on non-'sOptimal
PE Parameter. this requires must :  - formula definition not yet 观测变quantity  \(Z\) - construct 
\(\mathcal{L}_{\text{aux}}\) 'sconcrete formula  - Proofat  \(W_A\) and  \(W_B\)
in 'sOptimalvalue non-

theorem  4.1 's陈述(regarding 
\(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{sup}} + \lambda \mathcal{L}_{\text{aux}}\)
'snon-Optimalvalue 导致can be property )at 逻辑on YesCorrect's\textbf{conditions陈述}, but conditionsYesNocan at 
Theorem 3 'sconstruct in is satisfies , Purpose: has construct property Proof. 

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.4 修正's Theorem
3'------Complete陈述}\label{ux4feeux6b63ux540eux7684-theorem-3ux5b8cux6574ux9648ux8ff0}

\textbf{Theorem 3'(non-can be property 'sconditionsmaintain ------修正版)}: 

\[\boxed{\begin{aligned}
\textbf{前提 (A): } & \text{physicalLocationcode  PE Yesfixed 's, or only through independent target  } Y \text{ 's信号Learning. } \\
\textbf{前提 (B): } & \text{physicalLocation } P \text{ at give  } X \text{ under Conditional Independencebound Selection: } P \perp\!\!\!\perp W \mid X. \\
\textbf{Conclusion: } & \text{Theorem 3 'snon-can be property complete maintain : } \\
& \forall \text{ 决策规then  } d, \quad \max_{W \in \{W_A, W_B\}} P_W(d \neq W) \geq \frac{1}{2}. \\
\textbf{if 前提 (A) non-立: } & \text{non-can be property YesNois break Purpose: is a**放Problem**. } \\
& \text{Learningtype  PE can be can through 任务Learningor physicalone property 约束etc.Mechanism区世bound , } \\
& \text{but when 前缺乏construct property Proof.  §4.3 's讨论. }
\end{aligned}}\]

\textbf{Corollaries 4.4.1(for 践'sinfluence )}: 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{fixed 正弦 PE or fixed 旋转 PE}: Theorem 3
  complete maintain . Locationcode non-will 损害最坏况'snon-can be property Guarantee------but also \textbf{non-will 帮助}Noiseand DifficultySample(to at Information Theoryon , through 观测DistributionNomethod 区). 
\item
  \textbf{FromNotarget LocationDataLearning's
  PE}(such as through 监督重构Location):such as Trainingnon-涉and target  \(Y\), Theorem 3
  maintain . PE Parameterat bound in learning to 'svalue . 
\item
  \textbf{through ObjectiveLearning(including target correlated Objective)'s PE}: Theorem 3
  \textbf{can be can is break }, but Purpose: 前缺乏construct property Proof. this Yes\textbf{放Problem}------Location信息YesNo真'sprovide one 额's''窗口''区Noiseand DifficultySample, also is anot yet is 严格validate 's猜想. 
\item
  \textbf{风险}: i.e.make 辅助Objectivecan be can 帮助区, if its design introduce 虚假correlated property , can be can 导致Error's''区'', will DifficultySampleErrortarget as Noise. this requires must 仔细'sExperimentvalidate . 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.5 and  CC 审计报告 §2.5
's较}\label{ux4e0e-cc-ux5ba1ux8ba1ux62a5ux544a-2.5-ux7684ux6bd4ux8f83}

CC 审计报告for  Theorem 3 'sAnalysisYesCorrect's(fixed  PE maintain non-can be property , Learningtype 
PE can be can break ), but 遗漏with under key point : 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{CC 报告not yet determine 义''Learningtype 
  PE'''sMeaning}: only ''FromDatain Learning''Yesnon-够's------requires must 明确区''FromNotarget DataLearning''(安全)and ''Fromtarget dependence信号Learning''(can be can break 
  Theorem 3). 
\item
  \textbf{CC 报告not yet give can be property 'sprecise conditions}: theorem  4.2 填补this one 空白. 
\item
  \textbf{CC
  报告's评价''this Yes消息''requires must 限}: only has at 辅助Objective信号and target Noisecorrelated (while non-虚假correlated ), Learningtype 
  PE 'scan only then Yescan be 靠's消息. 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Summary}\label{ux603bux7ed3}

for  PPE at  SCX framework in 'susing lines严格'snumber learning 推导, must contribution including : 

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{编code Function'sformula ization }(Part 1
  part ):give physicalLocation域's正弦and 旋转编code 'sdetermine 义, 推导based on 
  Bochner theorem and  Laplace 核'sOptimalrate 谱, Proofcode 's Lipschitz
  continuous property Computationprecise number . 
\item
  \textbf{Theorem 1 's修正}(Part 2 part ):修正 CC 审计报告in 's sign
  error, give  \(\delta_s^{\text{PE}}\) 'sCorrect表达formula , Proof
  \(\delta_s^{\text{PE}} > 0\)
  'sconditions(\(I(Y; P \mid S) > 0\)), 并推导based on Data Processingnon-etc.formula and 
  Fano non-etc.formula 'son lower bound. 
\item
  \textbf{Theorem 2 's修正}(Part 3 part ):give 
  \(\varepsilon_{\text{PE}}\) 'sprecise Information Theorydefinition , Proof
  \(\varepsilon_{\text{PE}} \to 0\)
  Upper Boundrestore as 原始形formula , provide FromDatain can be 估计
  \(\varepsilon_{\text{PE}}\) 'susing Method(KSG
  估计device and 预测property can Difference). 
\item
  \textbf{Theorem 3 's修正}(Part 4 part ):precise 阐明fixed  PE and Learningtype  PE
  for non-can be property 'snon-influence , through minimum construct property e.g.示区'smust conditions, 提包含precise 前提conditions's修正
  Theorem 3'. 
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{推导完Date: 2026-06-29}\\
\emph{注: which has theorem 编号遵循 SCX framework 内约. 修正 CC
审计报告in 'sSymbolnon-one 致(especially Yes §2.3 命题 2.1 's sign
error), at 该报告's基础on increase 严格'son lower bound推导, construct property Proofand can be 估计形formula . }

\end{document}
