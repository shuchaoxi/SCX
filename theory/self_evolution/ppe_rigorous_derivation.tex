% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  12pt,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{SCX}
\date{2026}

\begin{document}

\section{Physical Positional Encoding (PPE) at  SCX
框架in 's严密推导}\label{physical-positional-encoding-ppe-ux5728-scx-ux6846ux67b6ux4e2dux7684ux4e25ux5bc6ux63a8ux5bfc}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{Document Status}: 严格数学推导\\
\textbf{Prerequisite Reading}:
\texttt{multi\_head\_spring\_and\_positional\_encoding\_analysis.md}（CC
Audit Report)\\
\textbf{Methodology}: conditions + Assumptions → 定理 → Proof，each 一步标注Assumptionsconditions\\
\textbf{修订}: 修正了 CC 审计报告in 'sSymbolnon-一致和两处 sign error

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Symbol约定}\label{ux7b26ux53f7ux7ea6ux5b9a}

as 保持一致，本文采using  CC 审计报告's主must Symbol，but at requires must 时重新精确定义。

\subsubsection{Status-专家System}\label{ux72b6ux6001-ux4e13ux5bb6ux7cfbux7edf}

\begin{itemize}
\tightlist
\item
  \(\mathcal{S} = \{s_1, \ldots, s_N\}\)：\(N\)
  个discrete state atom，\(s_i \in \mathbb{R}^{d_s}\)
\item
  \(\mathcal{P}\)：physicalLocation空间
\item
  \(M\)：独立专家数量，\(\{E_1, \ldots, E_M\}\)
\item
  \(y_i \in \mathcal{Y}\)：Status原子 \(s_i\) 's标签
\end{itemize}

\subsubsection{专家投票}\label{ux4e13ux5bb6ux6295ux7968}

for each 个Sample \((s_i, y_i)\)，专家 \(m\) 's输出as ：

\[v_m(s_i) = \mathbf{1}[E_m(s_i) \neq y_i] \in \{0, 1\}\]

\begin{itemize}
\tightlist
\item
  \(v_m = 0\)：专家同意give 定标签
\item
  \(v_m = 1\)：专家disagree withgive 定标签
\end{itemize}

at CleanSampleunder （标签Correct):\(v_m \sim \text{Bernoulli}(p_{\text{clean}, s})\)，where
\(p_{\text{clean}, s} < 0.5\)
at NoiseSampleunder （标签Error):\(v_m \sim \text{Bernoulli}(p_{\text{noisy}, s})\)，where
\(p_{\text{noisy}, s} > 0.5\)

\textbf{Detection边际}：

\[\boxed{\Delta_s = p_{\text{noisy}, s} - p_{\text{clean}, s} > 0}\]

（this and  CC 报告's
\(\Delta_s = |p_{\text{clean},s} - p_{\text{noisy},s}|\) 一致，because as 
\(p_{\text{noisy}} > p_{\text{clean}}\)。）

\subsubsection{一致性计数and 阈值}\label{ux4e00ux81f4ux6027ux8ba1ux6570ux4e0eux9608ux503c}

\[C(s) = \sum_{m=1}^{M} v_m(s)\]

决策阈值
\(\tau_s = M \cdot \frac{p_{\text{clean},s} + p_{\text{noisy},s}}{2}\)。标记as Noisewhen 
\(C(s) \geq \tau_s\)。

\subsubsection{Chernoff-Hoeffding
基础}\label{chernoff-hoeffding-ux57faux7840}

Hoeffding non-etc.式：for  i.i.d. Bernoulli 随机变量
\(v_1, \ldots, v_M\)，all 值as  \(\mu\)：

\[P\left(\frac{1}{M}\sum_{m=1}^{M} v_m - \mu \geq t\right) \leq \exp(-2M t^2)\]

should using 于NoiseDetection： - 假阳性（Type
I):\(P_{H_0}(C(s) \geq \tau_s) \leq \exp\left(-2M\left(\frac{\tau_s}{M} - p_{\text{clean},s}\right)^2\right)\)
- 假阴性（Type
II):\(P_{H_1}(C(s) < \tau_s) \leq \exp\left(-2M\left(p_{\text{noisy},s} - \frac{\tau_s}{M}\right)^2\right)\)

取 \(\tau_s = M(p_{\text{clean},s} + p_{\text{noisy},s})/2\)，两者all 
\(\leq \exp(-M\Delta_s^2/2)\)。

\begin{quote}
\textbf{注}：CC 审计报告make using  \(\exp(-2M\Delta_s^2)\)。this 表明its 
\(\Delta_s\) 's定义can be can non-同（can be can 多了一个 1/2
because 子）。本文at 保持数学精确性's前提under ，\textbf{遵循 CC 报告's约定}：will 
\(\Delta_s\) 重新校准，make  bound 写as 
\(\exp(-2M\Delta_s^2)\)。this 相when 于定义
\(\tilde{\Delta}_s = (p_{\text{noisy}} - p_{\text{clean}})/2\)，at 原始报告in 记as 
\(\Delta_s\)。后续Analysisin ，精确's常数because 子non-影响推导Structure。
\end{quote}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 1
部分：编码Function's严密数学形式化}\label{ux7b2c-1-ux90e8ux5206ux7f16ux7801ux51fdux6570ux7684ux4e25ux5bc6ux6570ux5b66ux5f62ux5f0fux5316}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.1
三种physicalLocation域's定义}\label{ux4e09ux79cdux7269ux7406ux4f4dux7f6eux57dfux7684ux5b9aux4e49}

\textbf{定义 1.1.1（标量Location域------蛋白质序列）}

\[\mathcal{P}_{\text{seq}} = \{1, 2, \ldots, L\} \subset \mathbb{N}\]

where \(L\) as 序列长度。for 连续近似，嵌入as 
\(\mathcal{P}_{\text{seq}} \cong [0, L] \subset \mathbb{R}\)。

physicalMeaning：残基索引携带局部Chemistry环境's一维顺序信息（N 端→C 端）。

\textbf{定义 1.1.2（3D toward 量Location域------原子坐标）}

\[\mathcal{P}_{\text{3D}} = \mathbb{R}^3\]

元素 \(\mathbf{p} = (x, y, z) \in \mathbb{R}^3\)
as 原子at 实空间in 's笛卡尔坐标。

physicalMeaning：原子's三维空间Location携带键长、键角、配位数etc.几何信息。this Yes\textbf{最丰富's}physicalLocation域。

\textbf{定义 1.1.3（标量总数域------原子数）}

\[\mathcal{P}_{\text{count}} = \mathbb{N}\]

元素 \(N\) as System's总原子数。

physicalMeaning：System大小决定了can 量面'sDimension、can be can 's振动Mode数、缺陷浓度etc.。for 小System特别重must （has 限尺寸效should ）。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.2
标量Location's正弦编码：Optimal频率谱}\label{ux6807ux91cfux4f4dux7f6eux7684ux6b63ux5f26ux7f16ux7801ux6700ux4f18ux9891ux7387ux8c31}

\subsubsection{1.2.1 编码定义}\label{ux7f16ux7801ux5b9aux4e49}

\textbf{定义 1.2.1（正弦标量Location编码）}：for  \(p \in [0, L]\)，Dimension
\(d\)（偶数):

\[\boxed{\text{PE}_{\text{scalar}}(p, 2j) = \sqrt{\frac{2}{d}}\,\sin\left(\frac{2\pi p}{\lambda_j}\right), \quad \text{PE}_{\text{scalar}}(p, 2j+1) = \sqrt{\frac{2}{d}}\,\cos\left(\frac{2\pi p}{\lambda_j}\right)}\]

where \(j = 0, 1, \ldots, d/2 - 1\)，\(\lambda_j > 0\)
as \textbf{feature波长}。

归一化because 子 \(\sqrt{2/d}\) Guarantee了
\(\|\text{PE}_{\text{scalar}}(p)\| = 1\) for which has  \(p\) 恒成立（\(d/2\) 个
\((\sin,\cos)\) for 各贡献 \((2/d) \cdot 1 = 2/d\)，总和as 
\(1\)），并make 编码核at  \(d \to \infty\) 时\textbf{Convergenceto }Objective核
\(k(\Delta)\) rather than diverging to \(O(d)\) 量级。

\begin{quote}
\textbf{Parameter化}：标准 Transformer make using 
\(\lambda_j = 2\pi \cdot 10000^{2j/d}\)（i.e.
\(\omega_j = 10000^{2j/d}\)，where
\(\text{PE}(p, 2j) = \sin(p/\omega_j)\)）。

本文make using \textbf{波长Parameter化}
\(\lambda_j\)，because as 它直接for should physical长度尺度：\(\lambda_j\)
is aComplete正弦周期for should 'sLocation差。
\end{quote}

\textbf{编码's内积Structure（核表示）}：

\[\langle \text{PE}_{\text{scalar}}(p), \text{PE}_{\text{scalar}}(q) \rangle = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos\left(\frac{2\pi(p-q)}{\lambda_j}\right)\]

this 定义了一个\textbf{平移non-变核}
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_j \cos(2\pi\Delta/\lambda_j)\)，where
\(\Delta = p - q\)。

at  \(\Delta = 0\)
处：\(\langle \text{PE}(p), \text{PE}(p) \rangle = (2/d) \cdot (d/2) = 1 = k(0)\)。when 
\(d \to \infty\)
时，\((2/d)K_{\text{PE}}(\Delta) \to \int_0^\infty S(\omega)\cos(\omega\Delta)d\omega = k(\Delta)\)。

\subsubsection{1.2.2
Optimal频率谱推导}\label{ux6700ux4f18ux9891ux7387ux8c31ux63a8ux5bfc}

\textbf{Problem}：give 定编码Dimension \(d\) 和physical区间长度 \(L\)，such as 何Selection
\(\{\lambda_j\}_{j=0}^{d/2-1}\) make 得编码''Optimal''？

\textbf{Assumptions
1.2.1（physical局部性公理）}：physicalon 相邻'sLocationshould 具has 相似's编码。具体地，存at 一个递减's\textbf{physical相关核}
\(k(\Delta)\)（where
\(\Delta = |p-q|\)）控制两个Location表示should has 's相似度。for 于凝聚态physicalSystem：

\[k(\Delta) = \exp\left(-\frac{\Delta}{\xi}\right)\]

where \(\xi > 0\) Yes\textbf{physical相关长度}（例such as ：键长 \(\approx 1.9\)
Å，or 屏蔽长度）。该核满足 Bochner 定理：它Yes正定's，because while Yes某个non-负测度's
Fourier 变换。

\textbf{定理 1.2.1（编码'sOptimal频率谱------Kernel Mean Embedding 视角）}

设Objective核 \(k(\Delta) = \exp(-|\Delta|/\xi)\)（Laplace
核）。then 规范化编码核
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos(2\pi\Delta/\lambda_j)\)
at  \(L^2([0, L])\) in Optimal逼近 \(k(\Delta)\)（最小化all 方误差
\(\int_0^L |\frac{2}{d}K_{\text{PE}}(\Delta) - k(\Delta)|^2 d\Delta\)）when and only when 
\(\{\lambda_j\}\) 满足with under 频谱Distribution：

\[\boxed{\lambda_j = 2\pi\xi \cdot \cot\left(\frac{\pi(2j+1)}{2d}\right), \quad j = 0, 1, \ldots, \frac{d}{2}-1}\]

\emph{Proof}：

\textbf{步骤 1（Bochner 表示）}：Laplace 核
\(k(\Delta) = \exp(-|\Delta|/\xi)\) 's Fourier 变换as ：

\[\hat{k}(\omega) = \int_{-\infty}^{\infty} e^{-i\omega\Delta} e^{-|\Delta|/\xi} d\Delta = \frac{2\xi}{1 + \xi^2\omega^2}\]

this is a Cauchy Distribution（尺度Parameter \(1/\xi\)）。

\textbf{步骤 2（积分表示）}：by  Fourier 反演Formula：

\[k(\Delta) = \frac{1}{2\pi} \int_{-\infty}^{\infty} e^{i\omega\Delta} \hat{k}(\omega) d\omega = \int_{0}^{\infty} \cos(\omega\Delta) \cdot \frac{2}{\pi} \cdot \frac{\xi}{1 + \xi^2\omega^2} d\omega\]

where利using 了 \(\hat{k}\) 's偶for 称性。定义谱密度
\(S(\omega) = \frac{2\xi}{\pi(1 + \xi^2\omega^2)}\)。

\textbf{步骤 3（has 限维逼近）}：规范化编码核
\(\frac{2}{d}K_{\text{PE}}(\Delta) = \frac{2}{d}\sum_{j=0}^{d/2-1} \cos(\omega_j \Delta)\)，where
\(\omega_j = 2\pi/\lambda_j\)，is a\textbf{etc.权离散和}（each 项权重
\(2/d\)），逼近连续积分
\(\int_0^{\infty} S(\omega)\cos(\omega\Delta)d\omega\)。

Optimal采样点should make 得 \(\{S(\omega_j)\Delta\omega_j\}\)
近似all 匀权重。this etc.价于From累积DistributionFunction
\(Q(\omega) = \int_0^{\omega} S(u)du\) in 按\textbf{分位数采样}。

\(Q(\omega) = \frac{2}{\pi} \arctan(\xi\omega)\)，总质量
\(Q(\infty) = 1\)。

\textbf{步骤 4（分位数采样）}：for 于 \(d/2\) 个频率点，Part \(j\)
个点should 覆盖Part \((2j+1)/d\) 分位数：

\[Q(\omega_j) = \frac{2j+1}{d}, \quad j = 0, 1, \ldots, \frac{d}{2}-1\]

求解：\(\omega_j = \frac{1}{\xi} \tan\left(\frac{\pi(2j+1)}{2d}\right)\)。

because 此波长
\(\lambda_j = \frac{2\pi}{\omega_j} = 2\pi\xi \cdot \cot\left(\frac{\pi(2j+1)}{2d}\right)\)。

\(\square\)

\textbf{Corollaries 1.2.1（低频截断）}：最大波长（\(j=0\)）as ：

\[\lambda_{\max} = 2\pi\xi \cdot \cot\left(\frac{\pi}{2d}\right) \approx 4d\xi \quad (\text{for 较大 } d)\]

this 表明编码can 捕获's最长空间相关性约as  \(4d\xi\)。\textbf{必须满足
\(\lambda_{\max} \geq L\)} only then can 覆盖整个序列。this give 出最小Dimensionmust 求：

覆盖整个序列区间requires 满足
\(\lambda_{\max} \geq 2L\)（Nyquistconditions），give 出最小Dimensionmust 求：

\[\boxed{d_{\min} \approx \frac{L}{2\xi}}\]

\textbf{Corollaries 1.2.2（and  Transformer 编码's比较）}：标准 Transformer make using 
\(\omega_j \propto 10000^{-2j/d}\)（几何级数），for should \textbf{for 数all 匀}频率Distribution。while physicalOptimal（Laplace
核）make using 
\(\omega_j \propto \tan(\pi(2j+1)/2d)\)（正切Distribution），更密集地采样\textbf{in 频区域}。Difference来自：NLP
in 'sLocation关系Yes长程while non-指数衰减's，while physicalSystem's相关Function通常Yes短程指数衰减。

\textbf{定理 1.2.2（逼近误差界）}：for  Laplace 核和 \(d/2\)
个频率点's分位数采样，规范化编码核's逼近误差满足：

\[\sup_{\Delta \in [0, L]} \left|\frac{2}{d}K_{\text{PE}}(\Delta) - k(\Delta)\right| = O\left(\frac{1}{d}\right)\]

As \(d \to \infty\) with  \(O(1/d)\) RateConvergence。精确常数dependence于 \(\xi\) 和
\(L\)，but  \(O(1/d)\) 'sConvergenceRatenon-dependence于this 些Parameter。

\emph{Proof}：this Yes带限Function Fourier
积分's标准数值积分误差。分位数采样make 得each 个频率区间's贡献平衡，最大误差受最大is 忽略's频率分量's
Fourier 系数控制。and 原版本non-同，此处make using 规范化编码核
\((2/d)K_{\text{PE}}\) while non-not yet 规范化's \(K_{\text{PE}}\)，确保逼近for 象
\(k(\Delta)\) and 逼近量处于同一量级（all as  \(O(1)\)），while non-
\(K_{\text{PE}}(0)=d/2\) and  \(k(0)=1\) 's跨量级比较。\(\square\)

\subsubsection{1.2.3 正弦编码's Lipschitz
连续性}\label{ux6b63ux5f26ux7f16ux7801ux7684-lipschitz-ux8fdeux7eedux6027}

\textbf{定理 1.2.3（正弦 PPE 's Lipschitz 常数）}：标量正弦编码
\(\text{PE}_{\text{scalar}}: [0, L] \to \mathbb{R}^d\) Yes Lipschitz
连续's，精确常数as ：

\[\boxed{L_{\text{PE}}^{\text{scalar}} = \frac{2\sqrt{2}\,\pi}{\sqrt{d}} \cdot \sqrt{\sum_{j=0}^{d/2-1} \frac{1}{\lambda_j^2}}}\]

\emph{Proof}：for each 个Dimensionfor  \((2j, 2j+1)\)，归一化编码
\(\text{PE}_j(p) = \sqrt{2/d}\,(\sin(2\pi p/\lambda_j), \cos(2\pi p/\lambda_j))\)：

\[\|\text{PE}_j(p) - \text{PE}_j(q)\|^2 = \frac{2}{d}\left[\left(\sin\left(\frac{2\pi p}{\lambda_j}\right) - \sin\left(\frac{2\pi q}{\lambda_j}\right)\right)^2 + \left(\cos\left(\frac{2\pi p}{\lambda_j}\right) - \cos\left(\frac{2\pi q}{\lambda_j}\right)\right)^2\right]\]

by 三角恒etc.式
\((\sin a - \sin b)^2 + (\cos a - \cos b)^2 = 2 - 2\cos(a-b) = 4\sin^2((a-b)/2)\)，again 利using 
\(|\sin\theta| \leq |\theta|\)：

\[\|\text{PE}_j(p) - \text{PE}_j(q)\|^2 = \frac{2}{d} \cdot 4\sin^2\!\left(\frac{\pi(p-q)}{\lambda_j}\right) \leq \frac{2}{d} \cdot \left(\frac{2\pi|p-q|}{\lambda_j}\right)^2 = \frac{8\pi^2|p-q|^2}{d \cdot \lambda_j^2}\]

for which has  \(j\) 求和：

\[\|\text{PE}_{\text{scalar}}(p) - \text{PE}_{\text{scalar}}(q)\|^2 \leq \frac{8\pi^2|p-q|^2}{d} \sum_{j=0}^{d/2-1} \frac{1}{\lambda_j^2}\]

开方i.e.得which 述 Lipschitz 常数。该Upper BoundYes紧's（\(p, q\) No限接近时
\(\sin\theta \approx \theta\)，etc.号渐近成立）。\(\square\)

\textbf{Corollaries 1.2.3（Lipschitz 常数asymptotic behavior）}：for 分位数Optimal谱
\(\lambda_j = 2\pi\xi \cdot \cot(\pi(2j+1)/2d)\) 和归一化编码：

\[L_{\text{PE}}^{\text{scalar}} \sim \frac{1}{\xi} \cdot \sqrt{\frac{d}{6}} \quad (d \to \infty)\]

归一化后 Lipschitz 常数's增长From \(O(d)\) to
\(O(\sqrt{d})\)------归一化because 子 \(\sqrt{2/d}\) 抵消了一个 \(\sqrt{d}\)
because 子，make 得编码for Location变化's敏感度随Dimension\textbf{亚线性}增长。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.3 3D
旋转编码：旋转矩阵Parameter化}\label{d-ux65cbux8f6cux7f16ux7801ux65cbux8f6cux77e9ux9635ux53c2ux6570ux5316}

\subsubsection{1.3.1 编码定义}\label{ux7f16ux7801ux5b9aux4e49-1}

\textbf{定义 1.3.1（3D 旋转Location编码）}：for 
\(\mathbf{p} = (x, y, z) \in \mathbb{R}^3\)，Dimension
\(d\)（\(d \geq 6\)，偶数):

\[\boxed{\text{PE}_{\text{rot}}(\mathbf{p}) = \mathbf{R}(\mathbf{p}) \cdot \mathbf{e}_0}\]

where： - \(\mathbf{e}_0 \in \mathbb{R}^d\) Yes固定's参考单位toward 量（例such as 
\(\mathbf{e}_0 = (1, 0, \ldots, 0)^T\)) -
\(\mathbf{R}(\mathbf{p}) = \mathbf{R}_x(\alpha x) \cdot \mathbf{R}_y(\beta y) \cdot \mathbf{R}_z(\gamma z) \in SO(d)\)
- \(\alpha, \beta, \gamma > 0\)
Yes三个空间Dimension's\textbf{频率Parameter}（rad/Å）

each 个 \(\mathbf{R}_a(\theta)\) is aat 指定 2D 平面on 's \(d\) 维旋转：

\[\mathbf{R}_a(\theta) = \begin{pmatrix} \mathbf{I}_{i_a-1} & & & \\ & \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix} & & \\ & & & \mathbf{I}_{d-i_a-1} \end{pmatrix}\]

where三个旋转平面分别作using 于Dimensionfor  \((0,1)\), \((2,3)\),
\((4,5)\)（分别for should  \(x, y, z\) 轴）。剩余's \(d-6\) 个Dimensionis 
\(\mathbf{R}_x, \mathbf{R}_y, \mathbf{R}_z\) non-变地保留（单位矩阵）。

\subsubsection{1.3.2
旋转矩阵'sParameter化------群论基础}\label{ux65cbux8f6cux77e9ux9635ux7684ux53c2ux6570ux5316ux7fa4ux8bbaux57faux7840}

\textbf{命题 1.3.1（\(\mathbb{R}^3 \to SO(d)\) 's嵌入）}：映射
\(\mathbf{p} \mapsto \mathbf{R}(\mathbf{p})\) Yes \(\mathbb{R}^3\) to 
\(SO(d)\) 's \(3\) 维 Abel 子群's\textbf{李群同态}。

for 换conditions（Abel 性):\(\mathbf{R}_x, \mathbf{R}_y, \mathbf{R}_z\)
作using 于non-相交's坐标for ，because while Yes\textbf{彼此交换}'s：

\[[\mathbf{R}_x(\theta), \mathbf{R}_y(\phi)] = 0, \quad [\mathbf{R}_y(\phi), \mathbf{R}_z(\psi)] = 0, \quad [\mathbf{R}_z(\psi), \mathbf{R}_x(\theta)] = 0\]

组合性质：\(\mathbf{R}(\mathbf{p}_1 + \mathbf{p}_2) = \mathbf{R}(\mathbf{p}_1) \cdot \mathbf{R}(\mathbf{p}_2)\)（加法群同态），because as ：

\begin{align*}
\mathbf{R}(\mathbf{p}_1 + \mathbf{p}_2) &= \mathbf{R}_x(\alpha(x_1 + x_2)) \cdot \mathbf{R}_y(\beta(y_1 + y_2)) \cdot \mathbf{R}_z(\gamma(z_1 + z_2)) \\
&= \mathbf{R}_x(\alpha x_1)\mathbf{R}_x(\alpha x_2) \cdot \mathbf{R}_y(\beta y_1)\mathbf{R}_y(\beta y_2) \cdot \mathbf{R}_z(\gamma z_1)\mathbf{R}_z(\gamma z_2) \\
&= \mathbf{R}_x(\alpha x_1)\mathbf{R}_y(\beta y_1)\mathbf{R}_z(\gamma z_1) \cdot \mathbf{R}_x(\alpha x_2)\mathbf{R}_y(\beta y_2)\mathbf{R}_z(\gamma z_2) \\
&= \mathbf{R}(\mathbf{p}_1) \cdot \mathbf{R}(\mathbf{p}_2)
\end{align*}

（Part三步利using 了交换性。）

\subsubsection{1.3.3
内积Structure（相for Location编码）}\label{ux5185ux79efux7ed3ux6784ux76f8ux5bf9ux4f4dux7f6eux7f16ux7801}

for Arbitrary两个Location \(\mathbf{p}, \mathbf{q} \in \mathbb{R}^3\)：

\[\boxed{\langle \text{PE}_{\text{rot}}(\mathbf{p}), \text{PE}_{\text{rot}}(\mathbf{q}) \rangle = \langle \mathbf{R}(\mathbf{p})\mathbf{e}_0, \mathbf{R}(\mathbf{q})\mathbf{e}_0 \rangle = \langle \mathbf{e}_0, \mathbf{R}(\mathbf{p})^T \mathbf{R}(\mathbf{q}) \mathbf{e}_0 \rangle = \langle \mathbf{e}_0, \mathbf{R}(\mathbf{q} - \mathbf{p}) \mathbf{e}_0 \rangle}\]

where最后一步利using 了
\(\mathbf{R}(\mathbf{p})^T = \mathbf{R}(-\mathbf{p})\) 和
\(\mathbf{R}(-\mathbf{p})\mathbf{R}(\mathbf{q}) = \mathbf{R}(\mathbf{q} - \mathbf{p})\)。

展开
\(\mathbf{R}(\Delta\mathbf{p}) = \mathbf{R}_x(\alpha\Delta x)\mathbf{R}_y(\beta\Delta y)\mathbf{R}_z(\gamma\Delta z)\)，取
\(\mathbf{e}_0 = (1, 0, 0, 1, 0, 0, \ldots)^T\)（at each for 旋转平面'sPart一维as 
1):

\[\langle \text{PE}_{\text{rot}}(\mathbf{p}), \text{PE}_{\text{rot}}(\mathbf{q}) \rangle = \cos(\alpha\Delta x) + \cos(\beta\Delta y) + \cos(\gamma\Delta z)\]

where
\(\Delta\mathbf{p} = \mathbf{q} - \mathbf{p} = (\Delta x, \Delta y, \Delta z)\)。

编码's内积\textbf{only dependence于相for Location}
\(\Delta\mathbf{p}\)，满足\textbf{平移non-变性}。

\subsubsection{1.3.4 旋转编码's Lipschitz
连续性}\label{ux65cbux8f6cux7f16ux7801ux7684-lipschitz-ux8fdeux7eedux6027}

\textbf{定理 1.3.1（旋转编码's Lipschitz 常数）}：映射
\(\text{PE}_{\text{rot}}: \mathbb{R}^3 \to \mathbb{R}^d\) Yes Lipschitz
连续's。取 \(\mathbf{e}_0\) as on 述形式（each for 平面'sPart一维as  1，its 余as 
0），精确常数as ：

\[\boxed{L_{\text{PE}}^{\text{rot}} = \max(\alpha, \beta, \gamma)}\]

\emph{Proof}：

\textbf{步骤 1（直接范数Computation）}：编码差as 
\(\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q}) = \mathbf{R}(\mathbf{p})\mathbf{e}_0 - \mathbf{R}(\mathbf{q})\mathbf{e}_0\)。by 于三个旋转作using 于互non-相交'sDimensionfor 
\((0,1), (2,3), (4,5)\)，编码差's各个分量解耦。for  \(x\)-平面（Dimension
0,1):

\[\|\mathbf{R}_x(\alpha x)\mathbf{e}_x - \mathbf{R}_x(\alpha q_x)\mathbf{e}_x\|^2 = (\cos(\alpha x)-\cos(\alpha q_x))^2 + (\sin(\alpha x)-\sin(\alpha q_x))^2 = 2 - 2\cos(\alpha\Delta x) = 4\sin^2\left(\frac{\alpha\Delta x}{2}\right)\]

同理for  \(y, z\) 平面。because 此：

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\|^2 = 4\sin^2\left(\frac{\alpha\Delta x}{2}\right) + 4\sin^2\left(\frac{\beta\Delta y}{2}\right) + 4\sin^2\left(\frac{\gamma\Delta z}{2}\right)\]

\textbf{步骤 2（利using  \(|\sin\theta| \leq |\theta|\)）}：

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\|^2 \leq \alpha^2\Delta x^2 + \beta^2\Delta y^2 + \gamma^2\Delta z^2 \leq \max(\alpha, \beta, \gamma)^2 \cdot \|\Delta\mathbf{p}\|_2^2\]

开方i.e.得
\(\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\| \leq \max(\alpha, \beta, \gamma) \cdot \|\mathbf{p} - \mathbf{q}\|_2\)。

\textbf{步骤 3（紧致性）}：取 \(\Delta\mathbf{p} = (\varepsilon, 0, 0)\)
and  \(\alpha = \max(\alpha, \beta, \gamma)\)，when  \(\varepsilon \to 0\)
时：

\[\|\text{PE}_{\text{rot}}(\mathbf{p}) - \text{PE}_{\text{rot}}(\mathbf{q})\| = 2|\sin(\alpha\varepsilon/2)| \to \alpha|\varepsilon| = \max(\alpha, \beta, \gamma) \cdot \|\Delta\mathbf{p}\|\]

该界Yes紧's。\(\square\)

\textbf{注 1.3.1（and 先前版本in 常数高估'sExplanation）}：先前版本make using 了
\(\|\mathbf{R}(\mathbf{p}) - \mathbf{R}(\mathbf{q})\|_F\)
's三角non-etc.式分解结合 Cauchy-Schwarz，得to 
\(2\sqrt{\alpha^2 + \beta^2 + \gamma^2}\)。this 一常数比真实值
\(\max(\alpha, \beta, \gamma)\) 高出约 \(2\sqrt{3} \approx 3.46\) 倍（when 
\(\alpha = \beta = \gamma\) 时），ReasonYes：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{三角non-etc.式at 正交子空间on 's松弛}：三个差分矩阵
  \(\mathbf{D}_x, \mathbf{D}_y, \mathbf{D}_z\)
  作using 于互non-相交'sDimensionfor on ，because 此
  \(\|\mathbf{D}_x + \mathbf{D}_y + \mathbf{D}_z\|_F^2 = \|\mathbf{D}_x\|_F^2 + \|\mathbf{D}_y\|_F^2 + \|\mathbf{D}_z\|_F^2\)，but 三角non-etc.式
  \(\|\mathbf{D}_x + \mathbf{D}_y + \mathbf{D}_z\|_F \leq \|\mathbf{D}_x\|_F + \|\mathbf{D}_y\|_F + \|\mathbf{D}_z\|_F\)
  at 最坏情况under can be 松 \(\sqrt{3}\) 倍（三项etc.范数时）。
\item
  \textbf{Cauchy-Schwarz 's \(\ell_1 \to \ell_2\)
  转换}：\(\alpha|\Delta x| + \beta|\Delta y| + \gamma|\Delta z| \leq \sqrt{\alpha^2 + \beta^2 + \gamma^2} \cdot \|\Delta\mathbf{p}\|_2\)
  at  \(\alpha=\beta=\gamma\) and  \(\Delta x = \Delta y = \Delta z\)
  时引入另一个 \(\sqrt{3}\) 倍松弛。
\end{enumerate}

两重松弛's复合导致常数高估约 \(2\sqrt{3}\)
倍。Correct'sProofshould 利using 子空间正交性直接Computation \(\ell_2\)
范数，避免this 两层松弛。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{1.4 统一 Lipschitz
连续性定理}\label{ux7edfux4e00-lipschitz-ux8fdeux7eedux6027ux5b9aux7406}

\textbf{定理 1.4.1（PPE 's统一 Lipschitz 性质）}：设
\(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
as with on 定义's任一编码。then 存at 常数 \(L_{\text{PE}} > 0\) make 得for which has 
\(\mathbf{p}, \mathbf{q} \in \mathcal{P}\)：

\[\|\text{PE}(\mathbf{p}) - \text{PE}(\mathbf{q})\| \leq L_{\text{PE}} \cdot d_{\mathcal{P}}(\mathbf{p}, \mathbf{q})\]

where \(d_{\mathcal{P}}\) Yes \(\mathcal{P}\) on 'sNature度量： -
标量域：\(d_{\mathcal{P}}(p, q) = |p - q|\) - 3D
域：\(d_{\mathcal{P}}(\mathbf{p}, \mathbf{q}) = \|\mathbf{p} - \mathbf{q}\|_2\)

各个域's精确 Lipschitz 常数：

\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.2381}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3571}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.4048}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
编码类型
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\(\mathcal{P}\)
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\(L_{\text{PE}}\)
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
正弦（标量） & \([0, L]\) &
\(\frac{2\sqrt{2}\,\pi}{\sqrt{d}}\sqrt{\sum_j 1/\lambda_j^2}\) \\
旋转（3D） & \(\mathbb{R}^3\) & \(\max(\alpha, \beta, \gamma)\) \\
\end{longtable}

physical意义：\(L_{\text{PE}}\) 控制编码for Location变化's\textbf{敏感度}。大's
\(L_{\text{PE}}\)
make 邻近Location's编码更加non-同（高分辨率），but can be can 破坏局部光滑性。Optimal
\(L_{\text{PE}}\) should at physical相关尺度 \(\xi\) 附近matching编码's分辨率。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 2 部分：Theorem 1
's严格修正}\label{ux7b2c-2-ux90e8ux5206theorem-1-ux7684ux4e25ux683cux4feeux6b63}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2.1
修正'sDetection边际------精确表达式}\label{ux4feeux6b63ux7684ux68c0ux6d4bux8fb9ux9645ux7cbeux786eux8868ux8fbeux5f0f}

\subsubsection{2.1.1 设定}\label{ux8bbeux5b9a}

引入 PPE 后，专家 \(m\) 's输入From \(s_i\) 变as ：

\[h_i = \phi(s_i) + \text{PE}(p_i)\]

where \(\phi: \mathbb{R}^{d_s} \to \mathbb{R}^d\)
YesStatusfeature映射（can be with Yes恒etc.映射or can be Learning's线性投影）。

\textbf{Assumptions 2.1.1（专家作using 于编码空间）}：专家Model \(E_m\) at 编码空间
\(\mathbb{R}^d\) on 操作。它们接受 \(h_i\)
作as 输入并输出预测，投票定义as ：

\[v_m^{\text{PPE}}(s_i) = \mathbf{1}[E_m(h_i) \neq y_i]\]

at Clean/Noiseconditionsunder 's期望as ：

\[p_{\text{clean}, s}^{\text{PPE}} = \mathbb{E}[v_m^{\text{PPE}}(s) \mid \text{clean state } s]\]
\[p_{\text{noisy}, s}^{\text{PPE}} = \mathbb{E}[v_m^{\text{PPE}}(s) \mid \text{noisy state } s]\]

\subsubsection{2.1.2 修正命题}\label{ux4feeux6b63ux547dux9898}

\textbf{命题 2.1（PPE under 's精确Detection边际变化------修正 CC 审计报告in 's
sign error）}

PPE 增强空间in 'shas 效Detection边际as ：

\[\boxed{\Delta_s^{\text{PPE}} = \Delta_s + \delta_s^{\text{PE}}}\]

where：

\[\boxed{\delta_s^{\text{PE}} = \underbrace{(p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s})}_{\text{NoiseSampleon 多出's分歧增益}} - \underbrace{(p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s})}_{\text{CleanSampleon 's（non-希望has 's）分歧变化}}}\]

\begin{quote}
\textbf{修正Explanation}：CC 审计报告in  \(\delta_s^{\text{PE}}\)
's表达式Symbol相反。Correct'sSymbolYes：\(\delta_s^{\text{PE}} > 0\) when and only when 
PPE 帮助区分Noise和CleanSample（增大Detection边际）。this must 求： -
NoiseSampleon 's分歧概率\textbf{增加}（\(p_{\text{noisy}}^{\text{PPE}} > p_{\text{noisy}}\)）------专家更好地''识别''出Error标签
-
CleanSampleon 's分歧概率\textbf{non-增加or 减少}（\(p_{\text{clean}}^{\text{PPE}} \leq p_{\text{clean}}\)）------专家to 少保持原has 's一致性
\end{quote}

\(\delta_s^{\text{PE}}\) 's值域：by 于概率at  \([0,1]\) in ，has 
\(\delta_s^{\text{PE}} \in [-\Delta_s, 1 - p_{\text{clean}, s}]\)。

\emph{Proof}：by 定义：

\begin{align*}
\Delta_s^{\text{PPE}} &= p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{clean}, s}^{\text{PPE}} \\
&= (p_{\text{noisy}, s} + (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s})) - (p_{\text{clean}, s} + (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s})) \\
&= (p_{\text{noisy}, s} - p_{\text{clean}, s}) + (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) \\
&= \Delta_s + \delta_s^{\text{PE}}
\end{align*}

值域：最小化 \(\delta_s^{\text{PE}}\) ------
\(p_{\text{noisy}}^{\text{PPE}} = 0\)（which has NoiseSampleis 误判as Clean），\(p_{\text{clean}}^{\text{PPE}} = 1\)（which has CleanSampleis 误判as Noise）→
\(\delta_s^{\text{PE}} = (0 - p_{\text{noisy}}) - (1 - p_{\text{clean}}) = -p_{\text{noisy}} - 1 + p_{\text{clean}} = -(1 + \Delta_s)\)。but this 超过了
\(p_{\text{clean}}^{\text{PPE}}\) 和 \(p_{\text{noisy}}^{\text{PPE}}\)
's合理范围\ldots 实际on ，at has 意义's专家Modelin ，\(p_{\text{noisy}}^{\text{PPE}} \in [0, 1]\)
和 \(p_{\text{clean}}^{\text{PPE}} \in [0, 1]\)
约束under ，\(\delta_s^{\text{PE}} \in [-\Delta_s, 1]\)。更精细's界by 
\(p_{\text{noisy}}^{\text{PPE}} \in [0,1]\) 和
\(p_{\text{clean}}^{\text{PPE}} \in [0,1]\) give 出。\(\square\)

\textbf{修正后's Theorem 1（PPE 增强）}：

\[\boxed{F_1^{\text{PPE}} \geq 1 - \frac{1}{\eta} \sum_{s \in \mathcal{S}} \rho_s \cdot \exp\left(-2M (\Delta_s + \delta_s^{\text{PE}})^2\right)}\]

Chernoff-Hoeffding Structure保持（指示变量at  \([0,1]\) in has 界），only Yes常数
\(\Delta_s\) 变as  \(\Delta_s + \delta_s^{\text{PE}}\)。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.2 \(\delta_s^{\text{PE}} > 0\)
's必must conditions和充分conditions}{2.2 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} \textgreater{} 0 's必must conditions和充分conditions}}\label{delta_stextpe-0-ux7684ux5fc5ux8981ux6761ux4ef6ux548cux5145ux5206ux6761ux4ef6}

\subsubsection{2.2.1 Information Theoryconditions}\label{ux4fe1ux606fux8bbaux6761ux4ef6}

\textbf{定理 2.2.1（\(\delta_s^{\text{PE}} > 0\)
's充分conditions------Information Theory形式，贝叶斯Optimal限定）}

设 \(S, P, Y\)
分别表示Status原子、physicalLocation和标签's随机变量（at DataDistributionunder ）。if ：

\[\boxed{I(Y; P \mid S) > 0}\]

i.e.：physicalLocationat \textbf{give 定Status原子'sconditionsunder }携带关于标签's额外信息，then \textbf{存at }一个
PPE Function \(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
和编码Dimension \(d_{\text{pe}}\)，make 得for 某个Status
\(s\)，\textbf{贝叶斯Optimal分类器}at 增广feature \((s, \text{PE}(p))\)
under 'sDetection边际变化满足 \(\delta_s^{\text{PE}} > 0\)。

\emph{Proof}：

\textbf{步骤 1（信息储存'sphysicalcan be and 性）}：by Assumptions
\(I(Y; P \mid S) > 0\)，存at 一for Distribution \((P_{Y|S,P}, P_{Y|S})\)
Yesnon-相同's：

\[I(Y; P \mid S) = \mathbb{E}_{S, P}\left[D_{\text{KL}}(P_{Y|S,P} \| P_{Y|S})\right] > 0\]

because 此存at 一个正测度's事件集
\(\mathcal{A} \subset \mathcal{S} \times \mathcal{P}\)，at its on 
\(P_{Y|S=s, P=p} \neq P_{Y|S=s}\)。

\textbf{步骤 2（构造编码）}：选取一个足够表达力's编码Function
\(\text{PE}\)。by 通using 逼近定理（例such as 具has 足够多神via 元's神via 网络），存at 
\(\text{PE}\) make 得编码表示 \(\text{PE}(p)\) 近似保留了 \(P\) in 关于
\(Y\) 'swhich has 相关信息。

具体地，取 \(d_{\text{pe}}\) 足够大，make 得存at  \(\text{PE}\)
满足Data Processingnon-etc.式\textbf{with etc.式成立}：

\[I(Y; \text{PE}(P) \mid S) = I(Y; P \mid S) - \varepsilon_{\text{PE}}\]

where \(\varepsilon_{\text{PE}} \geq 0\) Yes编码造成's信息丢失（见Part 3
部分）。for 于充分表达's \(\text{PE}\)，can be Guarantee
\(\varepsilon_{\text{PE}} < I(Y; P \mid S)\)，because 此：

\[I(Y; \text{PE}(P) \mid S) > 0\]

\textbf{步骤 3（贝叶斯Optimal分类器'slinesas ）}：conditions互信息
\(I(Y; \text{PE}(P) \mid S) > 0\) 意味着give 定 \(S\)，\(\text{PE}(P)\)
提供关于 \(Y\) 's额外''信号''。考虑贝叶斯Optimal分类器at 增广空间in 's表现。

for 于NoiseSample（标签 \(y\) YesError's):give 定
\((s, p)\)，贝叶斯Optimal预测器will 更倾toward 于输出\textbf{and  \(y\)
non-同's}真实标签（because as  \(p\) 提供了额外's证据）。because 此分歧概率on 升：

\[p_{\text{noisy}, s}^{\text{PPE}} > p_{\text{noisy}, s}\]

for 于CleanSample：额外'sLocation信息\textbf{进一步确认}Correct's标签，by Data Processingnon-etc.式Guarantee专家分歧概率\textbf{non-增加}（甚to 减少):

\[p_{\text{clean}, s}^{\text{PPE}} \leq p_{\text{clean}, s}\]

because 此\textbf{贝叶斯Optimal分类器}'s
\(\delta_s^{\text{PE}} = (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) > 0\)。

\textbf{步骤
4（From贝叶斯Optimalto 实际专家------猜想）}：\textbf{{[}启发式{]}}
on 述Conclusionfor 贝叶斯Optimal分类器严格成立。for 于 SCX 框架in 's实际神via 网络专家
\(E_m\)，must Guarantee \(\delta_s^{\text{PE}} > 0\)
requires 额外Assumptions：专家Model's输出一致地逼近贝叶斯Optimal分类器（at  \(\ell_1\)
意义under for conditionsDistribution \(P_{Y|S,P}\) 's逼近误差小于
\(|\delta_s^{\text{PE}}|\)）。for 于通过Stochastic Gradient DescentTraining's神via 网络，目前\textbf{没has }has 限Sampleunder 's严格Convergence Guarantee。此as when 前定理's\textbf{主must 局限}：步骤
1-3
Proof了贝叶斯Optimal分类器can be with FromLocation信息in 受益（Information Theoryon 's必然性），but From贝叶斯Optimalto 实际专家's推广Yes启发式猜想。

\textbf{退一步看}：if 步骤 4 's推广non-成立，定理 2.2.1
退化as 一个via 典Conclusion：\(I(Y;P|S) > 0\) 意味着贝叶斯Optimal分类器at make using  \(P\)
信息时表现更好------this YesData Processingnon-etc.式's直接Corollaries，Yes平凡's。\(\square\)

\subsubsection{2.2.2 必must conditions}\label{ux5fc5ux8981ux6761ux4ef6}

\textbf{命题 2.2.1（\(\delta_s^{\text{PE}} > 0\) 's必must conditions）}：

if for which has 专家 \(m\)，\(\delta_s^{\text{PE}} > 0\)，then 必须has ：

\[\boxed{I(Y; P \mid S) > 0 \quad \text{or } \quad \text{PE 改变了专家System's偏差-方差Structure（non-Information TheoryMechanism）}}\]

Part一种情况（Information TheoryMechanism）already at  2.2.1
in 处理。Part二种情况（non-Information TheoryMechanism）can be can 发生at ：PE
增加了编码Dimension's\textbf{随机正then 化效should }，make 专家输出's方差Structure发生变化。but this non-Yes''physicalLocation信息''at 起作using ------while YesArbitrary's加性Noisealso can 产生's效should 。

\textbf{physicalon 重must 's情形Yes \(I(Y; P \mid S) > 0\)}。this etc.价于：

\[\exists s, p_1, p_2 \text{ make 得 } P_{Y|S=s, P=p_1} \neq P_{Y|S=s, P=p_2}\]

i.e.：for 于\textbf{同一个Status原子} \(s\)，non-同'sphysicalLocation \((p_1, p_2)\)
will 导致non-同's标签Distribution。

\textbf{physical实例}： - 蛋白质in ：相同氨基酸类型（Status \(s\)）at 
\(\alpha\)-螺旋（Location \(p_1\)）和 \(\beta\)-折叠（Location
\(p_2\)）in 's功can Rolenon-同 ← \(I(Y; P \mid S) > 0\) -
晶体in ：相同'sChemistry环境（Status \(s\)）at Surface（Location \(p_1\)）和体相（Location
\(p_2\)）in 's缺陷形成can non-同 ← \(I(Y; P \mid S) > 0\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.3 \(\delta_s^{\text{PE}}\)
'sInformation TheoryUpper Bound（Data Processingnon-etc.式）}{2.3 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} 'sInformation TheoryUpper Bound（Data Processingnon-etc.式）}}\label{delta_stextpe-ux7684ux4fe1ux606fux8bbaux4e0aux754cux6570ux636eux5904ux7406ux4e0dux7b49ux5f0f}

\textbf{定理 2.3.1（\(\delta_s^{\text{PE}}\) 'sInformation TheoryUpper Bound）}：

\[\boxed{\delta_s^{\text{PE}} \leq \min\left(1, \sqrt{\frac{1}{2} \cdot I(P; \text{PE}(P) \mid S, Y)}\right) + \text{Bernstein 修正项}}\]

更精确地，by Data Processingnon-etc.式（DPI）和 Pinsker non-etc.式：

\[\delta_s^{\text{PE}} \leq \min\left(1 - p_{\text{clean}, s}, \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}|S,Y} \| P_{\text{PE}|S})}\right) + \delta_s^{\text{variance}}\]

where \(\delta_s^{\text{variance}}\) 来自has 限专家数 \(M\)
引入's估计方差。

\emph{Proof}：

\textbf{步骤 1（概率变化and  KL 散度）}：for 于CleanSampleon 's分歧概率变化：

\[|p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| = \left|\mathbb{E}_{\text{PE}(P)|S=s, Y=y_{\text{true}}}[P(E_m(h) \neq y_{\text{true}})] - P(E_m(s) \neq y_{\text{true}})\right|\]

by  Pinsker non-etc.式：for Arbitrary事件
\(A\)，\(|P(A) - Q(A)| \leq \sqrt{\frac{1}{2} D_{\text{KL}}(P \| Q)}\)。

should using to  \(p_{\text{clean}}\) 和 \(p_{\text{noisy}}\) 's变化，分别give 出：

\begin{align*}
|p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| &\leq \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}(P)|S=s, \text{clean}} \| P_{\text{PE}(P)|S=s})} \\
|p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}| &\leq \sqrt{\frac{1}{2} D_{\text{KL}}(P_{\text{PE}(P)|S=s, \text{noisy}} \| P_{\text{PE}(P)|S=s})}
\end{align*}

\textbf{步骤 2（组合）}：

\begin{align*}
\delta_s^{\text{PE}} &= (p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}) - (p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}) \\
&\leq |p_{\text{noisy}, s}^{\text{PPE}} - p_{\text{noisy}, s}| + |p_{\text{clean}, s}^{\text{PPE}} - p_{\text{clean}, s}| \\
&\leq \sqrt{\frac{1}{2} D_{\text{KL}}^{(1)}} + \sqrt{\frac{1}{2} D_{\text{KL}}^{(2)}}
\end{align*}

where \(D_{\text{KL}}^{(1)}, D_{\text{KL}}^{(2)}\)
分别YesClean和Noiseconditionsunder 's KL 散度。

\textbf{步骤 2b（值域Upper Bound's来源------补充Explanation）}：by 
\(\delta_s^{\text{PE}}\) 's值域（命题 2.1），恒has 
\(\delta_s^{\text{PE}} \leq 1 - p_{\text{clean}, s}\)（取
\(p_{\text{noisy}}^{\text{PPE}} = 1\),
\(p_{\text{clean}}^{\text{PPE}} = 0\) 时's最大值）。此Upper Bound来自概率's
\([0,1]\) 约束，non-dependence于任何DistributionAssumptions。Theorem Statementin 's
\(\min(1-p_{\text{clean},s}, \sqrt{\frac{1}{2}D_{\text{KL}}})\)
取自\textbf{两个独立has 效Upper Bound's最小值}：值域Upper Bound和 Pinsker Upper Bound。when 
Pinsker 界较松（KL 散度大but  \(\delta_s^{\text{PE}}\)
仍受限于值域）时，值域界更紧；反之when Location信息很少（KL 散度小）时，Pinsker
界更紧。

\textbf{步骤 3（DPI 约束）}：by Data Processingnon-etc.式（编码 PE only can 减少信息):

\[I(\text{PE}(P); Y \mid S) \leq I(P; Y \mid S)\]

because 此 \(\delta_s^{\text{PE}}\) 受限于Location \(P\) at give 定 \(S\)
under 携带have mutual information量。when  \(I(P; Y \mid S) = 0\)（LocationNousing 时），Upper Bound退化as ：

\[\delta_s^{\text{PE}} \leq 0 + \delta_s^{\text{variance}}\]

at 大量专家极限 \(M \to \infty\) under 
\(\delta_s^{\text{variance}} \to 0\)。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{2.4 \(\delta_s^{\text{PE}}\)
's启发式估计（Fano
界限------non-严格lower bound）}{2.4 \textbackslash delta\_s\^{}\{\textbackslash text\{PE\}\} 's启发式估计（Fano 界限------non-严格lower bound）}}\label{delta_stextpe-ux7684ux542fux53d1ux5f0fux4f30ux8ba1fano-ux754cux9650ux975eux4e25ux683cux4e0bux754c}

\textbf{命题 2.4.1（\(\delta_s^{\text{PE}}\)
's启发式估计------non-严格lower bound）}：

\begin{quote}
\textbf{{[}启发式{]}} \textbf{（警告：此non-严格定理。）}
\end{quote}

设Location \(P\) at give 定Status \(S\) under 提供关于标签 \(Y\) 's额外信息，and 编码 PE
保留where's
\(\rho = I(Y; \text{PE}(P) \mid S) / I(Y; P \mid S) \in (0, 1]\)
部分。if Assumptions Fano
non-etc.式at 两个世界all 达to etc.号（requires 特定Distributionconditions:Noiseall 匀Distributionat ErrorCategoryon ），then 存at Status
\(s\) make 得：

\[\delta_s^{\text{PE}} \approx \frac{2\rho \cdot I(Y; P \mid S) - \log 2}{\log |\mathcal{Y}|} - O\left(\frac{1}{\sqrt{M}}\right)\]

where \(M\) is the probability that experts数，\(O(1/\sqrt{M})\) 来自has 限专家's估计Noise。

\textbf{关键限定：此表达式 \emph{non-Yes} 严格's数学lower bound。} From两个 Fano
lower bound \(P_e \geq A\) 和 \(P_e^{\text{PPE}} \geq B\) non-can 逻辑地推出
\(P_e - P_e^{\text{PPE}} \geq A - B\)，because as  \(a \geq A\) 和 \(b \geq B\)
non-蕴含 \(a-b \geq A-B\)。此外，Fano
non-etc.式only at 特定Distributionon 达to etc.号（Noiseall 匀Distributionat ErrorCategoryon ，and  \(H_2(P_e)\)
饱和）。because 此，此处give 出's表达式should is 视as for  \(\delta_s^{\text{PE}}\)
量级's\textbf{启发式估计}，while non-定理级'slower boundConclusion。Correct'slower boundrequires must non-同'sProof策略，this Yeswhen 前's一个开放Problem。

\emph{推导思路（non-严格Proof）}：

\textbf{步骤 1（Fano non-etc.式------From误差to 信息）}：Fano
non-etc.式's标准形式：

\[H(Y \mid S, \text{PE}(P)) \leq H_2(P_e) + P_e \cdot \log(|\mathcal{Y}| - 1)\]

where \(P_e\) Yesgive 定 \((S, \text{PE}(P))\) under 's贝叶斯Error率，\(H_2\)
Yes二元熵。etc.效地，lower bound：

\[P_e \geq \frac{H(Y \mid S, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\]

\textbf{步骤 2（启发式关联------non-etc.号误using ）}：if  Fano
non-etc.式at 两个世界all 达to etc.号（this at 一般情况under \textbf{non-成立}），then 形式on can be 写：

\begin{align*}
P_e - P_e^{\text{PPE}} &\stackrel{\text{if etc.号}}{=} \frac{H(Y|S) - H(Y|S, \text{PE}(P))}{\log |\mathcal{Y}|} \\
&= \frac{I(Y; \text{PE}(P) \mid S)}{\log |\mathcal{Y}|} \\
&= \frac{\rho \cdot I(Y; P \mid S)}{\log |\mathcal{Y}|}
\end{align*}

again 加on  \(\Delta P_e\) and  \(\delta_s^{\text{PE}}\) 's启发式关联（Assumptions
\(\Delta P_e \approx -\pi \cdot \delta_s^{\text{PE}}\) for 最大获益'sStatus
\(s\) 成立），取
\(\pi = 1/2\)（最坏情况），\(\rho = 1\)（完美编码），加on has 限 \(M\) 's
Hoeffding 修正i.e.得which 述表达式。

\textbf{again 次强调：此推导Fromnon-etc.号推出了etc.号，non-Yes严格Proof。Correct'slower boundrequires must make using 更强's工具（such as 
Fano 's逆形式or 强 Fano non-etc.式），this requires must 关于Distribution's具体Assumptions。}

\textbf{physicalMeaning（量级估计）}：if  \(I(Y; P \mid S)\)
很小（Location几乎non-提供额外信息），then  \(\delta_s^{\text{PE}}\)
's启发式估计接近零or as 负------PPE
No法实质性地改善Detection。this 解释了as 什么\textbf{non-Yeswhich has physicalSystemall can From PPE
in 受益}。but this 一ConclusionYes方toward Correct'sphysical学直觉，while non-严格's数学Guarantee。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{2.5 空间局部性正then 化------CC
审计报告Result's修正版}\label{ux7a7aux95f4ux5c40ux90e8ux6027ux6b63ux5219ux5316cc-ux5ba1ux8ba1ux62a5ux544aux7ed3ux679cux7684ux4feeux6b63ux7248}

\textbf{定理 2.5.1（\(\delta_s^{\text{PE}}\) 's空间光滑性）}：设 PE 满足
Lipschitz conditions（常数as  \(L_{\text{PE}}\)），and 专家 \(E_m\)
's输出\textbf{软概率} \(E_m^{\text{soft}}: \mathbb{R}^d \to [0,1]\)（such as 
softmax 输出's最大Category概率）满足 \(L_E\)-Lipschitz
conditions。此外，Assumptions\textbf{边界conditions}：专家 \(E_m\) 's决策边界距离两个编码点
\(h_i, h_j\) to 少as  \(\tau > 0\)，i.e.
\(|E_m^{\text{soft}}(h) - 0.5| \geq \tau\) for  \(h \in \{h_i, h_j\}\)
成立。then for 同属一个Status \(s\)、具has physicalLocation \(p_i, p_j\) 's两个原子：

\[\boxed{|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| \leq 2 \cdot L_E \cdot L_{\text{PE}} \cdot d_{\mathcal{P}}(p_i, p_j) + O\left(\frac{1}{\sqrt{M}}\right)}\]

where \(O(1/\sqrt{M})\)
来自has 限专家's估计误差。\textbf{if 边界conditionsnon-成立}，该界non-Guaranteenon-空------指示Function
\(\mathbf{1}[E_m(h) \neq y]\) at 决策边界处non-连续，软概率's Lipschitz
性质non-can 约束指示Function期望's变化。

\emph{Proof}：

\begin{align*}
|\delta_i^{\text{PE}} - \delta_j^{\text{PE}}| &= |(p_{\text{noisy}, i}^{\text{PPE}} - p_{\text{noisy}, j}^{\text{PPE}}) - (p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}})| \\
&\leq |p_{\text{noisy}, i}^{\text{PPE}} - p_{\text{noisy}, j}^{\text{PPE}}| + |p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}}|
\end{align*}

each 个 probability difference
受输入变化约束。\textbf{关键}：\(p_{\text{clean}, i}^{\text{PPE}} = \mathbb{E}[\mathbf{1}[E_m(h_i) \neq y]]\)
Yes指示Function's期望。边界conditions \(|E_m^{\text{soft}}(h) - 0.5| \geq \tau\)
Guarantee了 \(h_i\) 和 \(h_j\)
all at 决策边界's同一侧并保持一定距离，此时软概率's \(L_E\)-Lipschitz
性质控制指示Function期望's变化：

\[|p_{\text{clean}, i}^{\text{PPE}} - p_{\text{clean}, j}^{\text{PPE}}| \leq L_E \cdot \|h_i - h_j\| = L_E \cdot \|\text{PE}(p_i) - \text{PE}(p_j)\| \leq L_E \cdot L_{\text{PE}} \cdot d_{\mathcal{P}}(p_i, p_j)\]

（利using  \(s_i = s_j = s\) 时 \(\phi(s_i) = \phi(s_j)\)。）两倍i.e.得。

\textbf{if No边界conditions}：决策边界can be can 恰好位于 \(h_i\) 和 \(h_j\)
之间，此时i.e.make  \(\|h_i - h_j\|\)
趋于零，分歧概率can be 相差Arbitrary大（such as 一个complete at Clean侧
\(p \approx 0\)，另一个at Noise侧
\(p \approx 1\)），界变as 空（vacuous）。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 3 部分：Theorem 2
's精确修正}\label{ux7b2c-3-ux90e8ux5206theorem-2-ux7684ux7cbeux786eux4feeux6b63}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{3.1 编码non-完美度 \(\varepsilon_{\text{PE}}\)
's精确Information Theory定义}{3.1 编码non-完美度 \textbackslash varepsilon\_\{\textbackslash text\{PE\}\} 's精确Information Theory定义}}\label{ux7f16ux7801ux4e0dux5b8cux7f8eux5ea6-varepsilon_textpe-ux7684ux7cbeux786eux4fe1ux606fux8bbaux5b9aux4e49}

\subsubsection{3.1.1
conditions互信息's链式分解}\label{ux6761ux4ef6ux4e92ux4fe1ux606fux7684ux94feux5f0fux5206ux89e3}

设随机变量： - \(X\)：原始feature（Status原子 \(S\) or its 表示 \(\phi(S)\)) -
\(P\)：physicalLocation（Complete信息) - \(\text{PE}(P)\)：编码后'sphysicalLocation -
\(Y\)：标签

信息流's Markov 链：

\[Y \leftrightarrow (X, P) \leftrightarrow (X, \text{PE}(P))\]

（i.e.
\(Y \perp\!\!\!\perp \text{PE}(P) \mid (X, P)\)------编码only 丢失信息，non-创造信息。）

\subsubsection{3.1.2
编码non-完美度's定义}\label{ux7f16ux7801ux4e0dux5b8cux7f8eux5ea6ux7684ux5b9aux4e49}

\textbf{定义 3.1（编码non-完美度
\(\varepsilon_{\text{PE}}\)------Information Theory定义）}：

\[\boxed{\varepsilon_{\text{PE}} = I(Y; P \mid X) - I(Y; \text{PE}(P) \mid X)}\]

\textbf{性质}：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Data Processingnon-etc.式}（DPI):\(\varepsilon_{\text{PE}} \geq 0\)
  恒成立。because as  \((X, \text{PE}(P))\) Yes \((X, P)\)
  's（can be can Yes随机's）Function，DPI Guarantee：

  \[I(Y; \text{PE}(P) \mid X) \leq I(Y; P \mid X)\]
\item
  \textbf{can be 逆性conditions}：\(\varepsilon_{\text{PE}} = 0\) when and only when  PE
  at give 定 \(X\) 'sconditionsunder for  \(Y\) 's预测Yes\textbf{充分统计量}，i.e.：

  \[Y \perp\!\!\!\perp P \mid (X, \text{PE}(P))\]

  etc.价地：编码后'sLocation \(\text{PE}(P)\) 保留了 \(P\) in which has and  \(Y\)
  相关's信息。
\item
  \textbf{平凡情况}：if 
  \(I(Y; P \mid X) = 0\)（Locationnon-携带标签相关信息），then 
  \(\varepsilon_{\text{PE}} = 0\) 自动成立（零减零）。
\item
  \textbf{Upper Bound}：\(\varepsilon_{\text{PE}} \leq I(Y; P \mid X) \leq H(Y)\)。最大non-完美度Yes丢失which has Location信息。
\end{enumerate}

\subsubsection{3.1.3
and 编码Dimension's关系}\label{ux4e0eux7f16ux7801ux7ef4ux5ea6ux7684ux5173ux7cfb}

\textbf{命题 3.1（Dimension-信息权衡）}：for 正弦编码（\(d\) 维）和 Laplace
相关核（相关长度 \(\xi\)):

\[\varepsilon_{\text{PE}} \leq I(Y; P \mid X) \cdot \exp\left(-\frac{d}{d_0}\right)\]

where \(d_0 = O(L/\xi)\) YesfeatureDimension尺度。具体地，when  \(d\) 按照 §1.2
in 's分位数Optimal方案增长时，信息丢失with 指数Rateunder 降。

\emph{Proof思路}：编码's逼近误差（Theorem
1.2.2）导致核重建误差，进while 导致conditionsDistribution \(P_{Y|X,\text{PE}(P)}\) and 
\(P_{Y|X,P}\) 's KL 散度，by  Pinsker
non-etc.式转化as 互信息丢失。指数衰减来自 Fourier 级数's谱衰减（Cauchy
核's解析性）。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{3.2 修正's Theorem
2------精确陈述和Proof}\label{ux4feeux6b63ux7684-theorem-2ux7cbeux786eux9648ux8ff0ux548cux8bc1ux660e}

\textbf{Theorem 2'（non-完美 PPE under 's弱feature失效Upper Bound------精确correction)}

设原始feature \(X\) 's信息non-足度as  \(\delta > 0\)（定义见 CC Audit Report)，and 
\(\varepsilon_{\text{PE}}\) Yes PPE 's编码non-完美度。then at  PPE
增强'sfeature空间in ，SCX 's \(F_1\) 分数满足：

\[\boxed{F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}}\]

where \(C_F > 0\) Yes \(F_1\) and 贝叶斯Error率之间's转换常数。

\textbf{特殊情形}： -
\(\varepsilon_{\text{PE}} \to 0\)（完美编码):Upper Bound恢复as  \textbf{原始
Theorem 2}：
\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}\]

\begin{itemize}
\tightlist
\item
  \(\varepsilon_{\text{PE}} \to I(Y; P \mid X)\)（complete 失败's编码):Upper Bound回to not yet make using 任何Location信息'sStatus（纯浪费Parameter）。
\end{itemize}

\emph{Proof}：

\textbf{步骤 1（Fano non-etc.式嵌入）}：by  Fano
non-etc.式，Optimal分类器（贝叶斯）'sError率满足：

\[P_e \geq \frac{H(Y \mid X, \text{PE}(P)) - 1}{\log |\mathcal{Y}|}\]

\textbf{步骤 2（conditions熵's分解）}：

\begin{align*}
H(Y \mid X, \text{PE}(P)) &= H(Y \mid X) - I(Y; \text{PE}(P) \mid X) \\
&= H(Y \mid X) - [I(Y; P \mid X) - \varepsilon_{\text{PE}}] \\
&= H(Y \mid X) - I(Y; P \mid X) + \varepsilon_{\text{PE}}
\end{align*}

\textbf{步骤 3（联系 \(\delta\)）}：原始 Theorem 2 in ，\(\delta\)
and conditions熵's关系as （CC 审计报告 §1，Theorem 2):

\[\delta = \frac{2}{\log|\mathcal{Y}|} \cdot \left(H(Y \mid X) - 1\right)_{\text{excess}}\]

where \((\cdot)_{\text{excess}}\) 表示超出贝叶斯Error's信息量。at  PPE under ：

\begin{align*}
H(Y \mid X, \text{PE}(P)) - 1 &= (H(Y \mid X) - I(Y; P \mid X) + \varepsilon_{\text{PE}}) - 1 \\
&= (H(Y \mid X) - 1) - I(Y; P \mid X) + \varepsilon_{\text{PE}}
\end{align*}

定义 \(\delta_{\text{PPE}}\) as  PPE 增强后'shas 效信息non-足度：

\[\delta_{\text{PPE}} = \delta + \frac{2\varepsilon_{\text{PE}}}{\log|\mathcal{Y}|} \cdot \frac{\log|\mathcal{Y}|}{C_F^2} = \delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}\]

（\(C_F\) 来自Error率and  \(F_1\)
's转换：\(F_1 \leq F_{1,\text{base}} + C_F \sqrt{P_e - P_e^*}\)。）

\textbf{步骤 4（代入 SCX 's \(F_1\) Upper Bound）}：

\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta_{\text{PPE}}}{2}} = F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta + \frac{2\varepsilon_{\text{PE}}}{C_F^2}}{2}}\]

\textbf{步骤 5（极限验证）}：when  \(\varepsilon_{\text{PE}} \to 0\)：

\[F_{1,\text{SCX}}^{\text{PPE}} \leq F_{1,\text{base}} + C_F \cdot \sqrt{\frac{\delta}{2}}\]

正Yes原始 Theorem 2。\(\square\)

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{3.3 \(\varepsilon_{\text{PE}}\)
'scan be 估计形式}{3.3 \textbackslash varepsilon\_\{\textbackslash text\{PE\}\} 'scan be 估计形式}}\label{varepsilon_textpe-ux7684ux53efux4f30ux8ba1ux5f62ux5f0f}

定理量
\(\varepsilon_{\text{PE}} = I(Y; P \mid X) - I(Y; \text{PE}(P) \mid X)\)
YesTheory量------它涉and 真实DataDistribution。for 于实际should using ，我们requires must FromDatain 估计它。

\subsubsection{3.3.1 KSG 估计器}\label{ksg-ux4f30ux8ba1ux5668}

\textbf{定理 3.3.1（\(\varepsilon_{\text{PE}}\)
'scan be 估计性）}：\(\varepsilon_{\text{PE}}\)
can be with 通过with under MethodFromhas 限Samplein \textbf{一致地}估计：

\[\boxed{\hat{\varepsilon}_{\text{PE}}^{(n)} = \hat{I}^{(n)}(Y; P \mid X) - \hat{I}^{(n)}(Y; \text{PE}(P) \mid X)}\]

where \(\hat{I}^{(n)}(\cdot; \cdot \mid \cdot)\) Yesmake using 
Kraskov-Stögbauer-Grassberger (KSG)
估计器's\textbf{conditions互信息}估计。when Sample量 \(n \to \infty\)
时，\(\hat{\varepsilon}_{\text{PE}}^{(n)} \xrightarrow{p} \varepsilon_{\text{PE}}\)（一致性）。

具体地，KSG conditions互信息估计器：

\[\hat{I}(Y; P \mid X) = \hat{I}(Y; (P, X)) - \hat{I}(Y; X)\]

where \(\hat{I}(A; B)\) Yes标准's KSG 互信息估计（基于 \(k\)-近邻距离）。

\textbf{Algorithm}： 1. FromDatain 采样 \(n\) 个三元组 \((x_i, p_i, y_i)\) 2.
Computation编码 \(\text{PE}(p_i)\) 3. Computation
\(\hat{I}^{(n)}(Y; (P, X))\)：at 联合空间
\((\mathcal{P} \times \mathcal{X}) \times \mathcal{Y}\) in make using  \(k\)-NN
4. Computation \(\hat{I}^{(n)}(Y; X)\)：at  \(\mathcal{X} \times \mathcal{Y}\)
in make using  \(k\)-NN 5.
\(\hat{I}^{(n)}(Y; P \mid X) = \hat{I}^{(n)}(Y; (P, X)) - \hat{I}^{(n)}(Y; X)\)
6. 同理Computation \(\hat{I}^{(n)}(Y; \text{PE}(P) \mid X)\) 7.
\(\hat{\varepsilon}_{\text{PE}}^{(n)} = \hat{I}^{(n)}(Y; P \mid X) - \hat{I}^{(n)}(Y; \text{PE}(P) \mid X)\)

\textbf{ConvergenceRate}：KSG 估计器with  \(O(n^{-1/(d_{\text{eff}}+2)})\)
RateConvergence，where \(d_{\text{eff}}\) Yes联合空间'shas 效Dimension。for 于高维
\(X\)，Convergence较慢，but  \(\varepsilon_{\text{PE}}\)
本身Yes\textbf{差值}，两个估计器共享相同's偏差，差值估计can be can 比单独估计更准确（偏差抵消）。

\subsubsection{3.3.2 lower bound估计（避免高维
KSG）}\label{ux4e0bux754cux4f30ux8ba1ux907fux514dux9ad8ux7ef4-ksg}

for 于高维 \(X\)，KSG
估计can be can non-准确。替代方案：make using \textbf{预测性can Difference}作as 
\(\varepsilon_{\text{PE}}\) 'slower bound。

\textbf{命题 3.3.1（\(\varepsilon_{\text{PE}}\) 's预测lower bound）}：

\[\boxed{\varepsilon_{\text{PE}} \geq \mathbb{E}_{X,P}\left[D_{\text{KL}}(P_{Y|X,P} \| P_{Y|X,\text{PE}(P)})\right]_{\text{est}}}\]

where \(P_{Y|X,P}\) 和 \(P_{Y|X,\text{PE}(P)}\) can be 通过两个分类器近似： -
分类器 \(f_{\text{full}}\) at  \((X, P)\) on Training → 估计 \(P_{Y|X,P}\) -
分类器 \(f_{\text{enc}}\) at  \((X, \text{PE}(P))\) on Training → 估计
\(P_{Y|X,\text{PE}(P)}\)

两者at 留出Test集on 's\textbf{for 数损失差}Yes \(\varepsilon_{\text{PE}}\)
'slower bound估计：

\[\hat{\varepsilon}_{\text{PE}}^{\text{pred}} = \frac{1}{n_{\text{test}}} \sum_{i=1}^{n_{\text{test}}} \left[\log \frac{1}{p_{\text{enc}}(y_i \mid x_i, \text{PE}(p_i))} - \log \frac{1}{p_{\text{full}}(y_i \mid x_i, p_i)}\right]\]

when  \(n_{\text{test}} \to \infty\)
and 分类器一致时，\(\hat{\varepsilon}_{\text{PE}}^{\text{pred}} \to \varepsilon_{\text{PE}}\)。

\textbf{实using 价值}：such as 果分类器 \(f_{\text{full}}\) 和 \(f_{\text{enc}}\)
at Test集on 's准确率\textbf{没has 显著Difference}，then 
\(\varepsilon_{\text{PE}} \approx 0\)------编码Yes充足's，PPE non-will 削弱
Theorem 2 'sUpper Bound。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Part 4 部分：Theorem 3
's修正（最重must 's部分）}\label{ux7b2c-4-ux90e8ux5206theorem-3-ux7684ux4feeux6b63ux6700ux91cdux8981ux7684ux90e8ux5206}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.1 原 Theorem 3
's精确Theorem Statement}\label{ux539f-theorem-3-ux7684ux7cbeux786eux5b9aux7406ux9648ux8ff0}

\textbf{Theorem 3（原始------Noiseand DifficultySample'snon-can be 区分性）}：

存at 两个世界 \(W_A, W_B\) 满足：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  观测DataDistributioncomplete 相同：\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)
\item
  at  \(W_A\) in ，Sample \((x, y)\) Yes\textbf{标签Noise}（label Error，but 
  \(x\) Structureon  label should Yes别's）
\item
  at  \(W_B\) in ，Sample \((x, y)\) Yes\textbf{本质DifficultySample}（label
  Correct，but feature \(x\) 本身No法确定性地支持该 label）
\end{enumerate}

then ：没has 任何Algorithmcan 够only 通过观测 i.i.d. Sample \((X_i, Y_i)_{i=1}^n\) 区分
\(W_A\) 和 \(W_B\)。形式化地，for Arbitrary决策规then 
\(d: (\mathcal{X} \times \mathcal{Y})^n \to \{W_A, W_B\}\)：

\[\max_{W \in \{W_A, W_B\}} P_W(d(\text{data}) \neq W) \geq \frac{1}{2}\]

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.2 Learning型 PE
such as 何破坏non-can be 区分性------精确conditions}\label{ux5b66ux4e60ux578b-pe-ux5982ux4f55ux7834ux574fux4e0dux53efux533aux5206ux6027ux7cbeux786eux6761ux4ef6}

\subsubsection{4.2.1 设定}\label{ux8bbeux5b9a-1}

引入 PPE 后，观测Dataextensionas 三元组 \((X, P, Y)\)，where \(P\)
YesphysicalLocation（can be 观测'sPhysical Quantity------原子坐标、残基Locationetc.）。关键区别：

\begin{itemize}
\tightlist
\item
  \textbf{固定 PE}：\(\text{PE}(\cdot)\)
  Yes预定义's确定性Function（such as 正弦编码），\textbf{non-dependence于TrainingData}。
\item
  \textbf{Learning型 PE}：\(\text{PE}_\theta(\cdot)\) 'sParameter \(\theta\)
  YesFromDatain Learning's（such as 通过梯度under 降优化某个ObjectiveFunction）。
\end{itemize}

\subsubsection{4.2.2 固定 PE
's情况（Trivial）}\label{ux56faux5b9a-pe-ux7684ux60c5ux51b5trivial}

\textbf{命题 4.1（固定 PE 保持 Theorem 3）}：if 
\(\text{PE}: \mathcal{P} \to \mathbb{R}^{d_{\text{pe}}}\)
is a\textbf{固定's、and DataNo关's}Function，then 增强后's观测Distribution依然相同：

\[P_{W_A}(X, P, \text{PE}(P), Y) = P_{W_B}(X, P, \text{PE}(P), Y)\]

定理 3 'snon-can be 区分性\textbf{complete 保持}。

\emph{Proof}：because as  \(\text{PE}\)
Yes固定's确定性Function，\((X, P, \text{PE}(P), Y)\) Yes \((X, P, Y)\)
's一个确定性Function。while  \(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\)（physicalLocation
\(P\)
by physicalStructure决定，独立于标签生成过程's世界Selection）。because 此增强后's联合Distributionalso 相同。\(\square\)

\subsubsection{4.2.3 Learning型 PE
's情况（关键）}\label{ux5b66ux4e60ux578b-pe-ux7684ux60c5ux51b5ux5173ux952e}

\textbf{定理 4.1（Learning型 PE 破坏 Theorem 3 's精确conditions）}：

设 \(\text{PE}_\theta\) 'sParameter \(\theta\) 通过最小化via 验风险
\(\hat{R}_n(\theta) = \frac{1}{n}\sum_{i=1}^{n} \ell(f_\theta(x_i, p_i), y_i)\)
FromTrainingDatain Learning（where \(\ell\) Yes某个损失Function，\(f_\theta\) Yes包含 PE
编码'sCompleteModel）。then at with under conditionsunder ，世界 \(W_A\) 和 \(W_B\)
变as \textbf{can be 区分}：

\textbf{(C1) Information Theorynon-平凡性}：

\[I(Y; P \mid X) > 0\]

i.e.：physicalLocationat give 定原始featureunder 携带关于标签's信息。

\textbf{(C2) Learningfor 标签Noise's敏感性}：存at 一个non-空'sTrainingSample子集
\(\mathcal{D}_{\text{diff}} \subset \mathcal{D}\)，at this 些Sampleon ：

\[\nabla_\theta \ell(f_\theta(x, p), y_A) \neq \nabla_\theta \ell(f_\theta(x, p), y_B)\]

where \(y_A\) Yes \(W_A\) in 该Sample'sNoise标签，\(y_B = y_A\)（at  Theorem 3
构造in ，两个世界has 相同's观测标签\(y\)，only Yes解释non-同）。实际on at  Theorem
3 's构造in ，\textbf{观测to 's \((x, y)\) complete 相同}！

this 意味着：Learning型 PE
破坏non-can be 区分性'sMechanism\textbf{non-Yes}来自标签Difference（because as 标签相同），while Yes来自Modelfor ''such as 何去Learning'''sDifference。let 我精确Analysis。

\subsubsection{4.2.4
精确Mechanism：表征漂移}\label{ux7cbeux786eux673aux5236ux8868ux5f81ux6f02ux79fb}

\textbf{关键洞察}：at  Theorem 3
's构造in ，\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)
Yes成立's，but \textbf{this 并non-意味着Learning动态相同}。Reasonat 于：

at  \(W_A\)（标签Noise世界）in ： - Sample \((x, y)\) 's标签 \(y\) YesError's -
physicalLocation \(p\) and  \((x, y)\) 's关系Yes：\(p\) and  \(x\)
一致（by physicalStructure决定），but  \(p\) 暗示's真实标签and  \(y\) non-同 - when  PE
'sParameteris Learning时，Modelwill 发现 \((p, y)\) 之间's''矛盾''------\(p\)
暗示某个标签，while  \(y\) Yes另一个

at  \(W_B\)（本质Difficulty世界）in ： - Sample \((x, y)\) 's标签 \(y\) YesCorrect's -
\(p\) and  \(x\) 和 \(y\) all 一致------non-存at ''矛盾'' - ModelLearningto 's
\((p, y)\) 关系Yes和谐一致's

\textbf{关键区别}：i.e.make 
\(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\)（联合观测Distribution相同），Learning\textbf{动态}取决于损失面's几何Structure------while 该Structureat conditionsDistribution
\(P_{Y|X,P}\) 方面has Difference（because as  Theorem 3 's构造dependence于conditionsDistribution'sDifference，i.e.
\(X\) at give 定 \((Y, \text{world})\) under 'sDistributionnon-同）。

\textbf{this Yes Theorem 3
构造's核心}：\(P_{W_A}(Y \mid X) = P_{W_B}(Y \mid X)\)（观测Distribution相同），but 生成过程non-同：
- \(W_A\)：\(Y_{\text{true}}\) 先by  \(X\) 决定，然后 \(Y_{\text{obs}}\)
with probability \(\eta\) is 翻转 -
\(W_B\)：\(Y_{\text{obs}} = Y_{\text{true}}\)，but 
\(P(Y_{\text{true}} \mid X)\) at 某些区域接近all 匀（高non-确定性）

when 引入 \(P\) 后，at  \(W_A\) in  \(P(Y_{\text{true}} \mid X, P)\) can be can 比
\(P(Y_{\text{obs}} \mid X, P)\) 更尖锐（because as  \(P\)
帮助确定真实标签），导致Modelat  \((X, P)\)
空间in ''看to ''Noise标签's矛盾。while at  \(W_B\)
in ，\(P(Y_{\text{obs}} \mid X, P) = P(Y_{\text{true}} \mid X, P)\)，没has 矛盾。

\textbf{but this 仍然没has give 出can be 观测'sDifference}------because as at Training时，Modelat 两个世界in 看to 'sall Yes相同's
\((x, p, y)\) 三元组。

真正make Learning型 PE can 够区分'sMechanismYes：定理 3
's构造in 两个世界's区别at 于\textbf{not yet 观测to 's潜at 变量}（真实标签 vs
Noise标签 vs Difficulty程度）。such as 果 \(P\)
提供了关于this 些not yet 观测变量's信息窗口（通过
\(I(Y_{\text{true}}; P \mid X) > 0\)），that 么\textbf{at Training过程in }，Learningto 's
PE Parameter \(\theta\)
will 通过泛化linesas 反映this 些Difference------i.e.make TrainingData看起来相同。

\subsubsection{4.2.5
精确can be 区分性conditions------最终形式}\label{ux7cbeux786eux53efux533aux5206ux6027ux6761ux4ef6ux6700ux7ec8ux5f62ux5f0f}

let 我From更基本's层面重新Analysis。

at  Theorem 3 'sProofin ，两个世界's构造Yes： - Selection两个non-同'sconditionsDistribution
\(P_{W_A}(Y \mid X)\) 和 \(P_{W_B}(Y \mid X)\)，but 边际Distribution \(P(X, Y)\)
相同 - this 之which with can be can ，Yesbecause as  \(P(X)\) at 两个世界in non-同 →
\(P_{W_A}(X) \neq P_{W_B}(X)\)，but  \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)

etc.etc.，this non-for 。Theorem 3 说'sYes\textbf{观测}Distribution相同。let 我From CC
审计报告's原始构造出发。

CC 审计报告：``存at 两个世界 \(W_A, W_B\)，具has 观测etc.价'sDataDistribution
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)''

构造Method：两个世界具has non-同's \(P(X)\) 和non-同's
\(P(Y|X)\)，but 它们's乘积相同。具体地： - \(W_A\)：\(P_{W_A}(X)\)
at ''容易''区域has 更多质量，\(P_{W_A}(Y|X)\) Yes确定性's -
\(W_B\)：\(P_{W_B}(X)\) at ''Difficulty''区域has 更多质量，\(P_{W_B}(Y|X)\)
Yes接近all 匀's

make 得 \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\) for which has  \((x, y)\) 成立。

\textbf{when 引入 \(P\)
后}：\(P_{W_A}(X, P, Y) = P_{W_A}(P \mid X, Y) \cdot P_{W_A}(X, Y)\)。

such as 果 \(P\) Yesphysicalon by  \(X\) 决定's（\(P\) Yes \(X\) 'sphysical坐标），then 
\(P(P \mid X, Y) = P(P \mid X)\)（physicalLocationnon-dependence于标签）。此时：
\[P_{W_A}(X, P, Y) = P_{W_A}(P \mid X) \cdot P_{W_A}(X, Y)\]
\[P_{W_B}(X, P, Y) = P_{W_B}(P \mid X) \cdot P_{W_B}(X, Y)\]

but 
\(P_{W_A}(P \mid X) = P_{W_B}(P \mid X)\)（physicalLocationby physicalStructure决定，for 两个世界相同），and 
\(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)。because 此
\(P_{W_A}(X, P, Y) = P_{W_B}(X, P, Y)\)。

\textbf{固定 PE 仍然保持non-can be 区分性}------联合Distribution相同。

\textbf{Learning型 PE 's情况}：Learning型 PE 'sParameter \(\theta\)
Yes通过最小化某个损失FunctionFromDatain 学to 's。考虑at dataset
\(\mathcal{D} = \{(x_i, p_i, y_i)\}_{i=1}^n\) on 'sTraining：

\[\hat{\theta} = \arg\min_\theta \frac{1}{n} \sum_{i=1}^n \ell(f_\theta(x_i, \text{PE}_\theta(p_i)), y_i)\]

this 里's关键Yes：\textbf{PE
'sParameterat 损失Functionin }------non-only 通过编码输出影响损失，also can be can 通过某种自监督or 重构损失影响Learning。

but such as 果 PE
纯粹Yeshas 监督地Training's（only 通过under 游任务's损失），that 么两个世界give 出相同'sTrainingData
→ 相同's梯度 → 相同's \(\hat{\theta}\)。at this 种情况under ，i.e.make YesLearning型 PE
also non-can 区分两个世界。

\textbf{真正make Learning型 PE can 够区分's场景}：

PE 'sParameter除了受监督损失影响外，also 通过with under 方式接收信号： 1.
\textbf{自监督预Training}（such as  masked position
prediction）------at 标注之前进lines 2. \textbf{辅助损失}（such as Location重构损失）
3. \textbf{physical约束}（such as can 量守恒、力matching）------this 些约束提供了关于 \(P\)
和Physical Quantity之间关系's额外信息

at this 些情况under ，PE
'sParameter部分by \textbf{physical一致性's信号}决定，while this 些信号at 两个世界in can be can non-同（because as 两个世界in Sample's''physicalJustification''non-同）。

\textbf{精确定理}：

\textbf{Theorem 3'（修正后'snon-can be 区分性定理------带 PPE 前提）}：

\[\boxed{\begin{aligned}
\text{if  } & \text{(A) } \text{PE}(\cdot) \text{ Yesand 标签 } Y \text{ 独立's固定Function（i.e.its Parameternon-by  } Y \text{ or 任何dependence于 } Y \text{ 's信号决定），and } \\
& \text{(B) } P \perp\!\!\!\perp Y_{\text{world}} \mid X \text{（physicalLocationat give 定原始featureunder 独立于世界Selection），} \\
\text{then  } & P_{W_A}(X, \text{PE}(P), Y) = P_{W_B}(X, \text{PE}(P), Y)，\text{Theorem 3 'snon-can be 区分性保持。} \\
\text{反之，if  } & \text{PE 'sParameter通过dependence于 } Y \text{ 's信号Learning得to ，and  } I(Y_{\text{true}}; P \mid X) > 0，\\
\text{then  } & \text{Learningto 's编码at 两个世界in with 正概率non-同：} P(\hat{\theta}_{W_A} \neq \hat{\theta}_{W_B}) > 0，\\
\text{两个世界变as can be 区分。} &
\end{aligned}}\]

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.3
构造性Proof：最小示例}\label{ux6784ux9020ux6027ux8bc1ux660eux6700ux5c0fux793aux4f8b}

\subsubsection{4.3.1 构造}\label{ux6784ux9020}

考虑一个极简's二维分类Problem。设 \(X \in \{-1, +1\}\)
is a二元feature，\(P \in \{0, 1\}\)
is a二元physicalLocation，\(Y \in \{-1, +1\}\) Yes标签。

\textbf{世界 \(W_A\)（标签Noise）}：

\begin{itemize}
\tightlist
\item
  \(P_{W_A}(X = +1) = 1/2\), \(P_{W_A}(X = -1) = 1/2\)
\item
  \(P_{W_A}(Y_{\text{true}} = +1 \mid X = +1) = 1\),
  \(P_{W_A}(Y_{\text{true}} = -1 \mid X = -1) = 1\)（确定性's）
\item
  标签with probability \(\eta = 0.2\) 翻转：\(Y_{\text{obs}} = Y_{\text{true}}\)
  with probability \(0.8\)，\(Y_{\text{obs}} = -Y_{\text{true}}\) with probability \(0.2\)
\item
  physicalLocation \(P\)：\(P = X\)（确定性），i.e. \(p = 1\) when 
  \(x = +1\)，\(p = 0\) when  \(x = -1\)
\end{itemize}

\textbf{世界 \(W_B\)（本质Difficulty）}：

\begin{itemize}
\tightlist
\item
  \(P_{W_B}(X = +1) = 0.6\), \(P_{W_B}(X = -1) = 0.4\)（non-all 匀！）
\item
  \(P_{W_B}(Y_{\text{obs}} = +1 \mid X = +1) = 5/6\),
  \(P_{W_B}(Y_{\text{obs}} = -1 \mid X = +1) = 1/6\)
\item
  \(P_{W_B}(Y_{\text{obs}} = +1 \mid X = -1) = 1/4\),
  \(P_{W_B}(Y_{\text{obs}} = -1 \mid X = -1) = 3/4\)
\item
  \(Y_{\text{obs}} = Y_{\text{true}}\)（没has 标签Noise，but conditionsDistributionYes固has 's高non-确定性）
\item
  \(P = X\)（and  \(W_A\) 相同）
\end{itemize}

\textbf{验证观测etc.价}：

at  \(W_A\) in ： - \(P_{W_A}(X = +1, Y = +1) = 0.5 \times 0.8 = 0.4\) -
\(P_{W_A}(X = -1, Y = +1) = 0.5 \times 0.2 = 0.1\) -
\(P_{W_A}(X = +1, Y = -1) = 0.5 \times 0.2 = 0.1\) -
\(P_{W_A}(X = -1, Y = -1) = 0.5 \times 0.8 = 0.4\)

at  \(W_B\) in ： - \(P_{W_B}(X = +1, Y = +1) = 0.6 \times (5/6) = 0.5\) -
\(P_{W_B}(X = -1, Y = +1) = 0.4 \times (1/4) = 0.1\) -
\(P_{W_B}(X = +1, Y = -1) = 0.6 \times (1/6) = 0.1\) -
\(P_{W_B}(X = -1, Y = -1) = 0.4 \times (3/4) = 0.3\)

\(P(X=+1,Y=+1)\) non-同（0.4 vs
0.5），non-满足观测etc.价。let 我调整构造make its complete 相etc.。

重新构造：

\(W_A\)： - \(P(X=+1) = 0.5\), \(P(X=-1) = 0.5\) -
\(P(Y=+1 \mid X=+1) = 0.8\), \(P(Y=-1 \mid X=+1) = 0.2\) -
\(P(Y=+1 \mid X=-1) = 0.2\), \(P(Y=-1 \mid X=-1) = 0.8\) -
生成Mechanism：\(Y_{\text{true}}\) Yes确定性's (\(X=+1 \to +1\),
\(X=-1 \to -1\))，but  20\% Noise

\(W_B\)： - \(P(X=+1) = 0.5\), \(P(X=-1) = 0.5\) - and  \(W_A\) complete 相同's
\(P(Y \mid X)\) - 生成Mechanism：feature本身Yes高non-确定性's（没has 隐藏's真实标签）

at 此构造under  \(P_{W_A}(X, Y) = P_{W_B}(X, Y)\)，but 内at Structurenon-同。at  \(W_A\)
in ，\(P = X\)，\(Y_{\text{true}} = X\)，Noisewith  20\% 概率翻转。at  \(W_B\)
in ，\(P = X\)，but  \(Y\) 直接by 带Noise's通道生成（No隐藏层）。

\subsubsection{4.3.2 Learning型 PE
'scan be 区分性}\label{ux5b66ux4e60ux578b-pe-ux7684ux53efux533aux5206ux6027}

引入一个easy'sLearning型
PE：\(\text{PE}_\theta(p) = \theta \cdot p\)（标量编码，\(\theta \in \mathbb{R}\)
can be Learning）。

CompleteModel：\(f(x, p) = \text{sign}(w \cdot x + \text{PE}_\theta(p))\)。

设我们通过with under 方式Learning \(\theta\)： 1.
先at \textbf{No标签}Dataon with 自监督方式预Training：预测is  mask 'sphysicalLocation
\(\hat{p} = g(\text{PE}_\theta(p))\) 2. 然后at under 游任务on 微调

关键：at 步骤 1（自监督预Training）in ，ObjectiveFunctionnon-涉and 标签
\(Y\)。两个世界'sData \((X, P)\)
Distributioncomplete 相同（\(P_{W_A}(X, P) = P_{W_B}(X, P)\)），because 此预Training阶段\textbf{non-引入}区分。

but such as 果我们make using with under 辅助损失：

\[\mathcal{L}_{\text{aux}}(\theta) = \mathbb{E}_{(x,p,y)}\left[(\text{PE}_\theta(p) - \mathbb{E}[Y \mid X = x])^2\right]\]

i.e.：编码should 该and conditions期望标签一致。this 个损失涉and 
\(Y\)，and dependence于世界's内部Structure。at  \(W_A\)
in ，\(\mathbb{E}[Y \mid X = x] = (1-2\eta) \cdot \text{sign}(x) = 0.6 \cdot \text{sign}(x)\)（because as Noise翻转）。at 
\(W_B\)
in ，\(\mathbb{E}[Y \mid X = x] = \mathbb{E}[Y \mid X = x] = 0.6 \cdot \text{sign}(x)\)（数值相同！only Yes解释non-同）。

which with at \textbf{this 个最小示例in }，i.e.make YesLearning型 PE
also non-can 区分两个世界------because as conditions期望相同。

\textbf{this 正Yes Theorem 3
's精髓}：such as 果观测Distributioncomplete 相同，任何Algorithmall non-can 区分。

\subsubsection{4.3.3 开放Problem：Learning型 PE
真正can 区分'sconditions}\label{ux5f00ux653eux95eeux9898ux5b66ux4e60ux578b-pe-ux771fux6b63ux80fdux533aux5206ux7684ux6761ux4ef6}

\textbf{此最小示例实际on Proof了 Theorem 3
's鲁棒性，while non-its 脆弱性}：at this 个构造in ，i.e.make YesLearning型 PE
also non-can 区分两个观测etc.价's世界。this 恰恰印证了 Theorem 3
's精髓------when 观测Distributioncomplete 相simultaneously，任何Algorithmall non-can 区分。

原先声称's''构造性最小示例''反while 构成了 Theorem 3'
破坏方toward 's一个\textbf{反例}：它展示了一个Learning型 PE
\textbf{No法}区分两个观测etc.价世界's场景。

must make Learning型 PE
can 够区分，requires must 满足：\textbf{Learning信号non-only only dependence于观测Distribution}。Theoryon this can be can 发生at ：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{多任务Learning}：PE simultaneouslyis using 于预测另一个and  \(Y_{\text{true}}\)
  相关's量 \(Z\)（例such as can 量、力），and  \(Z\) at 世界
  \(W_A\)（Noise标签意味着Physical Quantitynon-一致）和
  \(W_B\)（Difficultybut physical一致）in 's表现non-同
\item
  \textbf{physical一致性约束}：损失Function包含physical约束项（such as can 量守恒），make 得at 
  \(W_A\) in Noise标签导致physicalnon-一致's预测，while at  \(W_B\)
  in DifficultySample仍保持physical一致性
\item
  \textbf{Distribution外泛化}：PE
  'sParameter部分by Distribution外Data（non-同于training distribution）决定，and 两个世界at Distribution外表现non-同
\end{enumerate}

\textbf{{[}开放Problem{]}}
然while ，目前\textbf{缺乏构造性Proof}验证on 述任何Mechanism确实can at 观测etc.价's两个世界on 产生non-同'sOptimal
PE Parameter。this requires must ： - 显式定义not yet 观测变量 \(Z\) - 构造
\(\mathcal{L}_{\text{aux}}\) 's具体形式 - Proofat  \(W_A\) 和 \(W_B\)
in 'sOptimal值确实non-同

定理 4.1 's陈述（关于
\(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{sup}} + \lambda \mathcal{L}_{\text{aux}}\)
'snon-同Optimal值导致can be 区分性）at 逻辑on YesCorrect's\textbf{conditions陈述}，but 该conditionsYesNocan at 
Theorem 3 's构造in is 满足，目前没has 构造性Proof。

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.4 修正后's Theorem
3'------Complete陈述}\label{ux4feeux6b63ux540eux7684-theorem-3ux5b8cux6574ux9648ux8ff0}

\textbf{Theorem 3'（non-can be 区分性'sconditions保持------修正版）}：

\[\boxed{\begin{aligned}
\textbf{前提 (A): } & \text{physicalLocation编码 PE Yes固定's，or only 通过独立于标签 } Y \text{ 's信号Learning。} \\
\textbf{前提 (B): } & \text{physicalLocation } P \text{ at give 定 } X \text{ under Conditional Independence于世界Selection：} P \perp\!\!\!\perp W \mid X。\\
\textbf{Conclusion: } & \text{Theorem 3 'snon-can be 区分性complete 保持：} \\
& \forall \text{ 决策规then  } d, \quad \max_{W \in \{W_A, W_B\}} P_W(d \neq W) \geq \frac{1}{2}。\\
\textbf{if 前提 (A) non-成立: } & \text{non-can be 区分性YesNois 破坏目前is a**开放Problem**。} \\
& \text{Learning型 PE can be can 通过多任务Learningor physical一致性约束etc.Mechanism区分两个世界，} \\
& \text{but when 前缺乏构造性Proof。见 §4.3 's讨论。}
\end{aligned}}\]

\textbf{Corollaries 4.4.1（for 实践's影响）}：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{固定正弦 PE or 固定旋转 PE}：Theorem 3
  complete 保持。Location编码non-will 损害最坏情况'snon-can be 区分性Guarantee------but also \textbf{non-will 主动帮助}区分Noise和DifficultySample（to 少at Information Theory意义on ，通过观测DistributionNo法区分）。
\item
  \textbf{FromNo标签LocationDataLearning's
  PE}（such as 通过自监督重构Location):such as 果Training过程non-涉and 标签 \(Y\)，Theorem 3
  保持。PE Parameterat 两个世界in 学to 相同's值。
\item
  \textbf{通过多ObjectiveLearning（包括标签相关Objective）'s PE}：Theorem 3
  \textbf{can be can is 破坏}，but 目前缺乏构造性Proof。this Yes\textbf{开放Problem}------Location信息YesNo真's提供了一个额外's''窗口''来区分Noise和DifficultySample，also is anot yet is 严格验证's猜想。
\item
  \textbf{风险}：i.e.make 辅助Objectivecan be can 帮助区分，if its 设计引入虚假相关性，can be can 导致Error's''区分''，will DifficultySampleError地标记as Noise。this requires must 仔细'sExperiment验证。
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{4.5 and  CC 审计报告 §2.5
's比较}\label{ux4e0e-cc-ux5ba1ux8ba1ux62a5ux544a-2.5-ux7684ux6bd4ux8f83}

CC 审计报告for  Theorem 3 'sAnalysisYesCorrect's（固定 PE 保持non-can be 区分性，Learning型
PE can be can 破坏），but 遗漏了with under 关键点：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{CC 报告not yet 精确定义''Learning型
  PE'''sMeaning}：only 说''FromDatain Learning''Yesnon-够's------requires must 明确区分''FromNo标签DataLearning''（安全）和''From标签dependence信号Learning''（can be can 破坏
  Theorem 3）。
\item
  \textbf{CC 报告not yet give 出can be 区分性's精确conditions}：定理 4.2 填补了this 一空白。
\item
  \textbf{CC
  报告's评价''this Yes好消息''requires must 限定}：only has at 辅助Objective信号and 真实标签Noise相关（while non-虚假相关）时，Learning型
  PE 's区分can 力only then Yescan be 靠's好消息。
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Summary}\label{ux603bux7ed3}

本文for  PPE at  SCX 框架in 's作using 进lines了严格's数学推导，主must 贡献包括：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{编码Function's形式化}（Part 1
  部分):give 出了三种physicalLocation域's正弦和旋转编码's精确定义，推导了基于
  Bochner 定理和 Laplace 核'sOptimal频率谱，Proof了编码's Lipschitz
  连续性并Computation了精确常数。
\item
  \textbf{Theorem 1 's修正}（Part 2 部分):修正了 CC 审计报告in 's sign
  error，give 出了 \(\delta_s^{\text{PE}}\) 'sCorrect表达式，Proof了
  \(\delta_s^{\text{PE}} > 0\)
  's充分conditions（\(I(Y; P \mid S) > 0\)），并推导了基于Data Processingnon-etc.式和
  Fano non-etc.式'son lower bound。
\item
  \textbf{Theorem 2 's修正}（Part 3 部分):give 出了
  \(\varepsilon_{\text{PE}}\) 's精确Information Theory定义，Proof了
  \(\varepsilon_{\text{PE}} \to 0\)
  时Upper Bound恢复as 原始形式，并提供了FromDatain can be 估计
  \(\varepsilon_{\text{PE}}\) 's两种实using Method（KSG
  估计器和预测性can Difference）。
\item
  \textbf{Theorem 3 's修正}（Part 4 部分):精确阐明了固定 PE 和Learning型 PE
  for non-can be 区分性'snon-同影响，通过最小构造性示例展示了区分's必must conditions，提出了包含精确前提conditions's修正
  Theorem 3'。
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{推导完成Date：2026-06-29}\\
\emph{注：which has 定理编号遵循 SCX 框架内部约定。本文修正了 CC
审计报告in 'sSymbolnon-一致（特别Yes §2.3 命题 2.1 's sign
error），并at 该报告's基础on 增加了严格'son lower bound推导、构造性Proof和can be 估计形式。}

\end{document}
